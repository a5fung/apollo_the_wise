# PLAN — the single source of truth (projects · tasks · ETAs)

**This file is THE plan.** Every task lives here, under a project, with an `ETA` date and a `status`.
The long-horizon plan (the 6/22 launch) lives here too — as dated tasks, not a separate doc. There is no
second plan surface: the calendar is phone reminders only, the harness #-task list is a session scratch
mirror, `CHANGELOG.md`/git hold history. If it's planned, it's a line here.

**Line format** (strict — parsed by `scripts/check_plan.py`):
`- #<id> | <YYYY-MM-DD ETA> | <status> | <title>`   ·  status ∈ pending · in_progress · blocked

**The two rituals operate ONLY on this file:**
- **OPEN** (every session): run `python scripts/check_plan.py --today` → it prints OVERDUE + due-today open
  tasks. That list IS the day's plan. State it before reacting to anything.
- **CLOSE** (every session): every open task's ETA must be ≥ today. Rebump any past-due one to a real
  future date (or close it). File every new item as a line here under a project. Then commit — the
  pre-commit gate (`check_plan.py`) REFUSES the commit if any open task lacks a project, an ETA, or has a
  past ETA, or if an open harness task is missing from this file. No gaps, mechanically.

_Last CLOSE: 2026-06-16._

---

## Launch — 6/22 GO/NO-GO (the long-horizon plan, as dated tasks · SSoT-spec: docs/roadmap/launch-2026-06-22.md)

> **DoD-7 TRIAGE SWEEP — 2026-06-18** (the "all projects deliberately dispositioned" gate). Every
> open task is now CLOSED / RUNWAY (≤6/22) / explicit POST-LAUNCH-with-reason. Spine GREEN: DoD-1
> measured+governed (#268+kill/scale signed), DoD-2 split live (#256), W1 replay (#268), W4
> chart-vision shadow (#343). Re-dispositioned drift: **#255** (W5 precedent-retrieval — was an
> accidental gate-rebump; now explicit post-launch, the W5 *labeling-ritual* bar is already met),
> **#226** (advisory + blocked → post-launch), **#291** (launch-critical reviews done; methodology
> batch → post-launch). Close-pending (done, awaiting snapshot refresh): **#284 #286 #270**.
> **COHORT DISPOSITION:** the ~46 tasks dated **2026-06-23** = the explicit POST-LAUNCH FAST-FOLLOW
> bucket (first working day after launch) — the v1.1 judge/catalyst tail, the catalyst-sourcing
> backbone (#210–#236), op-split residuals (#277–#281), SiP detection backlog, hygiene tail — NONE
> launch-blocking; the meta-rubric LOAD-BEARING flip is gated post-cutover by design (#335). The
> 6/19–6/22 runway = the launch-grade work (rehearsal #304, evidence pack #305, DoD-5 hygiene
> #237/#248/#260, DoD-6 dashboards #193/#185/#194 [#194 deploy key added 7/19 → auto-export live, verify 7/20]).
> Live-money GO-direction gates (#150/#151/#183/#184/#275) sit around the cutover, appropriately.

- #261 | 2026-08-15 | in_progress | scripts/ namespace reorg. SAFE SUBSET DONE 6/17 (5117ed1): 45 throwaway probes → scripts/probes/ (0 pipeline refs; 2 safety greps caught 19 false-throwaways = 4 imported shared modules + _270_* cluster + 4 code-referenced + YAML-cited review tools, all KEPT at root; operator's _reconcile left alone); 6 docs swept; 887 tests pass; no deploy needed. ⚠ REGRESSION FOUND + FIXED 6/20 (via #285's new guard test): the safe subset MIS-MOVED 5 ACTIVE monthly-sweep tools (_b53/_b54/_b77/_b88/_b92) into scripts/probes/ while their registration (quarterly_review.QUARTERLY_BACKWARD_CHECK_SCRIPTS) + their own docstrings still said scripts._bNN root → the 7/1 sweep would have 🔴'd all 5. Moved back to root (commit 2c518e4). ROOT CAUSE for the remaining reorg: the #261 safety greps checked YAML-cited tools but NOT Python-cited ones (a .py list) → when continuing the reorg, grep BOTH (incl quarterly_review.py's script list + any importlib/`python -m` refs); the new tests/test_quarterly_sweep_registration.py now backstops the sweep set. REMAINING (needs a focused block + per-file judgment): ops/evals split of the 94 real tools (touches deploy.sh/CI paths — careful sweep + verify-deploy) + delete-vs-keep the 45 untracked scratch scripts (incl non-_ WIP like analyze_/replay_/backfill_) [ok:date-roll-0701] [b6] [ok:Sunday — fold with the F4-done reorg remainder] [ok:big-rock ops/evals reorg touches deploy.sh/CI - needs a focused careful session, not an EOD roll; keeps deferring to higher-value work] [ok:2026-07-14 roll — big-rock ops/evals reorg keeps yielding to higher-value work; needs a dedicated deploy.sh/CI-careful session, not an EOD roll] [ok:operator-CLOSE reconcile 7/17 — verify-live pending Monday market / remaining-deliverable / event-gated] >> RE-HOMED to #419 Phase-2 (operator 2026-07-24 at the v1.0 declaration): ruled NOT v1.0-blocking, removed from the closeout blocking set (v1_closeout_status.BLOCKING_TASK_IDS). Deferred post-launch; no date pressure. [ok:re-home to Phase-2, operator 7/24]
- #299 | 2026-08-01 | blocked | P2 tape-features. BLOCKED ON FUNDING (operator decision). RIG DONE 6/17 — SLICE A+B shipped, eval rig built + machinery-verified (scripts/eval_tape_judge.py; binding finding: OR÷ATR available+ORB-tradeable for 79.1% of HIGHs; 11 tests; doc tape_judge_eval_299_2026-06-17.md), baked into prod image. FULL RUN DEFERRED 6/17 — requires FUNDING (~$170 full, or ~$50-90 directional first pass; ~3,420 Opus calls). TRADE-OFF of waiting: the judge keeps grading WITHOUT the tape axis (opening-range violence / premarket pace / liquidity) — an ENHANCEMENT we forgo, NOT a regression; no realized harm, the rig is turnkey whenever funded. NEW DATE 6/25 = decide-fund-and-run as the first step of post-launch tape-wiring, clustered with the #320/#321 post-launch grade-quality batch (grade-affecting WIRING is post-6/22 anyway). When funded: run → operator labels deltas (verify on theme/audit, ADR 0011 self-cert gate) → if majority-correct, CHANGE_PROCESS + sign-off → wire tape into live _judge_shadow ▶ HOLD (operator 6/24): do NOT fund-and-run the tape eval standalone — BATCH it with ALL judge changes and run the eval ONCE at the pre-#335 checkpoint (batch-judge-regrades discipline — save $/be efficient; cf. feedback_batch_judge_regrades_and_review_yield). Rebumped off 6/25 to the #335 checkpoint. blocked_by:#335 [ok:awaiting operator eval-spend decision (~$170 full / ~$50 subset)] ▶ OPERATOR 7/4: HOLD the spend until the judge pipeline settles (the batching principle, feedback_batch_judge_regrades: one batched eval at the pre-#335 checkpoint, not per-axis spends) — the pipeline is NOT change-complete (#347 corpus flip pending, #328/#330 axis shadows landing, #331 undesigned). Fold the $50 subset INTO the #335-checkpoint batched run. [b4] [ok:operator-held to the batched checkpoint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [blocked:eval-fund is an operator $ decision, folds into the batched judge eval — 7/18 CLOSE re-date]
- #255 | 2026-07-30 | pending | judge precedent-retrieval v1 (kind=review) + periodic-review nudge. DoD-7 TRIAGE 6/18 (was an accidental 6/18→6/26 gate-rebump): EXPLICIT POST-LAUNCH decision. This is W5's "experience seed" build — but the W5 LAUNCH BAR ("labeling ritual running") is already MET + accruing live (chart-vision #343, mgmt-judge #300, judge-review #337 each emit operator-label streams). Precedent-retrieval has thin value until that corpus has VOLUME → correctly gated on corpus accrual, NOT launch-blocking. Clusters with the W5 ritual program #307/#219/#254 (7/08). defer_until:2026-07-30 [b3] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [blocked:corpus-accrual — precedent-retrieval thin until the operator-label corpus has volume; re-check 7/23] [blocked:operator decision/gate — 7/23 CLOSE re-date to 2026-07-30]
- #301 | 2026-07-25 | pending | P1 ensemble-divergence SHADOW (2nd-model verdict on HIGH-tier). DELIBERATELY DEFERRED to post-cutover (✅ operator-confirmed 6/19 "revisit after cutover"; named reason, not skipped): it adds a 2nd-model call in/near the grade path, and per the trade-set-stability discipline that gates #335 post-launch, new grade-path-adjacent code does NOT land pre-6/22 GO — even as shadow. Clusters with the post-launch grade-quality batch (#320/#321/#299/#335). Distinct from #233 (Perplexity grade AS a judge INPUT, CHANGE_PROCESS-gated) — #301 is a pure zero-authority divergence MONITOR (2nd model grades independently, divergence logged for operator visibility, no grade change); complementary, build as the shadow telemetry version. Ready to build post-cutover (design: integration at the HIGH-tier judge verdict; cheap 2nd model ~2-5 calls/day; telemetry table + digest section, consolidate-not-proliferate). blocked_by:#335 [ok:blocked_by-#335, eval runs 7/3] [b4] [ok:blocked_by #335 checkpoint (7/18-adjacent); build the shadow when the corpus settles] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> BUILD-SPEC'D 7/11 (block 1 T2, docs/analysis/block1_tier2_sketches_2026-07-11.md): trigger post-judge-HIGH persist · identical payload, model-swapped Sonnet, fail-open · mi_judge_divergence append-only table · ONE weekly-review line, no Telegram · gated review ≥30 rows (>25% divergence → grounding investigation). Card mechanical when the post-launch grade-quality batch (#320/#321/#335) opens. [blocked:post-launch grade-quality batch, gated on the #335 M1-d ruling — 7/18 CLOSE re-date]

## Live-money cutover — MAGNA53 (the GO-direction gates)

- #503 | 2026-08-01 | pending | 🔴 **LIVE COHORT IS 0-FOR-9 (−7.50R) — per-trade forensic: variance, or an upstream defect?** Broken out of #454 at the operator's call 2026-07-24: nine losses on real money is not a footnote to a calibration task. FACTS (verified vs prod 7/24, `mi_live_trades` account_mode='live' status='closed'): **9 closed, 0 wins, losing streak 9, cum −7.50R (−$180), mean −0.833R**, spanning 2026-07-06 → 07-24; **9/9 in NON-BULL regimes** the #268b envelope never priced (`docs/analysis/454_band_review_2026-07-24.md`). **THE QUESTION IS NOT 'RETUNE'** — at n=9 nothing clears CHANGE_PROCESS rule 1, and the band review already ruled KEEP. The question is diagnostic: **is this normal variance for a 20-30%-win-rate breakout system, or is something upstream broken?** Prior arithmetic to test, not assume: P(9 straight losses) ≈ 4% @30% WR · 7.5% @25% · 13% @20% — uncommon but NOT self-evidently diagnostic. Note the mean loss (−0.833R) is TIGHTER than −1R, i.e. **stops are working; the anomaly is the total absence of winners**, where ~2-3 were expected. DoD = a per-trade forensic on all 9 (entry price vs plan · MFE reached before exit · exit reason · holding period · regime label at alert), each classified into exactly one of: **(a) variance** · **(b) entry quality** — wrong names selected · **(c) entry mechanics** — bad fill / chase / no-trigger (composes #414 D2 gap-through + #500's price-aware fix, live since 7/23) · **(d) exit discipline** — stopped or time-stopped before the move (composes #306 giveback/peak-lock, +$8,075 lock-attributable offline) · **(e) regime** — a momentum system correctly finding nothing in a hostile tape. Output = either "consistent with variance, continue" WITH the MFE evidence that supports it, or a NAMED upstream defect with its fix routed to the owning task. ⚠ **TIME-CRITICAL LINK**: the kill/scale bands mechanically ACTIVATE at n=20 (projected ≈ 2026-08-20, verified cadence 9 closes / 15 trading days) and the band review's arithmetic says **REDUCE at activation is the modal outcome** (needs only ≤ −0.59R over the next 11; current mean −0.83R) — landing a Bull-calibrated REDUCE on a non-Bull tape. Knowing WHICH of (a)-(e) is operating before that fires is the point of doing this now, not at n=20. Route: **Fable** (complex analysis) per the operating model; read-only, no live change; any resulting fix is its own CHANGE_PROCESS + sign-off (THE LINE). Refs: `mi_live_trades`, `mi_ep_alerts`, `mi_market_regime`, `mi_orb_shadow_trades`, #454 (parent), #414, #306, #500, #452. >> ✅ **FORENSIC DELIVERED 2026-07-25** (`docs/analysis/503_zero_for_nine_forensic_2026-07-25.md`, Fable; I verified the load-bearing numbers vs prod myself). **VERDICT: not pure variance, not a broken detector — a 3-part decomposition.** (1) **SELECTION IS ALIVE**: MFE (from `highest_price_seen`, code-verified as a real tracker) shows winners occurred at roughly the calibrated rate — **MANE +7.92R**, NVCR +2.00R, WKC +0.90R, THC +0.64R, WULF +0.48R (plus open SMCI +3.21R). Entry mechanics clean on all 9 (max chase +0.52%, the designed limit); class (c) = ZERO. (2) **THE STRUCTURAL GAP IS EXIT-SIDE → #306**: NO profit protection exists on Days 1-2 (SMA trail needs ≥10 closes · partials start Day 3 · the 9:35 'refresh' re-places the SAME stop · the built giveback hook is DEFAULT-OFF with no live caller). Both runners fully reclaimed: **MANE 7.92R → −0.23R**, NVCR 2.00R → −1.00R. Honest counterfactual: the SIGNED close-below giveback saves only MANE (~+1.7R; cohort −7.5R → −5.8R); reaching NVCR/SMCI needs an INTRADAY-arm variant — that arm-basis fork is #306's decision and now has first live-money evidence. (3) residual −5.8R reads as regime-priced variance. Tally: (a)×1 (b)×3 (c)×0 (d)×3 (e)×2; all 9 exits `stop_hit`, none reached Day 3. **⚠ CORRECTION TO THIS TASK'S OWN PRIOR (which I wrote): 'mean −0.833R is TIGHTER than −1R ⇒ stops working better than designed' is a DENOMINATOR ARTIFACT.** `risk_dollars` is BUDGETED risk; on DEPLOYED risk (`shares×(entry−hard_stop)`, after integer-floor + the 20% notional cap) I measured mean **−0.930R**, median **−1.006R**, **8 of 9 at full stop**. Stops hit exactly as designed — the 'tighter' framing was wrong and the inference built on it is discarded. Participation note (descriptive, n=4, no change proposed): 4 of 9 entries BYPASSED the regime-raised threshold (judge-promote ×2, earnings-override ×2) and all 4 lost (−3.78R, max MFE +0.90R). DoD MET — propose CLOSE once the operator has read it; live decisions route to #306 (arm basis) + #454 (denominator). >> 🔑 **HOLDING-PERIOD CUT 2026-07-25 (operator-prompted — 'what's typical for a run to last?'). This may be the most important number in the whole forensic.** PAPER cohort: 34 closed, **10 WINNERS, mean hold 4.76 days** — and **every single winner was held ≥4 days** (BW 16.0d $2492 · GOOGL 18.0d · CRSR 12.2d $1391 · QURE 15.0d · IBM 19.0d · RCAT 8.1d $1166 · FPS 24.0d · PURR 8.0d · FTRE 7.0d · KURA 4.0d); the P&L is concentrated in 8-16 day holds. LIVE cohort: 9 closed, **0 winners, mean hold 0.28 days (6.7 HOURS)**. Within live, holding time tracks MFE almost perfectly: the 5 trades dead inside 25 min had MFE ≤0.48R (4 never went green at all), while the only two to survive past a day reached **MANE 7.92R (24.0h)** and **NVCR 2.00R (30.1h)**. ⚠ **THE THESIS THIS SUPPORTS: a run needs DAYS-to-WEEKS to become a run, but the exit schedule's first protection arrives on DAY 3 (partials) / ≥10 closes (SMA trail) — i.e. AFTER the window where runners are actually lost.** MANE and NVCR are the live analogues of the paper winners and both died in the unprotected Days 1-2 gap. ⚠ HONEST CAVEAT: 8/9 live exits were FULL stops on deployed risk, so most live losses are price genuinely going against us, NOT premature exits — the comparison is not apples-to-apples and only MANE/NVCR are true giveback cases. ⚠ SECTOR EFFECT (operator asked, e.g. 'bio runs fast'): **NOT ANSWERABLE at n=10 winners** — and the data cuts against the folk model anyway (QURE bio 15.0d, KURA bio 4.0d). A real sector/character cut needs the sector join (`mi_stock_scores` / `mi_ticker_overrides`) over a bigger cohort — filed as the next cut, NOT concluded here. ⚠ ORB-shadow cohort is NOT usable for this: 14 closed, **0 winners**. >> ✅ **RE-SCOPED DURABLE + RECURRING 2026-07-25 (operator: "this needs to run a bunch more times as n grows... it needs different regimes, themes, conditions to get right").** This is explicitly **NOT one-and-done**. Mechanised in `data_gated_reviews.yaml::exit_tune_cohort_review` (predicate = COUNT of closed live trades; **threshold 20 → 40 → 60 → 80 → 100**, re-bumped each run — the same recurring idiom as `model_selection_quarterly_review`), so it AUTO-SURFACES weekly instead of depending on anyone remembering. Each run re-answers on the larger cohort: (a) the per-trade forensic · (b) the intraday-partial sweep (1/3 out at trigger L then stop to breakeven; L swept over %, R-multiple and ADR/ATR bases) · (c) **SEGMENTED BY CHARACTER** (operator 7/25: fast/gappy/small-cap/high-ADR names need more aggressive taking than slow large caps — reuse `pivot_analysis.character_profile`, do NOT invent a parallel taxonomy) · (d) the holding-period distribution. **Populating (c) is the main reason it recurs** — at n=9 the character buckets hold ~2-3 trades each, which cannot support a segmented rule; n=40+ is the realistic floor. ⚖ Produces EVIDENCE + a recommendation only; every exit-rule change stays CHANGE_PROCESS + #151 + sign-off. **TERMINAL CONDITION**: once the exit rule is finally decided and signed, this CONVERTS to a routine monthly/quarterly EFFECTIVENESS review (did the shipped rule do what the calibration predicted?) — re-keyed onto a date cadence instead of an n-threshold. **Do NOT close it at that point; convert it.** ETA tracks the review's earliest_review_date, not a guess. [b1] [ok:operator-approved re-scope to a RECURRING n-gated series 7/25 — the ETA now tracks data_gated_reviews::exit_tune_cohort_review.earliest_review_date, not a guessed date] >> ⚠ **MFE TABLE CORRECTED 2026-07-25 (found by the #306 intraday card; I verified from cached Polygon minute bars).** `mi_live_trades.highest_price_seen` is **FILL-SEEDED + polled every ~5 min**, so it is BLIND on holds under ~10 minutes — for CRCL and WDFC it equals the entry price EXACTLY (70.650 / 296.235), which the forensic read as 'MFE 0.00R, never went green'. **Real intraday peaks: CRCL 72.855 @09:37 ET = +1.62R (+3.12%) · WDFC 298.899 @09:31 = +0.29R.** So CRCL did NOT fail to go green — it made +1.62R and gave ALL of it back inside 9 minutes, which reclassifies it from (b) entry-quality toward (d) exit-discipline. **The exit-side case is STRONGER than the forensic stated: 3 of 9 closed trades reached ≥+1.6R intraday** (CRCL 1.62R · NVCR 2.00R · MANE 7.92R), plus SMCI 3.21R open. ⚠ Any future MFE work must use minute bars, NOT `highest_price_seen`, for sub-10-minute holds — a real instrumentation limit, not a data gap.

- #184 | 2026-08-15 | in_progress | broker-authoritative trade-state mirror (ADR 0008) — increments 2/3 (inc.1 write-side regression fence SHIPPED 6/6, audit_trade_state_demotions.py). (2) read-only DB↔broker coverage-drift detector (observe-only, no mutation) + ingest untracked broker orders (the a41e7c6a mirror gap) + fix /syncnow ("Unknown command" despite full wiring); (3) guarded auto-correction (broker-wins, gated by the #137 degraded/empty-read guard — never the first increment). DoD: coverage-drift alerts live + mirror complete + /syncnow works → cutover prerequisite. SSoT docs/decisions/0008-trade-state-broker-source-of-truth.md ▶ **PART (c) /syncnow RESOLVED 6/24 (STALE-OPEN):** wiring is COMPLETE on main (4 sites align); the "Unknown command" was a zero-width-char input artifact ALREADY FIXED 6/04 (`_normalize_slash_cmd`, commit 6fe4114, ancestor of HEAD) → LIVE (container redeployed many times since). Operator: re-test /syncnow in Telegram to confirm. The only real gap = MISSING routing-test coverage, now ADDED (`test_operator_commands_partialnow_syncnow.py` + `/syncnow` in the `test_execute_task_routing.py` freeze; 83 pass; test-only → no deploy). ⚠ ADR 0008 text "/syncnow currently broken" is STALE — fix when (a)/(b) land. REMAINING (updated 7/5): (a) BUILT 7/5 (broker/coverage_drift.py, D1/D2/D3 + 24h DB-dedup + degraded-read guard, wired into the 15-min reconcile; card-built Fable-reviewed; VERIFY-LIVE Mon 7/6 = first reconcile cycles write coverage_drift audit rows, expect quiet/D2-INFO only) + (b) broker-order ingest = trade-state mutation -> careful tested session, the a41e7c6a closer. [b5] [ok:(b)-ingest build + #151 exercise VERIFIED-LIVE 7/11; only the operator dry_run→live_r1 flip remains → date-fires 7/25 via broker_order_ingest_promotion, not a can-kick] [ok:date-roll-0701] [ok:#418 glide path - (a) DONE 7/5, ETA is the (b) ingest careful session Fri 7/10] >> FABLE-WINDOW NOTE (operator 7/5 eve): the (b) ingest DESIGN-to-execution-depth lands Tue 7/7 in the Fable block; the build+#151 exercise stays Fri 7/10 on Opus+cards. >> (b) SPEC DONE 7/6 EVE (Fable block 2 pulled forward from Tue; docs/analysis/184b_broker_order_ingest_spec_2026-07-06.md): 3 case classes R1 stop-pointer-repair (the a41e7c6a class itself, LOWEST risk, enables first) / R2 untracked-entry reconstruction / R3i untracked-position (dry-run-only until observed); 3-state toggle off->dry_run->live-per-class, each enable operator-signed after >=3 clean dry-run proposals; COID parse+validate, no-overwrite re-asserts, per-cycle cap 2, #151 paper exercise scripted, 7 unit tests specced; #436 reuses the same plumbing (both mirror directions, one mechanism). REMAINING = Fri card builds + dark deploy + paper exercise + phased enables. >> (b) MODULE BUILT 7/11 (Sat): `broker/order_ingest.py` — plumbing (FAIL-CLOSED 3-state toggle `get_ingest_mode` off/dry_run/live_r1-3; COID parse+validate; proposal dedup + Telegram; per-cycle cap 2) + R1 stop-pointer repair LIVE-CAPABLE (no-overwrite re-assert `IS NOT DISTINCT FROM`; upserts mi_live_orders) + R2/R3i DRY-RUN proposals only (recon contract §3; live write awaits observations + sign-off). Wired into `detect_coverage_drift` AFTER detection in its OWN guard (a throw can't abort FL-4 detection; loud `ingest_error` audit). 10 tests (COID truth table · fail-closed toggle · R1 repair-when-live / dry-run-writes-NOTHING / no-overwrite-live-pointer / cap / foreign-never); suite 2904 green + boundary/no-silent/job-partition gates. Advisor-vetted (fail-closed + no-break-detection = the 2 killers). REMAINING = #151 paper exercise (`scripts/_184b_ingest_paper_exercise.py`, run on paper this session) → dark deploy (toggle 'off') → operator flips `dry_run` → ≥3 clean proposals → sign-off enables `live_r1` (CHANGE_PROCESS, THE LINE). >> DARK-DEPLOYED (apollo-execution) + #151 EXERCISE VERIFIED-LIVE 7/11: the paper exercise PROVED R1 end-to-end on REAL paper Alpaca — placed a real SELL stop, NULLed the DB link, `run_ingest(live_r1)` repaired stop_order_id → the real broker stop (`ingest_reconstructed` + `stop_order_id_changed reason=ingest_r1_repair` audit), `dry_run` mutated NOTHING; clean cleanup, zero residue, toggle DARK (no row → 'off'). Deploy-gate fix: R1 routes through `order_manager.set_stop_order_id` (T1.5a authorized owner) with a no-overwrite `expected_prior` compare-and-set. GO-LIVE TRIGGER wired (operator standing rule 'dark always needs a built-in trigger to go live'): data-gated review `broker_order_ingest_promotion` date-fires 7/25 to drive off→dry_run→live_r1 (can't stall dark). PRODUCTION PATH CONFIRMED (advisor 7/11): the scheduled reconcile that calls detect_coverage_drift = `order_status_reconcile` ∈ EXECUTION_OWNED_JOB_IDS → the execution deploy carries the new wiring in the LIVE job (dark). So the module is paper-verified AND the prod job path is confirmed; only the first real PROPOSAL awaits the dry_run flip (not a #173 0-rows trap). R2/R3i now have tests too (propose-without-mutate + foreign-never; 13 tests total). REMAINING (all operator-gated, THE LINE): flip `dry_run` → review ≥3 clean R1 proposals → sign off `live_r1` (CHANGE_PROCESS + ADR 0008 build-status). R2/R3i stay dry-run. [ok:operator wrapped early 7/10 (busy) — the careful (b)-ingest session is TODAY's focus, Sat market-closed = ideal] >> **INGEST dry_run FLIPPED 7/13 00:00 PT (rulings-pack R1, operator-ruled)**: criterion amended on the gate (2-leg: synthetic-R1 paper exercise + ≥5 clean dry_run days), review pulled →7/17; execution image rebuilt same hour (freshness ✓). LEG-B clock day 1 = Mon 7/13. live_r1 sign-off ~7/17 → FL-4 green ~7/24. >> R2-PRECONDITION FINDING (7/17 F2b soak review): the ingest's ONE dry-run proposal (CLSK 7/14 10:00:00 ET, r2 propose_entry_reconstruction) was a FALSE POSITIVE — the row existed since 9:31 (same order id) and was being cancelled by the 10:00 ORB-window cleanup at the exact scan second; the matcher lacks cleanup-window awareness. MUST fix before any live_r2 promotion (r1 unaffected — promoted 7/17). >> ABSORBED #436(b) self-heal 2026-07-18: #436(a) phantom-prevention verified-live 7/17 (expire_stale_proposals ran clean, 0 stale) + CLOSED; its (b) cosmetic self-heal now lands HERE as an explicit deliverable — fold a broker-absent stale-proposal expiry (DB->broker direction) into the same 15-min order_status_reconcile job this ingest work already extends (one reconcile mechanism, both mirror directions). 3-state guard: pending_confirmation safe-cancel, confirmed/submitting must be broker-absent OR ingested via R1. Low-priority cosmetic (fork B already removed the cap-starvation harm); ~7/25 with the ingest reconcile. >> RE-HOMED to #419 Phase-2 (operator 2026-07-24 at the v1.0 declaration): ruled NOT v1.0-blocking, removed from the closeout blocking set (v1_closeout_status.BLOCKING_TASK_IDS). Deferred post-launch; no date pressure. [ok:re-home to Phase-2, operator 7/24]
- #316 | 2026-08-15 | pending | PDT / SEC-FINRA Rule 4210 — confirm ALPACA's rollout (not Fidelity's) + relax BLOCK_PDT_LOCKOUT via CHANGE_PROCESS (memory pdt_rule_4210_change_2026; revisit at cutover) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5 - external gate (Alpaca 4210 rollout), check at M3/M4]

## Family A — consolidation plays post a runup (ADR 0013 · reset of #270)

- #502 | 2026-07-27 | deployed | **HTF fresh-deal-pin filter — ✅ SIGNED + SHIPPED + DEPLOYED 2026-07-24 (operator: "yes to all three, ship it").** Trigger: operator 7/24 — the nightly HTF digest surfaced ATAI as the single 🌀 COILED actionable setup; ATAI is a buyout. DIAGNOSED (`docs/analysis/htf_deal_pin_fresh_2026-07-24.md`, full evidence): a cash-deal pin is mechanically INDISTINGUISHABLE from a coil — the tightness that scores it COILED *is* the pin. Both existing M&A layers structurally cannot catch a FRESH pin: (a) Polygon news returned only 2 ATAI articles in 21d, neither with an M&A keyword (#416 guards NOT implicated — nothing ever matched to veto); (b) the deal-pin backstop needs median-range over a fixed 10-session window <0.5% + ≥5 sub-0.5% days, which a 6-session-old deal cannot fill (ATAI median 1.735%, 1 sub-0.5% day). Measured: the backstop has fired **5 times ever** (KALV, ASRT) and caught KALV ~29 days AFTER its first 4-session COILED leak; AVNS/CCRN/PAYO/ATAI leaked and were never caught. PATH TESTED, EVIDENCE AGAINST (documented §6 — my LEAN is don't extend it at N=2; ⚖ NOT my ruling, `is_likely_ma` is shared with the EP/9M money paths so scope is the operator's): reusing the EP Claude-classifier `catalyst_quality='mna'` verdict — it lives only in `mi_audit_log` (the filter suppresses before the alert row is written), and the store is 1-true/1-junk (ACLS graded `mna` while its own summary reads "No recent news or catalysts found" + a second event describing a DIFFERENT company; ACLS then ran $164→$191). Guards cannot separate them — `matches_mna_keywords` returns None for ATAI too. PROPOSAL: a two-axis conjunction on the flag path only — 5-session price band ≤2.5% AND ≥5× volume spike (max vol last 10 sessions / avg vol sessions 11-40). 405-row replay (window 05-04→07-24, 89 tickers, ⚠ single-regime): affects 11 rows / 4 tickers (ATAI/KALV/CCRN/PAYO), preserves 394 incl. HUM (+25.7%) and SXT (+7.2%). Every fresh pin ≥12.6×, every non-pin ≤2.9× — band ∈{2.0,2.5,3.0} × spike ∈{5,10} all select the same 11 rows, so the threshold is not load-bearing. Self-healing (deal breaks → band widens → suppression stops; no new ban surface, per the `mi_theme_exclusions` scar). REFINEMENT of the 2026-05-11 deal-pin change, not a reversal. ⚖ BLOCKED ON OPERATOR: CHANGE_PROCESS rule 3 forbids me classifying the filter list — the 11 rows are in §4/§5 for your judgment; forks in §7 (sign the conjunction? · AVNS-class mature pins left to the old rule? · correct ATAI's 2 stale COILED rows = a prod data mutation). DoD = signed → implement in `flag_detector.py` + change-log in `docs/setups/htf.md` (same commit; the SSoT documents geometry gates but NOT the two existing M&A layers — backfill them) → replay-verify ATAI flips / HUM doesn't → verify-live = ATAI absent as COILED from the rendered nightly HTF digest. Scope: flag-only, HTF is shadow/no-money; EP/9M extension deliberately out of scope (money paths, own sign-off + own FN number). >> **SHIPPED 7/24 (all 3 forks signed):** layer-3 conjunction in `flag_detector.py` (`_evaluate_fresh_pin`, band ≤2.5% AND spike ≥5×; single fetch now serves both evaluators, rows sorted defensively since the fresh rule slices positionally; both fail OPEN) + SSoT change-log in `docs/setups/htf.md` SAME COMMIT incl. a backfill of ALL THREE M&A layers (they had lived only in flag_detector.py, absent from the setup doc) + 12 tests (suite 3622 green) + `scripts/probes/_502_fresh_pin_replay.py`. **PRE-DEPLOY GATE PASSED** — replay used the SHIPPED `_evaluate_fresh_pin` over all 405 actionable rows (NOT a lookalike sim; the #416 lesson) and reproduced the signed numbers exactly: 11 suppressed (8 fresh-only + 3 both) across ATAI/CCRN/KALV/PAYO · 393 preserved · HUM free at ALL 5 of its rows. Deployed market-agent (fa6e45e, all preflight green; G6 skipped — market closed). VERIFIED IN RUNNING CONTAINER: ATAI fresh_pin=True/mature_pin=False · HUM 5.02%/1.6× not suppressed · KALV/PAYO/CCRN evaluated at today's date now trip the MATURE rule — the fresh→mature handoff working as designed. **Fork 3 DONE (operator-approved prod mutation):** ATAI's 2 pre-deploy COILED rows (7/23, 7/24) rewritten to `unqualified`/`mna_filter:deal_pin_fresh` in a guarded transaction (+ an audit row marking the manual backfill); the 7/24 actionable board is now EMPTY — ATAI was its only entry. Fork 2 per ruling: AVNS-class mature pins stay with the existing layer-2 rule. ⚠ **VERIFY-LIVE = Monday 2026-07-27 17:25 ET** — the flag scan is `mon-fri`, so the next rendered nightly HTF digest is the first that can confirm ATAI is absent as COILED. Do NOT close on the green suite or on the DB row — the RENDERED digest [[verify-operator-facing-surface]]. ⚠ calibration is single-regime (05-04→07-24) → re-cut at the quarterly band review, per the #454 bull-conditional finding.

- #402 | 2026-07-30 | deployed | [ok:5/7 items applied + integrated + DEPLOYED 7/18; verify-live Mon w/#356 (median liq gate + populated flagpole fields on the next break). Deferred: item-1 order_manager money-path + item-6 agent.py rename (rides #444)] **HTF (#356) Phase-3 shadow /simplify cleanup (post-stability; ALL shadow, no live path).** From the 4-agent /simplify 6/28 (advisor: defer ALL — the night real money was armed). (1) Boundary-safe shared PURE-MATH module: stop_limit_buy_price + the VIX sizing arithmetic (the 0.5/0.20/0.02 magic numbers, dup'd flag_detector↔order_manager) + the R-settlement loop → ONE pure module imported from both sides (the #154 execution boundary is WHY it should exist, not why 3 copies are OK). ⚠ the R-settlement piece = parameterize anticipation.entry_bet_outcome (add entry_price + start_offset) → touches the LIVE anticipation path → gated, under change-discipline. (2) The ADV floor uses a MEAN (sum(volume)/len) but the canonical adv_20 is a spike-robust MEDIAN — pull the existing primitive (search-before-build). (3) flagpole_ratio/flag_depth_pct NULL stub is over-scoped — the inputs (pivot_high/base_low/runup_pct) ARE persisted → a 2-line compute at the insert site (supersedes the Phase-1-persist follow-up). (4) Free wins: dead slice-guard flag_detector:585; max(within or cands) collapse; fix the _htf_settle_from_bars docstring (overclaims a reuse it doesn't do). (5) per-break conn-acquire in the shadow insert loop → fold into the existing async-with conn block. (6) #356-RENAME LEFTOVERS (display, no-money): the `/setup` handler still prints "🚩 FLAG" for mi_flag_candidates rows — rename to "HTF" to match `/flags` single-ticker "HTF History" (agent.py `_handle_setup_query`; operator caught 6/30 — same class as the /htf skip-set bug). (7) FREEZE the /htf regression: add a handler-level test "bare `/htf` → the board, NOT a single-ticker lookup for HTF" — the #356 rename broke /htf by omitting "HTF" from the ticker-skip set (FIXED 6/30, 949838b) but the routing test only froze WHICH handler, not the within-handler parse → the regression slipped; a parse-level test stops the next rename re-breaking it. Refs the 4 /simplify agent outputs 6/28. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> PARKED-FOR-CARD 7/16 eve (10-min timebox hit): the 6/28 /simplify line numbers + claims have DRIFTED (flag_detector:585 dead-guard gone; compute_flag_metrics no longer computes flagpole_ratio/flag_depth_pct — item 3's '2-line compute' needs formulas re-derived, not copied). Fri card: re-verify all 7 items vs TODAY's code, apply what holds; item 1's order_manager side = Friday daytime only (money path, deploy+operator awake); item 6 (agent.py rename) AFTER the #444 card lands (same file). >> 7/20 VERIFY (CORRECTED — I first checked the wrong table, then wrongly suspected a key-bug): the flagpole fields are NOT missing and NOT a bug. They live in mi_htf_breakout_shadow (HTF entry-shadow #356-P3), NOT mi_flag_candidates (the detect table I mis-checked). Compute+wiring CORRECT + deployed 7/18 (#402(3): flagpole_ratio=1+runup_pct · flag_depth_pct=1-base_low/pivot_high; the break dict maps pivot_high_price→pivot_high at flag_detector.py:1665 — traced, no key mismatch). The 6 existing shadow rows are NULL only because they ALL predate the 7/18 deploy (latest break 7/07). So the flagpole-populate confirmation is EVENT-GATED on the NEXT flag-high break (none since 7/07; breaks are rare — 6 ever). Median-liq-gate likewise needs a qualified break. >> ⚠ **STALE-SENTENCE STRUCK 7/24** — the trailing "parked /simplify 7-item re-verify (7/24 card)" line was left over from a 7/20 note written BEFORE the 7/18 apply was reconciled into this same line, and it generated a DUPLICATE card tonight (Sonnet re-verified all 7 items vs today's code: 4 applied in `0763105` on 7/18, 2 correctly not-applied [item 1 money-path, item 6 blocked], 1 already-done — **nothing remained**). Real waste, exactly the class the `deployed` status exists to stop. Remaining here is ONLY the event-gated flagpole/median-liq verify on the next HTF break (none since 7/07) → re-dated 7/30. Item-6 note: the /setup label is now `🏁 FLAG` (emoji drifted, substance unchanged) and the #444 blocker is FILE-level not logic-level (#444 re-scoped to money-path finalizers; its one agent.py touchpoint is `mode_prefix()` at :1165, unrelated to `SRC_TAG` at :3299) — operator may unblock item 6 independently.
- #327 | 2026-08-02 | in_progress | **#270 Step 3 — consolidation entry-watch + shadow-settle + 9M-universe feed (critical path for #326).** ✅ PHASE A (the #326 DIRECTIONAL READ) DONE 6/18 — replay-first (live shadow can't reach decision-grade N by 7/7). Symmetric two-arm replay over the 121-row mi_9m_day2_candidates cohort (Day-2 ORB vs consolidation FIRST5-break, same SETTLE_RULE/forward window, full-universe expectancy, both-ways universe, MFE-free via anticipation.simulate). scripts/_327_replay.py + _327_pull_minute.py + _327_dump.py; doc docs/analysis/ninem_consolidation_vs_day2_replay_327_2026-06-18.md (advisor-hardened: within-name cut + split claims). VERDICT: **Claim A (SOLID) — Day-2 ORB earns NO robust edge** (filled median −0.24R, +3.2R total all 3 outliers, −1.4R on the 17 consolidation names) → carries the #326 RETIRE half; **Claim B (PROMISING, selection-inflated → Phase-B-gated) — consolidation better** (+2.00R median, 82% win, broad). Mechanism = stop tightness (first5-low vs wide prior-day-low). ✅ PHASE B — OFFLINE caveat-#1 correction DONE 6/18 (advisor-reconciled): extended _327_replay.py (consolidation_scan exposes every post-coil day w/ as-of base_high; Phase-A confirmed-close numbers byte-identical) + wider 266-day minute pull → re-timed Arm-2 to the FIRST intraday OR-break a live watcher takes. ⚠ MATERIAL FINDING (changes the #326 replacement narrative): **Claim B's +2.00R was an ENTRY-MODEL ARTIFACT, not a live edge** — inflated jointly by (1) daily-close selection (20/35 names break intraday on an EARLIER, weaker-close day; filled median +2.00R→−1.00R re-timed) AND (2) a 2%-sub-base OR tolerance (only 7/17 Phase-A "fills" had OR5-high actually ABOVE the base). De-rated, consolidation (+3.7R loose / +5.0R genuine, both outlier-carried) drops into the SAME break-even bucket as Day-2 ORB (+3.2R) → the QUANTITATIVE case for "consolidation is the strong replacement" is GONE. HARNESS LIMIT (explicit): every Arm-2 variant is an OR-break-NEAR-base proxy, NOT the live all-day volume-confirmed #94 watcher (blind to mid-session base breaks) — so it CANNOT settle whether the real watcher has an edge; no single de-rate number is faithful. **Claim A (retire Day-2 ORB) UNCHANGED/solid** (Arm-1 has no base-break gate). RESOLUTION = the live forward shadow with the REAL #94 detector is now the ONLY instrument that can answer the live-edge question → measure-before-wire JUSTIFIES (not moots) the live wiring. ✅ ENTRY-SIGNAL STUDY DONE 6/18 (operator "what signals the best entry"): scripts/_327_entry_signal.py — parameterized coil-readiness (rmv+range+vol gate × consecutive-day COUNT × stop), stop-matched curves not an argmax cell. FINDING: **PERSISTENCE (N consecutive tight days), NOT rmv DEPTH, times the entry** — N=3 lifts capture 32%→44% + median uncapped MFE +0.8R→+2.1R, STOP-MATCHED + robust across the rmv/range/vol knobs (Δ+9–12pp); VANISHES under over-tight level gates (range≤4% Δ0). Robust REGION: range≤5–7%, vol≤1.0×, rmv≤30–40, N=2–3. ✅ FORWARD SHADOW WIRED 6/18 (operator "wire it, we have potential edge, let's not wait"): new mi_consolidation_entry_shadow table + anticipation.entry_signal_at (the SINGLE-SOURCE signal — the offline sweep delegates is_tight to it, pinned by tests/test_consolidation_entry_signal.py, 8 tests) + entry-watch folded into _consolidation_readiness_job (17:35 ET, one shadow row per OPEN coil, open-dedup pins first-fire, 9m/family_a origin tag, point-in-time) + Telegram digest. Stop recorded coiled_low (validated default) + structural_low (alt) for later settlement. ALL SHADOW (zero execution). ✅ VERIFY-LIVE DONE 6/18 (deploy b8245ec, both): write-probe proved the insert path (insert✓ open-dedup✓ CHECK✓ self-cleaned) + the real job wrote **18 sane backfill rows** (all N=3, rmv≤28, range 1.8–6.0%, vol≤0.98×, both stops, HLIT correctly tagged [9m]) + a 2nd run held at 18 (live open-dedup confirmed). Container fresh on HEAD; job 17:35 ET mon-fri (un-paused) → tomorrow's run is the first scheduled fire. ✅ SETTLEMENT MACHINERY BUILT 6/18 (operator "build the machinery now, turnkey before data lands"): anticipation.entry_bet_outcome (the validated asymmetric bet — capture/stop/open + uncapped fwd_mfe_r under coiled_low, 12-bar horizon; the offline sweep's bet_outcome now delegates to it, pinned) + settle_entry_shadow (abstains <12 fwd bars, else outcome+fwd_mfe_r+realized_r; realized_r REUSES the tested settle_row +1R/+3R day-5 harvest) + db settle/fetch/summary helpers + _consolidation_entry_settle_job (17:40 ET mon-fri, registered + INTELLIGENCE_OWNED) + tests/test_consolidation_entry_settle.py (11 tests). SURVIVORSHIP FIX (advisor 6/18): settle on DEFINITIVE outcome (capture/stop final at any bar count; only 'open' waits the full window) so a stop/capture-then-HALT name isn't orphaned (would silently bias capture%); the residual halt-while-open is surfaced via get_consolidation_entry_shadow_summary.open_overdue_n (orphan canary, never silent). Turnkey: settles automatically once a row reaches the 12-bar window (~first settles ~2026-07-07). ✅ VERIFY-LIVE DONE 6/18 (deploy 575b412): all 4 first-run SQL paths clean on real Postgres — settle-probe (UPDATE write-back + double-settle guard + open-dedup-free), run-settle (get_settleable + clean 0-settle), summary SQL (READOUT "18 open / 0 OVERDUE / 0 settled", by-origin splits clean, medians null until ripe). 17:40 ET job registered/fresh. ✅ SURFACE CONSOLIDATION DONE 6/18 (operator "consolidate, don't add a new command every time" + plain-words readability; AskUserQuestion: Family-A-only + plain words): instead of adding /readout, FOLDED the whole lifecycle into ONE board + ONE digest (6 surfaces → 2). /anticipation now shows edge-header (the readout) → 🎯 entry fired → 🪙 coiling → 👁 post-runup → 📐 settled; the 3 separate daily pushes (🪙🎯📐) collapse into ONE unified Family-A digest by folding settlement INLINE into _consolidation_readiness_job (removed the standalone settle job + its INTELLIGENCE_OWNED id → _run_entry_shadow_settlement helper). format_consolidation_row rewritten to plain words (+70% · coiling 14d · very tight · tightening↓) + new format_entry_fired_row/format_entry_settled_row (single-source, all pinned). RULE going forward: new lifecycle stages = sections, never new commands. ✅ VERIFY-LIVE DONE 6/18 (deploy 12d8aff): /anticipation board rendered off the 18 live rows — edge header + 🎯 entry-fired (12, HLIT tagged 9M, stop %s) + 🪙 coiling (tightness word) + 👁 post-runup (runup + "Nd since peak", tightness word omitted — a live-render readability fix, the rmv-axis would contradict the not-coiled state) + 📐 settled (0). ⏭ REMAINING: nothing until data accrues → OBSERVE-ONLY, first settles ~7/7 answer the #326 live-edge → if confirmed, the shadow→PAPER graduation (register + wire entry → phase=paper, in PARALLEL with live magna53) = #353. Execution-side/split-adjacent [b5] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [ok:observe-only — entry-shadow accruing forward data, no action pending settle] >> DISPOSITION 7/14 (operator: DON'T abandon — we're mechanizing a REAL setup; a mechanical loss means we're MISSING something, not that the edge is absent). SPECIFIC = the ANTICIPATE coil-apex entry (anticipation.entry_signal_at): runup peak → 3 consecutive tight days after the peak → fire AT the coil apex (PRE-breakout), stop=coiled_low. DISTINCT from the CONFIRM arm (enter ON the base_high breakout+volume, stop=base_low — different code path, likely different edge). The −1.04R (N=41) is ALL JUNE entries (one month/regime; July unsettled) → a regime-specific mechanical read, NOT a setup verdict. NOT RETIRING. INVESTIGATE the WHY (next): (a) REGIME split (coils need a trend; was June correcting?), (b) runup/coil QUALITY gates (are we taking coils a discretionary trader skips?), (c) ANTICIPATE-vs-CONFIRM (entering pre-breakout may be the miss — measure the Confirm arm's R). [ok:operator-corrected — diagnose the mechanization gap, don't retire] >> DIAGNOSTIC RUN 7/14 (Fable, Opus-verified): setup NOT dead — anticipate −1.23R/N34, Confirm ~breakeven; we mechanized TIMING not the SELECTION STACK + RISK GEOMETRY + anticipate-vs-confirm. GAPS: ~85% of anticipated coils never broke out (Confirm is the likely edge); coiled_low stop has NO floor (min 0.06%, 64% of loss = gap-through slippage); RS/trend-location quality (⚠ Fable's SQQQ-inverse example was WRONG — SQQQ +2.00R; ETF% unverified). +0.41R counterfactual is IN-SAMPLE/post-hoc — NOT proof. PROPOSAL (operator sign-off): docs/analysis/327_shadow_fix_proposal_2026-07-14.md — quality-gate flag · structural_low stop w/ floor · Confirm control arm · record regime-at-entry · fix measurement bounds; validate FORWARD through a trending leg. ⚠ BLOCKER: the consolidation_readiness settlement job HUNG>2h + aborted 7/13 (Polygon-call hang suspect; 170 rows stuck past-ripe) — defensive-timeout fix SHIPPED 7/14 (operator-signed). [ok:operator-capacity-replan-7/15 — Wed-Fri lightweight, verify/close on the Sat push] >> JOB-HANG FIX VERIFIED-DONE 7/23 (code-checked scheduler.py:3188-3235: SCAN runs under asyncio.wait_for(_CONS_SCAN_BUDGET_S)+FAIL-OPEN → CONSOLIDATION_READINESS_SCAN_TIMEOUT audit event + partial results kept, _run_entry_shadow_settlement then runs UNCONDITIONALLY; no hang events since 7/14; the 7/13 170-stuck backlog drained to 209/247 settled). >> SETTLE-GAP diagnosed 7/23 (2/247 open rows can NEVER settle — root-caused, NOT systemic, canary-surfaced NOT silent): entry_bet_outcome→None when risk≤0 (stop≥entry-close, anticipation.py:1094) → the row abstains every run forever, correctly counted by get_consolidation_entry_shadow_summary.open_overdue_n (orphan canary, advisor 6/18). **CRWD id=280** = a 4:1-SPLIT-DRIFT artifact (recorded entry 701.09/stop 673.61 = a valid ~4% coiled-low stop at record time; mi_daily_closes was later split-re-adjusted to ~175/168 = exactly ÷4, so the STORED stop is now 4× the ADJUSTED bars → inverted). **APGE id=377** = a legit ZERO-RISK edge (the 6/29 entry bar closed exactly on its low: o132.70 h132.83 l132.61 c132.61 → coiled_low stop = close → risk=0). >> NARROW REAL FINDING (shadow-only, no money): settlement compares a STORED record-time stop_price against LATER split-ADJUSTED bars, so a split inside a row's ~17-day open window orphans it — a down-split → abstain/canary (harmless, VISIBLE); an UP-split/reverse-split could instead settle with a garbage inflated-denominator R → silent pollution of the #326 readout (NOT observed yet). The exactly-zero-risk case (APGE) also slips the sub1pct_reject guard (coiled_low NULL → guard skipped). >> OPERATOR-DECISION (working day, on operator-signed settle code — advisor 7/23: don't hand-edit signed settle logic on a skip day): (a) split-adjust the stored levels at settle (or store+apply the adjustment factor); (b) un-settleable-row cleanup — auto-void-terminal vs SQL (⚠ outcome='void' INFLATES summary.settled_n, which keys off outcome IS NOT NULL → a reader change is needed to keep capture% honest); (c) extend the sub1pct_reject guard to risk≤0. The 2 rows are LEFT AS-IS for now (canary=2 = working as designed, trivial per advisor). [blocked:job-hang defensive-timeout fix VERIFIED-DONE 7/14; remainder = operator sign-off on the shadow-fix proposal (docs/analysis/327_shadow_fix_proposal_2026-07-14.md) + the split-drift/zero-risk settle hardening (all shadow, no money) — 7/23 diagnosis complete, re-date to the 8/02 family-evidence checkpoint]
- #353 | 2026-07-26 | pending | Consolidation entry → PAPER graduation (the shadow→paper rung of the promotion ladder; runs IN PARALLEL with magna53 LIVE — per-strategy phase routing, `_check_safeguards` per-mode isolated so a paper strategy can NEVER touch real $). ▶ GATE: graduate ONLY if #327's ~7/7 first-settlements show a real consolidation edge (the #326 live-edge question, on the REAL #94 watcher not the replay proxy); no edge → stay observe-only shadow, do NOT graduate. ▶ BUILD: (1) register consolidation in `mi_strategies` (family=family_a · signal_type · outcomes_table · promotion_model) — it is NOT a registered strategy today (absent from /status); (2) wire its entry into `entry_pipeline.submit_trade_entry` via a consolidation `spec_builder` (stop=coiled_low = the shadow's validated default; per-strategy sizing) — the entry+settle mechanics exist in shadow (`anticipation.entry_signal_at` + `entry_bet_outcome`/`settle_entry_shadow`), so this promotes the shadow RECORDER to a REAL paper-fill path, not a shadow row; (3) `phase=paper` → routes to the PAPER Alpaca account (real fills, fake $); magna53 stays untouched on live. ▶ VALIDATE: accrue closed paper trades → the promotion check (median R / win rate — the magna53 ladder) + digest readout. ▶ PROMOTE paper→live later = a SEPARATE Phase-2-style flip with its own gate (NOT this task). ▶ BONUS: a paper strategy beside live magna53 also yields a real-time IEX-vs-SIP selection-gap control (the GATE-3 thesis). DoD: consolidation registered + `phase=paper` + accruing REAL paper fills with a promotion-check readout (not a shadow row). Refs #327 (shadow edge), #289 (selection layer), #326 (retire-9m-for-consolidation). [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> ON HOLD 7/9 (operator): gated on the #327 consolidation-edge gate — do NOT build until #327's first-settlements show a real edge; step 1 is to CHECK that gate, then it's an operator go/no-go on a real-money-adjacent Phase-2 build. [b3] [blocked:#327 consolidation-edge gate] >> 7/12: #395 RULED NO-GO on real entries; the #327 shadow keeps observing (operator) — this stays gated on a future sign-flip; no date pressure.
- #354 | 2026-08-02 | pending | MERGE flag_continuation INTO the consolidation play (Family A) — it is NOT a separate strategy, it IS the **Confirm-flag entry (b)** (base-high breakout); operator-directed 6/22 ("flag_continuation = one and the same as the consolidation play"). Code-verified diff (Explore 6/22, file:line cited): same runup-anchored family + SHARED tightness primitives (`_compute_rmv`/`_compute_fresh_tightening` imported by anticipation.py — no dup). DIFFERENCES = flag_continuation carries stale/stricter params; reconcile to the consolidation play's SIGNED model: (1) UNDERCUT — flag_detector.py:513 INVALIDATES on undercut; consolidation ALLOWS it (ADR 0013 §1, operator's veto of undercut-as-gate) → re-gate INVALIDATED→WATCH_UR so undercut routes to entry (c) U&R, not rejection; (2) RUNUP 50%/60d → 15%/10d (Pradeep signed; 50% misses COO@15%); (3) ANCHOR pivot-walk → runup-peak-close; (4) TIGHTNESS ratio gates → volatility-relative RMV/ATR. KEEP: 0.4% tight-close as ranking-not-gate (operator veto), $20M/day universe, the shared primitives. COMBINE: flag's breakout logic becomes Confirm entry (b); retire flag_continuation as a standalone mi_strategies row → ONE strategy = consolidation play w/ 3 entries (Anticipate/Confirm/U&R). ⚠ LOAD-BEARING: flag_detector drives the live /flags board → detection-criterion change = CHANGE_PROCESS (docs/setups SSoT) + N≥10 backtest + operator sign-off (the undercut re-gate = ADR 0013 deferred Phase 4 WATCH_UR). GATED on the #327 ~7/7 edge read (don't merge before the play proves edge). DoD: one strategy, flag_continuation absorbed, undercut→U&R, gates reconciled to the signed model, /flags unbroken. Refs #327/#353/#326, ADR 0013. ▶ GROUNDED 6/22 (read ADR 0013 + CHANGE_PROCESS + flag_continuation.md exists) — CORRECTIONS: (1) this merge IS ADR 0013's already-DESIGNED Phases 2–4 (Family A = one universe + 3 entry modes; §1 "they share ONE universe + ONE coil detection, undercut ALLOWED") — NOT a one-deploy param-swap. (2) the "Confirm" entry is `_flag_break_scan_job` (#94 INTRADAY break) per §4 Phase 3; flag_continuation (5:25 PM EOD) is the REDUNDANT standalone whose role is SUBSUMED + retired, NOT "entry b". (3) the undercut→WATCH_UR fix is gated BY THE ADR (§4 Phase 4: DECOUPLED, SEQUENCE LAST, own sign-off + N≥10 backtest, SSoT flag_continuation.md same commit). (4) reconcile to anticipation.py's SIGNED §2 universe (`db.get_anticipation_universe`, 15%/10d/runup-peak/RMV-ATR) — do NOT edit flag_detector's 50%/60d in place. (5) NOT gated on 7/7 (that gates TRADE-graduation #353); the merge build is its own shadow track. EXECUTE as a focused build (Phase 2 shared universe+coil → Phase 3 the 3 entries → Phase 4 undercut [gated]), after-close deploys, NOT launch day. ▶ BUILT 6/22 (Phase 2-3 SHADOW · commit befb41e · NOT deployed — rides Phase 2): `anticipation.confirm_signal_at` (new PURE breakout detector on the §2 universe via daily bars — deliberately NOT the live #94 job/mi_flag_breaks, which would gate Confirm by the flag 50%/60d cohort = #270 phantom in miniature) + `mi_consolidation_entry_shadow.entry_mode` column (DEFAULT 'anticipate' back-tags existing rows) + the dedup FIX the planner caught (pre-#354 2-col open-index/ON-CONFLICT silently DROPPED the 2nd mode on the same coil → now 3-col ticker/anchor/entry_mode) + `by_mode` readout (ADR §1 never-blend) + `_consolidation_readiness_job` fires BOTH modes into the one lifecycle. 5 tests, full suite 1042 green. ISOLATION verified (anticipation.py pure; entry_pipeline + ep_detector do NOT import it). ✅ DEPLOYED 6/22 after-close (16:50 ET, bundled w/ the mgmt-format deploy · `deploy.sh market-agent` DEPLOY OK · all preflight gates green, G6 skipped=closed · apollo-market fresh · entry_mode migration + the open_mode 3-col index VERIFIED LIVE in pg_indexes; old 2-col index dropped · execution/orch untouched). ▶ VERIFY-LIVE = tonight's 17:35 ET consolidation job writes the first `entry_mode='confirm'` rows + the `by_mode` readout populates (check mi_consolidation_entry_shadow next session). REMAINING (still gated): Phase 4 undercut→WATCH_UR (sign-off + N≥10 backtest); then retire the standalone flag_continuation scan once the play subsumes its role. ▶▶ **SUPERSEDED 6/22 — THE OPERATOR SPLIT (the merge premise was WRONG; un-merge instead).** The flag→consolidation merge built a Confirm/breakout entry on a **structurally-broken** consolidation detector — anchor measured the runup INSIDE the base (STM-class excluded) + the entry fired on quiet DECLINES (BTU/UFO/DRUG) with NO holds/consolidation gate at all. Full diagnosis: `docs/analysis/consolidation_runup_defect_2026-06-22.md`. **Operator decision 6/22 — clean SPLIT + rename:** (1) **HTF (High Tight Flag)** = the former flag/flag_continuation → its OWN setup (big runup → tight base → enter on the BREAKOUT); operator details later (own #-task when they land). (2) **Anticipation** = the former "consolidation play" (#270/Family A) → SIMPLIFIED to STRICTLY the Pradeep tight-day ANTICIPATE entry (enter IN the coil, before the break); DROP the Confirm/breakout merge (#354 befb41e) + U&R. (3) **Sugar Babies** = a stock CONDITION (recurring 9M), NOT a setup → likely a confluence point on a setup, TBD/revisit. DEFINITION = the captured Pradeep blueprint (`operator_shared_notes.md`: 6/16 thread + 6/22 Gemini cross-ref): runup 15%/10d → HOLDS (Upper-Third ≤20–30% retrace + One-Strike ≤1×4% breakdown, **volatility-relative**) → series of tight days (volatility-relative, NR-day) → anticipate. REBUILD PROGRESS 6/22 (SHADOW, committed: `_consolidation_acceptance_test.py` + `_anticipation_holds_measure.py` + `_anticipation_shortlist.py` + fixtures): anchor refined to the recent runup-leg peak (3–20d base, not the global max); holds gate **data-grounded** on the 6/15 Pradeep cohort (declines 2–5 breakdowns/23–34% retrace vs tight names 0–1/≤11%); confirmed the gate must be volatility-relative (HYLN). ⏸ **PAUSED for operator labels** on the 303-name shortlist (`docs/analysis/anticipation_shortlist_to_label_2026-06-22.md`) → calibrate the volatility-relative thresholds → wire into the detector (shadow, sign-off + ADR-0013 provenance rule) → re-validate. The #354 merge text ABOVE is superseded by this split. ▶ **ROADMAP (all 3 setups + next steps + WHY): `docs/roadmap/family_a_setups_split_2026-06-22.md`** — #354 here = SETUP 1 (Anticipation). #356/#357 = the other two setups; #358 = the systemic provenance fix. [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> FABLE-DESIGNED 7/11 (block 1): **ADR 0026 D1** — retire flag_continuation strategy row (phase→deprecated); breakout logic becomes Confirm entry (b) via the #94 intraday event → entry_mode='confirm' shadow rows; params reconcile to the SIGNED family model (15%/10d runup · runup-peak-close anchor · RMV/ATR tightness · 0.4% ranking-only); HTF keeps its sourced 90/40. Regression pins named. REMAINING = sign-off (F3: land D1 with D2+D3 in one flip) → C4 card. >> NOTE 7/11 (premortem prep, prod-verified): the flag_continuation mi_strategies row is ALREADY phase='deprecated' on prod — ADR 0026-D1's retire step is partially pre-done; the C4 card's remaining scope = param reconciliation + entry_mode wiring + SSoT rewrite (don't re-deprecate). >> SITTING-RULED 7/12: **0026 D1+D3 SIGNED · D2 PARKED** (R1 — premise reversed by the #146 probe; reopens only WITH a faithful-harness table, Block 4 T3). Build = machinery-consolidation (R2: each entry mode earns its own N≥10). >> **C4 (D1) DONE 7/19** (sonnet card, Opus-reviewed): flag_continuation.md + htf.md SSoT rewrite (retired-as-standalone → Confirm(b) entry_mode; ADR-0022/#424 deprecation correctly attributed; flag_detector/`/flags` untouched); C5 `consolidation_unification_review` filed (data_gated_reviews.yaml, threshold 10, earliest 8/02 → the #397 money gate); param reconciliation confirmed code-complete (Confirm on the signed §2 universe); regression pins + full suite 3495 green; SHADOW/doc-only, THE LINE clean. REMAINING = C3 (D3 undercut→WATCH_UR, signed 7/12, unbuilt — the sole open DoD item) + the C5 confirm-mode accrual. [blocked:C4/D1 shipped 7/19; only C3/D3 (undercut→WATCH_UR — shadow follow-on, no money path) + the C5 go-live accrual remain, gated to the 8/02 family-evidence checkpoint — 7/19 re-date]
- #356 | 2026-07-30 | deployed | HTF — Setup 2. ✅ DETECTION DONE (6/27, 90/40 + ≤25% + 10/20/50 + Stage-2 200MA/52w-high; flag→HTF rename; docs/setups/htf.md; AGL/XMTR live). ✅ PHASE 3 BREAKOUT-ENTRY SHADOW BUILT + DEPLOYED-LIVE 6/28 (feac9a0; table mi_htf_breakout_shadow LIVE + 0 rows; apollo-market fresh; advisor-vetted 4 corrections; 7 tests): mi_htf_breakout_shadow + insert/settle/summary funcs + prepare_htf_breakout_order (PURE MATH in flag_detector, NOT broker — execution-boundary fix) + the #94 hook (records ALL breaks, would_reject_reason NOT dropped, error-wrapped so it can't break the live scan) + _htf_settle_from_bars (the breakout-fill NUANCE: entry=base_high NOT the bar close, break-day-inclusive) + the settle job folded into the daily Family-A job + the digest "🚩 N settled" self-verify; FIXED notional (R is equity-independent — no live account fetch). ✅ VERIFY-LIVE DONE 6/29: the first HTF break WROTE a shadow row (1 #94 break → 1 mi_htf_breakout_shadow row, all-time count 1) — the Phase-3 shadow hook fires live. Settlement shows ~12 trading days after the 1st capture. ✅ VERIFIES RUN 6/28 (0a4f825): flagpole = CONFIRMED PRIMARY (keep pivot-anchored; a today-anchored trailing-40d diverges ~3/10 but was a non-primary interpretation, REJECTED — operator: HTF is well-defined, use the primary definition not our own [[feedback_established_setup_use_primary_definition]]); liquidity = GAPS (ADV>500k PARTIAL — high-priced names clear $5M dollar-vol on <500k shares; ADR>4% ABSENT). ✅ LIQUIDITY FLOORS ENCODED 6/28: ADV>500k shares + ADR>4% as a per-ticker gate in compute_flag_metrics (so ALL universe paths are covered, not just the organic SQL); TUNABLE named constants (_HTF_MIN_ADV_SHARES firm; _HTF_MIN_ADR_PCT=0.04 STARTING — operator: ADR isn't canonical, DATA-GATED tune `htf_adr_threshold_tune` at N≥10 settled winners); impact 2→1 current candidates; 10 tests. STILL OPEN: (b) get_recent_daily_history trading-rows refactor (calendar-vs-trading ratio-bandaid); (c) persist flagpole_ratio/flag_depth_pct on mi_flag_candidates (recorded NULL now — value-level offline analysis); NB capture-on-same-bar-tie skews capture% slightly high. FLIP shadow→paper = N≥10 settled + CHANGE_PROCESS + sign-off (money gate #397). Then #396 (Phase 4 EMA-trail). Refs ADR 0013, docs/setups/htf.md. [ok:date-roll-0630] [b4] [blocked:HTF Phase-3 shadow N-accrual → #397 GO/NO-GO gate 7/18; verify-live ongoing] [blocked:#397 GO/NO-GO gate is an operator ruling — 7/18 CLOSE re-date] [blocked:operator decision/gate — 7/23 CLOSE re-date to 2026-07-30]
- #397 | 2026-08-07 | pending | **HTF money gate — breakout-entry shadow→paper→live (the real edge validation).** TARGET date — EVIDENCE-gated (the GO rides on the shadow edge data, NOT the date). The detector is SHADOW (no money); the breakout-entry is NEW money-firing capability — does HTF-breakout-trading make money? After ~1-2 wks of `mi_htf_breakout_shadow` outcomes: review the edge (capture / MFE / realized-R on the breakouts) → if it holds, promote to **PAPER** (real fills, fake $) → then **LIVE** (CHANGE_PROCESS + operator sign-off). WAITING ON: #356 + #396 + ~1-2 wks of shadow breakout outcomes. DoD: edge reviewed; paper/live GO-NO-GO recorded. Refs #356/#396, [[feedback_shadow_needs_dated_flip_eta]], THE LINE. >> **DEFER 7/13 (7/18-readiness redteam, operator-signed):** the htf_breakout_paper_graduation gate is mathematically unreachable by 7/18 — mi_htf_breakout_shadow = 6 rows / 0 settled / only 1 takeable; gate re-dated earliest 8/07. Operator set **N>=5 TAKEABLE** (HTF rare → 5 not 10; count only would_reject_reason IS NULL — 5/6 current rows are stop>8% rejects the raw count would inflate). [b1] [ok:evidence-forced DEFER — gate unreachable by 7/18, operator-signed 7/13]
- #357 | 2026-07-28 | pending | **Persistent Sugar Babies — role decision (the 3rd STREAM of the split — a CONDITION, NOT a Family-A setup).** Recurring-9M stock CONDITION (≥3 9M EOD prints/180d, `mi_sugar_babies_cohort`); the surface overlays the flag/HTF stage on the 9M cohort. Operator 6/22: any setup can include a sugar-baby or not → likely an additional CONFLUENCE/SCORE input on a stock already in a setup, NOT a standalone play. NEXT: (1) operator decides the role (confluence/score input vs standalone watchlist); (2) re-frame the surface (cohort = Family-B/EP universe; stage overlay → HTF). DoD: role decided + surface re-framed (or kept observational). Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`. [ok:date-roll-0630] [b4] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> FABLE-DESIGNED 7/11 (block 1): decision memo docs/analysis/357_sugar_babies_role_memo_2026-07-11.md — rec = CONFLUENCE (branch A) staged w/ the axis discipline: Stage-1 DecisionContext visibility (ships regardless) → Stage-2 axis credit ONLY on a green C1 STEP-0 (member-vs-non-member EP fwd returns, N≥15) + sign-off; branch B (watchlist-only) = the automatic fallback encoded in the gate, not a bet. Surface re-frame (C3) = Family-B universe + stage overlay, role-neutral. REMAINING = C1 STEP-0 → operator rules the fork → C2/C3 ship → C4 gated. >> PRE-BUILT 7/11 (#458, Opus): C1 STEP-0 run — `docs/analysis/357_sugar_step0_table_2026-07-11.md` + `scripts/probes/_357_sugar_step0.sql`. **Direction CONFIRMED** (member EP HIGH = 80% win / +11.6% median vs non-member 40% / +7.4%, across **9 distinct names**) — but 11 member-alerts is BELOW the N≥15 Stage-2 gate. REC = ship Stage-1 visibility badge now (safe); HOLD the axis credit until +4 more member-alerts, then re-run this probe. Sitting rules the role. >> SITTING-RULED 7/12: **role = CONFLUENCE (staged) SIGNED** → Stage-1 badge build UNBLOCKED now; Stage-2 credit at N≥15 via the 0028 classifier (R11). [blocked:Sugar Babies 3rd-stream role decision (operator) — 7/18 CLOSE re-date] [blocked:operator decision/gate — 7/23 CLOSE re-date to 2026-07-28]
- #297 | 2026-07-29 | pending | FAMILY B EP rework — inherits the gap-anchored replay()/evaluate_candidate + mi_anticipation_lifecycle + ARCHIVE/clean its phantom rows (decoupled from #270 Phase 1). 9M-ENTRY retirement (Day-2 ORB → consolidation) is the dated cut-over decision #326 (9/15) [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> FABLE-DESIGNED 7/11 (block 1): **ADR 0027** — Family-B inherits the gap-anchored lifecycle: D1 `origin` column + phantom archive-by-tag (per-consumer entry_tactic dodge retires); D2 seeds = 9M EOD confirms + un-entered EP HIGHs (window-out/cancelled/judge-HIGH-unfilled — the #446 cohort); D3 pure inheritance of evaluate_candidate w/ seed-gate bypass for pre-qualified B-seeds → gdl_reclaim shadow entries, per-origin never-blend settlement. `family_b_lifecycle_first_read` review (≥15 settled, earliest 8/25) lands BEFORE the #326 9/15 sitting = its evidence. REMAINING = sign-off (F1/F2) → C1-C4 cards. >> SITTING-RULED 7/12: **0027 F1-F2 SIGNED** → C1-C4 UNBLOCKED w/ register constraints: R4 #445-first · R3 one generalized detect_gdl_reclaim · R9 cooldown_suppressed tag.
- #283 | 2026-07-27 | pending | wick_fill promotion eval (shadow→live), gated behind Family A [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [ok:market push — wick_fill promotion eval needs weekday shadow evidence — operator-directed 7/25, 2026-07-25→2026-07-27]
- #488 | 2026-08-15 | pending | **Authoritative halt data for the LIVE path — Alpaca-WS `statuses` capture (split from #386 on close, operator 7/19 chose A: the RMV heuristic stands for the consolidation guard; THIS is the separate live-path project).** #386's feed audit (branch ba7533b, SDK file:line-verified): the Alpaca WS `statuses` channel is the ONLY authoritative per-security halt source in our stack (Polygon Starter + FMP stable expose NONE) — real-time, WS-only, helps the LIVE ORB path (not the nightly consolidation guard). BUILT on branch ba7533b (default OFF, verified safe, `_compute_rmv` byte-identical): `scripts/probe_alpaca_statuses.py` (S1 entitlement probe) · `broker/halt_status_shadow.py` (capture → mi_halt_status_events, HALT_STATUS_CAPTURE_ENABLED default OFF) · `flag_detector.rmv_none_reason` · `dead_data_guard_shadow` (nightly 2×2 inferred-vs-authoritative compare) · 11 tests. Rollout + THE-LINE sign-offs (S1 probe · S2 flag flip · S3 guard fork augment/replace/keep · S4 fallback): `docs/analysis/386_authoritative_halt_data_2026-07-18.md` (on branch). ⚠ **GATED on operator S1**: subscribing un-entitled KILLS the shared ORB stream (websocket.py:352) → the off-prod throwaway entitlement probe MUST confirm entitlement before any merge/deploy. NEXT: operator runs S1 → if entitled, merge branch (default OFF; market+exec deploy) → capture accrues → the nightly compare shows if authoritative halt beats the heuristic → S2/S3 (operator-signed). If un-entitled → path dead, close (#386-A heuristic already stands). Refs #386 (closed), branch ba7533b. [b0] >> ✅ **S1 ENTITLEMENT PROBE RUN 2026-07-25 — VERDICT: PASS (operator-approved, Saturday chosen deliberately because the shared ORB stream is idle).** Ran the branch-ba7533b probe on a THROWAWAY connection inside apollo-market (script staged to /tmp, removed after — it is not on main): `connected to wss://stream.data.alpaca.markets/v2/**sip**` → `subscribed to statuses: ['*']` → stream survived the full 60s window → **0 events (zero is normal — halts are rare and the market is closed)**. So the answer to the S1 question is YES: **our feed DOES carry the authoritative `statuses` channel on SIP.** Note the probe ran against **sip**, i.e. the feed prod actually uses (Algo Trader Plus), not the `iex` default — the entitlement is proven on the right feed. Worst-case risk was INCONCLUSIVE (Alpaca allows one concurrent data-stream per account+feed; a second is refused) — it was not hit. NEXT (operator action, still gated): the capture module (`broker/halt_status_shadow.py`, `HALT_STATUS_CAPTURE_ENABLED` default OFF) lives on branch ba7533b and is **NOT on main** — it must be merged before the flag can be flipped, and the capture piggybacks the SHARED bar stream that fires real-money ORB entries, so that flip is THE LINE and needs its own sign-off. The probe existed precisely so that flag is only ever flipped on a proven feed. It is now proven.
- #394 | 2026-08-01 | pending | **Tune the coil-finder thresholds + decide the orderliness rank, on forward shadow.** After ~1 wk of forward candidates (daily 🪙 board + entry-signal fires + settlement R on corrected bases): TUNE the ~50% hold cap + the rank ordering against REAL data (NOT n=5 — operator's anti-overfit rule); add the ORDERLINESS/gappy metric (overnight-gap / max-daily-move, NOT the intraday-ADR my first cut used) + demotion IF gappy coils surface. OPTIONAL (no dependency, ~2d, LOW-pri): peak-detection accuracy for long bases — aged>20 already drops the harm (PTGX confirmed absent). WAITING ON: ~1 wk forward candidates (market's clock). DoD: cap tuned + orderliness decided. Refs #393, ADR 0013. [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> FABLE-DESIGNED 7/11 (block 1 T2): methodology doc docs/analysis/394_coil_tuning_methodology_2026-07-11.md — decision rules mechanical (hold-cap sweep w/ recall guard · rank-ordering Spearman-vs-settled-R w/ ≥0.15 adoption bar · orderliness = P95 overnight-gap ÷ ATR14%, ranking-demotion-only Phase-2, never a day-one gate) + N-gates (≥20 settled primary, ≥10 per decisive cell). REMAINING = wire the `coil_tuning_ready` gated review (C-card) → the tune executes mechanically when the shadow N arrives (market's clock — the re-date is the data gate, not a slip). [ok:design done 7/11; ETA = data-gated tune window] >> SITTING-RULED 7/12: methodology SIGNED as-is; tuning revisits only at shadow-N accrual (shadow continues per the #395 observe ruling).

## Stocks in Play — detection / setups / entries / 9M / flags

- #490 | 2026-07-27 | in_progress | 🔴 **FULL REAL-TIME DETECTION — the weekend priority (operator-ruled 2026-07-24: "live real-time data a priority for this weekend", starting Sat 7/25).** SUPERSEDES the #489 hybrid, which the operator ruled a stop-gap: *"we really should trade and make decisions on real-time data at all times... there isn't a rational reason to not use real-time data when we are trading real-time."* **THE CASE** (measured, not asserted): execution is already Alpaca SIP while DETECTION reads a ~15-17min-stale Polygon bulk snapshot — a Starter-plan artifact that silently became the decision layer. Staleness is NOT confined to the gap: volume, RVOL and $-volume inherit it. **⚠ CORRECTED 7/25 — the EXTENSION CHECK DOES NOT** (I code-verified `ep_detector.py:2205-2210`: it compares settled `prev_close` vs settled `mi_daily_closes` 5d-ago, deliberately PRE-gap — my earlier claim that it inherits staleness was wrong). The 5% superset is a SAMPLING heuristic (measured 55% catch), not a correctness fix, and needs perpetual retuning. The class the hybrid STRUCTURALLY cannot catch holds the biggest moves — top 5 intraday-from-cross are ALL residual (NVVE +95.3%, TRAX +46.6%, CLRO +32.6%, ARQQ +20.9%, CRIS +14.8%); TRAX 7/09 was a +56% day on 8× volume that Apollo NEVER alerted, its delayed gap reading **−1.01%** at the moment RT was +10.01% — no superset threshold above ~0% reaches that. **TRIGGER RE-RANKED**: #490's original gate was "escalate if the residual data proves material" — it does, once measured from the CROSS instead of the day CLOSE (the #489 baseline flaw); that condition is MET, it was just invisible under the old metric. **FEASIBILITY ALREADY PROVEN IN PROD**: `_rt_miss_watchdog` fetches the FULL ~3,325-symbol RT universe (~17 Alpaca calls) on every in-window tick today = #490 Pass-0 in OBSERVE mode, already the shadow. No new cost (Algo Trader Plus paid, 10k req/min, same tape). **WHAT FULL-RT NEEDS THAT THE HYBRID DIDN'T** (the real engineering, and the reason this isn't just a toggle): (1) TICK-QUALITY GUARDS — a single RT print is noisier than a consolidated snapshot; a thin/stale tick, a halted name or a bad quote could mint a phantom 10% cross that the smoothed delayed feed absorbed; (2) PREV_CLOSE AUTHORITY — the denominator of every gap; sources already disagree (TRAX 7/23 `ep_rt_prev_close_mismatch`: alpaca 37.37 vs polygon 38.17, ~2% — enough to manufacture or erase a 10% cross on its own); (3) HALT / SSR handling; (4) the scan-cadence + rate-limit budget across the 7:00-10:00 5-min ticks; (5) the shadow→authoritative cutover path with a named rollback. ⚖ **THE LINE** (detection criterion → real money): SSoT + CHANGE_PROCESS + operator sign-off + shadow-first + backtest before any live flip — Fable DESIGNS + implements to execution depth, I verify, operator signs. DoD: design signed → built behind a runtime toggle → shadow proves it catches the residual class with NO phantom-cross false positives → operator flip → verify-live on a real fast-mover. Refs: `docs/analysis/realtime_detection_feed_design_2026-07-20.md` (the hybrid design + §13), #489 (the superseded stop-gap + the residual-metric flaw), `mi_ep_delayed_residual`, `_rt_miss_watchdog`. >> ⚠⚠ **DATA WARNING — `mi_ep_delayed_residual.fwd_1d_pct` / `fwd_5d_pct` are COMPUTED ON A KNOWN-WRONG BASIS (from the day CLOSE, i.e. AFTER the intraday move) and the 16:35 ET EOD job KEEPS WRITING THEM that way every trading day.** Monday's rows will be wrong in the same direction. **Do NOT read those two columns as evidence** when designing #490 — recompute from the cross: `cross_px = prev_close * (1 + rt_gap/100)`. FIRST TASK of the #490 card (small, no-money): add a `cross_to_close_pct` (and `cross_to_high_pct`) column + backfill, so the residual dashboard stops understating the very winners it exists to find. Neither cross→close nor cross→high is ORB-entry P&L — the honest measure still needs the exit ladder. >> ✅ **DESIGN LANDED 7/25** — `docs/analysis/490_full_realtime_design_2026-07-25.md` (Fable card, grounded on measurements I took first). **4 CORRECTIONS TO MY OWN 7/24-25 ANALYSIS, all re-verified by me against code+prod:** (1) the pre-open prev_close problem is NOT 'Alpaca hasn't settled' — it is a DETERMINISTIC OFF-BY-ONE DAY: **190/190 mismatch events match the T-2 close exactly, 0/190 match T-1** (pre-open, today's bar doesn't exist so `previous_daily_bar` shifts back a day). That turns a hard constraint into a clean fix — select whichever Alpaca bar has bar-date == the known prev trading date. (2) **`ep_rt_gap_clamped` HAS NO EMITTER anywhere in the codebase** — my 'zero clamp events = healthy' was VACUOUS; the clamp at ep_detector.py:1538 silently `continue`s. (`ep_rt_pass2_degraded` DOES exist, so that half stands.) (3) the extension check does NOT inherit staleness (corrected in the case text above). (4) '8 fires all in-window' is a CODE GATE (the watchdog is window-gated), not a data property — tautological as evidence. >> ⚠ **NEW BUG FOUND — TODAY'S PASS-2 IS PRE-OPEN-BLIND:** the T-2 mismatch hits the >0.5% guard and `continue`s, so pre-open EVERY candidate whose prior day moved >0.5% silently loses its RT read. Visible as censoring in the shadow evidence: `ep_rt_floor_flip_up` = **29 RTH vs 2 pre-open** (verified). Fail direction is SAFE (falls back to delayed) but the shadow sample is CENSORED pre-market — so the '31 flip-ups' figure understates the pre-market case. Fix rides the RT-1 dark build. >> DESIGN SPINE: Polygon `prevDay.c` stays the SOLE gap denominator in every window (staleness lives in the NUMERATOR; the 190 events contaminated nothing precisely because Alpaca was never the denominator) · Alpaca prev-close becomes a DATE-KEYED cross-check only · tick-quality = NBBO band + quote freshness + **mandatory minute-bar corroboration for any RT-only admission** (no name enters on a single print) · the 30pp delta clamp is REPLACED for the universe path (it structurally rejects the NVVE class) · halts = heuristic quarantine (no feed exists) · SSR argued out of scope (long-only stop-buys) · volume/RVOL refresh co-requisite w/ its own toggle · RT-1 dark → RT-2 shadow (8 measurable gates + 3 operator-reviewed named lists per CHANGE_PROCESS r3) → RT-3 operator runtime flip `ep_rt_universe_authoritative` (~60s, no deploy) → RT-4 verify-live → RT-5 volume flip separately; 5-rung named rollback R1-R5 to byte-identical prior behaviour. ⚠ UNRESOLVED (pinned §12): a Monday 07:00 probe to confirm pre-open `daily_bar`=T-1 · `latest_quote` pre-market population rate · 200-symbol batch stability at full scale · pre-open RT false-positive rate (unmeasured — RT-2 gate 5 is the measurement). NEXT = operator reads the design + signs, THEN RT-1 dark build. >> ✅ **DESIGN SIGNED BY OPERATOR 2026-07-24** — all 6 §11 forks ruled, aligned on recs: design signed (prev_close authority + Q1-Q4 tick guards + architecture) · 9:43 tick YES at RT-3 · shadow = LONGER of ≥10 trading days AND ≥5 residual-catch days · shadow surfacing DIGEST-ONLY (upholds the 7/21 noise ruling) · batch 100 now / probe 200 in RT-0 · O-9 retire-as-trigger + cross-basis column rebase folded into RT-1. **NOT authorized by that signature: the RT-3 gap flip, the RT-5 volume flip, and any deploy** — each stays separately operator-executed. NEXT = **RT-1 dark build** (Fable, complex impl): Pass-0 overlay · date-keyed prev_close cross-check (ships as a BUG FIX regardless of cutover — it also fixes the pre-open-blind censoring) · split guard · Q1-Q4 + reject events · halt quarantine · G1 scan-log population · `ep_rt_universe_catch` shadow events · volume shadow · current_price coherence · cross-basis residual columns · **freeze tests proving flags-OFF is byte-identical**. Env `EP_RT_UNIVERSE_ENABLED` default FALSE; runtime toggles `ep_rt_universe_authoritative` / `ep_rt_volume_authoritative` both default off. >> ✅ **RT-1 DARK BUILD DONE 2026-07-25 (Fable; I verified vs code, not the report).** ~1,032 insertions / 7 files; **44 new tests, full suite 3667 passed** (3628 baseline +44 −5 retired O-9). Built: Pass-0 universe overlay (ONE Alpaca fetch/tick, reused by Pass-2 + the watchdog) · date-keyed prev_close cross-check · split guard · Q1-Q4 tick quality (NBBO band · quote freshness · **mandatory Q3 bar corroboration for every RT-only admission** · Q4 insanity bound replacing the 30pp clamp on the universe path) · halt quarantine · G1 scan-log population · `ep_rt_universe_catch` shadow events · volume/RVOL shadow · current_price coherence · cross-basis residual columns · digest-only surfacing (fork 4 honoured) · O-9 retired to `residual_regression_stats` + `rt_shadow_capture_join`. **SAFETY PROPERTY VERIFIED IN CODE (not just tests):** `gap_pct` — the admission field — is written ONLY inside `if authoritative:` (ep_detector.py ~:2019), so with `ep_rt_universe_authoritative` off the DECIDED cohort is unchanged; frozen by `test_shadow_flipup_cohort_is_byte_identical` (tests/test_490_prev_close_datekey.py:135 — asserts out==[] while the flip evidence still logs). ⚠ **THIS DEPLOY IS NOT A PURE NO-OP on prod** (prod runs `EP_RT_PASS2_ENABLED=true`): 4 sanctioned deltas, all telemetry or bug-fix, NONE changing which stocks trade — (a) the date-key fix un-censors pre-open RT reads (the 29-RTH-vs-2-preopen bug); (b) `ep_rt_gap_clamped` now actually EMITS (it previously existed nowhere, which is why my 'zero clamp events = healthy' was vacuous); (c) G1 scan-log columns now populated; (d) a boot migration ADDs 2 residual columns + an idempotent backfill (`WHERE cross_to_close_pct IS NULL`, old columns preserved — I read it, it is additive and re-runnable). Universe overlay + volume shadow stay OFF (`EP_RT_UNIVERSE_ENABLED` default false). ⚠ **4 JUDGMENT CALLS made inside design coverage, operator may want to review before RT-5**: (1) reject-reason enum picks the most-informative failed guard on multi-guard failures; (2) `ep_rt_volume_authoritative` also updates today_volume/rel_volume/projection (a literal read of §6.2 — a 6-line trim narrows it to RVOL@T inputs only if wanted); (3) volume cohort = `candidates[:20]` vs the design's '≤~50'; (4) halt/Q3 never-loosen extensions applied to the Pass-2 authoritative flip-up path too (inert in shadow). NEXT = operator `deploy.sh market-agent` (avoid 9:25-10:05 / 16:00-17:00 ET) → then `EP_RT_UNIVERSE_ENABLED=true` starts RT-2 shadow. >> ✅ **RT-1 DEPLOYED + RT-2 SHADOW STARTED 2026-07-25 (operator-directed).** `deploy.sh market-agent` all gates green; RT-1 code verified present in the running container (`_apply_rt_universe_overlay` · `_alpaca_ref_close` · `_rt_quality_read` · `_q3_bar_corroborated` · `_is_halt_suspect` · `_snap_candidate`). Boot migration RAN CLEAN: **all 114 `mi_ep_delayed_residual` rows backfilled** with the cross-basis columns — and they confirm the old basis was not merely understating but SIGN-INVERTING on the key case: **NVVE 7/10 reads −42.7% on the old fwd_5d basis vs +31.8% cross→close and +95.3% cross→high**; TRAX 7/09 +11.8% old vs **+41.8%** cross→close. That metric had been quietly arguing AGAINST full-RT. >> **RT-2 SHADOW ARMED**: `EP_RT_UNIVERSE_ENABLED=true` appended to prod `.env` (backup `.env.bak.20260725-142136-pre-rt2`) + market-agent force-recreated + verified in the running process. ⚠ **MONEY TOGGLES CONFIRMED STILL OFF** — no `%authoritative%` row exists in `mi_safeguard_state`, so `ep_rt_universe_authoritative` / `ep_rt_volume_authoritative` both resolve to their default OFF; the overlay OBSERVES and decides nothing. 0 boot errors, healthy. **First shadow data = Monday 2026-07-27 07:00-10:00 ET scans** (no weekend scan). Shadow runs the LONGER of ≥10 trading days AND ≥5 residual-catch days per the signed fork 3 → then the RT-2 gate packet (8 gates + 3 operator-reviewed named lists) → operator RT-3 flip. >> 🔗 **CONVERGENCE NOTE 2026-07-25: RT-1 supplies the only feed that makes intraday profit-taking possible (#306).** #490 was scoped for DETECTION, but the #306 analysis found the intraday extremes tracker is blind to sub-15-min holds because it reads Polygon minute bars — the same delayed source this task replaces. The Alpaca RT universe overlay + the existing `trade_stream` websocket are the natural feed for an intraday partial-profit evaluator on OPEN POSITIONS (a handful of tickers, so marginal cost ~0 on infrastructure deployed today). Do NOT let #306 build a parallel price path on Polygon. No scope change to #490 — recorded so the two workstreams don't diverge.
- #496 | 2026-08-01 | pending | **Pass-2 RT prev_close sanity-gate falsely vetoes on Alpaca's pre-market-stale prevDailyBar — fix before the `ep_rt_gap_authoritative` flip.** Shadow finding 7/21 (Pass-2's first live day; surfaced via the `9m_alerts_per_day` L2 → 83 `ep_rt_prev_close_mismatch`). ROOT CAUSE **verified, not inferred**: Alpaca's snapshot `prevDailyBar.close` rolls over ONE session late — pre-market 7/21 it returned the 7/17 (Fri) close while the correct prior trading day was 7/20 (Mon); confirmed by a live mid-session re-fetch (MU/LITE/LRCX self-corrected to 7/20 by 17:37 ET). The gate (`ep_detector.py:1528-1535`: >0.5% Alpaca-vs-Polygon prev_close diff → drop the RT read) then vetoes ~83 liquid names/day (semi-heavy). NB the RT gap **denominator already uses Polygon's correct prev_close** (line 1537) + Alpaca's LIVE price is fine — only the sanity-CHECK field is stale. Impact today = **ZERO** (shadow: `ep_rt_gap_authoritative` off → live cohort byte-identical); real cost = shadow DATA-QUALITY (83 names log no RT read → understates catch-rate) + would suppress ~83/day if flipped authoritative. FIX (detection-path — **operator-steered, THE LINE**): stop trusting Alpaca's session-boundary-stale prevDailyBar for the veto — sanity-check the RT price against Polygon's (settled-overnight) prev_close, or gate on live-price sanity instead of prev_close agreement. DoD: gate no longer trips on Alpaca's pre-market daily-bar staleness; shadow logs RT reads for the previously-vetoed names + re-measure the mismatch count. Pointer: `docs/analysis/realtime_full_cutover_design_2026-07-20.md`; pairs with #489/#490. >> ⚠ **POSSIBLE DUPLICATE OF WORK ALREADY SHIPPED (flagged 7/25, NOT closed).** #496's root cause (Alpaca `prevDailyBar` rolling a session late, vetoing ~83 RT reads/day) appears to be the EXACT defect #490's RT-1 date-keyed prev_close cross-check fixes — and RT-1 deployed 7/25. This line still reads as unstarted. Not closeable (its own DoD requires re-measuring the mismatch count, which needs Monday's scans) but VERIFY BEFORE BUILDING — same class as the #402 stale-sentence incident that burned a card. Re-measure `ep_rt_prev_close_mismatch` pre-open counts Monday: a collapse toward ~0 confirms RT-1 already fixed it.

- #482 | 2026-08-01 | pending | **Bracket-geometry evidence lab — shadow alternative EP entry/stop geometries vs the live 1-min ORB** (operator-directed 7/18; routes the #468b HIGH-realized-R geometry finding). ⚠ NO live bracket change until full-scope evidence + operator sign + N≥10 backtest (THE LINE). #468b found the live 1-min-ORB bracket (entry = stop-limit-buy @ ORB-high, stop = ORB-low, R = range-height ~3%) is ~ZERO-EDGE on the HIGH cohort: median 12-min hold, **20/21 losers were on stocks that then ROSE over 5d** (a 1-min-range stop shakes out winners; even perfect exits recover only half). **Operator ruling 7/18: KEEP 1-min ORB live for now**; SHADOW-accrue realized-R for alternatives so a geometry decision has evidence. Variants to shadow: (a) 5-min ORB bar (wider stop basis), (b) re-entry 2-3× after a stop, (c) wait-for-established-intraday-low entry, (d) ATR/structure-based stop vs the 1-min ORB-low. Extend the #468b reconstruction probe (`scripts/probes/_468b_high_diagnosis.py`) for the backtest + piggyback the #94 forward-shadow lane for live accrual. Revisit scattered prior art: `dead_zone_reeval_2026-07-09`, `edge_dossier_451`, the #327 Day-2-ORB-vs-consolidation replay, #414 gap_through. DoD: a shadow lab logging realized-R per bracket variant → enough evidence for an informed operator geometry decision. Big-rock — 8/01 = scoping/design milestone; accrual + decision are longer. >> 7/19 READ (operator-directed REVISIT-WITH-MORE-SAMPLES): the live 5-min ORB shadow lane (mi_orb_shadow_trades, bar_size_minutes=5) is now **0/14 wins, avg −0.89R / median −0.99R** — decisively worse than the 1-min baseline, corroborating the lab first-read (no bracket variant robustly beats 1-min; ATR-0.5× the lone marginal lever). Geometry is NOT the edge lever — #306 exit-mgmt is. KEEP 1-min live (operator-ruled); task stays OPEN as evidence-accrual. **Revisit the geometry decision with more samples** — when the 5-min lane clears N≥30 (have 14) + the re-entry / intraday-low / ATR-stop variants accrue. No `bracket_lab.py` build warranted now (the evidence already points away from geometry).

- #359 | 2026-09-22 | pending | **EP market-cap floor ($500M) — review-over-time whether it's the right floor** (operator 6/23, seeded by BLZE=$487M, a $13M near-miss clean gapper that was observe-laned by `filter:mcap_too_small`, NOT alerted). DATA-GATED + auto-accumulating: the scan already logs every mcap-filtered candidate in `mi_ep_scan_log` (filter reason + mcap + gap/rvol/extension). Accumulate the **NEAR-MISS cohort** — clean gappers ~$400-500M that PASSED the EP entry gates (gap≥10% / rvol / extension / price≥$5) but were excluded ONLY by the <$500M floor — + their **FORWARD outcomes** (would alerting them have won, or just added noise?). DoD: at **N≥~15** near-miss samples with forward returns (anti-overfit, batch-not-single-case; ~Q3 2026), evaluate whether lowering `MIN_MARKET_CAP` (`backtester/filters.py:23`, the shared live+backtest gate) adds net winners → **OPERATOR decision** (detection/safeguard criterion → CHANGE_PROCESS + sign-off + filter-list judgment, NEVER the agent's). Registry: `ep_mcap_floor_500m_review` (data_gated_reviews.yaml, weekly auto-surface, earliest 2026-09-22).

- #168 | 2026-07-28 | in_progress | shadow-detector quality/actionability filter for LIVE-ping graduation (evidence-gated) [b3] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> 7/22 rolled (light day; not worked). ⚠ multi-rolled evidence-gated item — worth a real work/descope/split decision next session, not another silent roll.
- #167 | 2026-07-27 | in_progress | Lane-2 narrative theme detector (shadow) — ongoing [b3] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [ok:market push — Lane-2 shadow accrues only on market days — operator-directed 7/25, 2026-07-25→2026-07-27]

## Judge & catalyst — v1.1 program
- #448 | 2026-09-15 | pending | **B6 forward backtest — does the rubric (composite_min=22) downgrade LOSERS more than WINNERS?** (the `b6_forward_backtest_first_eval` data-gated review, READY since 6/19, cohort 102). Dedicated backtest session (surfaced 7/11: the 6-axis scores are NOT cached in mi_ep_catalyst_metrics.raw_json → re-scoring means re-deriving axes from the stored fundamentals via the current rubric OR re-extracting from the raw corpus = LLM cost; SCOPE the cheaper path first). Build scripts/_b6_forward_backtest.py: SELECT post-5/19 rows w/ raw corpus → re-score with the current rubric → JOIN mi_ep_missed_outcomes/mi_live_trades for fwd returns → crosstab verdict×outcome → precision/recall/fwd-edge. DECISION MATRIX (from the review): downgrade-precision >80% & PASS-edge > DOWNGRADE-edge by ≥1R → rubric sound, keep 22; downgrade-precision <60% → dropping winners, lower threshold; PASS-edge ≤ DOWNGRADE-edge → too lenient, raise 25-28. Read-only analysis → operator decision on the threshold (THE LINE). DoD: the crosstab + a keep/lower/raise rec. Advisor-check the approach (lookahead + re-score fidelity) before building. >> SCOPED 7/11 (#458, Opus): `docs/analysis/448_b6_rescore_scoping_2026-07-11.md`. **PATH DECIDED = deterministic re-derive, NO LLM** (the rubric `catalyst_rubric.py` is pure code; the alert-quarter extraction is cached in raw_json; only the multi-quarter prior deltas need a deterministic FMP pull — NOT the 96-corpus LLM re-extraction the line feared). Cohort N=96; outcomes in mi_ep_missed_outcomes (2520). This session's first steps (per the mandated advisor-check): reuse the LIVE delta-assembly fn (don't re-implement), FMP as-of alert_date (no-lookahead), validate re-derived composite vs the live grade store (locate it — mi_ep_grade_decisions was empty; check ep_detector._emit_grade_decision) anchored to the same rubric_hash. Crosstab stays THIS dedicated session (not pre-run). >> ADD-ON (Block 3 T6b extraction, 7/11): also compute the boolean `pradeep_explosive_growth` (q0≥39% AND q-1≥39% AND projected≥39% sales — operator_shared_notes.md:394, Pradeep's explicit bar, currently encoded NOWHERE) + one crosstab line vs outcomes — costs minutes on the same deterministic re-derive; if it separates → a 0028 `pradeep_explosive` profile feature + axis-6 candidate (CHANGE_PROCESS); if not → checked-and-refuted. Detail: `block3_t6_gaphunt_extraction_2026-07-11.md`. >> SITTING-RATIFIED 7/12: the no-LLM path; crosstab in the 7/16 session (+ the 39%-bar add-on). >> BACKTEST DONE 7/16 (scripts/_b6_forward_backtest.py + docs/analysis/448_b6_forward_backtest_2026-07-16.md; deterministic PATH-1, no LLM, live code path via yfinance as-of shim). **FINDING — the composite gate is INVERTED on this cohort (replay-proof):** PRIMARY live-verdict crosstab (zero replay error): live PASS n=7 ret_5d med −5.1%/win 29% vs live DOWNGRADE n=40 med 0.0%/win 50%; downgrade-precision 50% (coin flip); replayed sweep monotone — every cut 16→28 has PASS worse (cut 16: n=19 PASS med −5.1% vs n=28 DOWN +1.1%). Downgraded winners thrown away: WDAY+22% BB+18% MLKN+17% PRGS+16% AVO+15% VSAT+14% OKTA+10%; passed losers: ALNY−25% SNDK−22% MU−21% AGX−8% (big-cap beats that fade vs small/mid momentum that runs). BOTH decision-matrix failure legs fire at once (<60% precision AND PASS-edge ≤ DOWNGRADE-edge) ⇒ matrix contradicts itself ⇒ the composite carries no forward signal here — threshold retuning is the WRONG lever. REC: don't move 22; operator sitting on the GATE'S ROLE (keep-as-is + re-eval at bigger N | neutralize tier-downgrade authority via CHANGE_PROCESS). CAVEATS filed in the doc: PASS n small (6-19 across cuts), fidelity 28/44 ±2 (replay under-scores accel axes — history-depth gap; live-verdict crosstab immune), ret basis = close_d0→close_d5. T6b Pradeep 39%-bar: NOT evaluable — q1-YoY needs the deeper FMP pull + FY guidance extracted in only 4/100 rows (checked-and-BLOCKED-ON-DATA, not refuted). [b2] [ok:analysis shipped same-day 7/16; remaining = operator threshold/gate ruling at the Sat sitting] >> OPERATOR RULED 7/16: **A — OBSERVE** (n=7 live-PASS too thin to change a live gate; market-condition hypothesis CHECKED and refuted — vs-QQQ excess made PASS WORSE, med −7.2% in rising tapes, dates spread over 5 wks; SNDK was live-DOWNGRADED not passed, MU ~−14% vs its own sector). Threshold 22 UNCHANGED. Re-look pre-committed: data_gated_reviews `b6_gate_inversion_recheck` (predicate = 8 more live-PASS post-7/16 via extraction-minus-downgrade-anchor SQL, tested-on-prod; earliest 2026-09-15) → re-run scripts/_b6_forward_backtest.py → if inversion CONFIRMED, fork B (neutralize the composite's tier-downgrade authority, CHANGE_PROCESS + sign-off); if flat/reversed, keep 22 + close. [ok:operator-ruled-A 7/16 — task now event-gated on the review predicate; ETA = earliest_review_date]
- #368 | 2026-07-26 | pending | **Meta-rubric STEP-2 — operator labeling + the WEIGHTING decision** (gated on STEP-1 #367). Operator labels the cohort's theme-relevance (ground truth) + decides D2/D3: the theme-boost MAGNITUDE + the stage→credit mapping (Pradeep ~1.5-2× rides on theme-as-DRIVER, ASYMMETRIC — never penalize themeless). Methodology = OPERATOR's call (THE LINE). DoD: the weighting spec'd + the labeled themeless-winner-inclusive cohort. [STEP-3 = the #335 flip (theme-axis load-bearing, gated on grade-CORRECTNESS over that cohort, post-Phase-2) = FINAL DELIVERY.] [b6] [blocked:operator theme-labeling at the M1 sitting 7/18 (THE LINE — operator's call)] [blocked:#367 matcher-vs-text-source read lands Mon 7/6 - STEP-2 labeling needs that fork resolved] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> INPUT READY 7/6 (from the #367 read): theme-relevance signal must come from NARRATIVE text / co-movement, NOT the subject filing (signal-a name-attribution confirmed dead, self-referential corpora). Operator labels + weighting decision proceeds with signal (a) known non-viable; S3 (0019 §2.3 theme-narrative block) is the sourcing fix. [blocked:meta-rubric STEP-2 weighting decision sequences after #335 — 7/18 CLOSE re-date] [ok:operator-decision push (weighting + labeling = THE LINE, operator's call) — operator-directed 7/25, 2026-07-25→2026-07-26]

- #329 | 2026-07-25 | in_progress | META-RUBRIC composition (Phase 5) — ANCHOR the orphaned registry review + reconcile-first (operator 6/18 sweep; the parent of #328/#330/#331/#332). ⚡ ACCELERATED (operator 6/18: "rubric is critical to EPs — we're already trading EPs with the judge live; integrate earlier the better"). ALIGNMENT to the launch spine: the ADVISORY/SHADOW composite is UN-GATED (touches no live grade) → build it NOW, pre-6/22, in parallel with launch rehearsal; the LOAD-BEARING FLIP is grade-affecting → post-6/22 by the established trade-set-stability rule (#299/#320/#321 cluster), tracked as #335. So aggression lands on the shadow build (this task) + a fast post-launch flip (#335), NOT on destabilizing the cutover. NOT "we have no composition layer" — the live holistic judge ALREADY composes theme/catalyst/narrative qualitatively (#249/#240). The real gap: (a) theme/structure/gap-alignment aren't EXPLICIT, calibrated, traceable inputs (judge weighs them implicitly/inconsistently), and (b) the registry review `phase5_meta_rubric_calibration` (data_gated_reviews.yaml: threshold=30, earliest 2026-09-08) had NO PLAN #-task/ETA — yet its OWN text records operator 6/04 DE-GATING it to "ship advisory NOW" (the N≥30 gate only governs weight CALIBRATION, not the advisory build). So the advisory composite was un-gated but never moved for lack of a task = this sweep's headline miss. FIRST DELIVERABLE ✅ DONE 6/18: RECONCILIATION SPEC `docs/analysis/meta_rubric_reconciliation_329_2026-06-18.md` (ground-truth read of grade_holistic/_RUBRIC v3/assemble_judge_inputs + det. rubric + floor). KEY FINDINGS: the judge IS the composition layer but (a) theme STAGE/SCORE is computed + shown on the alert yet NOT passed to the judge (only the bool in_active_theme) — theme heat is invisible to the judge; (b) structure context (MA/resistance/base) isn't passed so clause-5 gap-alignment is unjudgeable; (c) no per-axis traceability. ARCH ✅ DECIDED 6/18 (operator): PATH A = enrich the ONE judge (pass the inputs it's blind to + add `axis_reads` per-axis traceability to the grade_ep schema; calibration rides the operator-labeled eval). Path B's separate logistic numeric composite stays the forward-gated registry cross-check (NOT a 2nd authority). So #330/#331/#328 build JUDGE AXIS INPUTS, not a separate composite. ⚠ NEW FINDING (call-site verify, ep_detector.py:2431): the live judge call does NOT thread `has_direct_source` OR `revenue_stage` (nor theme stage/score) → the prompt renders both as always-"no". Rubric v3 clause 1 LEANS on has_direct_source (=false→prefer-floor skepticism) → the judge applies freshness skepticism UNIVERSALLY + can't credit a fresh SEC-sourced 8-K = LIKELY LIVE GRADE-AFFECTING DEFECT (has_direct_source is computed — threaded for #317 today — just not passed). Judge is LOAD-BEARING (toggle ON 6/10) so the live payload edit is grade-affecting → build+measure on the EVAL ARM, flip via #335. Priority: has_direct_source > theme stage/score > structure. SSoT target when #330/#331 land = NEW docs/setups/meta_rubric.md + own SSoT per signal (catalyst_rubric.md SSoT confirms fundamentals-only + "add signals as separate inputs, each own SSoT"). STEP 1 ✅ SHIPPED 6/18 (fe6206a, live byte-identical): theme_stage/theme_score plumbed into assemble_judge_inputs + rendered on the in_active_theme line when present; eval-only `axis_reads` per-axis traceability (`_judge_tool(include_axis_reads)` deepcopy variant + grade_holistic flag + _normalize passthrough); 2 byte-identity tests lock the live prompt+tool. STEP 2 ✅ BUILT 6/18: scripts/eval_judge_enrich.py — `--size-only` NO-SPEND blast-radius pass (recompute has_direct_source DETERMINISTICALLY from stored grounded_text markers [SEC…/[Benzinga… vs [Web summary], segment out pre-W1 thin rows) + gated `--regrade` two-arm (BLIND vs ENRICHED) with AS-OF-date theme heat (mi_themes theme_date<=alert_date — NOT get_theme_membership which is lookahead) for operator labeling; build_judge_payload gained byte-neutral enrich kwargs; ep_grade_decision now logs has_direct_source (data-readiness for #337); recompute_has_direct_source pure helper + 6 tests, 910 suite green. SIZING RAN 6/18 (45d, no spend): 181/184 ASSESSABLE rows (98%) had a DIRECT source the live judge was shown 'no' for; 126 AT-RISK (direct + promote/material-lean). ⚠ RIGHT-SIZED (advisor 6/18): the 98% is the flag-wrongness rate, NOT the impact — the judge reads the corpus substance INCLUDING the dated marker ([SEC 8-K filed <date>…]), so it can establish source+freshness directly even with the meta-flag stuck at "no". The sizing data itself shows this: it graded HIGH/hold on nearly all 184 → largely ROBUST to the flag. EXPECT FEW has_direct_source flips in the re-grade. So this axis is likely LOW-IMPACT; if the batched re-grade confirms ~zero flips, that's the evidence to DROP has_direct_source from the #335 wire-in entirely (yield discipline working). The re-grade is still run (it measures theme-heat/structure/gap too) — has_direct_source just rides along, it doesn't justify the spend alone. NEXT (operator cost directive 6/18 — DON'T pay per-axis): the sizing pass cost $0 (no LLM). The `--regrade` (Opus spend) is DEFERRED and BATCHED into ONE consolidated re-grade at the pre-#335 checkpoint covering ALL pending payload enrichments at once (has_direct_source + theme-heat here + structure #330 + gap #331 + tape #299 + chart #267 as built) — re-judging the cohort ONCE, not once per axis. See #335. Live wire-in + axis_reads-on-alert ride #335. THEN ship the ADVISORY methodology-ordered composite (theme dominant ≥1.5–2× fundamentals per user_pradeep_catalyst_hierarchy; un-gated) by this ETA. Weight calibration stays forward-data-gated (registry review, now #-linked here); the load-bearing flip = #335. CHANGE_PROCESS + sign-off before any load-bearing flip. ▶ **GROUNDWORK RE-VERIFIED AT HEAD 6/24** (`docs/methodology/meta_rubric_groundwork_2026-06-24.md` — consolidates the 6/18 reconcile, current-at-HEAD, + frames the open within-Path-A composition decisions D1[theme-axis scoring mechanism: qualitative-read vs numeric-sub-score]→D7 for the operator's design pass). **KEY CLARIFICATION:** STEP-1 (6/18, fe6206a) was **FUNCTION-SIDE-ONLY** — `assemble_judge_inputs` ACCEPTS `theme_stage`/`theme_score` + renders them "when present," but the live CALL (`ep_detector.py:2829`) does NOT PASS them (verified 6/24: `theme_stage`/`theme_score` appear NOWHERE in ep_detector.py), so **the theme axis is still DARK to the judge** (only the `in_active_theme` bool reaches it). Same for `has_direct_source`/`revenue_stage`. So the call-side WIRE-IN (pass the axes the fn already accepts) = **#335** (grade-affecting, operator-gated) — NOT done by STEP-1; corrects any "theme axis is live" misread. NOT a launch blocker (current grade = the theme-heat-blind baseline + fail-open-to-floor; the theme axis enriches it POST-launch via #335). Reconcile/anchor (this task) = current-at-HEAD; composition + wire-in = operator's design pass + #335. ▶ **THEME-AXIS DIRECTION DECIDED 6/24 (operator + 2 advisor passes):** (b) engine = SoT for theme DETECTION (existence/membership/heat), NOT re-judged. Relevance ("is theme the DRIVER of this move?") attributed **STRUCTURALLY** — catalyst named-entities ∩ theme-cohort tickers/keywords (deterministic, traceable, auditable) — **NOT** the LLM-catalyst-axis (that + a judge-audit = the SAME LLM = CIRCULAR; measures self-consistency not correctness). Audited by an **INDEPENDENT mechanism** (judge's qualitative read OR co-movement/tape — must differ from the attributor). **ASYMMETRIC** — boost theme-as-driver, NEVER penalize themeless (doubly-justified: themeless may be an undiscovered-theme blind-spot, not a real absence). Pradeep ~1.5-2× rides on theme-as-driving-catalyst, decays to context otherwise. **FLIP (#335) GATE = grade-CORRECTNESS on a themeless-winner-INCLUSIVE labeled cohort, FULL STOP** — disagreement-rate is a review-load regulator + health gauge ONLY, NEVER a flip-gate. RATE vs COUNT: review-load = the COUNT (operator feels); health/gating = the RATE over sufficient N (small-N caveat — don't read low early counts as green). **STEP-0 (next, shadow):** wire theme-heat to the judge + log the structural attribution + 1 independent check beside it + start the themeless-winner-inclusive labeled cohort → let DATA size the weighting/monitor (scaffold, not cathedral). **CARVED OUT of v1 (someday-maybe):** the discovery-feeder (undiscovered-theme → narrative-lane #167) — confirmation-bias loop (judge→discovery→engine→boost→"validated"), adjudication brake fails at volume. ALL shadow + post-Phase-2 (#335); NOT today's clock. [ok:Track-2 Saturday deep-focus] [b3] [ok:composition anchor rides the #335 checkpoint (the axes accrue first)] >> INTEGRATION DESIGNED in ADR 0024 (7/7, Fable block A): the judge-vs-axis composition ANSWERED (F1: judge owns the CATALYST verdict; axes own calibrated CONTEXT credit; compose_final_tier caps net movement -1..+1; the double-count killed via a rubric amendment riding the M1 flip commit). Awaiting ADR signature. >> ADR 0024 SIGNED 7/7 — the composition question is CLOSED (F1). [blocked:composite Phase-5 anchor gated on the #335 M1-d ruling — 7/18 CLOSE re-date] >> #335 M1-d RULED 7/19 (KEEP shadow / don't flip / DATA-gated re-eval) — this composite anchor proceeds knowing the theme axis stays dark. LINKED to the #335 theme-axis cluster: full rationale + re-eval criteria in docs/analysis/335_theme_boost_keep_rationale_2026-07-19.md. Cluster: #335 · #486 (judge↔engine cross-val) · #322 · #467(closed) · review theme_axis_boost_reeval.
- #331 | 2026-08-25 | blocked | GAP-vs-STRUCTURE ALIGNMENT as an EXPLICIT scored meta-rubric axis (#329 child; meta-rubric memory item 3). Does the gap punch THROUGH resistance (institutional confirmation) or fade INTO a congestion zone? Same precision as #330: the judge sees raw gap_pct + structure and can reason about alignment qualitatively (ADR-0011 clause 4) — the gap is a CALIBRATED, traceable alignment score, distinct from raw gap magnitude (already in ep_score). Gated behind #329 spec + #330 structure primitives. [b4] [blocked:#329/#330 upstream still open - #330 shadow build queued post-sprint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] [ok:STEP-0 greenlit 7/19 (operator: keep/explore/live-when-right) — ACTIONABLE, build STEP-0 (calibrate the credit table); the operator sign-off is DOWNSTREAM, after STEP-0 produces the table] >> SIMPLIFY-DEFERRED 7/18 (fold into THIS build — factor before the 3rd axis-shadow copy locks in): (a) extract a shared `axis_shadow` scaffold (`write_axis_shadow(conn, table, ticker, alert_date, fields, credit)` + a `@shadow_safe` try/except-to-audit decorator) that theme_axis_shadow + structure_axis_shadow + this gap axis all reuse — today each hand-rolls the same "pure compute → boost-only credit → upsert-guarded write → never raise into grade" skeleton; (b) share `build_stub_name` between coverage_probe + judge_theme_gap (already deduped the upsert into `_upsert_theme_candidate_shadow` 7/18); (c) in ep_detector `_judge_shadow`, compute `compute_structure_features` ONCE per candidate and pass into both compute_setup_class_fields + log_structure_axis_shadow (today the ~380-day bar fetch runs twice on the live 7-10AM scan). All non-money shadow quality — no operator gate. >> APPROVED 7/19 (operator: 'keep, explore, go live when right') — proceed: build STEP-0 (calibrate the credit table + the magnitude-independence check) → ship the gap-alignment axis SHADOW → accrue evidence → live-flip to operator when earned (operator will eyeball the credit table + STEP-0 result before it ships shadow). >> STEP-0 DONE + DEFERRED-ACCRUING 7/21 (operator: not enough to kill/promote — collect more): the calibration + evidence-enrichment ran (docs/analysis/331_gap_alignment_step0_2026-07-21.md). Findings: punch_through FAILS the magnitude-independence gate (doesn't beat the 0-credit classes on avg); fades_into_congestion is a high-variance BARBELL (owns the biggest winners AND losers — the avg is fat-tail-driven); regimes underpowered (Bull-heavy). NOT ship, NOT kill — it's a distribution-shape question. Probes (all READ-ONLY): _331_gap_alignment_step0.py (calibration) · _331_gap_alignment_evidence.py (regime/timeframe/extremes) · _331_gap_alignment_charts.py (annotated as-of-date mplfinance charts → Telegram, 12 sent 7/21). Accrual re-run gated on data_gated_reviews.yaml::gap_alignment_331_accrual (cohort ≥700 + 2026-08-25). Any grade-flip = ADR 0033 CHANGE_PROCESS + operator sign-off.
- #333 | 2026-07-31 | pending | CATALYST DURABILITY forward axis (catalyst program; user_catalyst_depth 6/16 Pradeep tweet). The rubric ALREADY scores trailing acceleration (revenue YoY + decel/accel); the GAP is the FORWARD/PROJECTED leg — Pradeep durability = ≥2Q realized + ≥4Q PROJECTED high-revenue-growth (39%-class illustrative; the SHAPE is the point). Needs forward guidance/projection from STRUCTURED data → gated on the direct-sourcing backbone (#210/#211), not LLM prose. Distinct from #269 (revenue-over-EPS weighting of existing axes). [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> SKETCHED 7/11 (block 1 T2, docs/analysis/block1_tier2_sketches_2026-07-11.md): a7_durability = deterministic RUBRIC axis (per ADR 0024 F1 catalyst-side); data contract named (point-in-time consensus estimates, stored-at-grade, n_analysts<3 → None w/ missing-data scaling); build gate = #210/#211 backbone + ≥60d stored estimates → STEP-0 → shadow. Class-salience feeds ADR 0028 P1.
- #335 | 2026-08-25 | deployed | PULLED FWD 6/28 (post-cutover deferral EXPIRED — launch was 6/22; eval runs this week, flip on operator sign-off — a REAL gate, not a parking date). ⚡ LOAD-BEARING FLIP of the meta-rubric composite into the LIVE (paper) EP grade — THE integration the operator actually wants ("improve EP trading", we're already trading EPs with the judge). The grade-affecting milestone: the #329 advisory composite becomes authoritative (theme #328 + catalyst-magnitude + catalyst_type, enriched by #330/#331 structure/gap as they land — incremental, flip the core first). EARLIEST = just post-6/22 by the established grade-affecting-wiring-post-cutover rule (#299/#320/#321 cluster — don't shift the trade set right before the live-money GO/NO-GO), then move fast. GATES (non-negotiable, fail-open-to-floor like ADR 0011): CHANGE_PROCESS + operator sign-off on the exact weighting + a DB toggle for instant revert + the BACKTEST = ⚡ ONE BATCHED RE-GRADE (operator cost directive 6/18: don't pay per-axis). Instead of a separate eval-spend run per axis, accumulate ALL pending payload enrichments (has_direct_source #329 + theme-heat #328/#329 + structure #330 + gap #331 + tape #299 + chart #267 as each is BUILT) and re-judge the cohort ONCE at this checkpoint via eval_judge_enrich --regrade — measures every axis's verdict-delta in a single paid run, operator labels the flips. (The $100+ Phase-B replay was 1307 rows; batching avoids re-paying that per-axis.) Clusters with the #320/#321/#299 post-launch grade-quality batch. NOTE: if the operator wants this PRE-6/22 instead, it overrides the trade-set-stability rule — flag for explicit sign-off (my rec: shadow pre-launch, flip immediately after). At the flip, the per-axis `axis_reads` decomposition surfaces on the alert + /why (the richer half of #336). [ok:flip decision = Saturday operator-together] ▶ $0-EVAL RUN 7/4 (docs/analysis/composite_vs_judge_eval_2026-07-04.md): gates OBJECTIVELY UNMET — N=22 thin funnel (38/390 alerts carry extractions); 64% disagreement ALL one-directional (composite-weak vs judge-HIGH; judge's keeps 11/12 positive 5d); the specced composite doesn't fully exist (theme axis = shadow as of TODAY, 0 accrued; type weights undefined). DISPOSITION: hold to the checkpoint — flip decision AFTER the axis shadows accrue + the ONE batched regrade eval (where #299's subset folds in, the batching principle). [b4] [ok:gates-unmet per the 7/4 eval; checkpoint ~7/18] >> M1 CARD-DECOMPOSED in ADR 0024 §6 (7/7): M1-a compose_final_tier dark · M1-b the ONE batched regrade (eval_judge_enrich --regrade; theme credit + has_direct_source-cache + structure-as-text) · M1-c rubric amendment draft · M1-d the operator sitting/flip · M1-e verify + recurring composite_effectiveness review + authority-registry row. Scope = theme axis ONLY (core-first); deps all green. >> ADR 0024 SIGNED (operator 7/7, all forks per recs) — M1 prep cards (M1-a compose_final_tier dark, M1-b batched regrade, M1-c amendment draft) are EXECUTABLE now; the flip stays gated on the M1-d sitting. >> M1-a DONE 7/8 (c9d0caa — `meta_rubric_compose.compose_final_tier` DARK, `composite_authority` default-off, net ±1 cap, 11 golden tests; TIER_LATTICE/TIER_RANK promoted to constants SSoT, briefing reuses). M1-c DONE 7/8 (285ea5d — `docs/analysis/m1_rubric_amendment_draft_2026-07-08.md`: clause-4/5 amendment text + theme stage→credit table + worked examples + sitting checklist). REMAINING: **M1-b** (the ONE batched regrade — Opus SPEND, operator-gated) → **M1-d** sitting 7/18 (sign composition+cap+amendment → flip `composite_authority`) → **M1-e** verify + composite_effectiveness review + authority-registry row. >> M1 PACK READY 7/11 (Block 3 T5a): **`docs/analysis/m1_sitting_pack_2026-07-18.md`** — turnkey: preconditions checklist (0030-C3 robustness map + theme_axis_gating slot in during the week; T2c drift band = build pre-7/18), the pre-agreed interpretation contract (hard-class failures → amendments BEFORE authority; only positive-control >20% or an unfixable hard cluster blocks), flip mechanics (toggle, no deploy) + verify-live + rollback. The sitting runs off the pack. >> SITTING-RATIFIED 7/12 (C5): R5 preconditions confirmed — T2c band + 0030 map build pre-7/18. >> 7/12: M1-pack precondition slot 1 FILLED — robustness map 36/36 CLEAN (zero hard-class failures; positive controls 1.0) → per the interpretation contract NOTHING blocks the flip from the robustness side; the [5m/7] regression gate is ARMED. Remaining pre-7/18: T2c drift band build + the theme_axis_gating review. >> T2c BUILT 7/12 (Block 4 flex): `judge_high_rate_daily` + `judge_demote_share_daily` MetricSpecs live in `_TRADE_METRICS` (Python-side detail parse, malformed-rows-skipped; cold-start ceilings 0.85/0.90; NOT regime-conditional; 4 tests, suite 2958). M1 precondition 3 = FULLY BUILT; awaits the batched deploy → samples accrue from the next post-EOD scan (cold-start ceilings ARE the tripwire until n≥14). >> **M1-d WIRE-IN BUILT DARK 7/13** (Fable build, Opus-verified vs the actual diff): the composite-authority wire-in ships DARK — `get/set_composite_authority_enabled` DB toggle (mi_safeguard_state, fail-CLOSED like holistic_judge) + `set_composite_authority.py` operator CLI + pure `resolve_composite_tier` + `compute_theme_axis_credit_live` + the ep_detector `_judge_shadow` wire-in (composes theme-axis credit onto the authoritative tier via compose_final_tier, behind the toggle). CODE-ONLY — **zero** `_RUBRIC`/prompt change (advisor's THE-LINE catch: the amendment is what the operator SIGNS at the sitting, not shipped early; unchanged hash → [5m/7] passes, deploys dark with no $5 eval). Byte-identical with toggle OFF (default/no row). 8 new tests + 3097 suite green. Sitting pack F1 corrected. REMAINING pre-7/18: M1-b batched regrade (operator-gated $5 spend) + T2c accrual confirm (first sample 7/13 16:15 ET); flip = M1-d sitting. >> **M1-b RAN 7/13** (operator-authorized ~$5/32 Opus calls; harness bug fixed same run — cohort query omitted build_judge_payload columns, KeyError:'catalyst', now _REGRADE_SQL + regression-pinned): 2 flips / 0 noise (HYMC+MMYT MODERATE→none). ⚠️ **MECHANISM MISMATCH — the headline:** M1-b re-grades with theme heat in the JUDGE'S PROMPT (#329 enrichment path), NOT the deterministic theme_axis_credit→compose_final_tier that `composite_authority` flips live → **this run produced ZERO evidence on the live M1-d mechanism**. The 2 flips are catalyst-quality/source-driven demotions but CONFOUNDED (has_direct_source + theme heat moved together, not isolable); 0 theme-boost flips (88% themeless). The 461-row mi_theme_axis_shadow is the evidence that speaks to the live mechanism. Delta table: docs/analysis/m1b_regrade_2026-07-13.md. OPERATOR RULING at the sitting (methodology→THE LINE): does the deterministic shadow alone suffice to sign M1-d? A larger RANDOM regrade does NOT help (wrong mechanism); only an Accelerating-membership-TARGETED cohort would give judge-side theme evidence (harness needs a filter). >> T2c ACCRUING-VERIFIED 7/13: first judge_high_rate_daily + judge_demote_share_daily metric_samples landed 16:15 ET (precondition 3 verified-live). ⚠ M1-d REFRAME (7/13): the theme boost is RARE on MODERATE (~few/quarter), UN-EVIDENCEABLE from history (MODERATE was never instrumented — now fixed by the coverage-loop S1), and largely REDUNDANT (hot-theme EP names already grade HIGH, ~16:1). So 7/18 M1-d = a KEEP/SHELVE call, NOT a mechanical flip; the coverage-loop (#467) is the bigger theme-axis lever. Wire-in stays dark. Evidence: docs/analysis/{m1b_regrade,ep_theme_coverage_loop_design,theme_membership_investigation}_2026-07-13.md. >> **M1-d RULED 7/19 (operator): KEEP — do NOT shelve.** Rationale: theme/story/catalyst is CENTRAL to EP (Pradeep/Qullamaggie core — the narrative drives the move); a rare theme-boost upgrade can still matter. Rec REVERSED from SHELVE. NEXT = targeted upgrade backtest — do the would-be MODERATE→HIGH theme-boost upgrades beat their MODERATE grade on fwd outcomes? (#335 upgrade-backtest queued 7/19); if net-positive → flip composite_authority live (CHANGE_PROCESS + operator sign-off). Shadow keeps accruing meanwhile. >> UPGRADE-BACKTEST RAN 7/19 — **0 would-be upgrades in 466 rows, but that's an ARTIFACT** (465 HIGH + 1 MODERATE: MODERATE was HIGH-only-instrumented until the coverage-loop S1; + hidden-theme coverage gaps #467 + theme-engine instability/gaps Jun+). NOT evidence the boost is useless — we can't evaluate it yet. DISPOSITION (operator + evidence): **KEEP the shadow** (deterministic, 0 LLM = ~free, dark, zero-risk); DON'T flip; **re-eval DATA-gated** (≥10 MODERATE alerts WITH a detected theme + settled outcomes + stable theme-engine window + improved coverage) — auto-surfaces via review `theme_axis_boost_reeval`. Near-term payoff = judge↔theme-engine cross-validation (#486). Full findings + Q&A + reason-to-keep: docs/analysis/335_theme_boost_keep_rationale_2026-07-19.md. [blocked:re-eval DATA-gated (≥10 themed-MODERATE settled, NOT calendar) — see the keep-rationale doc — 7/19] >> CLOSE 7/21: stays [blocked:] data-gated (≥10 themed-MODERATE settled + stable theme-engine window); re-dated to the theme_axis_boost_reeval window. >> ⚠ **GATE CORRECTED 7/25 (I was wrong on 7/25 AM — I called this 'the most promising candidate' off the 476-row total).** The gate is ≥10 **themed-MODERATE settled**; the actual population is HIGH themed 59 · HIGH themeless 414 · **MODERATE themeless 3 · MODERATE THEMED 0**. So it is **0/10**, not nearly-there. Note the shape: only 3 of 476 rows are MODERATE at all and every one is themeless — like #310, this gate may be structurally slow rather than merely early. Earliest review date is 2026-09-15 regardless.
- #486 | 2026-08-15 | pending | **Judge ↔ theme-engine cross-validation (from the #335 7/19 review — the near-term payoff of the theme-axis shadow).** `mi_theme_axis_shadow` pairs, per EP alert, OUR engine's theme membership with the JUDGE's grade/narrative. Mine disagreements both ways: (a) judge implies a theme our engine MISSED → feed the theme engine / coverage loop (#467/#322/#235); (b) our engine has a theme the judge ignored → check the judge's theme-weighting. DoD: a periodic agreement readout (themed-per-engine vs themed-per-judge, the mismatch cohorts, an engine-improvement candidate queue). Non-money telemetry, NEVER a grade input (THE LINE). Evidence-gated on a stable theme-engine window + coverage accrual. Refs #335 (docs/analysis/335_theme_boost_keep_rationale_2026-07-19.md), #467, #322, #235. [b0]
- #485 | 2026-08-15 | pending | **LLM-advisor reviews the judge (operator idea 6/18 — split out when #337's monthly-review DoD closed 7/19).** Evaluate a meta-LLM that reads the judge's decisions + forward outcomes to flag questionable/miscalibrated calls for operator attention — a richer, automated variant of #337's operator-labeled-sample arm (shipped + verified-live 7/1). Relates ensemble-divergence #301 (real-time 2nd-model verdict), questioner/investigator #212, precedent-retrieval #255. DoD: feasibility read — does a meta-LLM add signal over the operator-labeled sample + the by-tier outcome cross-tab #337 already surfaces? → if yes, design + shadow (advisory only, NEVER a grade input — THE LINE); if no, retire the idea. Evidence-gated on the accruing judge-corpus (post-launch volume). [b0]
- #210 | 2026-07-28 | pending | catalyst-sourcing backbone umbrella (direct primary sources) [ok:date-roll-0701] [b4] [ok:sprint absorbed by the axis builds; sourcing next week] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> 7/22 rolled (light day; not worked). ⚠ multi-rolled evidence-gated item — worth a real work/descope/split decision next session, not another silent roll.
- #211 | 2026-07-28 | in_progress | news-gap discovery loop (unknown-rate KPI) — needs new scheduler job [ok:date-roll-0701] [b4] [ok:same] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> 7/22 rolled (light day; not worked). ⚠ multi-rolled evidence-gated item — worth a real work/descope/split decision next session, not another silent roll.
- #212 | 2026-07-29 | in_progress | questioner/investigator dialogic loop — productionize at larger cohort [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #230 | 2026-07-29 | pending | #212-as-sourcing-QA detector → feed #211 (low-precision triage) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #233 | 2026-07-29 | in_progress | Perplexity repositioning — KEEP + integrate (operator-adopted 6/17, NOT cut): (a) feed its independent GRADE into the JUDGE as a LABELED second-opinion/disagreement signal — judge currently reads its [Web summary] text but not the Claude-vs-Perplexity grade disagreement (grade-path INPUT change → CHANGE_PROCESS + shadow eval, judge is load-bearing); (b) reposition Perplexity as a labeled RECENCY source only (its live-web edge), judge-adjudicated vs primary (#210), and RETIRE the mechanical floor confidence_multiplier boost (with #320). Was "demote Perplexity/Tavily to labeled candidate (Wave C)" [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #215 | 2026-07-31 | pending | surgical prompt de-bias / OPTX-residual [b2]. ▶ #214 blocker CLEARED 6/29 (theme name-breadth rule shipped 48db3c0 + cooldowns verified healthy). REMAINING gate = a CLEAN grade cohort (the de-bias needs a debiased baseline to measure the OPTX-residual against) — rides on the grade-quality cluster settling (#335/#320/#321, ~7/3+). Re-evaluate when a clean cohort has accrued. [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #258 | 2026-08-08 | in_progress | ensure_schema() consolidation (fold 33 ALTERs) — branch+staging-validated, post-#277 [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #239 | 2026-09-01 | pending | ⚠ KEEP-GATED (eval 6/28): part (a) trailing-baseline dup is real BUT the deferral gate is UNMET — both consumers (verify_monday_firstfire, _wave_a_grade_inflation_check) are DISPOSABLE scripts; extract at the 3rd PERMANENT consumer. Part (b) _emit_shadow_row envelope = over-abstraction (provenance is intra-grade, tape/perplexity post-grade — unifying hides the context diff). Revisit (a) at a permanent consumer; (b) won't-do. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #265 | 2026-08-12 | pending | judge-flip-day review residuals (SQL-text SSoT, shared feeds list, WATCH cancel window) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #207 | 2026-08-01 | pending | model-eval governance quarterly review (data-gated)
- #197 | 2026-08-18 | in_progress | cap+1 game_changer slot SHADOW — promotion-gated N≥30 [b1] [ok:date-roll-0701] [ok:operator 7/21 rebump — promotion-gated N>=30, event-gated]
- #274 | 2026-08-01 | pending | 2-member theme immortality fix (dissolve-on-flagged-pair, CHANGE_PROCESS) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> DATA-BACKED 7/8 (L2 anomaly: theme_count_active 78 vs 40 median + cooldowns 7× + 54 Nascent). ROOT CAUSE (investigated 7/8, NO regression): the shipped fragmentation guards are ALL intact + running — MIN_SHARED_FOR_MERGE=3 (Pass1 merge, theme_engine.py:252) · Pass1.5 near-dup absorption · #125 description-quality cap (:2167) — but by design they DON'T delete coherent thin/overlapping DISTINCT themes (merge needs ≥3 SHARED tickers; the 4 near-dup insurance + 3 REIT themes have distinct names). The flood = raised discovery sensitivity (ADR 0007, deliberate — fixed the nascent-MISS) meeting UN-shipped precision: this #274 dissolve-fix never built + #126 coherence guard filed data-gated + the deliberate "description-guard-NOT-2-member-cap" call (theme_engine.py:1879, from a 26-theme/27%-noise corpus — the tradeoff shifted at 54 Nascent). NO money risk (themes feed only the SHADOW judge theme-axis #328). SCOPED LEVERS for the weekend Fable design: (1) ship #274 dissolve-on-flagged-pair; (2) SECTOR/NARRATIVE-overlap merge arm (merge on sector+thesis even <3 shared tickers → collapses the insurance/REIT dups — highest leverage); (3) re-open the description-guard-not-cap decision incl. a possible BIRTH min-member floor, with the ADR 0007 §3 anti-noise caveat (don't reintroduce the nascent-miss). Detection-criterion → SSoT + CHANGE_PROCESS + N≥10 legit-kill backtest. Opus preps a read-only evidence pack (which themes each lever merges/dissolves + legit-kill check) to feed the design. → **FABLE WEEKEND DESIGN BLOCK** (operator 7/8). >> FABLE-DESIGNED 7/11 (block 1): **ADR 0025** — Arm A dissolve-on-flagged-pair (validation-evidence-triggered + retro-sweep) + Arm B thesis-coherence merge (Stage-A deterministic pairing w/ one-side-<4-members + Stage-B LLM adjudicator w/ negative exemplars + MERGE/DISTINCT/PARENT_CHILD verdicts, distinct-cooldown 30d, ≤3 merges/night) + Arm C floor RE-REJECTED w/ a named re-open gate. Validation = offline replay over the cached 7/8 cohort (C4 `_274_merge_replay.py`) = the sign-off artifact; post-ship `theme_fragmentation_resolution` gated review wired. REMAINING = operator signs F1-F3 → C4 replay → C1-C3 build (cards in the ADR). >> PRE-BUILT 7/11 (#458, Opus): C4 replay run — `docs/analysis/274_merge_replay_table_2026-07-11.md` + `scripts/probes/_274_merge_replay.py`. **✓ PASS — sign F1-F3.** 31 candidate pairs/12 families → Stage-B (Haiku, ADR prompt) = 3 MERGE · 3 PARENT_CHILD · 25 DISTINCT · 0 err. LEGIT-KILL ✓ PASS (both anchors safe: Specialty-Cat→child-of P&C not merged; Office×Multifamily DISTINCT). Driver-precision confirmed: merged Multifamily→Coastal-Residential (same housing driver) while keeping Office distinct; all 8 insurance + 7 fintech sub-lines stayed DISTINCT (no sector-label merging). Caveat: single non-det Haiku run → C3 adds temp=0/retry before live flip. Action list = the post-deploy sanity-check for the `theme_fragmentation_resolution` review. >> SITTING-RULED 7/12: **0025 F1-F3 SIGNED** → C1-C3 build UNBLOCKED (behind THEME_MERGE_ARM; C3 must pass the T2b golden corpus — hard pairs 100%; R7 pre-declare the L2 shift). >> **C1-C3 BUILT DARK 7/12 eve (Fable) + DEPLOYED (toggle OFF = byte-identical, verified)**: Arm-A dissolve+retro-sweep · Stage-A pure pairing (replay logic extracted, probe now imports it) · Stage-B adjudicator (Haiku temp=0, negative exemplars, ≤3 merges/night, 30d distinct-cooldown) + corpus eval + skip-loud gate test. Corpus v1 FAILED (M03/04/05 PARENT_CHILD-vs-MERGE) → operator ruled the boundary (rulings-pack R3: identical-driver slice=MERGE; PARENT_CHILD needs a distinct sub-driver) → prompt v2 → **14/14 PASS ×2 (hard 5/5, M12 control held), pass record written, gate test armed. FLIP IS CORPUS-CLEARED = operator's call** (THEME_MERGE_ARM=true + redeploy; the theme_fragmentation_resolution gated review + CLAUDE.md pointer land in the flip commit).


## Operational safety / hardening
- #500 | 2026-07-27 | deployed | 🔴 **[TOP-PRI NEXT SESSION, operator 7/22] Initial ORB entry has NO price-aware fallback — violent gappers blow through the ORB high before the stop-limit entry lands → Alpaca cancels it (ARWR 7/22, LIVE).** CONFIRMED read-only: `order_manager.py:215/231` (submit_entry) ALWAYS calls `place_bracket_order(stop_price=orb_high, limit_price=stop_limit_buy_price(orb_high))` with NO check of current price vs orb_high. The RE-ENTRY path (`order_manager.py:622/633`) already branches to `place_limit_buy_with_stop` when `latest.price > orb_high` — the initial entry lacks it. ARWR 7/22 (live, HIGH, +19.57% Phase-3 SHASTA): gapped through orb_high $87.92 (now $89.06); entry went pending_new→CANCELLED by Alpaca within ~1min (9:31→9:32 reconcile, NOT the 10:00 cleanup); SMCI (near its orb_high) filled. ⚠ the BEST EPs (violent gappers) are the MOST exposed. FIX (money/exec-path → THE LINE: operator + CHANGE_PROCESS + backtest, N≥ historical cases): (a) port the re-entry price-aware fallback to the initial entry (limit buy when price already > orb_high); (b) CAPTURE + surface Alpaca cancel/reject status on the entry (currently a bare event_norm=cancelled with no reason — the "no reason" the operator saw; Alpaca dash shows nothing, so we must log it). DoD: fast-gapper-above-orb_high gets a real fill (or a named skip), + entry cancels carry a reason. Evidence: this thread + entry_order_rejected/order_status_reconciled audit 7/22. >> BUILT + committed 7/23 (Fable-implemented off the signed CHANGE_PROCESS proposal; I verified vs code: full suite 3555 green incl. 22 new #500 tests; caught+fixed 1 regression myself [test_enum_boundary_183 reason-capture assertion — Fable's test-exec was classifier-blocked]; OTO/naked-guard hardening + submit_entry price-aware branch + chase-cap verified faithful; imports present; 5 noted deviations all sound). NOT DEPLOYED — deploy TONIGHT market-closed (deploy.sh market-agent) + paper OTO smoke → then verify-live L1-L3 → close. Proposal: docs/analysis/500_orb_entry_price_aware_proposal_2026-07-23.md. >> ✅ DEPLOYED 7/23 ~19:03 ET (TWO-STEP — #500 is broker/ = EXECUTION-owned: `deploy.sh market-agent` built the shared image + shipped #498/carveout on apollo-market, THEN `deploy.sh execution` [operator-approved, live-money container] restarted apollo-execution where the entry actually runs — else silent-dark, the LZB class). ⚠ Gate 5 G (column-write authority / trade-state-ownership) CAUGHT that #500's new `_submit` helper wasn't in ALLOWED_WRITERS for entry_order_id/status/stop_order_id → registered it (5cb7162, operator-approved; `_submit` = submit_entry's relocated multi-col atomic entry-write, same authorized write) → re-deploy GREEN both containers. Clean boots, 0 errors; the 2 open live positions (NVCR/SMCI) re-synced with broker-side stops intact through the execution restart. VERIFY-LIVE 7/24+ = a violent gapper crossing orb_high in-window gets a real fill (or a named skip) — EVENT-GATED on a qualifying entry — OR the G6 replace-smoke on the next MARKET-HOURS deploy (G6 skipped tonight, market closed); + entry cancels now carry a broker reason. NOT closeable until a qualifying live entry or the market-hours G6 re-gate. [b2] [ok:shipped→deployed 7/23, ETA=next-market-day verify] >> **EVENT-GATED CARRY 7/25** (not a slip): the 7/24 verify-date passed with NO qualifying event — verification needs a violent gapper crossing orb_high IN-window, or the G6 replace-smoke on a MARKET-HOURS deploy, and 7/25-26 is a weekend. Re-dated to the next market day. The fix itself is deployed + code-verified on apollo-execution since 7/23; nothing is unbuilt here. [blocked:event-gated on a qualifying live entry OR a market-hours deploy — market closed 7/25-26]
- #495 | 2026-07-27 | pending | **Evening-brief send observability — a silent brief-miss currently goes unnoticed (operator 7/20).** send_evening_briefing send success/failure is LOG-ONLY: send_telegram_message returns False on failure without raising; only logger.info/error, no DB row, no alert. So a silently-failed 18:00 ET brief is invisible + undiagnosable after a container restart (7/20 false-alarm exposed this — couldn't confirm from the DB whether it sent). FIX: emit an audit event on brief send (success + failure) + Telegram-alert on failure (mirrors the #381 no-silent-failures rule + notify_job_failure pattern). No-money observability. DoD: evening_brief_sent / evening_brief_send_failed audit row per run + a failure alert; verify next brief. [b1] [ok:market push — built today, verify-live is the next 18:00 ET evening brief (mon-fri) — operator-directed 7/25, 2026-07-25→2026-07-27]

- #479 | 2026-07-28 | in_progress | **Telegram EOD noise + evening-brief length review (operator 7/16 night: 'high signal — surface what is actionable and critical').** Two halves: (1) POST-CLOSE VOLUME — operator gets ~6-8 msgs after the close (the 16:00-18:00 chain: 9M-pace + entry-technique digests · EP recap · judge-delta digest · news-quality drift · partial/time-stop results · position update · evening brief; exact senders to be inventoried from scheduler + code sites). Direction: fold the routine digests into ONE consolidated 'Market Close Digest' (~16:55 ET); keep REAL-TIME alerts (fills/stops/errors) separate; anything self-healing/routine → mi_audit_log only (existing house rule). (2) EVENING BRIEF LENGTH — currently ~3 Telegram messages; cut to ≤1 (4096 chars): keep regime 1-liner + compact ecosystem scorecard + top-10 RS + actionable watch items; collapse/drop velocity·turners·pullbacks·wick/fishhook-stats sections to their EXISTING on-demand commands (/detectors, /htf, /fishhook — detail moves behind commands, not deleted). DELIVERABLE Fri: the sender inventory + a concrete before/after mock → operator rules the cut list (taste call) → implement + deploy. DoD: a normal post-close day = ≤2 messages (1 close digest + 1 evening brief ≤4096 chars) + real-time alerts only for terminal/actionable events; operator confirms signal quality. Observability-only — no strategy/trade-state changes (THE LINE). >> PROPOSAL + MOCK DELIVERED 7/17 (docs/analysis/479_telegram_noise_proposal_2026-07-17.md): 12-sender inventory (15:45-21:00 ET) → ~6 routine digests fold into ONE 'Market Close Digest' 16:55 ET (mock in doc; quiet-day = 4 lines; every line names its drill-down command); evening brief → 1 msg (keep regime/ecosystem/RS/book/watch; cut unanchored·velocity·turners·pullbacks·wick/fishhook·sugar·EP-stats to their EXISTING commands). Already-actionable-only senders untouched (L1/L2, concentration, spend, 🎓, backstop). AWAITING R1/R2/R3 rulings (fold list · cut list · 16:55 time) → then implement (digest job + render, sends→contributions, brief section trim; observability-only). >> R1/R2/R3 ALL RULED OK 7/17 + HALF 2 SHIPPED: the evening-brief cut is BUILT + DEPLOYED (one message: closeout/regime/scorecard/RS-top-10/cooldowns + the on-demand footer; demoted sections' params untouched; 85 tests green) — VERIFY tonight 18:00 ET = brief arrives as ONE styled message. Mock corrected: 9M Day-2 deprecated = NO digest line (detection pace only, non-empty days). REMAINING = HALF 1, the 16:55 Market Close Digest: `close_digest.py` contribute-buffer (same-process scheduler jobs 16:00-16:50 append; 16:55 flushes ONE message; per-job sends → contributions at the JOB level only — a send buried in a shared on-demand function stays) + swap the ~6 folded senders + tests. FIRST CARD tomorrow (SONNET — capacity rule) or post-resume. >> HALF-1 DEPLOYED 7/17 ~16:15 ET (both containers; mid-chain so TONIGHT'S digest may be partial — first FULL 🔔 digest = Monday's close; verify = one 🔔 message + market_close_digest_sent audit row; eod_cleanup fold = named follow-up decision). >> ✅ VERIFY-LIVE 7/17: BOTH halves live. 16:55 🔔 close digest FIRED (market_close_digest_sent: 3 sections 9M/JUDGE/SIGNALS folded, 934 chars, empties BOOK/EP/NEWS correctly omitted — quiet no-trade day) + 18:00 one-message brief styled (see #477). First FULL multi-sender digest = Monday's active close. >> ⚠ ROUND-2 (operator 7/17 eve: 'both still there' — I under-delivered R2/R3, folded only the EOD chain + cut optional brief sections while the two BIGGEST bloat sources went untouched). ACTUAL FIXES SHIPPED: (brief LENGTH) regime section ~20→4 lines (was every metric TWICE — grouped 'why' + a raw re-dump both on /regime; now verdict+net-score+cluster-if-firing+1 EP line) · cooldowns footer 8→3 · (message COUNT) nightly_data_pull 'complete' telemetry → LOG-ONLY (notify_job_success no longer Telegrams — matches its own 'silent' docstring; house rule) · HTF 'dropped-out 18' roster → 1-line count (roster on /htf) · anticipation SHADOW board push gated to NOTABLE nights only (entry-fired/settled/timeout; coiling-only nights → /anticipation). Suite 3345. REMAINING TASTE CALLS for operator one-word: STATE-CHANGES theme-transitions message · 🎓 graduation ping · RS-finviz link — fold/keep? DoD: operator confirms signal on next active close. [b1] [ok:operator-CLOSE reconcile 7/17 — verify-live pending Monday market / remaining-deliverable / event-gated] >> TASTE-CALLS RESOLVED 7/19 (operator): KEEP all 3 for now (state-change message · 🎓 ping · RS→finviz link) — revisit later to assess whether each is needed / how to reduce. Remaining = Monday's active-market multi-sender close-digest verify (deployed). >> RE-SCOPE (operator 7/20): the #479 approach was a MISTAKE — we "simplified" by CULLING/removing sections, which ORPHANED real signal (RISING/ROTATION, see #492) rather than simplifying. Operator still wants the brief SIMPLIFIED but via CONSOLIDATION/restructure (fewer, denser, higher-signal sections), NOT deletion. NOT today. Next brief-simplification pass = redesign for genuine density (merge overlapping sections, tighten formatting, keep every signal reachable) — pairs with the #492 restore + #493 pulse now living in the brief. DoD: a proposed consolidated brief layout for operator sign-off before any cut. [ok:operator 7/21 rebump — real-simplification redesign deferred, not-today]
- #444 | 2026-07-27 | in_progress | [ok:6-site sweep DONE+deployed 7/16 (af7f4f0) + re-verified 7/18 (all 6 correct + tested, 2 legitimately-global). RE-SCOPE = the MONEY-PATH account_mode finalizers order_manager×6 (405/1978/2134/2223/2444/2448) · live_tracker×2 (422/455) · entry_pipeline:313 — account_mode IS in-scope/resolvable at each, but these are trade-fill/exit finalizers on the money path → operator must AUTHORIZE before threading (THE LINE; cancel_unfilled_entries also has a global-scope comment to judge). Ready as a scoped card on your go] Operational safety / hardening - **Same-class account-mode-label sweep: the remaining `mode_prefix()`-without-account_mode sites** (follow-up to #443, operator 7/8 track-it). The #443 fix covered the two operator-flagged surfaces; the scout found the SAME legacy-paper-default pattern in lower-traffic spots that a live strategy's signal would also mislabel: `broker/bar_stream.py:128,184` (ORB subscribe alerts), `broker/telegram_confirm.py:60` (trade proposal), `ep_detector.py:2759` (catalyst-downgrade ping), `scheduler.py:4007/4045/4074/4099` (9M-day2 job — currently `deprecated`, so dormant), `agent.py:1083`. DoD: each strategy-bound surface resolves the owning strategy's account_mode (same pattern as #443 / entry_pipeline); non-trade surfaces (e.g. /status boot audit) legitimately keep `current_account_mode()`. Themes/observability = no money → shippable full once scoped. Lower urgency (magna53 is the only live strategy; these fire rarely for it). >> BUILT 7/16 night (sonnet card, orchestrator-verified): 4 sites THREADED (bar_stream per-ticker subscribe-fail via new account_mode param on subscribe_ep_candidate · telegram_confirm proposal — WHICH ALSO FIXED A REAL BUG: the STAGED-PAPER caveat condition read the legacy global, so a live-not-armed proposal could render as routine paper noise · ep_detector downgrade ping (lazy-memoized magna53 mode) · scheduler 9M-day2 ×4 reusing the already-fetched strategy row) + 2 sites JUDGED legitimately-global with # mode-ok comments (stream-level failure · /dryrun multi-strategy audit view). +6 tests; suite 3314. ⚠ DEPLOY NOTE: subscribe_orb_candidate's new kwarg crosses the intelligence↔execution facade — BOTH money containers must deploy from the same commit (Friday's deploy does). RESIDUAL same-pattern sites flagged out-of-scope (order_manager ×6, live_tracker :400/:433, entry_pipeline:297) — assess at verify-live whether they're strategy-attributable (same judgment pass) before closing. >> ✅ **BUILD AUTHORIZED by the operator 2026-07-25** — the money-path `account_mode` finalizer card (order_manager ×6, live_tracker ×2, entry_pipeline:313) is cleared to build **Monday 2026-07-27 during market hours** (operator awake, `deploy.sh execution` two-step per [[deploy-broker-changes-need-execution-step]]). Authorization given in advance so Monday is execution, not re-deciding. [b1] [ok:market push — money-path build, operator awake + market open, deploy.sh execution two-step — operator-directed 7/25, 2026-07-25→2026-07-27]
- #442 | 2026-07-31 | pending | Operational safety / hardening - **FL-3 clock hardening: retire _watchdog_target's summary-PROSE regex for structured audit fields** (/simplify altitude note 7/7, deferred - gated on watchdog work unfreezing). The #441 FL-3 refinement (real_service_down_dates) recovers container-name + down/recovered pairing by REGEX-parsing the watchdog's human-readable summary ('watchdog: <svc> DOWN - ...'). If that prose ever drifts, _watchdog_target silently returns None -> the selftest-exclusion + transient-pairing both stop matching -> FL-3 over-counts resets again (reintroduces the exact bug #441 fixed). Durable fix: emit STRUCTURED audit fields (container name + machine state) from infra/service_watchdog.sh that FL-3 reads directly. NOT urgent (FL-3 works today); bundle with the next watchdog change. DoD = watchdog emits structured fields + real_service_down_dates reads them not the summary + the regex retired.
- #484 | 2026-07-27 | pending | **entry_pipeline #461 cap-path cleanup — 2 /simplify money-path flags (7/18, flagged-only per the money-path guardrail; NOT auto-applied — deployed cap lock, THE LINE).** (1) QUALITY: the cap-block reason f-string is hand-copied in `_check_safeguards` (live_tracker.py ~:154/:177) AND `submit_trade_entry` STEP-6 (entry_pipeline.py ~:530-547), differing only in SQL scope / `BLOCK_*` constant / label — and the **#197 CAP+1 alert string-matches on it**, so a silent desync breaks that alert (or ledger's `cap_blocked` mapping). Fix = one shared `_cap_block_reason(kind,count,cap,mode,signal_type)` (or `evaluate_position_caps(conn,...) -> reason|None`) called by BOTH the cheap early gate and the authoritative locked recheck (`count_open_positions` itself was already deduped — only the reason-building wasn't). (2) POSSIBLE BUG: `submit_trade_entry` ~:592 writes a `cap_recheck_blocked` audit row manually AND then `_skip(cap_reason, audit_event="orb_blocked")` logs its own row → **two audit rows per fired race**; confirm intentional (a distinct verify-live signal) vs fold into one `_skip(audit_event="cap_recheck_blocked")`. DoD: operator rules (2) intended-or-fix + (1) applied behind #461's existing tests → re-deploy both money containers → verify the cap holds under the concurrent-entry sim. Money-path → CHANGE_PROCESS + operator sign-off. Refs #461, #197, `docs/setups/safeguards.md`. >> **DEFERRED TO MONDAY 2026-07-27 by the operator 2026-07-25** — batch the two /simplify rulings (shared cap-block-reason helper? · is the 2-audit-row race intentional?) into the same money-path session as #414's ADR-0029 D1 build. One session with the operator awake and the market open beats three separate ones. [b1] [ok:market push — money-path rulings, batched with #414 in one session — operator-directed 7/25, 2026-07-25→2026-07-27]
- #464 | 2026-08-01 | pending | Operational safety / hardening - **Money-path audit R3 (T2 audit 7/12): sync alpaca-py SDK calls block the event loop** — every `client.get_account()/get_order()/place_*` is a bare SYNC call inside `async def` (zero to_thread/run_in_executor in alpaca_client.py, verified); a hung Alpaca endpoint freezes the ENTIRE loop (WS fills + all jobs + the reconcile safety net). Latent-chronic (live since day 1). FIX: wrap money-path SDK calls in `asyncio.to_thread` + explicit `asyncio.wait_for` budgets (or the SDK's async client if suitable); verify no ordering assumptions break (finalizers rely on sequential WS dispatch — to_thread must not reorder handler-internal awaits). Medium refactor, own careful session + G6-style paper exercise. Register: `docs/analysis/moneypath_audit_2026-07-12.md` R3.
- #465 | 2026-08-01 | pending | Operational safety / hardening - **Money-path audit R4 (T2 audit 7/12): same-day dedup is CROSS-MODE at the schema level** — `mi_live_trades UNIQUE(ticker, alert_date)` has no account_mode (verified on prod pg_indexes): a PAPER row silently suppresses a LIVE entry on the same ticker/day via `ON CONFLICT DO NOTHING` (entry_pipeline:442) + the un-filtered pre-checks (:292/:315); fail-safe direction (skip, never double-order) but a missed REAL entry mislabeled `window:duplicate`; overlap plausible (a 9M day is often an EP day — 9M-Day2 paper × magna53 live). FIX: migrate the unique key to (ticker, alert_date, account_mode) + mode-filter both pre-checks + audit every ON CONFLICT site; live-table schema migration = careful path + dry-run script (#151). Register: `docs/analysis/moneypath_audit_2026-07-12.md` R4.
- #407 | 2026-08-08 | pending | **/promotetheme + anticipate /simplify cleanup (6/29; ALL no-money, collect-only — apply with the next theme-area change, not a standalone deploy).** 4-agent /simplify, 3 converged: (1) EXTRACT `_upsert_promoted_theme(conn, today, name, tickers, score, desc, days_active) -> bool` — `promote_candidate_by_name` (theme_engine.py:1515-1529) re-implements the BYTE-IDENTICAL guarded upsert of `promote_shadow_themes` (1432-1443) incl. the source-scoped ON CONFLICT + the str(res).endswith(' 1') tag-parse; a schema/guard change must move in lockstep or drift silently (NOT yet drifted). Extract the WRITE only; leave the RS-fetch/days_active compute per-caller (nightly batches, manual is N=1 — forcing one shape is premature). (2) drop the redundant `_rs` dict — the SQL already filters `WHERE ticker = ANY(tickers)` so `_vals = [r['rs_composite'] for r in _rs_rows if not None]`. (3) the 3 inline alpaca alert sites = KEEP inline (the established collector.py _polygon_get/_fmp_get convention) — swap the in-code reason from "raise-vs-[] asymmetry" (surmountable) to "explicit money-path clarity + minimal LOC". (4) the vestigial 4th `entries_fired` tuple element ('anticipate' constant) folds into #404 on the confirm_signal_at removal. Tests clean (29 new pass). [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #363 | 2026-08-05 | pending | **/simplify fast-follow: dedup the live_tracker exit-state PREAMBLE shared by `run_partial_exits` (3:45) + `update_open_positions_live` (4:45).** 3-agent /simplify consensus (reuse+altitude+simplification, 6/23): the two jobs now share a BYTE-FOR-BYTE identical state-load preamble — the `SELECT...WHERE status='filled' AND remaining_shares>0`, the running_closes/exits JSON coerce, the `hard_stop = trade.get('hard_stop') or stop_price` fallback, the `remaining_shares<=0 or today<=alert_date` skip guard, the `get_index_history`+no_data branch, and the 8-key `state` dict. RISK = this codebase's recurring maintenance-divergence class — change the preamble in one and the other silently drifts, and one feeds the PARTIAL, the other the TRAIL (money-adjacent). FIX: extract `_load_exit_state(trade, today) -> (state, daily_bars) | None` + call from both; the decision call (`apply_daily_exit_step` + the per-job `skip_*` kwargs) stays PER-JOB — the validated trigger flow UNtouched. Deferred from the 6/23 /simplify because it edits the EXISTING `update_open_positions_live` (partly outside the reviewed diff) + is deployed-live → warrants its own careful build+test+deploy, not a tail-of-session churn. DoD: shared loader + both jobs consume it + tests green + deployed. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #348 | 2026-08-05 | pending | consolidate the mi_safeguard_state get/set + conditional-bump upsert into a generic db helper (deferred from the 6/19 /simplify reuse review). There are now 4 inline copies of the `SELECT state FROM mi_safeguard_state WHERE safeguard/account_mode` read and 2 of the `last_transition_at = CASE WHEN state<>EXCLUDED.state THEN NOW()...` conditional-bump upsert: broker/drawdown_breaker.py:351-369, kill_scale_bands.set_last_band, plus the always-bump variants db.py get/set_holistic_judge_enabled + get_manual_halt_state/set_trading_halted. Extract `get_safeguard_state(safeguard, account_mode)` + `set_safeguard_state(..., *, bump_on_change: bool)` into db.py and migrate all copies. POST-LAUNCH (touches the #345 manual-halt panic button + the live-money drawdown breaker → behavior-preserving but needs careful per-call verification + the band module already imports db inside fn bodies so no new circular-import). Low priority; the inline copies are idiomatic/consistent today, so this is cleanup not a bug. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #343 | 2026-07-31 | deployed | Chart-vision axis SHADOW (post-#267 eval, operator 6/18, $30 approved). #267 result: text framing dead (B−A=0), chart 2/2 operator-correct but thin (C−B=2/36). Judge is LOAD-BEARING on paper → promoting into _build_judge_prompt would feed paper EP grades; too thin at N=2. ✅ BUILT (this session, not yet deployed): new `agents/market_intelligence/chart_axis.py` SINGLE-SOURCES the candidate notes (CHART_AXIS_NOTE / _TEXT_ONLY, cut-paste byte-identical from the eval) + modal_stable + render_prior_day_chart + grade_b_c (B text-only vs C +chart, fresh ×3 each); eval_chart_judge.py now IMPORTS them (arm A stays inline) so the two surfaces can't drift. `_chart_axis_shadow_job` (17:50 ET mon-fri) grades today's HIGH+MODERATE (cap 8/day), emits `chart_axis_shadow_graded`/`_delta`/`_norender` audit (delta detail carries floor/b_modal/c_modal/direction/BOTH rationales/png_path); SHADOW invariant = audit-only writes, own client+sem, fail-open, off the 9:45 path. `_chart_axis_shadow_weekly_digest_job` (Sun 19:30) sends each delta's chart inline via new briefing.send_telegram_photo + running N vs 7/31 backstop. db helpers: get_prior_daily_ohlcv / get_chart_axis_shadow_cohort / _processed_tickers (idempotency) / _delta_count. Tests: tests/test_chart_axis.py (12 green incl. note pin). DECISION GATE (registry chart_vision_axis_shadow_decision): N≥10 deltas OR 2026-07-31 → promote-load-bearing (CHANGE_PROCESS, ADR 0011) vs hold. ✅ DEPLOYED + VERIFIED-LIVE 6/18 (commit 1ed704f, deploy.sh both, DEPLOY OK all 7 preflight; apollo-market recreated on HEAD; job partition kept 46 = +2 [chart_axis_shadow + _weekly_digest], neither removed). Read-only cohort+render probe vs real 6/18 data: 2 HIGH candidates (UUUU/SWBI), BOTH charts rendered (111KB/102KB) — cohort SQL + prior-day render proven live; the grade-with-chart path was already live-proven by the 6/18 eval (same chart_axis fns). ✅ ADVISOR-HARDENED 6/18 (commit c804d30, redeployed both DEPLOY OK): (Gap1, BLOCKER) delta PNGs were on ephemeral /app disk → wiped every deploy → Sunday digest would lose the chart to label; FIXED = digest RE-RENDERS from the audit row's ticker+alert_date (render is deterministic), job saves NO files. (Gap2) confirmed MODERATE persists in mi_ep_alerts (21d: 56 HIGH / 17 MOD ≈ 4.9/day → matches $30 economics). Split into date-param _run_chart_axis_shadow core. INTEGRATION SMOKE on real 6/18 cohort (UUUU/SWBI): 2 graded markers, 0 deltas → full render→grade B/C×3→modal→audit-write path proven live, 0 pollution (graded uncounted; only _delta counts). FIRST REAL (counted) FIRE = Mon 6/22 17:50 ET (Fri 6/19 = Juneteenth holiday → empty no-op). This row closes when the registry gate decides promote/hold. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> ⚠ **PRE-SIGN CHECK ADDED 7/24 (operator-agreed):** prod shows `chart_axis_shadow_graded`=38 (6/19→7/24) and **`chart_axis_shadow_delta` = ZERO rows all-time**. A 5-week dark mechanism reading exactly 0 effect is more often an INSTRUMENTATION artifact than a true null — verify the delta emitter actually fires on a graded chart (and that its acting-population is logged) BEFORE signing the 7/31 'hold, no change' branch. [[shadow-zero-effect-check-instrumentation]] >> ✅ **PRE-SIGN INSTRUMENTATION CHECK DONE 7/25 — the 0/38 is a GENUINE NULL, not a broken emitter.** The emitter (`scheduler.py:3893`, gated on `visual_changed`) is correctly wired; all 37 detail rows hand-inspected — every stable B/C pair is byte-identical and every divergent pair is single-arm-unstable and correctly excluded. So the [[shadow-zero-effect-check-instrumentation]] concern I raised 7/24 is CLEARED: safe to sign the 7/31 'hold, no change' branch as designed.
- #414 | 2026-08-15 | pending | Entry-tuning for the stop-limit gap/no-trigger class (successor to the CLOSED alpaca_stop_trigger_reliability review, operator 7/4): widen the entry limit offset OR stop-MARKET entry with a chase cap — CHANGE_PROCESS (entry mechanics = methodology; N>=10 backtest per the review's own evidence base) + composes with #180's would-have-filled auto-classifier. DoD = a backtested proposal presented for operator sign-off (no unilateral ship). [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> +ROOT-CAUSE finding 7/6 (WULF live): entry OTO bracket auto-attaches a stop leg, then the SEPARATE 9:35 stop-refresh job places a 2nd stop -> self-conflict (insufficient-qty APIError, resolved by cancel-OTO-leg + keep-own-stop). Works but fragile + generates FALSE naked alarms (#433 = the UX half). Design half here: entry+stop should be ONE authority - decide OTO-leg-is-the-stop vs refresh-owns-it, one source; careful path. >> DESIGN folded into FABLE BLOCK #438 (operator 7/6): the single-stop-authority model (WULF root cause) + the gap-no-trigger backtest spec get designed there; card executes after sign-off. >> DESIGN LANDED in ADR 0023 (7/7): B2 single-authority spec (Card 4, fork F2) + the C1/C2 class split (C1 entry no-trigger -> Card 6 backtest + F3; C2 SYRE = gap-FILL, instrument correct -> Card 5 premarket gap alert + F5 parked). >> FABLE-DESIGNED 7/11 (block-1 extension): **ADR 0029** — D1 explicit stop-ownership contract (entry day = OTO child by construction; morning_stop_refresh gains a SAME-DAY exclusion → removes the WULF insufficient-qty conflict at the root, #433 retry stays belt-and-suspenders; NULL pointer = mirror defect healed by capture/sync/R1-ingest, never by refresh) + D2 gap-through fork measured-not-pre-decided (the paper-artifact split is load-bearing: would_have_filled = IEX artifact per #182, ONLY gap_through justifies live change; C1 backtest settles simulated fills through the real exit ladder; ship bar median>0 AND WR≥35% AND mean>0 at N≥10, park if gap_through N<10) + D3 chase-cap-is-the-sizing-overrun-bound (2% design ceiling). REMAINING = sign-off (F2: ship D1/C2 next execution deploy) → C1 backtest → D2 decided on the table → C3 gated + #151 exercise + effectiveness review. >> SITTING-RULED 7/12: **0029 F1-F3 SIGNED** → D1/C2 rides the NEXT execution deploy; F1 D2-winner stays evidence-decided (C1 table); F3 2% ceiling confirmed. >> C1/D2 BACKTEST PARKED 7/17 (per the ADR's OWN pre-committed bar, not a slip): the D2 gap-through fork ships/parks on gap_through N — ADR 0029 said 'park if gap_through N<10'. Prod classification (orb_cancellation_classification, all-time): clean_miss 16 · would_have_filled 6 · **gap_through 3** · data_unavailable 1. gap_through=3 < 10 → D2 PARKED, event-gated on the population reaching 10 (accrues slowly at the live cancel rate). D1 stop-ownership + C2 (F1-F3 signed 7/12) ride the execution deploy track separately. NEXT: re-check gap_through N when it crosses 10 (or fold into a data_gated_reviews predicate so it auto-surfaces). [ok:operator-CLOSE reconcile 7/17 — verify-live pending Monday market / remaining-deliverable / event-gated] >> ⚠ **ADR 0029 D1 CONFIRMED NOT SHIPPED (I code-checked 7/24):** `broker/live_tracker.py::morning_stop_refresh` still selects `WHERE status='filled' AND remaining_shares>0` with NO same-day / entry-day exclusion — the D1 stop-ownership fix signed 7/12 (F1-F3 SIGNED, 'rides the NEXT execution deploy') never landed, so the WULF insufficient-qty self-conflict class is still live. Operator-agreed 7/24: BUILD IT MONDAY MARKET-HOURS (money path — operator awake, market open, `deploy.sh execution` two-step). ETA held 8/15 for the broader entry-tune; the D1 build is the near-term action.
- #416 | 2026-07-28 | deployed | M&A-filter FP amendment (OPERATOR LABELS 7/4 — the Tier-2 sitting, Set A): of the 4 top material-miss suppressions, 3 labeled FALSE-POSITIVE (FRMI +25% & ONDS +23% via polygon_news; MMED +23% via keyword_in_text_1 — the crudest path; ONDS fired repeatedly across weeks) vs 1 TP (SUNE +216%, correctly suppressed). NEXT: pull the matched headlines for the 3 FPs → identify the match-mechanism failure (polygon_news false headline-match ×2 + raw-keyword ×1) → propose the amendment via CHANGE_PROCESS (filter changes = methodology; N-based, composes with #410's pin-guard + the mna_filter_direction_blindness 78%-unknown-path finding). DoD = amendment proposal with the FP mechanisms named, presented for operator sign-off. [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> SITTING-RATIFIED 7/12: PROCEED (headline-pull → mechanism → CHANGE_PROCESS draft, back for sign-off by 7/16). >> DRAFT DONE 7/12: `docs/analysis/416_mna_fp_amendment_2026-07-12.md`. The 3 ratified FPs root-caused to 3 DISTINCT paths/mechanisms: MMED=negated-keyword (keyword_in_text) · FRMI=polygon exploration/agitation (Path B) · ONDS=classifier acquirer-side/completed-deal (`claude_classifier`, NOT polygon as the old note said). Mechanism = BLACKLIST/3 targeted guards A/B/C (whitelist rejected: measured FN-surface 890/896); backtest flips all 3 ratified + ≤3 assessed (IMAX/D/PZZA), 0 known TPs, SUNE preserved; backtest-limits flagged (truncated-ILIKE lower bound) → precise full-text sim = the pre-deploy N-gate. ACTION (operator, a sitting ruling ~10min, no build): rule §6 forks [1] assessed-FP scope · [2] Guard-C depth (rec: port the existing polygon acquirer-heuristic) · [3] FP/FN asymmetry (tailed) + sign/fork the CHANGE_PROCESS amendment. BLOCKED ON OPERATOR DECISION (defer #3) — stays pending/7/16. >> **SIGNED 7/12 eve (rulings-pack R6, all 3 forks: IMAX=FP confirmed · Guard-C=surgical port · FN-tail accepted).** N-gate FULL-TEXT SIM RUN on prod: 896→862 parsed → 7 flips/5 distinct — MMED✓ ONDS×2✓ IMAX×2✓ **+2 proxy-missed finds (WEN 5/12 · IMVT 5/20, guard-A speculation — eyeball at build)**; ⚠ FRMI didn't flip (likely among the 34 unparseable truncated-at-write rows — live guards run PRE-write so unaffected, but PROVE guard-B on FRMI's reconstructed inputs before shipping). NEXT (this week, DoD=deployed+verified): guards A/B/C behind `mna_context_is_binding` + regression cases (MMED/FRMI/ONDS reject; SUNE + plain 'acquired by' still fire) + change-log in catalyst_rubric.md + deploy market-agent + verify-live per path. >> **FRMI PROVEN + GUARDS BUILT 7/13**: guard-B rejects FRMI ('proxy campaign' exploration, no binding escape) + preserves SUNE ('definitive merger'). Shared `mna_context_is_binding` + 3 per-path guards wired into ma_filter.py (A keyword-negation/spec · B polygon-exploration · C classifier-acquirer); SSoT change-log in catalyst_rubric.md; 15 guard tests + 29 ma_filter regression + suite 3089 green. ⚠ PRE-DEPLOY GATE (advisor 7/13): the N-gate sim used inline regexes ≠ the shipped guards (bare `could` broader than sim's `could be`) — re-run the replay with the SHIPPED functions, confirm 5 ratified FPs flip + no EXTRA flip is a real binding deal (FN check), THEN deploy market-agent (no same-day urgency: today's EP scan ran; affects next-day). >> **GATE PASSED 7/13**: shipped-code replay (896 rows) = same 5-ticker flip set as the signed sim (IMAX/IMVT/MMED/WEN/ONDS; no bare-`could` broadening), SUNE preserved, FRMI proven separately, the 2 extras (WEN/IMVT) speculation-not-binding = no FN. DEPLOY-READY (market-agent, operator go; affects next-day scans). >> **DEPLOYED 7/13 + smoke-verified in-container** (MMED/FRMI/ONDS guards veto ✓ · SUNE binding keeps firing ✓ · all preflight green). Full verify-live per path = the next EP scan where a real M&A candidate hits each guard (7/14 7–10 ET); close #416 when a live fire confirms un-suppression. [b4] [ok:push-plan 7/16 operator-signed — Fri verify / Fri-eve decision digest] [blocked:M&A Tier-2 sitting (operator labels done 7/4; sitting pending) — 7/18 CLOSE re-date] [blocked:operator decision/gate — 7/23 CLOSE re-date to 2026-07-28] >> **NEW FINDING 7/24 (same failure family — classifier-side FPs, surfaced by the #502 HTF dig): the stored `catalyst_quality='mna'` verdict can be SELF-CONTRADICTING.** ACLS 5/06 was graded `mna` while its own `news_summary` reads "No recent news or catalysts found for ACLS (Axcelis Technologies)", and a second same-day verdict's summary describes a DIFFERENT company (MRBK earnings — multi-ticker news bleed). That verdict FIRED the EP filter = a false suppression on a MONEY path; ACLS then ran $164→$191 (+16%). The #416 guards A/B/C do NOT catch this class — they test the TEXT for acquirer/negation framing, but here the LABEL itself is wrong and the text contains no M&A keyword at all (`matches_mna_keywords`=None), so nothing vetoes. Sample checked N=2, 1 junk. NOT actioned — needs the operator's read on whether a label-vs-evidence corroboration guard belongs in the amendment. Evidence: docs/analysis/htf_deal_pin_fresh_2026-07-24.md §6.
- #340 | 2026-08-01 | pending | DQ delta-check (the more-correct long-term design, deferred from #339; operator chose self-heal for the rare-step case): alert on a >X% night-over-night DROP vs the trailing median rather than vs a frozen baseline — catches sudden real drops AND auto-adapts to intended step-changes without the deadlock. Scope: applies to both ingest + rs_engine + sector checks; pick X; keep a one-time transition-window alert so an intended step-down is NOTED once, not silent. [b2] [ok:no-project-push-today] ▶ 2ND INSTANCE 7/2: the #286 liquidity floor (6/15) stepped the scored universe 3.9k→2.5k and nightly_data_pull sat empty_result EVERY market day for 2+ wks (dead-red signal, nobody noticed) — frozen-floor recalibrated 3500→2200 same-day (scheduler.py), but this class recurs until the delta-check ships. [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #281 | 2026-08-15 | pending | staging copy-not-derive hardening (restore fork + compose/env drift) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> [precondition folded from #280 (dropped 7/19, operator-deferred): before ANY market-hours staging run, provision an ISOLATED staging paper account — do NOT reuse the main paper account (staging test fills would pollute its 5-position cap + P&L). Not needed until a market-hours staging run is actually scheduled.]
- #176 | 2026-08-15 | pending | SoT/roster drift tooling — create-time hook + scheduled ping (residual) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #216 | 2026-08-12 | pending | jsonb codec consolidation (root-fix of #179) — POST-launch by design (protects window) — POST-launch condition MET 6/28, eligible. [b2] [ok:dep-sweep-0630] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Apollo Trades dashboard (portfolio-app2)


## Apollo Themes (portfolio-app2)

- #315 | 2026-07-31 | pending | RS-theme dashboard R-items (memory rs-theme-dash-backlog): R1 mobile/public-URL (Tailscale) · R2 bump chart · R3 canonicalization · R4 forward returns [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Theme ecosystems — primary → sub-theme hierarchy (ADR 0032)

_Marios-litmus borrow: a two-level ECOSYSTEM (E-\*) → sub-THEME (T-\*) surface. Litmus verdict (verified 7/14): Apollo DETECTS cyber dominance (cohort RS 75→89; cyber = #1 theme by member RS) but SURFACES it terribly (1 line of ~63; `/themes` renders Mainstream LAST; the engine kills its own cyber sub-theme splits twice — 7/07+7/13 Pass1 protect-strip). Design of record: `docs/decisions/0032-theme-ecosystems.md`; evidence: `docs/analysis/theme_ecosystem_litmus_design_2026-07-14.md`. Operator-signed 7/14: D1 curated taxonomy + auto-discovery (48h-grace one-tap veto) · D2 group+re-granularize (REQUIRED) · D3 member-union breadth-weighted score · D4 flip THEME_MERGE_ARM (#274). Themes = no-money detection (ships full); re-granularization THRESHOLDS carry CHANGE_PROCESS N≥10._

- #471 | 2026-07-27 | deployed | **Phases 2-3 — re-granularization (N≥10-gated) + new-ecosystem auto-discovery lane.** Phase 2 (D2, REQUIRED per ADR 0032 Part C): route Pass1 protect-strip subset-of-parent conflicts to the ADR-0025 Arm-B PARENT_CHILD adjudicator → persist child w/ `parent_theme` + register in `sub_theme_parents`; + bounded deliberate-split trigger for a sole-sub-theme ecosystem-dominant theme (≥10 members, ≥8 RS-80+). Acceptance fixture: the two killed vuln-mgmt births (7/07, 7/13) survive as T- children of the cyber theme. Thresholds → CHANGE_PROCESS N≥10 backtest before flip. Folds member-coverage repair: sub-theme discovery re-opens seats for the unthemed elite NET(97)/S(96); CYBR needs the RS-universe gap fixed first (small sub-item). Phase 3 (D1): weekly ecosystem-discovery pass over E-UNASSIGNED (cluster ≥3 themes, shared thesis+ticker overlap, sustained ≥2wk) → auto-promote to pending-grace + one-tap veto alert (`/promotetheme` idiom) → live at 48h grace-end, or vetoed+30d-cooldown; auto-promoted marked 🆕/auto + retroactively-retireable. DoD: fixture replays green + a synthetic unassigned cluster fires the veto alert + promotes on no-veto. blocked_by:#274 (THEME_MERGE_ARM flip → trustworthy depth(); #470 Phase-1 dep CLEARED 7/14). >> DESIGNED 7/14 to pure-execution depth (Fable, pending Opus-verify): `docs/analysis/theme_ecosystem_phase23_design_2026-07-14.md`. Phase 2 behind new `THEME_SUBTHEME_ARM` (default OFF) — Route A: at the Pass1 strip site a this-run newborn with **containment ≥C_MIN of ONE protected parent** routes to the corpus-cleared adjudicate_merge_pair → PARENT_CHILD sets parent_theme + sub_theme_parents; Route B: widened _split_fat_theme (sole-theme-in-ecosystem ∧ ≥S members ∧ ≥K RS-80+). Phase 3: Sunday weekly pre-cluster→Sonnet-propose→two-sighting/14d-sustain→sighted/pending/live-vetoed grace machine; **DB-taxonomy solve** = `mi_theme_ecosystems_dynamic` ∪ YAML base (YAML-wins, soft-delete retire) so auto-promote needs no image rebuild; veto = `/vetoecosystem` (mirrors /promotetheme, deploy scope BOTH). **Verified findings**: blast radius tiny (155 historical protect-strips, only the 2 vuln-mgmt fixtures are newborn-kills — 153 both-protected); a **latent one-directional coexistence bug (`theme_engine.py:3761`)** that also bites Arm-B once THEME_MERGE_ARM flips → symmetric fix rides the flip commit; D3 depth needs no change. **NEXT (this week)**: Opus-verify the load-bearing claims (blast-radius, the :3761 bug, the adjudicator's fixture verdict) → operator signs the **13 forks (§4)** → sequence: THEME_MERGE_ARM flip (+symmetric fix) → Phase 2 dark build → N≥10 backtest (71-birth sample, A1–A4 accept criteria) → sign-off → flip → Phase 3. MAPPING-QUALITY note (7/14): Haiku mis-weights the PRIMARY subject vs a modifier word ("Bitcoin Mining & Crypto Infrastructure Operators" → E-AIINFRA on "Infrastructure Operators"; manually fixed to E-CRYPTO + "Life Science Tools" E-UNASSIGNED→E-MEDTECH via mi_theme_ecosystems UPDATE method='operator_fix') — Phase 2/3 should add an operator reassignment path (beyond /vetoecosystem), tighten the assignment prompt (subject-over-modifier + exemplar anchoring), and periodically re-validate existing mappings. >> OPUS-VERIFIED 7/16 (design claims hold, one STRENGTHENED): blast radius now **156 strips / 3 newborn-kills** (Fable's 7/14 count was 155/2) — a THIRD cyber vuln-mgmt split ("Cyber Vulnerability & Exposure Management") was killed 7/15, same pattern → **acceptance-fixture set is now 3 (7/07, 7/13, 7/15)** + confirms the split-kill recurs DAILY (Phase 2 well-motivated); 153 both-protected (unchanged). The **`:3761` one-directional coexistence bug CONFIRMED** — the carve-out only checks `j_is_subtopic_of_i` (sub_theme_parents.get(j)==i), never the reverse, so a child that precedes/out-iterates its parent isn't protected → bites once PARENT_CHILD sub-themes exist; the symmetric fix must ride the THEME_MERGE_ARM flip. Adjudicator fixture-verdict deliberately NOT run (it's the paid flip-gate A1). Sign-off is prepped; with full capacity (operator 7/16) it can pull forward. >> FORK SIGN-OFF 7/16: operator APPROVED the 10 clear-rec forks wholesale — Phase-2 F-3 (DISTINCT strip-as-today+audit) · F-4 (:3761 symmetric fix rides the flip) · F-5 (Route-B removal semantics) · F-7 (divergence pins); Phase-3 F-8 (pre-cluster+Sonnet) · F-9 (two-sighting+age≥14d) · F-10 (YAML∪dynamic taxonomy) · F-11 (/vetoecosystem) · F-12 (48h/30d grace); F-13 (flip THEME_MERGE_ARM). REMAINING gates: **F-2** = operator labels the ~15-row kill-population (should-survive-as-child vs legit-kill; sets backtest accept-criterion A2 — Fable pulling the table 7/16) · **F-1/F-6** thresholds = N≥10 backtest sets them (operator signs the validated cell). NEXT after F-2 labels: build Phase 2 DARK (THEME_SUBTHEME_ARM off, toggle-off byte-identical) → §1.4 probe/backtest → operator signs {labels, thresholds} → flip. >> F-2 LABELED 7/16 (operator, verified vs prod — Fable table `scratchpad/F2_table.md`): **3 cyber (rows 5/14/15 = TENB/RPD/QLYS±VRNS) = SURVIVE-AS-CHILD** — ONE canonicalized child of Network Security (cohort stable containment=1.00; only the LLM NAME churns daily → Phase 2 must canonicalize by TICKER-SET not name); **12 biotech = LEGIT-KILL for the PARENT_CHILD backtest ONLY** (no protected parent to nest under → PARENT_CHILD rightly skips them). ⚠ The biotech kills exposed a SEPARATE real gap the operator's gut caught: **24/26 killed biotech tickers are ORPHANED, 12 still ELITE** (NRIX98·AGIO94·ZBIO94·ELVN94·TGTX92·RARE91·XENE90·ANNX86·ACAD85·KURA85·DNTH84·ALMS84) — the engine discovers+kills+orphans a real biotech cohort DAILY (name/member churn + no stable parent) → filed **#476** (theme-STABILITY/canonicalization, distinct from Phase-2 re-granularization; Fable's "absorbed into a broader theme" was WRONG — they're homeless). Phase-2 gates now: 10 forks + F-2 DONE; only F-1/F-6 (backtest-set) remain. >> PHASE-2 DARK DEPLOYED 7/16 (market-agent, arm OFF/fail-closed — byte-identical VERIFIED: snapshot-equal across 3 call shapes + poison-adjudicator + zero Phase-2 events + routed=0; 3→1 ticker-set canonicalization fixture pinned; fail-closed MERGE/DISTINCT/ERROR→strip; :3761 symmetric fix un-toggled; 21 tests, suite 3282). Flag row absent = dark; the flip is now a DB toggle. NEXT: §1.4 probe running (Fable, REAL adjudicator ~30 Haiku, C_MIN + (S,K) grids, A1-A4 accept criteria) → F-1/F-6 thresholds for operator sign → flip THEME_MERGE_ARM + THEME_SUBTHEME_ARM. ⚠ WATCH: G6 preflight replace-pending race recurred both 7/16 deploys (#439-part-b harness `_sweep_test_orders_until_clear` doesn't cover the replace-pending state; non-fatal, preflight-only). >> ⚠⚠ FLIP BLOCKED — §1.4 A1 PROBE FAILED 7/16 (Opus-run, real adjudicator): all 3 cyber fixtures return **DISTINCT, not PARENT_CHILD** (order-independent; re-verify proved the adjudicator DISCRIMINATES — MERGE on near-dups, PARENT_CHILD on a genuine broad×narrow like Endpoint×"Cybersecurity"). So vuln-mgmt is a **SIBLING** of Network Security, NOT a child — both distinct sub-domains under E-CYBR (matches Marios). Route A (PARENT_CHILD) can NEVER rescue the real kills: no broad "Cybersecurity" parent theme exists, only the sibling blob. PROPOSED REFRAME (under operator advisor/Fable review 7/16): **F-3 → coexist-on-DISTINCT-same-ecosystem** (newborn survives as a sibling, don't strip) — FLIPS the approved F-3 "strip-as-today" (that rec assumed A1 passes; it doesn't). Dark-build routing structure holds; swap the DISTINCT branch strip→coexist. **NOT FLIPPED.** Re-probe after the rework. The flip-gate did its job — caught the wrong mechanism before flipping it. >> RESOLVED (advisor + Route-B probe 7/16): the fix is **Route B (SPLIT), not Route A/coexist**. Advisor caught that coexist DUPLICATES (doesn't decompose) — the goal is members MOVING OUT = a split, which Route B (already dark-built) does. PROBE: real `_split_fat_theme` on the 11-member blob cleanly split off "Vulnerability Management & Cyber Exposure" (QLYS/TENB/RPD), leaving the network-security core (CRWD/PANW/FTNT/OKTA/VRNS/CVLT/RBRK/BB), 1 LLM call — the decomposition we want, NO PARENT_CHILD needed. Route A stays inert-but-harmless (fail-closed on the DISTINCT the cyber case actually gets). NO F-3 rework (coexist reframe SCRATCHED). Redefined: A1=clean vuln-mgmt split ✓; A2=no biotech over-fire (no ≥10-member dominant biotech theme) holds. **REMAINING before flip (→ Fable analyzing): (1) does the split STICK across days** — daily discovery re-proposes the full 11-member blob; does Route B's split persist, churn, or duplicate? THE question for fix-vs-churn; **(2) re-run §1.4 on the Route-B (S,K) criteria**, not the dead Route-A/PARENT_CHILD ones. Mis-named-blob insight: "Network Security & Zero-Trust Edge" is really all-of-cyber; Route B decomposes it into its real sub-themes over runs. >> STABILITY RESOLVED 7/16 (Opus code-trace — Fable 529'd on this so I ran it directly, it's load-bearing): **(1) THE SPLIT STICKS.** Two mechanisms: themes CARRY FORWARD daily (`_apply_carryforward_deterministic_filter`, strip-only — never re-adds removed members) so the split-off vuln-mgmt + the network core both persist; AND discovery EXCLUDES covered tickers (`theme_engine.py:629` `if tk in covered: continue`, covered = every non-Fading theme member). Once vuln-mgmt is its own carried-forward covered theme, QLYS/TENB/RPD are `covered` → never re-enter the `uncovered` discovery pool → never re-proposed → protect-strip never re-fires on them. The CURRENT churn = those names periodically dropping out of the blob's carryforward strip → falling uncovered → re-proposed → killed; a theme that's ABOUT them keeps them as its high-RS core (won't strip) → the oscillation ends. **(2) BLAST RADIUS = EXACTLY 1 THEME** trips the Route-B trigger (≥10 members ∧ ≥8 RS-80+) against the live universe: "Network Security & Zero-Trust Edge" (11/11 RS-80+) — the intended cyber blob, ZERO over-fire. A2 holds (no dominant biotech theme exists → #476). §1.4 redefined on Route-B (S,K)=(10,8): 1 fire, correct target. **VERDICT: GO on the flip.** Reversible DB toggle (`set_theme_subtheme_arm_enabled`, fail-closed OFF, no-money detection surface), operator-gated per code contract. Optional independent Fable second-look on the stickiness claim (operator offered to run Fable directly) before flipping. NOT FLIPPED — awaiting operator sign-off. >> FABLE DOUBLE-CHECK 7/16 (operator switched session model; adversarial re-verify): **CONFIRMED — GO**, with one evidence CORRECTION and two STRENGTHENINGS. Correction: the Opus trace cited the covered-exclusion at :629, which is the SHADOW path (persists via persist_theme_candidates_shadow) — the claim still holds because the LIVE path (`run_theme_engine`) has the identical guard at **:4928-4933** (`uncovered` excludes `covered_tickers`; velocity/turners too :4949-4953). Strengthening 1: Route B **self-disarms structurally** (:3098 — nominee must be the SOLE active theme in its ecosystem; after the split E-CYBR has 2 themes → can never re-fire on the parent; child itself skipped via parent_theme :3093; and even if the child's eco-mapping failed, the 8-member residue < SPLIT_DOM_MIN_MEMBERS=10 → still ineligible). Strengthening 2: **F-5 = MOVE semantics** (:5243 strips split members from the parent) → day-2 child↔parent ticker overlap is ZERO → the merge pass has nothing to adjudicate; the :4152 carve-out + `prior_sub_parents` reseed-from-persisted-parent_theme (:5162-5168, `_save_themes` persists it :1376-1390) are belt-and-suspenders on top. Pins: SPLIT_DOM_MIN_MEMBERS=10 / MIN_STRONG=8 / DOM_SPLITS_PER_NIGHT=2 / _SPLIT_MIN_STOCKS=3 (probe child = exactly 3 — passes). Blast radius re-confirmed = 1 theme (SQL is an UPPER bound; code adds sole-in-eco + eco-mapped + ≤MAX_THEME_STOCKS narrowing). Prod flag rows: BOTH arms absent=OFF (THEME_MERGE_ARM/F-13 also not yet executed — recommend staging it 1 clean armed run AFTER the subtheme flip for diff attribution). FLIP ATTEMPT BLOCKED by the permission classifier (wants the operator to name the flip explicitly) — exact SQL handed to operator; arm is read once per run (:5174) so flip timing vs tonight's 5PM-ET chain is race-free. >> **FLIPPED 7/16 17:15 ET (operator: "flip it")** — mi_safeguard_state row reads back `on`. Tonight's scheduled run beat the flip by ~10 min (run started 17:03 ET, flag read ~17:05 = OFF) → tonight's snapshot ran DARK. First ARMED run = operator-triggered "theme engine rerun" via Telegram (classifier blocked the agent-side rerun trigger — needs operator naming) OR Fri 7/17 ~5PM ET nightly. VERIFY-LIVE checkpoints on the armed run: (1) `theme_dominant_split_eligible` audit event on the cyber blob; (2) split applied — vuln-mgmt child (≈QLYS/TENB/RPD) born with parent_theme, blob residue ≈8 members; (3) child under E-CYBR on /themes; (4) NEXT-day run = the day-2 stability observation (child carries forward, not re-absorbed/re-killed, Route B self-disarmed). THEME_MERGE_ARM (F-13): still OFF — stage 1 clean armed run after, for diff attribution. >> ✅ VERIFY-LIVE 7/17 17:04 ET (FIRST ARMED RUN): Route-B SPLIT LANDED — 'Network Security & Zero-Trust Edge' 12→9 members, NEW child 'Cyber Exposure Management & Vulnerability Assessment' (3) with parent_theme set (theme_dominant_split_eligible fired eco=E-CYBR members=12 rs80=11). The whole flip-gate saga (Route-A-wrong→Route-B→stability-trace→Fable-double-check→operator-flip) landed CLEAN on run 1. DAY-2 STABILITY = tomorrow's run (does the split stick / not churn). [b2] [blocked:Phase-2 day-2 stability = Mon 7/20 run; then the N>=10 backtest gate — 7/18 CLOSE re-date] [ok:operator 7/21 rebump — blocked on the N>=10 backtest gate] >> ✅ SPLIT VERIFIED-LIVE 7/24 + ⚠ **REAL BUG FOUND (I confirmed vs prod, not agent-reported):** child "Cyber Exposure Management & Vulnerability Assessment" holds 3 members and the parent 9 for 6 straight sessions (7/17→7/24) — the split STUCK. BUT `mi_themes.parent_theme` is set ONLY on the birth row (7/17 = 'Network Security & Zero-Trust Edge') and is **NULL every day 7/20-7/24** — the parent link is NOT persisted across days, contradicting the design claim that `_save_themes` carries it. Stability currently holds because MOVE-semantics left ZERO ticker overlap (no ≥3-shared merge trigger), NOT because the linkage works. ⚠ Phase-2 `sub_theme_parents` logic would be built on a link that evaporates daily. FIX FIRST (no-money, theme read-model): persist `parent_theme` on every daily save + a regression test. ETA 7/25 (tomorrow, sonnet card). >> ✅ **PARENT-LINK BUG FIXED 7/25 (sonnet card; I verified the diff + independently re-ran the fail-without-fix proof).** ROOT CAUSE = lead 1, NOT the orphan block: `_rescore_existing_theme` rebuilds each theme dict from scratch every night and never included `parent_theme` in EITHER of its two returns (main + Fading branch), so `_save_themes`' `t.get("parent_theme")` wrote NULL every day after birth regardless of whether the parent lived. The orphan-remediation block (:965-993) was correctly exonerated — it only CLEARS a stale link and never had a positive restore path, and in the evidenced case `orphans` was always empty because the parent survived nightly. FIX (2 parts): (1) both returns carry `parent_theme` forward — this also closes a LATENT bug, the fat-theme re-split guard (`and not t.get("parent_theme")` — 'never split a sub-theme further') had NO working signal for a continuing child since the key was always absent; (2) new `_restore_sub_theme_links()` as the single reconciliation point immediately before `_save_themes` — re-sets the link when the parent is live, CLEARS it when genuinely orphaned (preserving the real orphan semantics), checked against the FINAL list so it also catches parents dropped by `_run_thesis_merge_pass` (which runs after the last diagnostic) and by auto-retire. Retired rows are skipped — their `parent_theme` is a deliberate `theme_auto_retired` SUCCESSOR pointer, not a sub-theme link. 6 regression tests (`tests/test_theme_parent_link_persistence.py`) — I stashed the fix and confirmed 6 FAIL without it / 6 pass with; full suite **3628 passed** (3622 baseline + 6). SSoT `docs/architecture/theme_engine.md` updated same-commit, incl. an honest known-gap note (`_canonicalize_theme_names` runs INSIDE `_save_themes` AFTER reconciliation and could rename a parent/child in the same run → link briefly mismatched until the next night re-derives it; pre-existing, shared with the older sub_theme_parents merge-protection code, NOT introduced here). ⚠ **FORWARD-ONLY**: the already-NULL rows 7/20-7/24 have no source to restore from (`get_active_themes` reads only the latest row per name) — a backfill would be a prod WRITE and is the operator's call, not taken. ⚠ **NOT DEPLOYED** — theme engine is market-agent-owned and its nightly job is `mon-fri`, so the fix first acts **Mon 2026-07-27 ~17:00 ET**; deploy is operator-gated. VERIFY-LIVE = `mi_themes.parent_theme` NON-NULL for the cyber-vuln child on Monday's row. [ok:market push — the theme-engine job is mon-fri, so a Saturday verify is IMPOSSIBLE; first confirmable run is Mon 17:00 ET — operator-directed 7/25, 2026-07-25→2026-07-27]
- #491 | 2026-08-02 | pending | **Theme thesis-drift / stale-identity anchoring — the "crypto-miner → neocloud" gap (operator 7/20).** CONFIRMED in prod 7/20: HUT + CIFR sit in "Bitcoin Mining & Crypto Infrastructure Operators" (Fading), and a SEPARATE "AI Cloud GPU & Datacenter Colocation Platforms" theme exists but does NOT absorb them — yet HUT/CIFR/IREN/CORZ/APLD/WULF (+peers) have PIVOTED their business to AI/GPU cloud compute ("neoclouds": repurposed power + datacenters for AI/HPC hosting deals). ROOT GAP: the engine anchors a theme's IDENTITY on legacy price-correlation + legacy-name LLM description, so a live business-model pivot is invisible — the cohort's real driver (AI-infra) is misattributed to a fading crypto theme (and, downstream, the wrong ecosystem). DISTINCT from #476 (name/member CHURN) and the #471 mapping-quality note (Haiku subject-vs-modifier): this is mis-THESISED, not unstable or mis-mapped. SCOPE (design-first; no-money detection surface — THE LINE untouched): (1) how does a bottom-up price-action engine DETECT a business-model pivot — candidate signals: per-ticker catalyst/news (AI/HPC deal announcements), a description-refresh cadence that re-thesises a carried-forward theme when members' catalysts shift, or a periodic thesis-revalidation pass; (2) MIGRATION — how HUT/CIFR EXIT the crypto theme into the neocloud/AI-cloud theme WITHOUT a manual ban (mi_theme_exclusions is operator-only PERMANENT bans, NOT the tool); (3) GENERALIZE — sweep for other themes carrying stale identities on pivoted cohorts. DoD: a design doc characterizing the gap + a proposed detection/migration mechanism + the concrete fix for the neocloud cohort → operator rules build. DEFER (bias-to-action reason #4 — big-rock, own scoping session): operator flagged as a side-note, not now.

## Market Strength Map — holistic multi-asset leadership (operator 7/20)

- #494 | 2026-07-27 | pending | **SCOPE the unified market-strength map — "where is strength, across ALL asset classes?" (operator 7/20 north-star).** Framing: the RS/theme work should roll up to ONE holistic view of where strength is — equities (sector/theme/ecosystem), crypto, metals, energy, whatever leads. WHAT WE HAVE (equities): the ADR-0032 ecosystem board IS this for stocks — ~9700 → themes → 20 ecosystems ranked by breadth-weighted RS (E-CYBR/E-INS...) + ROTATION WATCH (sector turns) + RS leaders; metals/energy appear as miner/ETF equities. GAPS: (1) EQUITY-ONLY frame — crypto is a separate shadow silo on its own RS scale, metals/oil only as equities; (2) FRAGMENTED across surfaces (ecosystem scorecard · rotation watch · RS leaders · crypto-hidden) — no single map; (3) NO CROSS-ASSET ranking — equity-RS vs crypto-RS percentiles aren't comparable, so nothing says "crypto is THE leading asset class now vs stocks vs metals" (the one-glance answer to the BTC/ETH question). DELIVERABLE of THIS task = a DESIGN DOC: the unified strength-map model (a common cross-asset RS frame + an asset-class leadership layer above the ecosystem board + rotation), the surface (brief/hud, NO new command), and a phased slice plan. SLICE 1 = #493 (crypto-vs-market pulse, prototyped). Crypto depth = #492-B; theme thesis-drift = #491. Big-rock → design/scope first (bias-to-action reason #4); operator rules the phasing. DoD: design doc + phased slices with the operator-approved sequence. >> DESIGN DOC created 7/20: `docs/design/market_strength_map.md` (durable goal + inventory + gaps + 2-layer model + findings + slices). ⚠ CORE OPEN QUESTION per operator (flesh out at design, DO NOT decide yet): the asset↔equity split is NOT clean — precious metals (asset) + miners (equities), crypto + crypto-miners/MSTR/COIN, energy/commodities + their equities all rise together; how to MIX so we see strength in BOTH + their relationship? Options: SEPARATE (asset-RS vs equity-RS layers) / UNIFIED (one common frame) / HYBRID "complex" (group asset+equity-expression, show the miner-vs-metal divergence — leading candidate). Sub-Qs: common cross-asset RS frame, commodity/spot price sources, how a complex maps onto themes/ecosystems, rotation across classes.


## Fable planning reserve (operator 7/4 — Fable plans in max detail, Opus then executes purely)

_Directive: reserve remaining Fable capacity for these two planning deliverables (post-Sunday-CLOSE);
heavy execution goes to Sonnet cards or waits for Opus. Plans must be written to PURE-EXECUTION depth:
explicit specs, DoDs, acceptance criteria, sequencing — zero open design forks except explicitly
operator-gated ones._

**WEEKEND FABLE BLOCK — assembled + operator-confirmed 7/8 (design to pure-execution depth; the meta-rubric
cluster #328/#330/#331/#335/#368 is EXCLUDED — already ADR-signed → Opus/Sonnet execution, not Fable).**
TRIGGER: `docs/roadmap/fable-weekend-blocks.md` — operator says **"run fable weekend block 1"** (inline Fable) → it executes this set. The full self-contained brief lives there; the per-task detail lives on each #-task below.
Each is design-only: SSoT/ADR amendment + CHANGE_PROCESS + operator sign-off + N≥10 backtest before ANY live
flip. THE LINE holds — Fable designs, it does not change live behavior.
_PRIMARY (Tier 1 — the core block):_
- **#274** — theme-fragmentation controls (data-backed by the 7/8 L2 anomaly): the 3 scoped levers
  (dissolve-on-flagged-pair · sector/narrative-overlap merge arm · re-open description-guard-vs-birth-min-member,
  ADR 0007 §3 anti-noise caveat). Opus preps a read-only evidence pack (which themes each lever merges/dissolves
  + legit-kill check) to feed the design.
- **#332** — setup-class / conviction rubrics (Pradeep small-cap-explosive vs Qullamaggie mature-leader vs
  episodic mid-cap): the per-class composition — meta-rubric long-pole, NOT yet designed.
- **#354** — merge flag_continuation → consolidation (ADR 0013 Phases 2–4): undercut→U&R re-gating, params to the signed model.
- **#170** — EP cooldown re-setup admission: shorten/override the ~60d cooldown; detection-criterion + backtest methodology.
- **#146** — flag direct TIGHTENING→TRIGGERED (drop the COILED prereq): detection-criterion change + N≥10 backtest design.
- **#297** — Family B EP rework (gap-anchored replay + mi_anticipation_lifecycle inherit): new detection-class design.
- **#357** — persistent Sugar Babies role decision (setup vs condition vs confluence): framing — needs operator direction in the design.
_STRETCH (Tier 2 — if time):_ **#394** coil-finder threshold-tuning methodology (design now; tune when #327 forward-shadow N is sufficient — thin this week) · **#333** catalyst-durability forward axis (design sketch; build gated on #210/#211 sourcing) · **#299/#301** tape-features / ensemble-divergence grade-path (gated on funding + the #335 batched eval).


## Apollo v1.1 — fast-follow program (spec: docs/roadmap/apollo-v1.1-v2.0.md PART I)
- #501 | 2026-07-30 | pending | **Silent-failure observability hardening — wire the 13 GATE-INVISIBLE gaps from the 7/23 taxonomy audit (complementary to #466).** SoT: `docs/analysis/silent_failure_taxonomy_2026-07-23.md` (Fable audit, Opus-verified F1 vs code). Finding: the money-path broad-except hygiene is GOOD (0 broad+silent in `broker/` post-#382); the dangerous residue lives in the classes the AST gate CANNOT see — so #466's "drive the 81 baseline → 0" would close NONE of these. 13 real-gap findings (~19 sites), ALL fixes **no-money observability-only** (add audit row / Telegram / counter / wording — zero strategy/safeguard/sizing/trade-state change; THE LINE untouched. The one adjacent money-path decision — F9 `read_breaker_state` fail-open DIRECTION — is operator-ruled + explicitly NOT proposed, alert-only). **TIER-1 (money-path safety-net silent-deaths):** F1 the naked-position watchdogs (`_stuck_fill_watchdog_job`/`_stop_ack_timeout_watchdog_job`) can die leaving only an UNWATCHED `mi_job_runs status='failed'` row — no Telegram/audit (VERIFIED 7/23: no global APScheduler error listener; watchdogs excluded from the once-daily `_EXPECTED_JOBS` liveness list; self-heals on a transient failure, goes DARK on a persistent bug) → ONE fix in `core/job_audit.py:135-144` covers every no-handler job · F2 "200-OK-but-empty" snapshot = silent detection blackout (6 sites: ninem/ep/flag×4) → 1 shared empty-snapshot guard · F3 WS-outage backstop chain's own failures log-only (`_check_fills_job`/`_stream_health_watchdog`) · F4 the 15-min DB↔Alpaca reconcile can lose a whole account-mode silently. **TIER-2/3 = F5–F13** (`%_failed` sweep-miss → naming rule + test, EP re-poll self-disable, 9M volume / EP parse null counters, boot SDK-guard, state-alert-failure aggregation). Prioritized 11-item fix list in §5 (mostly S/XS effort). DoD: the Tier-1 four surfaced (audit + deduped Telegram) + operator rules the Tier-2/3 batch. Refs #466 (gate-visible baseline sibling), #381 (no-silent-failures rule), `feedback_no_silent_trading_failures.md`.
- #466 | 2026-08-15 | pending | **Phase-2: drive the swallow-a-failure baseline to ZERO (#381 re-home — operator-ruled 7/5, #418 D-8: baseline-zero → a dated #419 Phase-2 milestone).** #381's v1.0 bar is DONE (money-paths clean via #382 + the `preflight_no_silent_failures` RATCHET gate is LIVE, blocking NEW swallows every deploy); this closes the EXISTING inventory to satisfy the ABSOLUTE "completely gone" directive (operator 6/25). DoD: classify each of the ~81 baseline swallows (deploy prints "81 known baseline swallow(s) — trend must go DOWN") A=swallows-a-real-failure (wrap in WARN/raise) vs B=benign control-flow (tag `#loud-ok:reason`), drive the preflight count to 0, then flip the gate from baseline-allowed to zero-tolerance. Evidence: scripts/preflight_no_silent_failures.py. Post-v1.0 hardening. [ok:#419 Phase-2 re-home of #381, operator-signed 7/13]

_W1 selection-replay + kill/scale = #268 (done) + #275; W2 entry-mechanics = #276; W4 chart-vision = #267 (Launch project). Below = the waves not yet broken into tasks._
- #306 | 2026-08-06 | deployed | v1.1-W3 exit & management: winner-harvest tune (partial size · trail-by-character · capture_pct KPI) + #91 time-stop refinement — trade-state, #151 harness discipline. STEP-0 DONE 7/4 eve (docs/analysis/w3_winner_harvest_step0_2026-07-04.md): N=10 partial-taken winners = 18% aggregate MFE capture ($5.9k of $32.8k peak; tier-one bar >50%) — 3 round-trippers (CRSR 15% / SMCI -27% closed-a-loss / PURR 0%); management leak > entry leak in $. SYRE -4.57R corroborates #414. STEP-1 DONE 7/5: capture_pct LIVE as weekly-review KPI (system_review deterministic appendix, SQL verified vs prod 10/$5,942/$32,775/18%; deploy pending). SCOPED 7/5 (doc §'The tune'): STEP-2 = axis-sweep backtest on the closed cohort (A giveback peak-lock [SMCI/PURR] · B 10/20MA trail-by-character [CRSR] · C partial size 33-50%) -> ranked parameterizations, no trade-state; STEP-3 = operator decision + CHANGE_PROCESS + #151 harness + paper-exercise before any live flip [ok:not-a-slip - STEP-1 shipped+verified 7/5, ETA advanced to the STEP-2 sweep] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> STEP-2 design folded into FABLE BLOCK #438 (Tue 7/6-filed): the sweep spec + backtest methodology get designed there, then a card executes. >> ADR 0023 CARDS 1–3 DONE 7/8 (dark evidence, committed ef39219, NOT deployed — exit discipline = THE LINE): Card 1 = `exit_logic.giveback_floor` peak-lock hook (arm ×floor, default-OFF) + Card 2's `sma_10_20_handoff` trail (both dark, 16 tests, existing callers byte-identical). Card 2 = `scripts/_306_harvest_sweep.py` — replays the 28-trade closed cohort through the A×B×C grid; MARGINAL model (non-early exits anchor to actual so replay error cancels), UNADJUSTED bars (fill-basis; deviates from ADR-A1 `adjusted=true` deliberately), fingerprinted+cached (`306_bars_f8fa620c.csv`); 7 tests. Card 3 = `docs/analysis/306_step2_sweep_2026-07-08.md` (ranked sweep + STEP-3 decision sheet). FINDING (evidence for F1, NOT a rec): peak-lock at +6–8% arm / 60% floor = **+$8,075 lock-attributable** (best−baseline, capture 23%→52%), concentrated in the round-trippers (SMCI +$1.2k / CRSR +$3.2k / RCAT +$1.5k); trail(B)/partial(C) second-order; losers untouched; 4 baseline-divergent names flagged (IBM's −$604 is recon noise, NOT a lock clip). REMAINING = STEP-3/**F1** operator sitting (adopt a parameterization or none; ADR §5 slots it Thu 7/9) → any live flip = CHANGE_PROCESS + #151 + the `harvest_rule_flipped` audit + the standing `harvest_rule_effectiveness` review. Card 5 (9:00 gap alert) DONE 7/8 (0ee86fc — briefing.premarket_gap_risk_scan + collector.get_premarket_prices + a 9:00 ET intelligence-owned job; read-only telemetry, deploy operator-gated via deploy.sh market-agent; WATCH-ITEM: min.c can be a thin/stale print on illiquids → occasional false gap-risk ping, confirm at verify-live). Card 6 (#414 entry backtest, Fri 7/10) still open. >> F1 (STEP-3 sitting) RESOLVED 7/9: operator picked the peak-lock DIRECTION (arm +6% / floor 60%) but chose to VALIDATE in SHADOW before any live flip — paper is dormant so the shadow rides the LIVE MAGNA53 book. `giveback_shadow.py` SHIPPED (committed 46c75aa, 4 tests, suite 2886 green + gates): `_giveback_shadow_job` @17:38 ET mon-fri replays each newly-closed live trade WITH vs WITHOUT the +6%/60% lock → logs the MARGINAL to `mi_giveback_shadow`; pure compute + DB, NO live-exit change (THE LINE). ACTIVATES on the next market-agent deploy (operator-gated). Accrues at MAGNA53's real close rate (only round-trip winners signal) → slow by design; the offline 306_step2_sweep (+$8k lock-attributable) stays primary evidence, the shadow adds forward confirmation. REMAINING = (a) deploy the shadow (market-agent), (b) evidence accrues → AUTO-SURFACED for review via data_gated_reviews `giveback_shadow_review` (predicate ≥10 rows / earliest 8/06; can't silently log forever), (c) the actual LIVE-flip = CHANGE_PROCESS + #151 harness + sign-off once the shadow confirms the offline finding forward. >> SHADOW DEPLOYED + MECHANISM-VERIFIED 7/9 (operator go, market-agent + apollo-execution) — NOT yet 'writes-rows' verified-live (no row exists until a live round-tripper): orchestrator untouched via a deploy.sh guard-classification fix (320c815 — root-file data_gated_reviews.yaml was dragging in full 3-svc scope; confirmed NO non-market-agent reader) + an exec-boundary-ok tag on the pure-ladder import (1f5360f, flag_detector #396 pattern). All 7 preflight gates green BOTH containers; dual_account_boot_verified. On prod WULF (245): mi_giveback_shadow auto-created + fetch + None-guard clean (WULF=0 daily closes, same-day stopout → len<2 None). WRITE half (compute-with-value + 12-col INSERT + jsonb-string parse) is pinned by 3 mocked-pool tests but has NOT run on prod yet — the first real INSERT awaits a live round-tripper. MONEY-PATH side effects of the deploy VERIFIED (advisor 7/9): the live live_tracker exit callers pass ZERO giveback kwargs → hook inert (THE LINE held); #443 is skip-row labeling only (read the hunk, not just the commit msg). 17:38 ET job registered. REMAINING = (b) FIRST-ROW verify-live on the next live round-tripper (the actual writes-rows bar) + the `giveback_shadow_review` gate, (c) the gated live-flip. [b5] [ok:push-plan 7/16 operator-signed — Fri verify / Fri-eve decision digest] [blocked:exit-tune axes B/C/D sign at the ~8/06 shadow review (digest) — 7/18 CLOSE re-date] >> 🔴 **FIRST LIVE-MONEY EVIDENCE 2026-07-25 (#503 forensic) — the arm-basis fork is now decidable, and the hole is COSTING money.** Not theoretical: **MANE reached +7.92R and closed at −0.23R; NVCR reached +2.00R and closed at −1.00R** — both fully reclaimed by day-2 reversals with NO mechanism in the path (SMA trail needs ≥10 closes · partials start Day 3 · the 9:35 refresh re-places the SAME stop · `giveback_floor` built but DEFAULT-OFF, no live caller). Measured counterfactual: the ALREADY-SIGNED close-below-decision-line giveback rescues **only MANE** (~+1.7R; cohort −7.5R → −5.8R) because NVCR/SMCI gave back INTRADAY — reaching those needs the intraday-arm variant. ⚠ **SMCI carries this exact exposure LIVE RIGHT NOW: +3.21R MFE seen, stop still −1R.** ⚖ THE LINE — any flip is CHANGE_PROCESS + #151 harness + sign-off. Note `mi_giveback_shadow` is still 0 rows because it is gated on a round-trip WINNER and the cohort has none — the shadow cannot inform this decision; the forensic is the evidence. Ref: docs/analysis/503_zero_for_nine_forensic_2026-07-25.md >> ✅ **INTRADAY PARTIAL ANALYSIS DONE 2026-07-25** (`docs/analysis/306_intraday_partial_analysis_2026-07-25.md` + `scripts/probes/_306_intraday_partial_sim.py`, offline replay over cached Polygon minute bars; Fable, key claims verified by me). Rule simulated = operator's: **sell 1/3 at trigger L, move stop to BREAKEVEN on the rest.** SWEEP vs actual −8.37R (deployed basis): +2% → −1.57R (+6.80) · +3% → −4.70 · +5% → −5.51 · +8% → −4.44 · +10% → −6.82 · 1×ADR → −4.83 (+3.54) · 2R → −5.96. **Every level helps and cost reads 0 — but that 0 is STRUCTURAL, not real: a 0-for-9 cohort has no winners to damage.** The +2%/0.5R rows are top-tick curve-fit (4 trades peaked at exactly +2.1-3.1%). ⚖ **BASIS FINDINGS (the operator's character scope-add):** (1) **fixed-% is wrong by construction, now quantified** — cohort ADR20 spans 2.6-9.7%, so +8% = 0.9×ADR on WULF but 3.1×ADR on WKC; (2) **my hypothesis that R-basis already normalises character is WRONG** — stop distance is 0.15×-1.19× of ADR20 across the 10 trades (the ORB low is one morning's range, not the stock's personality); +2R = +2.2% on MANE vs +12% on NVCR. The simple 'just use R' answer is NOT supported; (3) **ADR-multiple is the right frame** — 1×ADR captures both live runners near their peaks with ONE parameter, and on the paper cohort the ADR variants are the only ones whose benefit doesn't hinge on a single gap-dodge; (4) `character_profile()` checked per instruction and it is the WRONG tool — it profiles MA-respect/pullback depth (trail-side), not run-rate; the fast/slow axis is **ADR%**, whose convention already exists in flag_detector. No new taxonomy needed. 🔴 **THE COST IS REAL AND LARGE (this is the finding that should govern the decision):** replayed on the PAPER winners (+12.23R actual, holds 4-24d), at +3% all 10 trigger and **6 of 10 are BE-scratched mid-hold on days 1-8, winner cohort −2.46R (−20%)** — CRSR scratches day 1 and misses its +12.9% day-2 gap (−1.57R). At 1R: −4.53R. Only at +10% / 2×ADR does the paper cohort stay positive. **The operator's own 'BE stop on day 1 of a 16-day run' worry is confirmed at ~50-60% of triggered winners at low levels.** DEEP TENSION, unresolved at this n: **the levels that best rescue the dead live cohort are exactly the levels that most damage the winners.** STRUCTURAL TRIGGERS: **zero captures on 9 trades** for geometric reasons (prior-day highs all below entry; 20d/52wk highs either already broken at entry or 10-36% away — THC's 52wk sat +2.29% vs a +2.13% peak, missed by 0.16%). Inherent to EP entries. The **gap-up-open** variant fires 6/41 (NVCR d2 +3.5% banks +1.33R near the day's best price) — a plausible COMPLEMENT, never a primary (MANE gapped DOWN on d2). BUILD GAP: price access already exists (websocket seed + 5-min extremes poll), and the stop-replace-then-sell partial path exists but is **gated to 15:45** (`live_tracker.run_partial_exits:691`, #361); `breakeven_active`/ `partial_taken` columns and the `effective_stop=max(...)` composition point exist. Genuinely missing: the intraday trigger EVALUATOR, generalising the partial-sell out of its 15:45 window with idempotency, and ⚖ a new ORDER-EMISSION site = THE LINE, operator ruling required. **Recommended cheapest next evidence step: run this replay as a LOG-ONLY SHADOW on live positions — evaluator only, no order path, no strategy change.** >> 🔑 **FEED FINDING 2026-07-25 — the intraday tracker is STRUCTURALLY BLIND to the class the mechanism must protect, and it decides the shadow's design.** `track_open_position_extremes` (order_manager.py:3735) polls `hour=9-15, minute=*/5` and calls `collector.get_minute_bars(ticker, today, today)` — **Polygon 1-min aggregates**. Granularity (1-min) and cadence (5-min) are both FINE; **the SOURCE is not** — Polygon Starter is the same ~15-17min-delayed feed #489/#490 exist to fix. At the 09:35 poll the available bars stop ~09:18, i.e. BEFORE CRCL's 09:31 fill, so the `b['t'] >= filled_ms` filter returns empty → bare `continue` → nothing written; by the time Polygon publishes 09:31-09:37 (~09:52) the trade closed 09:40:34 and is no longer OPEN, so it is never revisited. **Evidence — the correlation is exact across all 9:** under ~15min CRCL 9m→0.00R · WDFC 9m→0.00 · TSEM 12m→0.00 · HUT 51s→0.00 (WULF 24m→0.48, on the boundary); over ~15min WKC 2.5h→0.90 · THC 3.2h→0.64 · MANE 24h→7.92 · NVCR 30h→2.00. ⚠ **DESIGN CONSEQUENCE: the intraday-partial shadow must NOT be built on Polygon minute bars** — on a delayed feed the evaluator would be blind at exactly the moment it needs to fire. It should read the **Alpaca real-time feed RT-1 shipped today** (the #490 universe overlay already pulls RT prices every scan tick, and `trade_stream` already holds a live websocket for fills; OPEN POSITIONS are a handful of tickers, so RT for them is near-free on infrastructure already deployed). ⚠ ALSO: any existing analytics keyed on `highest_price_seen` UNDERSTATE fast trades — that is what corrupted #503's MFE table. ⚠ **HYPOTHESIS, not yet proven**: inferred from the 9/9 correlation + known Starter behaviour; I could not test `/v2/aggs` latency with the market closed. **MONDAY ACTION (~1 min): during RTH, call `get_minute_bars(<liquid ticker>, today, today)` and compare the newest bar timestamp to now** — that settles it. >> ✅ **SHADOW DESIGN DONE 2026-07-25** (`docs/design/306_intraday_path_recorder_2026-07-25.md`, Fable; reviewed against a second opinion; I verified the claims that correct me). **PATH-recording confirmed over trigger-verdicts** (verdicts lock in a guessed level and need a redeploy to re-test; the path lets every level/basis/segment be swept offline) — with 2 refinements: (i) the recorded table is the LIVE-CAPTURE layer, not an eternal archive — BE-scratch counterfactuals on winners run DAYS past the real exit (paper cohort scratched on day 8), so the review stays hybrid-source: recorded bars for same-day fidelity + Polygon refetch for post-exit extension and anything past the 120d prune. **Net effect: NO schema change, no new table, prune unchanged.** (ii) extremes repair is SUBSUMED, not bolted on. ⚠ **TWO DEFECTS, NOT ONE — my card's root cause was incomplete.** #1 is the Polygon delay; **#2 is the selection predicate**: polling only CURRENTLY-OPEN positions means a trade filled 09:36 and stopped 09:39 ghosts entirely between polls, and EVERY trade loses its final ≤5 min. Fixing only the feed leaves the bug class alive. Predicate is now open-OR-closed-today as a tz-aware TIMESTAMPTZ compare (**no `::date` cast — that is the #310 bug class I confirmed this morning**) + a 16:10 ET sweep for the 15:55-16:00 tail and multi-day healing. ⚠ **CORRECTION TO MY PREMISE:** I told the operator `highest_price_seen` is read by no decision path — **WRONG**, verified: the time-stop scan gates its candidate set on it (`scheduler.py:1247-1279`, excursion <+3%), feeding operator-confirmed `/timestop` OPG sells. Argued safe (time-stop only touches ≥5-day holds where both vendors agree; the sub-15-min trades whose values change are closed and can never enter that population) but the ship note MUST state the provenance change. **On the 2nd opinion's fill convention: DISAGREED, and correctly** — fill-at-bar-CLOSE/VWAP models a fill BELOW the limit price, which is impossible; that's market-on-touch, not a limit. Contract is fill AT trigger L, fill-bar excluded from the scan, pessimistic same-bar tiebreak — which also keeps future sweeps COMPARABLE with the shipped n=9 baseline. **Cadence**: 5-min poll of 1-min bars, each poll refetching the full day-so-far per ticker (≤390 bars, 1 call) with `ON CONFLICT DO NOTHING` — stateless + self-healing across restarts; coverage is a property of the FETCH WINDOW, not poll frequency. Tick/websocket rejected for a shadow (would couple to the entry-critical `bar_stream`); named as the flip-time upgrade. **`track_open_position_extremes` IN scope — SUBSUMED**: same function name, same job id (already in `EXECUTION_OWNED_JOB_IDS`), same cron slot; body becomes fetch-Alpaca → upsert path → compute extremes from the SAME bars. One fetch, one truth. Sharp catch: the Alpaca SDK call is SYNC while today's Polygon fetch is async, so it needs `asyncio.to_thread` or a naive swap would ADD blocking to the execution loop during the ORB window. ⚠ Deploy is **BOTH `market-agent` AND `execution`** (broker/* → two-step, [[deploy-broker-changes-need-execution-step]]). Pre-step: Monday RTH latency probe (STOP only if Alpaca >~3min lag; if Polygon turns out NOT delayed, defect #2 alone still explains the blindness and the design stands). ~9 tests incl. a #310 UTC-rollover regression — **no test currently covers this function at all.** NEXT = Sonnet implementation card off the doc's step-by-step plan. >> ✅ **PATH-RECORDER SHADOW BUILT 2026-07-25 (sonnet off the signed design; I verified every load-bearing claim vs code).** Suite **3685 passed** (3672 baseline + **13 new — the function had ZERO test coverage before**). `track_open_position_extremes` SUBSUMED (same name/job id/cron): body is now fetch-Alpaca → upsert path → compute extremes from the SAME bars; new `get_minute_bars_range` + `persist_intraday_bars` in alpaca_client mirror `_persist_first_bar`'s fire-and-forget `ON CONFLICT DO NOTHING`. New 16:10 ET `position_path_eod_sweep` job, registered in `EXECUTION_OWNED_JOB_IDS`. `asyncio.to_thread` around the sync SDK call (alpaca_client.py:779) so the execution loop never blocks in the ORB window. Selection predicate `(status='filled' AND remaining_shares>0) OR closed_at >= $1` with a tz-aware TIMESTAMPTZ param — **no `::date` cast**, immune to UTC rollover by construction (the #310 bug class); pinned by a regression test that reproduces the old `closed_at::date = today` and shows it wrongly excludes a 20:00 ET close. ⚠ **THE CARD CAUGHT TWO ERRORS IN ITS OWN SPEC — both real, both verified by me:** (1) design Step 3.5 says the extremes upper bound for an OPEN position is 'unbounded', contradicting §4 clamp A and §2b's 16:00 recording boundary — at the 16:10 sweep `now_et`=16:10, so **every still-open position would have had 16:00-16:10 after-hours prints leak into `highest_price_seen`**. Fixed with `window_end_et = min(now_et, 16:00)`, proven by revert-and-fail. (2) **the design's 'only provenance moved' claim is WRONG**: the new window starts 09:30 ET, whereas the Polygon call it replaces returns extended-hours bars (its own docstring: *'Polygon returns extended-hours bars (pre/post-market) when present'* — I confirmed at collector.py:481). So **day-N PRE-MARKET prints can no longer raise `highest_price_seen`** on multi-day holds — a real computation change on exactly the ≥5-day population the time-stop scan gates on. **Direction is fail-safe:** `highest_price_seen` can only go LOWER or stay equal → the `excursion < +3%` filter admits MORE positions → the `/timestop` candidate set GROWS, never shrinks, and /timestop stays operator-confirmed (nothing auto-sells). Arguably the BETTER behaviour (a thin pre-market print is not an excursion you could have exited into) — but it is a behaviour change and the ship note must say so, not claim identical semantics. ⚠ KNOWN GAP flagged by the card, implemented as specified: the sweep's GROUP BY day-coverage query cannot see a day with ZERO recorded bars (absent from the result set entirely). ⚠ Latency probe `scripts/probes/_306_latency_check.py` WRITTEN but **UNRUN** (market closed) — Monday RTH pre-step; STOP if Alpaca lag > ~3 min. **NOT DEPLOYED — needs BOTH `market-agent` AND `execution` (broker/* → two-step, [[deploy-broker-changes-need-execution-step]]), outside 9:25-10:05 / 16:00-17:00 ET.** VERIFY-LIVE = a same-day round trip gets minute rows in `mi_intraday_bars` AND a non-zero `highest_price_seen`.
- #307 | 2026-07-30 | pending | v1.1-W5 experience-seed ritual: weekly operator-labeling cadence + precedent-retrieval shadow (with #219, #254, #255) [b3] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> 7/19: labeling-cadence half already accrues (judge-delta / chart-vision / judge-review streams live); the unique un-built piece = **precedent-retrieval v1 = #255**, gated on corpus-accrual (defer 7/23). Rebumped to align with #255's gate. [blocked:precedent-retrieval v1 (#255) gated on corpus-accrual — defer 7/23; labeling streams already live — 7/19 re-date] [blocked:operator decision/gate — 7/23 CLOSE re-date to 2026-07-30]

## Apollo v2.0 — tier-one trader (horizons H1 Q3'26 · H2 Q4'26 · H3 2027 · spec: apollo-v1.1-v2.0.md PART II)

- #308 | 2026-08-15 | pending | P1 Experienced Judge (H1→H2): precedent corpus at scale + self-review→rubric-distillation loop + ensemble/uncertainty (#301 seed) + auto-growing probe library [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #309 | 2026-08-15 | pending | P2 Full Sight (H1→H2): multimodal (#267 daily→intraday→peer/SPY) + intraday narrative radar + tape features (#299) + negative-catalyst (#238) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #310 | 2026-07-27 | deployed | P3 The Manager (H2): management judge mature (#300 shadow→load-bearing) + structure-stops via PIVOTS (docs/methodology/pivots-and-stock-character.md) + conviction sizing >> PULLED INTO BLOCK 3 T3 (operator 7/11, design-only): the pivots structure-stops DESIGN happens Sun 7/13 (last-Fable window) — pivot detection + stop semantics + own shadow spec; shadow may coexist w/ the giveback shadow (counterfactuals don't interact); any LIVE flip stays sequenced strictly BEHIND the giveback F1 resolution (never two concurrent live stop changes — the premortem/evidence-hygiene rule). >> T3 DESIGN DONE EARLY 7/11 eve (Fable): **ADR 0031** (`docs/decisions/0031-pivot-structure-stops.md`) — requirements source = `docs/methodology/pivots-and-stock-character.md`; v1 = COMPUTABLE tier only (structural tier stays #267). Deterministic per-stock character profile (episode detector → home_MA + undercut_p80 + duration; ABSTAIN first-class when <3 episodes/no MA ≥2/3; +50% re-rating re-anchors the window; profile snapshotted INTO the shadow row — no standalone table until a 2nd consumer). TWO arms measured not pre-decided: P1 confirmed-swing-pivot trail (fractal k=2, confirm+2, ratchet-only) · P2 character-MA trail (home_MA×(1−undercut_p80)) — vs the live-SMA baseline, per-arm columns never blended. Shadow = `mi_pivot_stop_shadow` nightly on closed trades (giveback_shadow template) + `pivot_stop_shadow_review` gated review SAME commit (≥10 non-abstained, +21d). Readout = §5 incl. the TAIL-CLIP test (dossier poison test — an arm that clips the +5R tail loses regardless of mean). LIVE flip gates: giveback F1 resolved FIRST + N≥10 + tail-test + CHANGE_PROCESS + behind ADR 0029-D1; if both live: max(arm_stop, giveback_floor) — the one precedence rule (T4 sweep + 0029 inherit it). CARDS C1 profiler → C2 pivot detectors → C3 shadow job+review → C4 readout (~1 day Opus/Sonnet, market-agent scope). FORKS F1 arms+abstention · F2 v1 detector defaults · F3 the sequencing gate (rec: sign all). >> SITTING-RULED 7/12: **0031 F1-F3 SIGNED** (arms+abstention · v1 defaults · sequencing gate) → C1-C4 UNBLOCKED behind R4 (#445-first); live fork stays behind giveback F1. >> C1-C3 BUILT 7/12 (Block 4 T5, on the #445 driver): `pivot_analysis.py` (C1 character_profile — episodes/respect/home_MA/undercut_p80, ABSTAIN first-class, +50% re-anchor pinned; C2 confirmed_swing_lows k=2 confirm+2 no-lookahead + annotate_pivot_stops ratchet-never-widens) · exit_logic gains 2 SHADOW-ONLY trail modes behind the enum guard (`pivot_swing` = bar-annotated ratcheting pivot, close-below semantics [recorded deviation: conservative]; `character_ma` = home_MA×(1−undercut), tolerance pinned both directions) · `pivot_stop_shadow.py` (C3 nightly 17:42 ET job on closed live trades; 3 arms via iter_exit_ladder, identical prefix; per-arm columns never blended; profile snapshots into the row; mi_pivot_stop_shadow DDL seeded; no-mutation pin) · `pivot_stop_shadow_review` gated review SAME commit (≥10 non-abstained, earliest 8/09, carries the tail-clip test + the #146 whipsaw cross-check + the §0 giveback-F1 sequencing gate). 9 tests; suite 2967. REMAINING = deploy (operator-authorized) → verify-live = job registers + first row on the next live close; readout at the review. >> ⚠ **TEXT WAS STALE — CORRECTED 7/24 (I verified vs prod):** the line said "REMAINING = deploy → verify-live = job registers + first row on the next live close." It is ALREADY deployed and running: `mi_pivot_stop_shadow` holds 2 `account_mode='live'` rows — MANE 2026-07-15 (non-abstained) and NVCR 2026-07-23 (abstained, `no_ma_cleared_respect_bar_or_lt_3_episodes`). Genuinely event-gated from here on N=10 non-abstained rows (1/10 so far), NOT awaiting a deploy. >> 🔴 **COVERAGE BUG CONFIRMED 7/25 (I verified code + prod myself) — THE N-GATE CANNOT REACH 10 ON THE DOMINANT TRADE CLASS.** `pivot_stop_shadow.py:105-114` fetches forward bars with `trade_date > alert_date`, then `if not fwd: continue` — SILENTLY, with no audit row, no error event, no skip-reason. For a SAME-DAY round trip (alert and close on the same date) no bar after alert_date can exist at the 17:42 ET run, so the trade is dropped; and the job's WHERE is `(closed_at AT TIME ZONE 'America/New_York')::date = $3` (exact-match on today) so it NEVER catches up. Measured against every live close since 7/06 — the correlation is perfect: **same-day → NO SHADOW ROW 7/7** (WULF, CRCL, WDFC, TSEM, HUT, WKC, THC) · **held-overnight → shadowed 2/2** (MANE, NVCR). Only 2 pivot-stop audit events exist in total and ZERO error events — the drops are silent by construction. ⚠ The excluded class (fast same-day stopouts) is EXACTLY what the current 0-for-9 losing streak is made of, so this gate may never reach N=10 unaided, AND the shadow is blind to the very trades #503's forensic needs. FIX (no-money, shadow-only): include the alert-date bar (or key forward bars off the CLOSE date), + emit a real skip event instead of a bare `continue`, + a catch-up path for previously-dropped closes. Feeds #503 directly. >> ✅ **COVERAGE BUG FIXED 2026-07-25 (sonnet card — the agent was killed by a session limit at its final lint step, but the work was complete; I verified every claim vs code + independently re-ran the fail-without-fix proof).** THREE parts: (1) **catch-up window** — the WHERE moves from exact `closed_at::date = today` to `BETWEEN today-45 AND today` + `NOT EXISTS(trade_id)`, so a trade with no forward bars on its close day is REVISITED on later runs instead of being seen once and lost; idempotent by `mi_pivot_stop_shadow.trade_id INT PRIMARY KEY` + `ON CONFLICT (trade_id) DO NOTHING` (both verified in code). (2) **`_MIN_FWD_BARS=5`, derived not guessed** — `annotate_pivot_stops` drives `confirmed_swing_lows(bars, k=2)`, whose candidate range `k <= i < len-k` is EMPTY below 2k+1=5, so under 5 forward bars a confirmed P1 pivot stop is PROVABLY impossible and the arm degenerates to hard-stop-only — indistinguishable from baseline, i.e. not a real counterfactual. Below 5 the replay is DEFERRED, never written partial. ⚠ The card correctly REJECTED the naive `trade_date >= alert_date` fix: the exit ladder skips any day <= alert_date, so the alert-day bar would never run a step and the replay would mark-to-close as if the trade 'held to end'. Forward bars stay strictly `> alert_date`. (3) **no more silent drops** — 3 emitter sites now log `pivot_stop_shadow_skipped` with a reason enum (`bad_inputs` / `insufficient_forward_bars`), incl. a THIRD silent `continue` (`base is None` = non-positive risk) that wasn't in the original spec. 5 new tests (14 in file); full suite **3672 passed**; I stashed the fix and confirmed **4 tests FAIL without it**. ⚠ **NO BACKFILL NEEDED — the historical drops SELF-HEAL**: the 45d window (today−45 = 6/10) covers all 7 previously-dropped closes (WULF 7/06 · CRCL+WDFC 7/10 · TSEM 7/14 · HUT 7/20 · WKC+THC 7/24); each is picked up on the next run where it has ≥5 forward bars (WULF et al. Monday; WKC/THC ~7/31). No prod write required. NOT DEPLOYED — market-agent owned; deploy is operator-gated. VERIFY-LIVE = a same-day round trip gets a `mi_pivot_stop_shadow` row (or a `pivot_stop_shadow_skipped` event naming the reason) on the next 17:42 ET run. >> ✅ **DEPLOYED 2026-07-25 14:1x ET** (`deploy.sh market-agent`, all gates green; verified in the running container: `_CATCHUP_LOOKBACK_DAYS=45` · `_MIN_FWD_BARS=5` · `_log_skip` present · catch-up BETWEEN in the SQL · the old `::date = $3` exact-day match GONE). Job is `mon-fri @ 17:42 ET`, so **VERIFY-LIVE = Monday 2026-07-27 17:42 ET**: the catch-up should immediately pick up the previously-dropped closes that now have ≥5 forward bars (WULF 7/06 · CRCL+WDFC 7/10 · TSEM 7/14 · HUT 7/20 — all well past 5 bars), while WKC/THC (7/24) defer with an `insufficient_forward_bars` skip event until ~7/31. Confirm BOTH halves: new `mi_pivot_stop_shadow` rows for the same-day round trips, AND `pivot_stop_shadow_skipped` events naming a reason instead of the old silent drop. That is the close condition — the N≥10 non-abstained gate itself stays event-gated (ETA re-dated to the verify date, not the N-gate date).
- #311 | 2026-08-15 | pending | P4 Multi-Setup Book (H1→H2): per-setup judges + tightness→expansion graduation + 9M→flag pipeline + parabolic-short counter-regime book + regime-adaptive selection [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #312 | 2026-09-01 | pending | P5 Capital & Autonomy Ladder (H2→H3): unified_allocator live + formal shadow→paper→live ladder w/ auto-DEMOTION on breach + scaling/slippage telemetry >> ⚠ RED-TEAM PRE-FLIP GATE (docs/analysis/composition_redteam_2026-07-12.md RED-3, code-verified): the composite sizing multiplier is applied AFTER the 20%-capital/1%-risk caps (entry_pipeline.py:418) with NO upper re-cap — the moment the allocator writes a >1.0 multiplier it sizes STRAIGHT THROUGH every safeguard. Latent today (all multipliers ≤1.0). BEFORE the allocator can write >1.0 live: bound the composite multiplier OR re-cap position_size/risk after it. Non-negotiable safeguard fix — must land before this flip. >> **CLAMP SHIPPED 7/13 (rulings-pack R5, operator-ruled)**: `_apply_composite_multiplier` (pure, 5 tests) — up-never-past-builder-baseline (re-imposes 20%/VIX-risk caps with zero duplicated math), emits `sizing_multiplier_clamped`; deployed BOTH money containers, ≤1.0 ramps byte-identical. RED-3 pre-flip gate SATISFIED.
- #313 | 2026-08-15 | pending | P6 Institution-grade ops (H1→H2): #256 split (done) + replay-everything CI (#302) + cost governance (per-role LLM spend vs P&L) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #314 | 2026-08-15 | pending | Trading-ideas detector book (P4 detail): TI1 parabolic-short · TI2 wick-fill (#283) · TI3 fishhook U&R · TI4 base+catalyst convergence · TI5 post-EP shape classifier · TI6 RMV (#54) — graduation gates in data_gated_reviews.yaml; detail in trading-ideas-backlog memory [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Spec / detail docs (PLAN.md is the INDEX — every program above; detail + evidence live here, referenced)

_Launch DoD: docs/roadmap/launch-2026-06-22.md · v1.1/v2.0 map: docs/roadmap/apollo-v1.1-v2.0.md · ADRs: docs/decisions/*.md · evidence-gated reviews (weekly auto-surface): data_gated_reviews.yaml · setup SSoTs: docs/setups/*.md · methodology: docs/methodology/*.md · idea detail: trading-ideas-backlog memory. If a program isn't a task line above, it isn't planned — add it._

## Miscellaneous (no home project)
- #449 | 2026-08-08 | pending | LOW-PRI tech-debt (from /simplify 7/11, reuse + altitude agents): four fail-closed / toggle safeguard-state readers share the same shape (`SELECT state FROM mi_safeguard_state WHERE safeguard=$1 …` → validate/coerce → except→fail-value): `order_ingest.get_ingest_mode` (#184b, fail→'off'), `db.get_manual_halt_state` (fail→'unreadable'/blocked), `db.get_holistic_judge_enabled`, `db.get_runtime_toggle` (fail-OPEN to env). DoD: extract ONE parametrized `_read_safeguard_state(name, account_mode, allowed, fail_value, env_var=None)` in db.py backing all four; the helper must RAISE (each caller keeps its OWN fail-direction — the load-bearing part: get_manual_halt_state is a CAPITAL gate that fails CLOSED, get_ingest_mode fails CLOSED, get_runtime_toggle fails OPEN). MUST ride its own reviewed change with tests pinning each fail-direction (touches a capital gate — THE LINE). NOT urgent: predates #184b (the dup already existed); get_ingest_mode followed the established local precedent, didn't introduce the smell.
- #452 | 2026-07-27 | in_progress | **R1 correlated-book controls (premortem TOP risk, docs/analysis/premortem_450_2026-07-11.md)**: the entry path has NO theme/sector/correlation gate — 5 same-family EP HIGHs can fill all 5 slots (= one bet at ~100% deployment), and the theme axis STEERS selection toward exactly that. STAGE 1 (telemetry, no-money, ship on sign-off): EOD digest line + audit row — open-book theme/family overlap + aggregate overnight notional. STAGE 2 (the gate, CHANGE_PROCESS + sign-off + replay): family-slot cap at entry (rec max 2/family, family = the ADR 0025 Stage-A notion, one mechanism reused) + skip_reason block:theme_concentration; backtest = replay historical books for bind-rate + cost/saved. Operator forks: cap value · game_changer exemption. DoD: stage-1 live; stage-2 backtested + decided. >> SITTING-RULED 7/12: **BUILD THIS WEEK as SHADOW-COUNT + alert** (log same-family exposure >2 on would-be entries); family key v1 = active-theme membership; `exposure_family` + `db.OPEN_POSITION_STATUSES` (R5); promote-to-blocking = operator fork after ~2 clean weeks. >> BUILT 7/12 (Block 4 flex, per the sitting B3 ruling): `exposure_family.py` (family key v1 = ACTIVE-theme membership set-overlap; `exposure_family` naming + `db.OPEN_POSITION_STATUSES` per register R5 — both PINNED in tests) + the entry_pipeline 2b SHADOW hook (observe-only, error-wrapped #94-pattern — can never affect an entry; emits `exposure_family_breach` audit + Telegram when an entry would lift same-family exposure above 2) + **`exposure_family_cap_promotion` review DATE-FIRES 7/27 regardless** (clean fortnight → the BLOCKING card via CHANGE_PROCESS + safeguards.md + sign-off; noisy key → fix the family definition first, possibly behind the 0025 merge arm). 5 tests; suite 2972. REMAINING = deploy (rides the next batch w/ T5) + verify-live = the hook logs on the next entry evaluation. >> STAGE-1 BUILT 7/16 eve (inline, card hit session cap): `book_concentration.py` — pure compute core (Stage-A `family_of` over active-theme membership; multi-family tickers count in EACH; entry-basis notional + %-of-latest-equity) + 16:18 ET job (after the 16:12 equity snapshot; audit row `book_concentration_snapshot` EVERY run, Telegram ONLY when ≥2 open live positions share a family) + 7 pin tests (flag/disjoint/empty/math/equity-None degrade/audit-always-telegram-flagged-only/read-only). STAGE-2 DESIGN: docs/analysis/452_r1_stage2_design_2026-07-17.md — family-slot cap at _check_safeguards, `block:theme_concentration`, replay-backtest spec, 4 operator forks (F-A cap=2 rec · F-B no game_changer exemption rec · F-C DB-toggle dark-ship rec · F-D stems-vs-ecosystems notion). REMAINING: deploy market-agent (Fri, operator-gated) → verify-live 16:18 audit row → Stage-2 = replay card + operator sign-off (CHANGE_PROCESS; entry discipline = THE LINE). >> ✅ Stage-1 VERIFY-LIVE 7/17 16:18 ET: book_concentration_snapshot fired (1 row; 0 open positions tonight so silent — correct). Stage-2 gate remains operator-fork. [b1] [ok:operator-CLOSE reconcile 7/17 — verify-live pending Monday market / remaining-deliverable / event-gated] >> 7/27 DECISION FRAMING added 7/19 (operator premortem walk; stage-2 design doc §'7/27 decision framing'): **family cap = PRIMARY (selective — allows a diversified 5, blocks concentrated)**; the 'family-blind notional axis' is DROPPED (for Apollo's ~uniform 20% sizing it collapses to a count cap → if a blunt total-exposure ceiling is wanted, just trim MAX_CONCURRENT_LIVE_POSITIONS); the trim is a SECONDARY/optional backstop for the un-themed correlated book ONLY. **7/27 = two questions: (1) family KEY (stem vs ECOSYSTEM — operator lean, closes the same-sector leak) + (2) add the MAX_POSITIONS blunt backstop?** Replay: (a)-(d) per-granularity + (e) MAX_POSITIONS-trim bind/cost + (f) worst un-themed-correlated exposure the family cap misses.
- #454 | 2026-07-28 | in_progress | **R3 calibration-blindness mitigations (premortem)** — PARTS 1-3 ALL DONE; only the pulled-forward band review remains (operator sitting): the kill/scale bands are silent below 20 closed trades (~3 months at the live rate) and calibrated on ONE mixed-regime 12mo window (n=399, no regime stratification); the drawdown breaker — the −12% backstop — has only ever fired on PAPER. DoD: (1) early-window drift line — weekly review prints rolling live expectancy vs the calibration envelope percentile from closed-trade 5 onward (informational, n<20 caveat printed); (2) regime-stratified re-cut of the #268b envelope (analysis card → feeds the quarterly band review); (3) drawdown-breaker LIVE-path verification (#151-style paper exercise of the live-mode code path). Only (3) touches live-path code. >> SITTING-SIGNED 7/12 as filed. >> 7/19 WALK (operator): gaps fully specified here + full per-F mechanism/mitigation/⚖-hardening in the doc. **TOP-3 to harden:** F1 → #452 stage-2 cap-flip (rec MAX_POSITIONS_PER_FAMILY=2, block the 3rd; 7/27 sitting) · **F2 → OK'd, execute via CHANGE_PROCESS** · F3/F4 → finish #454 part 3 (breaker live-path, never fired live) + decide the persistent-fail-open posture. New sub-item F7-zombie reaper added above. #456 stays the accountable home (net-0 vs ceiling); operator can break out to individual tasks w/ a carryover if granular tracking is wanted. >> PARTS 1-2 BUILT (sonnet card) + DEPLOYED 7/17: weekly-review early-window drift line (n<20 caveat printed; envelope has only 2 stored reference points — noted in-line) + regime-stratified analysis. ⚠ FINDING: the #268b envelope is BULL-CONDITIONAL (94% Bull window; only non-Bull = the March-crash cluster; per-trade R series NEVER durably persisted — the exact-recut is unreachable; 3 operator options in docs/analysis/454_regime_stratified_envelope_2026-07-17.md) → quarterly band review + Sat docket. VERIFY = the drift line renders in the 7/19 weekly review. Part 3 (breaker live-path exercise) remains. [b2] [ok:operator-CLOSE reconcile 7/17 — verify-live pending Monday market / remaining-deliverable / event-gated] >> **PART 3 APPROVED 7/19** (operator premortem F3, 'go with rec'): finish the drawdown-breaker LIVE-path exercise — the −12% backstop is load-bearing for every accepted risk + has NEVER fired live (⚖ #151 trade-state harness, careful). ALSO approved: **pull the kill/scale-band quarterly review FORWARD** off the #454 bull-conditional finding (bands are known-mispriced; don't wait for 8/01). Both are the F3 hardening in docs/analysis/450_premortem_2026-07-18.md. >> ✅ **PART 3 DONE 2026-07-25 — live-path exercise ALL 37 CHECKS PASSED, ZERO WRITES, ZERO RESIDUE** (`scripts/_454_drawdown_live_path_exercise.py`, run in apollo-execution; evidence `docs/analysis/454_drawdown_live_path_exercise_2026-07-25.md`). Scope honesty: this closes **never-EXECUTED-live**, NOT never-FIRED-live — a genuine fire stays event-gated on a real −12% drawdown and an exercise cannot manufacture one. Precondition read FROM THE CONTAINERS (not the doc — safeguards.md:97 carries an unresolved promotion-timing dispute): `DRAWDOWN_BREAKER_PHASE='active'` in BOTH apollo-execution and apollo-market, so enforcement is genuinely live. **DESIGN — in-process injection, NO state mutation** (advisor-redirected): the obvious set-tier-then-restore was REJECTED because REDUCE returns `ok=True` w/ 0.5× (live_tracker.py:278) → a crash mid-exercise leaves Monday's live entries SILENTLY half-sized, with the 16:12 ET cron not recomputing until AFTER the 9:31 window = a silent strategy alteration on the money path (BLOCK would be loud+fail-safe; REDUCE is not). Synthetic equity snapshots rejected outright too — a stray high-equity row raises the 30d rolling peak → a FALSE BLOCK days later w/ no obvious cause. Instead `read_breaker_state` is patched in-process; residue impossible by construction; everything else REAL (Alpaca auth, position count, daily-loss, the real `_check_safeguards`) — and the breaker gate is LAST so reaching it proves all prior gates passed. THE DELIVERABLE (§C): OK→(True,1.0) · WATCH→(True,1.0) · REDUCE→(True,0.5) · BLOCK→(False,0.0) carrying `block:drawdown_breaker`. Also covered: the `_next_state` matrix incl. release bands + hysteresis + legacy TRIPPED · the SIZING site (`_apply_composite_multiplier` — the link from 'reports REDUCE' to 'position is smaller', incl. 9M-Day2 0.25× compounding + the RED-3 clamp) · `intraday_drawdown`, the 2nd consumer. The recompute WRITE path verified by reading instead (mode is a pure bound param `$2` in both SELECT+UPDATE, no branch → live write structurally identical to the exercised paper write). 3 harness expectations were WRONG and the CODE was right (recorded in the doc): release thresholds are the level equity must RECOVER TO (−8% has not reached −7%, so BLOCK holds), and `_deepest_crossed_tier(−12%)`→REDUCE is DELIBERATE (Stage-1 alert vocabulary is WATCH/REDUCE only, raw dd% in the body) — no code defect found. Residue confirmed at the DB: `mi_safeguard_state.updated_at` still the 16:12 cron write. ⚠ **#454 STAYS OPEN** for the SECOND 7/19-approved item — pull the kill/scale-band quarterly review FORWARD off the bull-conditional finding (bands known-mispriced; no separate task owns it). [blocked:operator band-review sitting — part 3 shipped 7/25, re-dated for the remaining item] >> ✅ **BAND REVIEW DONE 7/24 (Fable card, `docs/analysis/454_band_review_2026-07-24.md`) — VERDICT: KEEP, operator-accepted.** No reachable evidence clears CHANGE_PROCESS rule 1 for any threshold change; the only new data is the n=9 live cohort, which the card was explicitly barred from retuning on. ⚠ **The finding that matters more than the verdict: the live cohort is 0 WINS in 9 trades, streak 9, cum −7.50R (−$180), mean −0.833R — and 9/9 trades sit in NON-BULL regimes the #268b envelope never priced.** Bands are HOLD since 6/19; zero `kill_scale_override` rows ever (no directional mis-set signal, but the bands have never bound). Demote-side signal HELD not inverted (live demotes n=13, mean fwd5d +19.3%, 10/11 positive — same direction as Phase B's '+1.01R, demotes are not saves'). §5 fork rec: **(a)-EXTENDED + (c)**, (b) stays gated — a SECOND independent non-Bull episode now exists (Jun-Jul Choppy/Correcting, 23 non-Bull weekdays), so extending option (a)'s window converts it from single-episode-weak to informative at the same $0 LLM cost. ⚠ Card-reported contradictions I have NOT yet re-verified myself: 'n=20 arrives ~8/20 not ~3 months' and 'surviving Phase-B rows decayed 107→85 in a week'. **The 0-for-9 deserves its own look, separate from the bands** — operator-agreed 7/24. >> **0-for-9 BROKEN OUT to #503** (operator 7/24) — it was riding here under a headline that didn't describe it, the exact pattern that loses things. #454's own remaining scope is now CLOSED (parts 1-3 done + band review done); it stays open only until #503 is filed-and-owned, then close. >> ⛔ **NOT CLOSEABLE — PART 1's VERIFY NEVER PASSED (I caught this checking the sweep's CLOSE-NOW recommendation; do NOT close on 'parts 1-3 done').** Part 1's stated verify was 'the drift line renders in the 7/19 weekly review'. Checked the PERSISTED review: `mi_system_reviews` id=13, created 2026-07-19 — `ILIKE '%Early-window drift%'` = **FALSE**, `ILIKE '%rolling mean%'` = FALSE. It did NOT render. Conditions that should have made it render: 5 live trades were closed by 7/19 (guard is `if n < 5: return ""`), and the code landed 7/17 (aa13607) with market-agent deploys on 7/18-19 — so it WAS live. The function works TODAY (ran it in-container: renders at n=9, '📉 Early-window drift … rolling mean −0.83R over 9 closed trades — below calibration's trailing-20 p5 (−0.63R)'). Most likely cause: `assemble_band_inputs('live').realized_rs` had <5 computable-R entries on 7/19 even though 5 trades had CLOSED (R needs settled pnl/risk_dollars), i.e. the guard counts a NARROWER population than 'closed trades' — or the `except Exception` swallow at system_review.py:220 fired (logged, never alerted). ⚠ **NEXT WEEKLY REVIEW = SUNDAY 2026-07-26 08:00 ET (tomorrow)** — that is the real verify point; confirm `Early-window drift` appears in review id=14. If it renders, part 1 is verified and #454 closes. If it does NOT, the n-guard population is the bug. Parts 2+3 and the band review ARE done and verified. [[verify-operator-facing-surface]] >> ⚠ **BAND-DENOMINATOR FINDING 2026-07-25 (#503 forensic, verified vs prod):** the kill/scale band evaluator reads realized R on **BUDGETED** risk (`risk_dollars`), but DEPLOYED risk (`shares×(entry−hard_stop)`, post integer-floor + 20% notional cap) is smaller — measured on the live cohort: mean **−0.833R budget vs −0.930R deployed**, median −1.006R deployed, 8/9 full stops. **The trailing-20 the bands read at n=20 activation is therefore FLATTERED ~10%** vs economic reality — which cuts AGAINST the band review's 'REDUCE is the modal outcome' projection. Not a retune (n<20, CHANGE_PROCESS) — a MEASUREMENT-BASIS question for the quarterly review: which denominator SHOULD the bands read?
- #456 | 2026-07-26 | pending | **R6 premortem residuals (bundled)**: >> #450 v2 premortem (`docs/analysis/450_premortem_2026-07-18.md`, 7/18) added 4 NEW tail-risks to triage here: **F2** daily-loss brake fail-open (by keying — sums on `alert_date`=ENTRY-day, so a HELD position stopping out TODAY is invisible = the overnight-gap loss class the brake most needs to see; **operator-OK'd 7/19 to FIX**: re-key on `(closed_at AT TIME ZONE 'America/New_York')::date` = restore safeguards.md §5's documented intent, ⚖ CHANGE_PROCESS + a replay of historical close-days + SSoT change-log + reversion-flag), **F4** breaker fail-open stack under infra stress (**operator-approved DIRECTION 7/19: FAIL-CLOSED** for live after a persistent fail-open — ≥2 consecutive drawdown_check_unavailable days OR read_breaker_state erroring at entry-time → block new live entries, matching the halt-state/account-fetch precedent; implement via ⚖ CHANGE_PROCESS + sign-off on the exact trigger), **F5** the LOUD-but-naked stop class (silent-stop is covered, this isn't), **F6** a bad ORB bar minting an oversized position (money-relevant — ties #482 ORB-bar reliability) >> **BACKTEST DONE 7/19** (no-money sizing, docs/analysis/456_F6_min_range_backtest_2026-07-19.md): 71 ORB bars, median 3.78% range (healthy) → the thin-bar danger is a RARE TAIL not systemic. A ~0.75%-of-price min-range floor blocks **2/71** (BZH 0.55%, **MANE 0.61% = a real live loser 7/15**) with ~zero collateral; 0.3-0.5% catches nothing, 1.5-2% over-blocks legit tight bars (MU/DELL ~1.1%). Small-N (2-11) → operator call; my lean = floor ~0.75% (cheap insurance vs the guaranteed-stop-out coin-flip; mechanism unambiguous regardless of N). ⚖ SSoT + sign-off to flip. >> RULED 7/19 (operator): **DO NOT ship the floor — F6 over-ranked.** (1) sample too small (2 cases); (2) a TIGHT range is often a GOOD coiled/low-vol setup (methodology WANTS it) — a range floor blocks the good tight opens too, and range alone can't tell 'corrupt bar' from 'good coil'; (3) CORRECTION — when a tight-stop entry clamps to 20% notional the DOLLAR RISK goes DOWN not up (20%×0.61%≈0.12% of equity; MANE lost $2), so 'oversized position' is mislabeled = it's a LOW-dollar-risk position w/ a tight stop. Down-ranked to LOW-severity; residual = watch for a genuinely-CORRUPT bar (a data-integrity Q, NOT a range floor) — none seen (BZH/MANE were real thin opens). Investigated → not actionable. [F1=R1 correlated book, F7=#461 cap-RACE done BUT the confirmed-ZOMBIE is DISTINCT + OPEN (F7(3): auto-enter sets status='confirmed' BEFORE submit_entry; a crash between = a `confirmed` row w/ no broker order counting toward the 5-slot cap FOREVER, no reaper covers it — slot STARVATION, #197: cap-blocked cohort were the winners → ⚖ stale-`confirmed` reaper mirroring expire_stale_proposals), F8=#444 mode-label, F9=R5 — already tracked.] (a) ⚠ CORRECTED 7/19 (premortem register-hygiene): VIX-scaled sizing **IS WIRED** (cc8f2e9 2026-05-14, order_manager.py:108-112 + :3530-3533, fed by mi_market_regime.vix) — the 'designed-not-wired' premise was STALE (wire predates it by 2mo). Real residuals RE-POINTED here: (i) STALENESS — 9:31 sizes off LAST-NIGHT's VIX, a crash-morning spike is unseen; (ii) `vix=None` → full-base-risk FAIL-OPEN (constants.py:35-36 mislabels it 'conservative'). No wire-it decision needed; (b) F16 audit card — every alpaca_client read caller whose next action treats [] as flat must pass raise_on_error=True (sync_positions guarded; verify the rest, mechanical sweep). DoD: (a) decided + filed accordingly; (b) sweep table + fixes. >> SITTING-SIGNED 7/12 as filed. [b1] [ok:operator-decision push — DoD(a) the VIX staleness/fail-open posture is an operator ruling; DoD(b) the F16 mechanical sweep can follow it — operator-directed 7/25, 2026-07-25→2026-07-26]

- #121 | 2026-07-30 | in_progress | **Telegram formatting layer — consolidate + re-home DONE; remaining = legacy-Markdown→HTML surface migration.** ✅ consolidate-escapers was already done (a6c45a0 6/15 — there's ONE canonical `_md_escape`, no real dupes; the "≥3" premise was stale). ✅ 6/23: re-homed `_md_escape` + `_humanize_downgrade_reason` (+ its maps) + `_build_judge_delta_message` from scheduler.py → briefing.py (its natural home, owns send_telegram_message; PURE move, byte-identical, 1065 tests green; commit-not-deployed — behavior-preserving, rides the next deploy). REMAINING (#121-epic): migrate the remaining legacy-Markdown Telegram surfaces to the shared HTML layer (`shared/telegram_format`; d9c872d/4e483ee in progress). DoD: all operator-facing Telegram on the HTML layer. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #334 | 2026-09-01 | pending | LOW-PRI theme-revive cooldown latch NOT YET WIRED (theme_engine.py:322, surfaced in 6/18 prose sweep): the one-way-latch arm is inert because no `revived_at` timestamp is persisted on mi_themes, so the only caller passes days_since_last_revive=None. Wire when revive goes live (persist revived_at, thread the delta). Filed not-dropped per the no-floating-prose directive; genuinely minor. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
