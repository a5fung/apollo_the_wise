# PLAN — the single source of truth (projects · tasks · ETAs)

**This file is THE plan.** Every task lives here, under a project, with an `ETA` date and a `status`.
The long-horizon plan (the 6/22 launch) lives here too — as dated tasks, not a separate doc. There is no
second plan surface: the calendar is phone reminders only, the harness #-task list is a session scratch
mirror, `CHANGELOG.md`/git hold history. If it's planned, it's a line here.

**Line format** (strict — parsed by `scripts/check_plan.py`):
`- #<id> | <YYYY-MM-DD ETA> | <status> | <title>`   ·  status ∈ pending · in_progress · blocked

**The two rituals operate ONLY on this file:**
- **OPEN** (every session): run `python scripts/check_plan.py --today` → it prints OVERDUE + due-today open
  tasks. That list IS the day's plan. State it before reacting to anything.
- **CLOSE** (every session): every open task's ETA must be ≥ today. Rebump any past-due one to a real
  future date (or close it). File every new item as a line here under a project. Then commit — the
  pre-commit gate (`check_plan.py`) REFUSES the commit if any open task lacks a project, an ETA, or has a
  past ETA, or if an open harness task is missing from this file. No gaps, mechanically.

_Last CLOSE: 2026-06-16._

---

## Launch — 6/22 GO/NO-GO (the long-horizon plan, as dated tasks · SSoT-spec: docs/roadmap/launch-2026-06-22.md)

> **DoD-7 TRIAGE SWEEP — 2026-06-18** (the "all projects deliberately dispositioned" gate). Every
> open task is now CLOSED / RUNWAY (≤6/22) / explicit POST-LAUNCH-with-reason. Spine GREEN: DoD-1
> measured+governed (#268+kill/scale signed), DoD-2 split live (#256), W1 replay (#268), W4
> chart-vision shadow (#343). Re-dispositioned drift: **#255** (W5 precedent-retrieval — was an
> accidental gate-rebump; now explicit post-launch, the W5 *labeling-ritual* bar is already met),
> **#226** (advisory + blocked → post-launch), **#291** (launch-critical reviews done; methodology
> batch → post-launch). Close-pending (done, awaiting snapshot refresh): **#284 #286 #270**.
> **COHORT DISPOSITION:** the ~46 tasks dated **2026-06-23** = the explicit POST-LAUNCH FAST-FOLLOW
> bucket (first working day after launch) — the v1.1 judge/catalyst tail, the catalyst-sourcing
> backbone (#210–#236), op-split residuals (#277–#281), SiP detection backlog, hygiene tail — NONE
> launch-blocking; the meta-rubric LOAD-BEARING flip is gated post-cutover by design (#335). The
> 6/19–6/22 runway = the launch-grade work (rehearsal #304, evidence pack #305, DoD-5 hygiene
> #237/#248/#260, DoD-6 dashboards #193/#185/#194 [#194 blocked on the operator deploy key]).
> Live-money GO-direction gates (#150/#151/#183/#184/#275) sit around the cutover, appropriately.

- #261 | 2026-07-13 | in_progress | scripts/ namespace reorg. SAFE SUBSET DONE 6/17 (5117ed1): 45 throwaway probes → scripts/probes/ (0 pipeline refs; 2 safety greps caught 19 false-throwaways = 4 imported shared modules + _270_* cluster + 4 code-referenced + YAML-cited review tools, all KEPT at root; operator's _reconcile left alone); 6 docs swept; 887 tests pass; no deploy needed. ⚠ REGRESSION FOUND + FIXED 6/20 (via #285's new guard test): the safe subset MIS-MOVED 5 ACTIVE monthly-sweep tools (_b53/_b54/_b77/_b88/_b92) into scripts/probes/ while their registration (quarterly_review.QUARTERLY_BACKWARD_CHECK_SCRIPTS) + their own docstrings still said scripts._bNN root → the 7/1 sweep would have 🔴'd all 5. Moved back to root (commit 2c518e4). ROOT CAUSE for the remaining reorg: the #261 safety greps checked YAML-cited tools but NOT Python-cited ones (a .py list) → when continuing the reorg, grep BOTH (incl quarterly_review.py's script list + any importlib/`python -m` refs); the new tests/test_quarterly_sweep_registration.py now backstops the sweep set. REMAINING (needs a focused block + per-file judgment): ops/evals split of the 94 real tools (touches deploy.sh/CI paths — careful sweep + verify-deploy) + delete-vs-keep the 45 untracked scratch scripts (incl non-_ WIP like analyze_/replay_/backfill_) [ok:date-roll-0701] [b3] [ok:Sunday — fold with the F4-done reorg remainder] [ok:big-rock ops/evals reorg touches deploy.sh/CI - needs a focused careful session, not an EOD roll; keeps deferring to higher-value work]
- #299 | 2026-07-18 | blocked | P2 tape-features. BLOCKED ON FUNDING (operator decision). RIG DONE 6/17 — SLICE A+B shipped, eval rig built + machinery-verified (scripts/eval_tape_judge.py; binding finding: OR÷ATR available+ORB-tradeable for 79.1% of HIGHs; 11 tests; doc tape_judge_eval_299_2026-06-17.md), baked into prod image. FULL RUN DEFERRED 6/17 — requires FUNDING (~$170 full, or ~$50-90 directional first pass; ~3,420 Opus calls). TRADE-OFF of waiting: the judge keeps grading WITHOUT the tape axis (opening-range violence / premarket pace / liquidity) — an ENHANCEMENT we forgo, NOT a regression; no realized harm, the rig is turnkey whenever funded. NEW DATE 6/25 = decide-fund-and-run as the first step of post-launch tape-wiring, clustered with the #320/#321 post-launch grade-quality batch (grade-affecting WIRING is post-6/22 anyway). When funded: run → operator labels deltas (verify on theme/audit, ADR 0011 self-cert gate) → if majority-correct, CHANGE_PROCESS + sign-off → wire tape into live _judge_shadow ▶ HOLD (operator 6/24): do NOT fund-and-run the tape eval standalone — BATCH it with ALL judge changes and run the eval ONCE at the pre-#335 checkpoint (batch-judge-regrades discipline — save $/be efficient; cf. feedback_batch_judge_regrades_and_review_yield). Rebumped off 6/25 to the #335 checkpoint. blocked_by:#335 [ok:awaiting operator eval-spend decision (~$170 full / ~$50 subset)] ▶ OPERATOR 7/4: HOLD the spend until the judge pipeline settles (the batching principle, feedback_batch_judge_regrades: one batched eval at the pre-#335 checkpoint, not per-axis spends) — the pipeline is NOT change-complete (#347 corpus flip pending, #328/#330 axis shadows landing, #331 undesigned). Fold the $50 subset INTO the #335-checkpoint batched run. [b3] [ok:operator-held to the batched checkpoint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #255 | 2026-07-24 | pending | judge precedent-retrieval v1 (kind=review) + periodic-review nudge. DoD-7 TRIAGE 6/18 (was an accidental 6/18→6/26 gate-rebump): EXPLICIT POST-LAUNCH decision. This is W5's "experience seed" build — but the W5 LAUNCH BAR ("labeling ritual running") is already MET + accruing live (chart-vision #343, mgmt-judge #300, judge-review #337 each emit operator-label streams). Precedent-retrieval has thin value until that corpus has VOLUME → correctly gated on corpus accrual, NOT launch-blocking. Clusters with the W5 ritual program #307/#219/#254 (7/08). defer_until:2026-07-08 [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #301 | 2026-07-18 | pending | P1 ensemble-divergence SHADOW (2nd-model verdict on HIGH-tier). DELIBERATELY DEFERRED to post-cutover (✅ operator-confirmed 6/19 "revisit after cutover"; named reason, not skipped): it adds a 2nd-model call in/near the grade path, and per the trade-set-stability discipline that gates #335 post-launch, new grade-path-adjacent code does NOT land pre-6/22 GO — even as shadow. Clusters with the post-launch grade-quality batch (#320/#321/#299/#335). Distinct from #233 (Perplexity grade AS a judge INPUT, CHANGE_PROCESS-gated) — #301 is a pure zero-authority divergence MONITOR (2nd model grades independently, divergence logged for operator visibility, no grade change); complementary, build as the shadow telemetry version. Ready to build post-cutover (design: integration at the HIGH-tier judge verdict; cheap 2nd model ~2-5 calls/day; telemetry table + digest section, consolidate-not-proliferate). blocked_by:#335 [ok:blocked_by-#335, eval runs 7/3] [b3] [ok:blocked_by #335 checkpoint (7/18-adjacent); build the shadow when the corpus settles] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #303 | 2026-07-07 | in_progress | full-program advisor review + launch-readiness checklist + DR restore rehearsal. ✅ LAUNCH-READINESS VERIFICATION AUDIT DONE 6/19 (commit pending): traced every launch-critical safeguard claim in the #304 evidence pack to SOURCE (not prose) — panic button both gates highest-priority+fail-safe (live_tracker.py:105 + order_manager.py:186); day-1 equity guards all in _check_safeguards (LIVE_TRADING_ENABLED master kill :92, daily-loss :191, circuit-breaker :220, tiered drawdown :240); creds boot-block incl the 2026-05-13 outage class (phase=live under ENABLE_LIVE_MODE=false → BOOT BLOCKED, agent.py:7037+); kill/scale band job registered 16:13 ET INTELLIGENCE_OWNED + unclassified-job boot guard (scheduler.py:185,4434); #349 DR fix in restore.sh:383 (execution then both); Monday verify scripts all present. NO new gaps (the same audit class that caught #349) → folded a "code-verified 6/19" §1 note into the evidence pack (consolidate-not-proliferate). ✅ SSoT-CONSISTENCY SUB-AUDIT 6/19 (safeguards.md the operator had open): every threshold in safeguards.md MATCHES constants.py (MAX_CONCURRENT_LIVE_POSITIONS=5, CIRCUIT_BREAKER_CONSEC_LOSSES=10 [deprecated], DAILY_LOSS_LIMIT_PCT=0.02, DRAWDOWN_PEAK_WINDOW_DAYS=30, MIN_SNAPSHOT_HISTORY_DAYS=7) + the kill/scale bands + 16:13 band job + /pause gate all match code. ONE drift FOUND+FIXED: CLAUDE.md's always-loaded "Paper Trading" blurb was STALE (said "max 4 positions, 5-loss circuit breaker" — code is 5 / 10-deprecated); corrected + added a safeguards.md SSoT pointer so it can't re-drift. ✅ DR REHEARSAL DONE 6/20 (#349 closed — PASSED on the split topology). ✅ MONDAY GO RUNBOOK BUILT 6/20 (docs/roadmap/monday_go_runbook.md, cross-linked from evidence-pack §5): the go-live procedure dry-run-validated against HEAD — each step traced to code (creds boot-block agent.py:6871/7037; phase=live routing constants.py:153; position_size_multiplier floor+recompute entry_pipeline.py:357-371; preflight live path; /pause #345) + exact commands/SQL + confirm + rollback. KEY GOTCHA surfaced: the flip is NOT one switch — live_real_enabled/position_size_multiplier/max_concurrent_positions are SQL-only (no /strategy command; promote only advances phase + is eligibility-gated), and the live creds must be in .env BEFORE the deploy or the boot-block fires. REMAINING: (a) a true cross-codebase advisor review is still only partially covered (the runbook validated the GO path specifically; advisor() sees only the transcript) — fold the broad sweep into the Monday GO walkthrough with the operator. This task is effectively complete pending that walkthrough. [ok:date-roll-0630] [b1]
- #413 | 2026-07-17 | pending | 🎯 FIRST REAL FILL watch (spun from #305, CLOSED 7/3 on the operator call "close as launched" — launch executed: Phase-1 6/22 · Phase-2 armed 6/28 · first auto-entry AVAV 6/30 armed-clean/cancelled-clean, arm VERIFIED-LIVE; full history in git + monday_go_runbook.md). REMAINDER = a real-money position has never FILLED. On the first live fill, verify the chain end-to-end: WS fill → stop-leg attach (extract_stop_leg_id, never-naked) → mi_live_trades row (account_mode=live, stop_order_id set) → #401 alarm stays silent → the exit ladder runs live (3:45 partial scan / 4:45 trail) the following days. DoD = one clean filled trade, chain verified, CHANGELOG entry. If no fill by ETA: re-date with a tape note (no-fire ≠ failure — thin-tape days don't produce MAGNA53 HIGHs).

## Live-money cutover — MAGNA53 (the GO-direction gates)

- #150 | 2026-07-15 | in_progress | Alpaca stop-trigger / share-reservation race — explicit sell-retry fix. CONFIRMED SHIPPED 6/18: `order_manager._is_share_reservation_lag()` + bounded retry (held_for_orders / "insufficient qty available") present + mature in HEAD since the FTRE 2026-05-09 incident. Remaining (verify-live) = observe a clean retry on the NEXT real share-reservation-lag event (event-driven, can't force) → then close. Live-money-promotion gate. [b1] [ok:date-roll-0701]
- #183 | 2026-07-07 | in_progress | Live-money cutover - MAGNA53 (the GO-direction gates) - **Wire-boundary enum normalization in _order_to_dict (v1.0 BLOCKING, careful-path; #418 D-7 recovered 7/5).** Prod probe: Python 3.12, str(OrderStatus.NEW)=='OrderStatus.NEW' - every order dict carries qualified enum strings for status/side/type. KNOWN LIVE CASUALTY: polling Day-1 re-entry fallback (_check_day1_reentry status!='filled' never matches; WS primary path masks it). FIX: normalize once at _order_to_dict (plain lowercase values) + audit EVERY downstream status/side/type comparison against both forms (the per-site defenses like extract_stop_leg_id stay) + tests pinning the wire contract. Was the 'ORB classifier IEX/window' ghost title - origin CHANGELOG session-5 (enum stringification root cause). [b1] [ok:scope-recovered+re-dispositioned at #418 walk] >> AUDIT DONE 7/6 (Fable block 1, docs/analysis/183_enum_boundary_audit_2026-07-06.md): exhaustive consumer classification - 4 BROKEN (check_fills :338 fill-detect DEAD, :396 cancel-tuple DEAD AND missing one-L 'canceled' so needs the tuple fix too, :709 day1-reentry, audit_invariants:116 = the never-naked L1's broker-coverage check FALSE-NAKED class), 9+ per-site defenses KEEP, WS/DB paths unaffected, NO data migration (prod mi_live_orders verified all-plain via the reconcile). Fix = _enum_value at _order_to_dict + 2 site repairs; the ONE risk = dead fallbacks coming ALIVE (dedup guards to pin, R3 already gates re-entry). Card-ready: 6 tests specced, single deploy both scopes. REMAINING = the card applies + deploy + next-morning verify.
- #184 | 2026-07-10 | in_progress | broker-authoritative trade-state mirror (ADR 0008) — increments 2/3 (inc.1 write-side regression fence SHIPPED 6/6, audit_trade_state_demotions.py). (2) read-only DB↔broker coverage-drift detector (observe-only, no mutation) + ingest untracked broker orders (the a41e7c6a mirror gap) + fix /syncnow ("Unknown command" despite full wiring); (3) guarded auto-correction (broker-wins, gated by the #137 degraded/empty-read guard — never the first increment). DoD: coverage-drift alerts live + mirror complete + /syncnow works → cutover prerequisite. SSoT docs/decisions/0008-trade-state-broker-source-of-truth.md ▶ **PART (c) /syncnow RESOLVED 6/24 (STALE-OPEN):** wiring is COMPLETE on main (4 sites align); the "Unknown command" was a zero-width-char input artifact ALREADY FIXED 6/04 (`_normalize_slash_cmd`, commit 6fe4114, ancestor of HEAD) → LIVE (container redeployed many times since). Operator: re-test /syncnow in Telegram to confirm. The only real gap = MISSING routing-test coverage, now ADDED (`test_operator_commands_partialnow_syncnow.py` + `/syncnow` in the `test_execute_task_routing.py` freeze; 83 pass; test-only → no deploy). ⚠ ADR 0008 text "/syncnow currently broken" is STALE — fix when (a)/(b) land. REMAINING (updated 7/5): (a) BUILT 7/5 (broker/coverage_drift.py, D1/D2/D3 + 24h DB-dedup + degraded-read guard, wired into the 15-min reconcile; card-built Fable-reviewed; VERIFY-LIVE Mon 7/6 = first reconcile cycles write coverage_drift audit rows, expect quiet/D2-INFO only) + (b) broker-order ingest = trade-state mutation -> careful tested session, the a41e7c6a closer. [b2] [ok:date-roll-0701] [ok:#418 glide path - (a) DONE 7/5, ETA is the (b) ingest careful session Fri 7/10] >> FABLE-WINDOW NOTE (operator 7/5 eve): the (b) ingest DESIGN-to-execution-depth lands Tue 7/7 in the Fable block; the build+#151 exercise stays Fri 7/10 on Opus+cards. >> (b) SPEC DONE 7/6 EVE (Fable block 2 pulled forward from Tue; docs/analysis/184b_broker_order_ingest_spec_2026-07-06.md): 3 case classes R1 stop-pointer-repair (the a41e7c6a class itself, LOWEST risk, enables first) / R2 untracked-entry reconstruction / R3i untracked-position (dry-run-only until observed); 3-state toggle off->dry_run->live-per-class, each enable operator-signed after >=3 clean dry-run proposals; COID parse+validate, no-overwrite re-asserts, per-cycle cap 2, #151 paper exercise scripted, 7 unit tests specced; #436 reuses the same plumbing (both mirror directions, one mechanism). REMAINING = Fri card builds + dark deploy + paper exercise + phased enables.
- #316 | 2026-08-15 | pending | PDT / SEC-FINRA Rule 4210 — confirm ALPACA's rollout (not Fidelity's) + relax BLOCK_PDT_LOCKOUT via CHANGE_PROCESS (memory pdt_rule_4210_change_2026; revisit at cutover) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5 - external gate (Alpaca 4210 rollout), check at M3/M4]

## Family A — consolidation plays post a runup (ADR 0013 · reset of #270)

- #404 | 2026-07-08 | pending | Remove the now-DEAD `confirm_signal_at` (anticipation.py:874) + its 4 tests (test_consolidation_entry_signal.py:156-200) + `ENTRY_CONFIRM_VOL_MIN` — Confirm UN-WIRED 6/29 (operator: ANTICIPATE-ONLY, a 2nd entry mode muddies the shadow's edge measurement; ADR 0013 §1 + scheduler `_consolidation_readiness_job` + the digest/board displays all updated + verified). The behavior change is DONE; this is just deleting the dead code. Low-priority. Keep the function until it's clearly not wanted — re-wiring Confirm would need operator sign-off (it changes what the shadow measures).
- #402 | 2026-07-22 | pending | **HTF (#356) Phase-3 shadow /simplify cleanup (post-stability; ALL shadow, no live path).** From the 4-agent /simplify 6/28 (advisor: defer ALL — the night real money was armed). (1) Boundary-safe shared PURE-MATH module: stop_limit_buy_price + the VIX sizing arithmetic (the 0.5/0.20/0.02 magic numbers, dup'd flag_detector↔order_manager) + the R-settlement loop → ONE pure module imported from both sides (the #154 execution boundary is WHY it should exist, not why 3 copies are OK). ⚠ the R-settlement piece = parameterize anticipation.entry_bet_outcome (add entry_price + start_offset) → touches the LIVE anticipation path → gated, under change-discipline. (2) The ADV floor uses a MEAN (sum(volume)/len) but the canonical adv_20 is a spike-robust MEDIAN — pull the existing primitive (search-before-build). (3) flagpole_ratio/flag_depth_pct NULL stub is over-scoped — the inputs (pivot_high/base_low/runup_pct) ARE persisted → a 2-line compute at the insert site (supersedes the Phase-1-persist follow-up). (4) Free wins: dead slice-guard flag_detector:585; max(within or cands) collapse; fix the _htf_settle_from_bars docstring (overclaims a reuse it doesn't do). (5) per-break conn-acquire in the shadow insert loop → fold into the existing async-with conn block. (6) #356-RENAME LEFTOVERS (display, no-money): the `/setup` handler still prints "🚩 FLAG" for mi_flag_candidates rows — rename to "HTF" to match `/flags` single-ticker "HTF History" (agent.py `_handle_setup_query`; operator caught 6/30 — same class as the /htf skip-set bug). (7) FREEZE the /htf regression: add a handler-level test "bare `/htf` → the board, NOT a single-ticker lookup for HTF" — the #356 rename broke /htf by omitting "HTF" from the ticker-skip set (FIXED 6/30, 949838b) but the routing test only froze WHICH handler, not the within-handler parse → the regression slipped; a parse-level test stops the next rename re-breaking it. Refs the 4 /simplify agent outputs 6/28. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #327 | 2026-07-08 | in_progress | **#270 Step 3 — consolidation entry-watch + shadow-settle + 9M-universe feed (critical path for #326).** ✅ PHASE A (the #326 DIRECTIONAL READ) DONE 6/18 — replay-first (live shadow can't reach decision-grade N by 7/7). Symmetric two-arm replay over the 121-row mi_9m_day2_candidates cohort (Day-2 ORB vs consolidation FIRST5-break, same SETTLE_RULE/forward window, full-universe expectancy, both-ways universe, MFE-free via anticipation.simulate). scripts/_327_replay.py + _327_pull_minute.py + _327_dump.py; doc docs/analysis/ninem_consolidation_vs_day2_replay_327_2026-06-18.md (advisor-hardened: within-name cut + split claims). VERDICT: **Claim A (SOLID) — Day-2 ORB earns NO robust edge** (filled median −0.24R, +3.2R total all 3 outliers, −1.4R on the 17 consolidation names) → carries the #326 RETIRE half; **Claim B (PROMISING, selection-inflated → Phase-B-gated) — consolidation better** (+2.00R median, 82% win, broad). Mechanism = stop tightness (first5-low vs wide prior-day-low). ✅ PHASE B — OFFLINE caveat-#1 correction DONE 6/18 (advisor-reconciled): extended _327_replay.py (consolidation_scan exposes every post-coil day w/ as-of base_high; Phase-A confirmed-close numbers byte-identical) + wider 266-day minute pull → re-timed Arm-2 to the FIRST intraday OR-break a live watcher takes. ⚠ MATERIAL FINDING (changes the #326 replacement narrative): **Claim B's +2.00R was an ENTRY-MODEL ARTIFACT, not a live edge** — inflated jointly by (1) daily-close selection (20/35 names break intraday on an EARLIER, weaker-close day; filled median +2.00R→−1.00R re-timed) AND (2) a 2%-sub-base OR tolerance (only 7/17 Phase-A "fills" had OR5-high actually ABOVE the base). De-rated, consolidation (+3.7R loose / +5.0R genuine, both outlier-carried) drops into the SAME break-even bucket as Day-2 ORB (+3.2R) → the QUANTITATIVE case for "consolidation is the strong replacement" is GONE. HARNESS LIMIT (explicit): every Arm-2 variant is an OR-break-NEAR-base proxy, NOT the live all-day volume-confirmed #94 watcher (blind to mid-session base breaks) — so it CANNOT settle whether the real watcher has an edge; no single de-rate number is faithful. **Claim A (retire Day-2 ORB) UNCHANGED/solid** (Arm-1 has no base-break gate). RESOLUTION = the live forward shadow with the REAL #94 detector is now the ONLY instrument that can answer the live-edge question → measure-before-wire JUSTIFIES (not moots) the live wiring. ✅ ENTRY-SIGNAL STUDY DONE 6/18 (operator "what signals the best entry"): scripts/_327_entry_signal.py — parameterized coil-readiness (rmv+range+vol gate × consecutive-day COUNT × stop), stop-matched curves not an argmax cell. FINDING: **PERSISTENCE (N consecutive tight days), NOT rmv DEPTH, times the entry** — N=3 lifts capture 32%→44% + median uncapped MFE +0.8R→+2.1R, STOP-MATCHED + robust across the rmv/range/vol knobs (Δ+9–12pp); VANISHES under over-tight level gates (range≤4% Δ0). Robust REGION: range≤5–7%, vol≤1.0×, rmv≤30–40, N=2–3. ✅ FORWARD SHADOW WIRED 6/18 (operator "wire it, we have potential edge, let's not wait"): new mi_consolidation_entry_shadow table + anticipation.entry_signal_at (the SINGLE-SOURCE signal — the offline sweep delegates is_tight to it, pinned by tests/test_consolidation_entry_signal.py, 8 tests) + entry-watch folded into _consolidation_readiness_job (17:35 ET, one shadow row per OPEN coil, open-dedup pins first-fire, 9m/family_a origin tag, point-in-time) + Telegram digest. Stop recorded coiled_low (validated default) + structural_low (alt) for later settlement. ALL SHADOW (zero execution). ✅ VERIFY-LIVE DONE 6/18 (deploy b8245ec, both): write-probe proved the insert path (insert✓ open-dedup✓ CHECK✓ self-cleaned) + the real job wrote **18 sane backfill rows** (all N=3, rmv≤28, range 1.8–6.0%, vol≤0.98×, both stops, HLIT correctly tagged [9m]) + a 2nd run held at 18 (live open-dedup confirmed). Container fresh on HEAD; job 17:35 ET mon-fri (un-paused) → tomorrow's run is the first scheduled fire. ✅ SETTLEMENT MACHINERY BUILT 6/18 (operator "build the machinery now, turnkey before data lands"): anticipation.entry_bet_outcome (the validated asymmetric bet — capture/stop/open + uncapped fwd_mfe_r under coiled_low, 12-bar horizon; the offline sweep's bet_outcome now delegates to it, pinned) + settle_entry_shadow (abstains <12 fwd bars, else outcome+fwd_mfe_r+realized_r; realized_r REUSES the tested settle_row +1R/+3R day-5 harvest) + db settle/fetch/summary helpers + _consolidation_entry_settle_job (17:40 ET mon-fri, registered + INTELLIGENCE_OWNED) + tests/test_consolidation_entry_settle.py (11 tests). SURVIVORSHIP FIX (advisor 6/18): settle on DEFINITIVE outcome (capture/stop final at any bar count; only 'open' waits the full window) so a stop/capture-then-HALT name isn't orphaned (would silently bias capture%); the residual halt-while-open is surfaced via get_consolidation_entry_shadow_summary.open_overdue_n (orphan canary, never silent). Turnkey: settles automatically once a row reaches the 12-bar window (~first settles ~2026-07-07). ✅ VERIFY-LIVE DONE 6/18 (deploy 575b412): all 4 first-run SQL paths clean on real Postgres — settle-probe (UPDATE write-back + double-settle guard + open-dedup-free), run-settle (get_settleable + clean 0-settle), summary SQL (READOUT "18 open / 0 OVERDUE / 0 settled", by-origin splits clean, medians null until ripe). 17:40 ET job registered/fresh. ✅ SURFACE CONSOLIDATION DONE 6/18 (operator "consolidate, don't add a new command every time" + plain-words readability; AskUserQuestion: Family-A-only + plain words): instead of adding /readout, FOLDED the whole lifecycle into ONE board + ONE digest (6 surfaces → 2). /anticipation now shows edge-header (the readout) → 🎯 entry fired → 🪙 coiling → 👁 post-runup → 📐 settled; the 3 separate daily pushes (🪙🎯📐) collapse into ONE unified Family-A digest by folding settlement INLINE into _consolidation_readiness_job (removed the standalone settle job + its INTELLIGENCE_OWNED id → _run_entry_shadow_settlement helper). format_consolidation_row rewritten to plain words (+70% · coiling 14d · very tight · tightening↓) + new format_entry_fired_row/format_entry_settled_row (single-source, all pinned). RULE going forward: new lifecycle stages = sections, never new commands. ✅ VERIFY-LIVE DONE 6/18 (deploy 12d8aff): /anticipation board rendered off the 18 live rows — edge header + 🎯 entry-fired (12, HLIT tagged 9M, stop %s) + 🪙 coiling (tightness word) + 👁 post-runup (runup + "Nd since peak", tightness word omitted — a live-render readability fix, the rmv-axis would contradict the not-coiled state) + 📐 settled (0). ⏭ REMAINING: nothing until data accrues → OBSERVE-ONLY, first settles ~7/7 answer the #326 live-edge → if confirmed, the shadow→PAPER graduation (register + wire entry → phase=paper, in PARALLEL with live magna53) = #353. Execution-side/split-adjacent [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #353 | 2026-07-09 | pending | Consolidation entry → PAPER graduation (the shadow→paper rung of the promotion ladder; runs IN PARALLEL with magna53 LIVE — per-strategy phase routing, `_check_safeguards` per-mode isolated so a paper strategy can NEVER touch real $). ▶ GATE: graduate ONLY if #327's ~7/7 first-settlements show a real consolidation edge (the #326 live-edge question, on the REAL #94 watcher not the replay proxy); no edge → stay observe-only shadow, do NOT graduate. ▶ BUILD: (1) register consolidation in `mi_strategies` (family=family_a · signal_type · outcomes_table · promotion_model) — it is NOT a registered strategy today (absent from /status); (2) wire its entry into `entry_pipeline.submit_trade_entry` via a consolidation `spec_builder` (stop=coiled_low = the shadow's validated default; per-strategy sizing) — the entry+settle mechanics exist in shadow (`anticipation.entry_signal_at` + `entry_bet_outcome`/`settle_entry_shadow`), so this promotes the shadow RECORDER to a REAL paper-fill path, not a shadow row; (3) `phase=paper` → routes to the PAPER Alpaca account (real fills, fake $); magna53 stays untouched on live. ▶ VALIDATE: accrue closed paper trades → the promotion check (median R / win rate — the magna53 ladder) + digest readout. ▶ PROMOTE paper→live later = a SEPARATE Phase-2-style flip with its own gate (NOT this task). ▶ BONUS: a paper strategy beside live magna53 also yields a real-time IEX-vs-SIP selection-gap control (the GATE-3 thesis). DoD: consolidation registered + `phase=paper` + accruing REAL paper fills with a promotion-check readout (not a shadow row). Refs #327 (shadow edge), #289 (selection layer), #326 (retire-9m-for-consolidation). [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #354 | 2026-07-22 | pending | MERGE flag_continuation INTO the consolidation play (Family A) — it is NOT a separate strategy, it IS the **Confirm-flag entry (b)** (base-high breakout); operator-directed 6/22 ("flag_continuation = one and the same as the consolidation play"). Code-verified diff (Explore 6/22, file:line cited): same runup-anchored family + SHARED tightness primitives (`_compute_rmv`/`_compute_fresh_tightening` imported by anticipation.py — no dup). DIFFERENCES = flag_continuation carries stale/stricter params; reconcile to the consolidation play's SIGNED model: (1) UNDERCUT — flag_detector.py:513 INVALIDATES on undercut; consolidation ALLOWS it (ADR 0013 §1, operator's veto of undercut-as-gate) → re-gate INVALIDATED→WATCH_UR so undercut routes to entry (c) U&R, not rejection; (2) RUNUP 50%/60d → 15%/10d (Pradeep signed; 50% misses COO@15%); (3) ANCHOR pivot-walk → runup-peak-close; (4) TIGHTNESS ratio gates → volatility-relative RMV/ATR. KEEP: 0.4% tight-close as ranking-not-gate (operator veto), $20M/day universe, the shared primitives. COMBINE: flag's breakout logic becomes Confirm entry (b); retire flag_continuation as a standalone mi_strategies row → ONE strategy = consolidation play w/ 3 entries (Anticipate/Confirm/U&R). ⚠ LOAD-BEARING: flag_detector drives the live /flags board → detection-criterion change = CHANGE_PROCESS (docs/setups SSoT) + N≥10 backtest + operator sign-off (the undercut re-gate = ADR 0013 deferred Phase 4 WATCH_UR). GATED on the #327 ~7/7 edge read (don't merge before the play proves edge). DoD: one strategy, flag_continuation absorbed, undercut→U&R, gates reconciled to the signed model, /flags unbroken. Refs #327/#353/#326, ADR 0013. ▶ GROUNDED 6/22 (read ADR 0013 + CHANGE_PROCESS + flag_continuation.md exists) — CORRECTIONS: (1) this merge IS ADR 0013's already-DESIGNED Phases 2–4 (Family A = one universe + 3 entry modes; §1 "they share ONE universe + ONE coil detection, undercut ALLOWED") — NOT a one-deploy param-swap. (2) the "Confirm" entry is `_flag_break_scan_job` (#94 INTRADAY break) per §4 Phase 3; flag_continuation (5:25 PM EOD) is the REDUNDANT standalone whose role is SUBSUMED + retired, NOT "entry b". (3) the undercut→WATCH_UR fix is gated BY THE ADR (§4 Phase 4: DECOUPLED, SEQUENCE LAST, own sign-off + N≥10 backtest, SSoT flag_continuation.md same commit). (4) reconcile to anticipation.py's SIGNED §2 universe (`db.get_anticipation_universe`, 15%/10d/runup-peak/RMV-ATR) — do NOT edit flag_detector's 50%/60d in place. (5) NOT gated on 7/7 (that gates TRADE-graduation #353); the merge build is its own shadow track. EXECUTE as a focused build (Phase 2 shared universe+coil → Phase 3 the 3 entries → Phase 4 undercut [gated]), after-close deploys, NOT launch day. ▶ BUILT 6/22 (Phase 2-3 SHADOW · commit befb41e · NOT deployed — rides Phase 2): `anticipation.confirm_signal_at` (new PURE breakout detector on the §2 universe via daily bars — deliberately NOT the live #94 job/mi_flag_breaks, which would gate Confirm by the flag 50%/60d cohort = #270 phantom in miniature) + `mi_consolidation_entry_shadow.entry_mode` column (DEFAULT 'anticipate' back-tags existing rows) + the dedup FIX the planner caught (pre-#354 2-col open-index/ON-CONFLICT silently DROPPED the 2nd mode on the same coil → now 3-col ticker/anchor/entry_mode) + `by_mode` readout (ADR §1 never-blend) + `_consolidation_readiness_job` fires BOTH modes into the one lifecycle. 5 tests, full suite 1042 green. ISOLATION verified (anticipation.py pure; entry_pipeline + ep_detector do NOT import it). ✅ DEPLOYED 6/22 after-close (16:50 ET, bundled w/ the mgmt-format deploy · `deploy.sh market-agent` DEPLOY OK · all preflight gates green, G6 skipped=closed · apollo-market fresh · entry_mode migration + the open_mode 3-col index VERIFIED LIVE in pg_indexes; old 2-col index dropped · execution/orch untouched). ▶ VERIFY-LIVE = tonight's 17:35 ET consolidation job writes the first `entry_mode='confirm'` rows + the `by_mode` readout populates (check mi_consolidation_entry_shadow next session). REMAINING (still gated): Phase 4 undercut→WATCH_UR (sign-off + N≥10 backtest); then retire the standalone flag_continuation scan once the play subsumes its role. ▶▶ **SUPERSEDED 6/22 — THE OPERATOR SPLIT (the merge premise was WRONG; un-merge instead).** The flag→consolidation merge built a Confirm/breakout entry on a **structurally-broken** consolidation detector — anchor measured the runup INSIDE the base (STM-class excluded) + the entry fired on quiet DECLINES (BTU/UFO/DRUG) with NO holds/consolidation gate at all. Full diagnosis: `docs/analysis/consolidation_runup_defect_2026-06-22.md`. **Operator decision 6/22 — clean SPLIT + rename:** (1) **HTF (High Tight Flag)** = the former flag/flag_continuation → its OWN setup (big runup → tight base → enter on the BREAKOUT); operator details later (own #-task when they land). (2) **Anticipation** = the former "consolidation play" (#270/Family A) → SIMPLIFIED to STRICTLY the Pradeep tight-day ANTICIPATE entry (enter IN the coil, before the break); DROP the Confirm/breakout merge (#354 befb41e) + U&R. (3) **Sugar Babies** = a stock CONDITION (recurring 9M), NOT a setup → likely a confluence point on a setup, TBD/revisit. DEFINITION = the captured Pradeep blueprint (`operator_shared_notes.md`: 6/16 thread + 6/22 Gemini cross-ref): runup 15%/10d → HOLDS (Upper-Third ≤20–30% retrace + One-Strike ≤1×4% breakdown, **volatility-relative**) → series of tight days (volatility-relative, NR-day) → anticipate. REBUILD PROGRESS 6/22 (SHADOW, committed: `_consolidation_acceptance_test.py` + `_anticipation_holds_measure.py` + `_anticipation_shortlist.py` + fixtures): anchor refined to the recent runup-leg peak (3–20d base, not the global max); holds gate **data-grounded** on the 6/15 Pradeep cohort (declines 2–5 breakdowns/23–34% retrace vs tight names 0–1/≤11%); confirmed the gate must be volatility-relative (HYLN). ⏸ **PAUSED for operator labels** on the 303-name shortlist (`docs/analysis/anticipation_shortlist_to_label_2026-06-22.md`) → calibrate the volatility-relative thresholds → wire into the detector (shadow, sign-off + ADR-0013 provenance rule) → re-validate. The #354 merge text ABOVE is superseded by this split. ▶ **ROADMAP (all 3 setups + next steps + WHY): `docs/roadmap/family_a_setups_split_2026-06-22.md`** — #354 here = SETUP 1 (Anticipation). #356/#357 = the other two setups; #358 = the systemic provenance fix. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #356 | 2026-07-11 | in_progress | HTF — Setup 2. ✅ DETECTION DONE (6/27, 90/40 + ≤25% + 10/20/50 + Stage-2 200MA/52w-high; flag→HTF rename; docs/setups/htf.md; AGL/XMTR live). ✅ PHASE 3 BREAKOUT-ENTRY SHADOW BUILT + DEPLOYED-LIVE 6/28 (feac9a0; table mi_htf_breakout_shadow LIVE + 0 rows; apollo-market fresh; advisor-vetted 4 corrections; 7 tests): mi_htf_breakout_shadow + insert/settle/summary funcs + prepare_htf_breakout_order (PURE MATH in flag_detector, NOT broker — execution-boundary fix) + the #94 hook (records ALL breaks, would_reject_reason NOT dropped, error-wrapped so it can't break the live scan) + _htf_settle_from_bars (the breakout-fill NUANCE: entry=base_high NOT the bar close, break-day-inclusive) + the settle job folded into the daily Family-A job + the digest "🚩 N settled" self-verify; FIXED notional (R is equity-independent — no live account fetch). ✅ VERIFY-LIVE DONE 6/29: the first HTF break WROTE a shadow row (1 #94 break → 1 mi_htf_breakout_shadow row, all-time count 1) — the Phase-3 shadow hook fires live. Settlement shows ~12 trading days after the 1st capture. ✅ VERIFIES RUN 6/28 (0a4f825): flagpole = CONFIRMED PRIMARY (keep pivot-anchored; a today-anchored trailing-40d diverges ~3/10 but was a non-primary interpretation, REJECTED — operator: HTF is well-defined, use the primary definition not our own [[feedback_established_setup_use_primary_definition]]); liquidity = GAPS (ADV>500k PARTIAL — high-priced names clear $5M dollar-vol on <500k shares; ADR>4% ABSENT). ✅ LIQUIDITY FLOORS ENCODED 6/28: ADV>500k shares + ADR>4% as a per-ticker gate in compute_flag_metrics (so ALL universe paths are covered, not just the organic SQL); TUNABLE named constants (_HTF_MIN_ADV_SHARES firm; _HTF_MIN_ADR_PCT=0.04 STARTING — operator: ADR isn't canonical, DATA-GATED tune `htf_adr_threshold_tune` at N≥10 settled winners); impact 2→1 current candidates; 10 tests. STILL OPEN: (b) get_recent_daily_history trading-rows refactor (calendar-vs-trading ratio-bandaid); (c) persist flagpole_ratio/flag_depth_pct on mi_flag_candidates (recorded NULL now — value-level offline analysis); NB capture-on-same-bar-tie skews capture% slightly high. FLIP shadow→paper = N≥10 settled + CHANGE_PROCESS + sign-off (money gate #397). Then #396 (Phase 4 EMA-trail). Refs ADR 0013, docs/setups/htf.md. [ok:date-roll-0630] [b1]
- #396 | 2026-07-11 | in_progress | **HTF Phase 4 — EMA-trail management SHADOW.** GATED on #356 (Phase 3 shadow exists). `exit_logic.py` uses SMA only — add an `ema(closes, window)` helper + the sourced management: scale **33-50% at 3-5d → breakeven → trail 10/20 EMA** (exit on a daily close below). Reuse `apply_daily_exit_step`'s partial/breakeven scaffolding (128-172); add the EMA-trail option + the variable 33-50% scale param. SHADOW: compute the would-be management on the `mi_htf_breakout_shadow` positions, DB-sourced state (restart-safe — `feedback_scheduler_aggregators_db_sourced`). DoD: the shadow positions get the full HTF management lifecycle recorded (entry→scale→BE→trail→exit). Refs #356, `docs/setups/htf.md`. blocked_by:#356 ✅ BUILT+MERGED 7/3 eve (pulled forward): ema() helper · opt-in trail_mode/scale_fraction (default byte-identical, 25+28 caller tests unchanged) · mi_htf_management_shadow sibling table (1:1 FK, full-snapshot upsert, restart-safe) · 17:36 ET intelligence-side job, audit-only. Deploys Saturday batch; verify = job registers + first rows. [ok:built 7/3, deploy+verify Saturday] [b3] [ok:deployed 7/4; the 17:36 job is mon-fri — debut = Mon 7/6, verify then] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #397 | 2026-07-18 | pending | **HTF money gate — breakout-entry shadow→paper→live (the real edge validation).** TARGET date — EVIDENCE-gated (the GO rides on the shadow edge data, NOT the date). The detector is SHADOW (no money); the breakout-entry is NEW money-firing capability — does HTF-breakout-trading make money? After ~1-2 wks of `mi_htf_breakout_shadow` outcomes: review the edge (capture / MFE / realized-R on the breakouts) → if it holds, promote to **PAPER** (real fills, fake $) → then **LIVE** (CHANGE_PROCESS + operator sign-off). WAITING ON: #356 + #396 + ~1-2 wks of shadow breakout outcomes. DoD: edge reviewed; paper/live GO-NO-GO recorded. Refs #356/#396, [[feedback_shadow_needs_dated_flip_eta]], THE LINE.
- #357 | 2026-07-22 | pending | **Persistent Sugar Babies — role decision (the 3rd STREAM of the split — a CONDITION, NOT a Family-A setup).** Recurring-9M stock CONDITION (≥3 9M EOD prints/180d, `mi_sugar_babies_cohort`); the surface overlays the flag/HTF stage on the 9M cohort. Operator 6/22: any setup can include a sugar-baby or not → likely an additional CONFLUENCE/SCORE input on a stock already in a setup, NOT a standalone play. NEXT: (1) operator decides the role (confluence/score input vs standalone watchlist); (2) re-frame the surface (cohort = Family-B/EP universe; stage overlay → HTF). DoD: role decided + surface re-framed (or kept observational). Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #358 | 2026-07-25 | pending | **Enforce ADR 0013's provenance rule — flag any live gate value with NO source citation** (advisor 6/22, the SYSTEMIC root). The detector silently DIVERGED from a signed methodology (`is_entry_tight`'s absolute range contradicted the operator's signed 6/16 volatility-relative conclusion for weeks, uncaught) because NOTHING checks code-against-captured-methodology. Build a check (test or deploy gate) that fails when a cohort-shaping gate constant lacks a source citation in `operator_shared_notes.md` / the ADR (ADR 0013's own provenance rule, currently UNENFORCED). Scope: Family-A/detection gates first. DoD: the check fails on an unsourced gate value + is wired into CI/pre-commit. Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #297 | 2026-07-29 | pending | FAMILY B EP rework — inherits the gap-anchored replay()/evaluate_candidate + mi_anticipation_lifecycle + ARCHIVE/clean its phantom rows (decoupled from #270 Phase 1). 9M-ENTRY retirement (Day-2 ORB → consolidation) is the dated cut-over decision #326 (9/15) [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #283 | 2026-07-25 | pending | wick_fill promotion eval (shadow→live), gated behind Family A [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #385 | 2026-07-08 | in_progress | **Volume dry-up telemetry on the consolidation shadow (Gemini review 6/27).** Record the 3-bar + 15-bar volume SMAs (and/or their ratio) alongside rmv_5d/rmv_15d on `mi_consolidation_entry_shadow`, and surface a volume-contraction column on the anticipation labeling worksheet — so the operator's labeling can cross-reference volume dry-up against range contraction (a genuine coil rests on reduced-but-active volume; the two together separate a coil from a drift). TELEMETRY ONLY (not a gate). Refs #354 (the labeling pass), the RMV ratio-to-baseline recalibration (commit 03b95e5). DoD: volume SMAs recorded on the shadow row + visible in the worksheet. ✅ BUILT 7/3: `anticipation.volume_dryup()` — pure helper mirroring `compute_rmv`'s own window shape (current_window=3/lookback=15) but on the VOLUME axis (rmv is range-only) — wired into `entry_signal_at` + `confirm_signal_at`; 3 new `mi_consolidation_entry_shadow` columns (vol_sma_3/vol_sma_15/vol_dryup_ratio — inline CREATE + ALTER-IF-NOT-EXISTS, same pattern as rmv_15d) + scheduler wiring; `scripts/_anticipation_shortlist.py` worksheet now shows a `vol_dryup` column. 9 new tests (`tests/test_anticipation_volume_dryup.py`, pinned arithmetic + point-in-time invariance) + full suite green (1342 passed, `tests/`). NOT yet deployed — verify-live = next 17:35 ET consolidation job after `deploy.sh market-agent` writes non-null vol_sma_3/15 on a new fire. [b2] [ok:deployed 7/4; verify = Mon 17:35 job writes vol_dryup] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #386 | 2026-07-29 | pending | **Leverage authoritative trading-halt data instead of inferring halts (operator idea 6/27).** The RMV halt-floor (recent vol>0 + max NTR≥0.05%, folded into the 03b95e5 amend) is a HEURISTIC for "is this dead/halted data." Ground-truth fix: check whether our market-data/news feed (Polygon/FMP) exposes a halt / LULD / T1–T12 status, and if so gate the dead-data guard on the authoritative flag rather than inferring from zero-volume bars (same "direct source beats inference" principle as catalyst sourcing — see [[feedback_catalyst_sourcing_direct_over_llm]]). DoD: confirm whether the feed carries halt data; if yes, wire the authoritative flag into the consolidation dead-data guard; if no, document that the heuristic stands. Refs #354, the RMV halt-floor. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #394 | 2026-07-11 | pending | **Tune the coil-finder thresholds + decide the orderliness rank, on forward shadow.** After ~1 wk of forward candidates (daily 🪙 board + entry-signal fires + settlement R on corrected bases): TUNE the ~50% hold cap + the rank ordering against REAL data (NOT n=5 — operator's anti-overfit rule); add the ORDERLINESS/gappy metric (overnight-gap / max-daily-move, NOT the intraday-ADR my first cut used) + demotion IF gappy coils surface. OPTIONAL (no dependency, ~2d, LOW-pri): peak-detection accuracy for long bases — aged>20 already drops the harm (PTGX confirmed absent). WAITING ON: ~1 wk forward candidates (market's clock). DoD: cap tuned + orderliness decided. Refs #393, ADR 0013. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #395 | 2026-07-14 | pending | **Load-bearing flip GO/NO-GO: #327 coil-finder shadow → real entries.** SHADOW today (records would-be entries + settlement-R, never trades). GATED on #393 (scheduled-run clean) + #394 (thresholds tuned + settlement-R outcomes looking right) + CHANGE_PROCESS (N≥10) + operator sign-off. ETA is a TARGET — evidence-gated (the GO rides on the R-data, not the date). WAITING ON: #393 + #394 + outcome-R. DoD: GO/NO-GO recorded; if GO, the live-entry wiring scoped. Refs #393/#394, [[feedback_shadow_needs_dated_flip_eta]]. blocked_by:#394

## Stocks in Play — detection / setups / entries / 9M / flags
- #359 | 2026-09-22 | pending | **EP market-cap floor ($500M) — review-over-time whether it's the right floor** (operator 6/23, seeded by BLZE=$487M, a $13M near-miss clean gapper that was observe-laned by `filter:mcap_too_small`, NOT alerted). DATA-GATED + auto-accumulating: the scan already logs every mcap-filtered candidate in `mi_ep_scan_log` (filter reason + mcap + gap/rvol/extension). Accumulate the **NEAR-MISS cohort** — clean gappers ~$400-500M that PASSED the EP entry gates (gap≥10% / rvol / extension / price≥$5) but were excluded ONLY by the <$500M floor — + their **FORWARD outcomes** (would alerting them have won, or just added noise?). DoD: at **N≥~15** near-miss samples with forward returns (anti-overfit, batch-not-single-case; ~Q3 2026), evaluate whether lowering `MIN_MARKET_CAP` (`backtester/filters.py:23`, the shared live+backtest gate) adds net winners → **OPERATOR decision** (detection/safeguard criterion → CHANGE_PROCESS + sign-off + filter-list judgment, NEVER the agent's). Registry: `ep_mcap_floor_500m_review` (data_gated_reviews.yaml, weekly auto-surface, earliest 2026-09-22).

- #168 | 2026-07-22 | in_progress | shadow-detector quality/actionability filter for LIVE-ping graduation (evidence-gated) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #167 | 2026-07-25 | in_progress | Lane-2 narrative theme detector (shadow) — ongoing [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #170 | 2026-07-29 | in_progress | EP cooldown re-setup admission: the ~60d cooldown over-suppresses legit re-setups (~7-10d separates extended-continuation from re-setup) → shorten or add a fresh-catalyst/re-based override. (Scope recovered 6/20; shadow classifier LIVE ep_detector.py:1345-1358 fail-open; live-flip ship gated on realized-R + CHANGE_PROCESS, review data_gated_reviews #170.) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #146 | 2026-07-29 | pending | Flag detector: allow direct TIGHTENING→TRIGGERED (drop/weaken the COILED prerequisite) on close>base_high_close + volume confirm — gated on N≥10 backtest. (Scope recovered 6/20 from ADR 0005 + flag_continuation.md; verified NOT shipped: flag_detector.py:626-635 still requires coiled_today/was_coiled_recent; intraday detector works around it.) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #178 | 2026-07-29 | pending | Merge /setup + /why into ONE filter-reason observability command (entry-execution-observability thread, relates #150/#171). (Scope recovered 6/20 from PORTFOLIO.md/ab38d85; verified NOT shipped: _handle_setup_query agent.py:3148 + _handle_why_query :5357 still separate.) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Judge & catalyst — v1.1 program
- #405 | 2026-07-08 | in_progress | **EP catalyst-cache re-grade fix (cost/noise bug; advisor-gated grade-path refactor).** BUG (L2 6/29, code-verified): the catalyst cache is set at ep_detector.py:2021, AFTER the filter `continue`s (M&A :1919, routine+low-gap :1927, pm-volume :1964) — so a GRADED-then-FILTERED ticker never caches → re-grades (full Claude+Perplexity LLM) every 5-min tick → ep_catalyst_provenance flood (CMCSA 36×; 124 vs ~5 median) + ~a couple $/day wasted LLM. ZERO trade impact (filtered stays filtered, survivors grade right) — cost/noise only. ⚠ NOT a one-liner: the cached path (:1635-1714) SKIPS the filters + produces an entry, AND filters are non-deterministic within a day (BFLY routine 7:00 → PR 8:12) — so naively caching a filtered ticker would let it SKIP the M&A filter on a later tick + wrongly ENTER. FIX = a control-flow refactor separating the GRADE (cache to skip the LLM re-call) from the FILTERS (re-run each tick) via a graded-sentinel that suppresses the expensive grade but still runs the filters. HARD GATE (advisor): a test proving a filtered ticker can NEVER produce an entry via the new path + a survivor behaves identically-or-strictly-more-conservatively. Advisor-design first. WHY 7/3: markets CLOSED (July 4 observed) — deploy the grade-path refactor with zero live trades + full test time, after the launch's first ~4 trading days prove the entry path. Kills BOTH the cost + the L2 noise (the noise stays a known-cause tracked item until then; the provenance log at :1776 is separable but not worth a standalone day-2 EP deploy). ✅ S6 BUILT+MERGED 7/3 per the locked design (grade cached at completion w/ filters_cleared sentinel; filters re-run every tick; 3 HARD-GATE tests green incl. filtered-can-never-enter; ONE flagged deviation: pplx always awaited → grade complete before filters, direction strictly-more-conservative, no threshold/string/order touched). Deploys evening; VERIFY = Tue 7/7 premarket scan clean + provenance ≤1/ticker/day + the L2 quiet. [b1] [ok:deployed 7/3 eve; scan-behavior verify = Tue 7/7 premarket] ✅ VERIFY PASSED Tue 7/7 premarket: 3 ep_catalyst_provenance events, 1/ticker (CRNX 07:00 / DOCN 09:20 / AGIO 09:50), flood GONE (was 124/day, CMCSA 36x), L2 catalyst-noise quiet — cache-fix VERIFIED-LIVE (cost + noise both killed). >> FOLD (#317 absorbed, operator 7/7): #405 now also owns the catalyst-DISPLAY coherence follow-ons — (A) add has_direct_source to the _catalyst_cache tuple (4 pack sites, ep_detector.py) so a cached MODERATE->HIGH re-grade PRESERVES the flag (currently dropped → catalyst line kept as safe default, not a regression but incomplete); (B) emit an `ep_catalyst_suppressed` audit on send_ep_alert's suppress branch (briefing.py:2432) so the display-only suppression is OBSERVABLE — closes #317's verifiability GAP (has_direct_source is a send-time flag, never persisted → the suppress was un-verifiable post-hoc; WULF 7/6 row ambiguous). Build off-hours w/ a test (cached re-grade preserves has_direct_source; suppress-audit emits); VERIFY-LIVE = next direct-source HIGH shows `ep_catalyst_suppressed` in the audit log + catalyst line absent.
- #368 | 2026-07-11 | pending | **Meta-rubric STEP-2 — operator labeling + the WEIGHTING decision** (gated on STEP-1 #367). Operator labels the cohort's theme-relevance (ground truth) + decides D2/D3: the theme-boost MAGNITUDE + the stage→credit mapping (Pradeep ~1.5-2× rides on theme-as-DRIVER, ASYMMETRIC — never penalize themeless). Methodology = OPERATOR's call (THE LINE). DoD: the weighting spec'd + the labeled themeless-winner-inclusive cohort. [STEP-3 = the #335 flip (theme-axis load-bearing, gated on grade-CORRECTNESS over that cohort, post-Phase-2) = FINAL DELIVERY.] [b3] [blocked:#367 matcher-vs-text-source read lands Mon 7/6 - STEP-2 labeling needs that fork resolved] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> INPUT READY 7/6 (from the #367 read): theme-relevance signal must come from NARRATIVE text / co-movement, NOT the subject filing (signal-a name-attribution confirmed dead, self-referential corpora). Operator labels + weighting decision proceeds with signal (a) known non-viable; S3 (0019 §2.3 theme-narrative block) is the sourcing fix.

- #322 | 2026-07-25 | pending | theme-detection gap (memory theme_detection_two_lane_architecture): judge inferred JBL's AI-infra theme that neither Lane-1 clustering nor Lane-2 narrative tracks — real coverage gap, not a label. Feed into P2/P4 narrative radar (#309/#311) [ok:weekend-northstar-track2] [b3] [ok:feeds P2/P4 radar, post-sprint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #328 | 2026-07-18 | in_progress | [ok:north-star-longweekend] THEME AS A SCORED RUBRIC INPUT (#329 child — the theme axis of the meta-rubric) (operator 6/18, emphatic — "absolutely critical; Pradeep's theme is the #1 catalyst driver; not having this filed was a disastrous miss"). TODAY theme is DISPLAY-only metadata (get_theme_membership on the EP alert) + the judge weighs it QUALITATIVELY in the holistic grade (#249/#233) — it is NOT a scored axis in the catalyst rubric (catalyst_rubric_runtime score_ep_with_rubric). Meta-rubric vision (user_meta_rubric_architecture + user_pradeep_catalyst_hierarchy: theme=#1, > policy/shortage/sales): theme stage+score should ADJUST the composite/gate (Accelerating→boost, Mainstream sustain, Nascent partial; standalone→no theme credit). GRADE-AFFECTING, judge/floor are load-bearing → CHANGE_PROCESS + operator sign-off on the mapping + N≥10 backtest before any live flip; safe shadow eval first. SOFT-PREREQ #325 (coverage must be trustworthy or we'd score on a blind-spot-ridden signal) + couples to #233 (judge theme-weight) + #322 (detection gap). FIRST DELIVERABLE SHIPPED 6/18: EP alert distinguishes BLIND-SPOT vs STANDS-ALONE for missing themes (briefing.py — see #319). NEXT: design spec (gate-shift vs composite-score input; how blind-spot/standalone feed it; stage→weight table) — scope by ETA. ▶ DESIGN SPEC DRAFTED 7/3 eve (pulled forward): **docs/decisions/0015-theme-axis-meta-rubric.md** — boost-only post-composite tier adjustment (Axis T), stage→credit table = the Saturday SIGN-OFF surface, STEP-0 = calibrate the table vs the #329 backfill cohort (N=452) BEFORE the shadow ships, blind-spot/standalone→0 (#319/#325), flip = CHANGE_PROCESS. Rejected: composite-axis (masks fundamentals) · gate-shift (illegible) · gating (refuted 6/5). NEXT: operator table sign-off → STEP-0 calibration → shadow build. (orig: the design-spec block over the long weekend — a focused North-Star session once the launch has a few days of real-money behavior behind it + no live trading splitting attention. This is the lead task; the rest of the judge cluster (#210/#211 sourcing, #330 structure axis, #367/#368 meta-rubric) sequences off the #328 design spec. [b3] [ok:shadow BUILT+DEPLOYED 7/4 (ADR 0015 signed, STEP-0 calibrated, 1376-case sweep, dedup-guarded); VERIFY = Mon scan writes theme_axis_shadow_adjusted rows; then accrual to the 7/18 checkpoint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #329 | 2026-07-18 | in_progress | META-RUBRIC composition (Phase 5) — ANCHOR the orphaned registry review + reconcile-first (operator 6/18 sweep; the parent of #328/#330/#331/#332). ⚡ ACCELERATED (operator 6/18: "rubric is critical to EPs — we're already trading EPs with the judge live; integrate earlier the better"). ALIGNMENT to the launch spine: the ADVISORY/SHADOW composite is UN-GATED (touches no live grade) → build it NOW, pre-6/22, in parallel with launch rehearsal; the LOAD-BEARING FLIP is grade-affecting → post-6/22 by the established trade-set-stability rule (#299/#320/#321 cluster), tracked as #335. So aggression lands on the shadow build (this task) + a fast post-launch flip (#335), NOT on destabilizing the cutover. NOT "we have no composition layer" — the live holistic judge ALREADY composes theme/catalyst/narrative qualitatively (#249/#240). The real gap: (a) theme/structure/gap-alignment aren't EXPLICIT, calibrated, traceable inputs (judge weighs them implicitly/inconsistently), and (b) the registry review `phase5_meta_rubric_calibration` (data_gated_reviews.yaml: threshold=30, earliest 2026-09-08) had NO PLAN #-task/ETA — yet its OWN text records operator 6/04 DE-GATING it to "ship advisory NOW" (the N≥30 gate only governs weight CALIBRATION, not the advisory build). So the advisory composite was un-gated but never moved for lack of a task = this sweep's headline miss. FIRST DELIVERABLE ✅ DONE 6/18: RECONCILIATION SPEC `docs/analysis/meta_rubric_reconciliation_329_2026-06-18.md` (ground-truth read of grade_holistic/_RUBRIC v3/assemble_judge_inputs + det. rubric + floor). KEY FINDINGS: the judge IS the composition layer but (a) theme STAGE/SCORE is computed + shown on the alert yet NOT passed to the judge (only the bool in_active_theme) — theme heat is invisible to the judge; (b) structure context (MA/resistance/base) isn't passed so clause-5 gap-alignment is unjudgeable; (c) no per-axis traceability. ARCH ✅ DECIDED 6/18 (operator): PATH A = enrich the ONE judge (pass the inputs it's blind to + add `axis_reads` per-axis traceability to the grade_ep schema; calibration rides the operator-labeled eval). Path B's separate logistic numeric composite stays the forward-gated registry cross-check (NOT a 2nd authority). So #330/#331/#328 build JUDGE AXIS INPUTS, not a separate composite. ⚠ NEW FINDING (call-site verify, ep_detector.py:2431): the live judge call does NOT thread `has_direct_source` OR `revenue_stage` (nor theme stage/score) → the prompt renders both as always-"no". Rubric v3 clause 1 LEANS on has_direct_source (=false→prefer-floor skepticism) → the judge applies freshness skepticism UNIVERSALLY + can't credit a fresh SEC-sourced 8-K = LIKELY LIVE GRADE-AFFECTING DEFECT (has_direct_source is computed — threaded for #317 today — just not passed). Judge is LOAD-BEARING (toggle ON 6/10) so the live payload edit is grade-affecting → build+measure on the EVAL ARM, flip via #335. Priority: has_direct_source > theme stage/score > structure. SSoT target when #330/#331 land = NEW docs/setups/meta_rubric.md + own SSoT per signal (catalyst_rubric.md SSoT confirms fundamentals-only + "add signals as separate inputs, each own SSoT"). STEP 1 ✅ SHIPPED 6/18 (fe6206a, live byte-identical): theme_stage/theme_score plumbed into assemble_judge_inputs + rendered on the in_active_theme line when present; eval-only `axis_reads` per-axis traceability (`_judge_tool(include_axis_reads)` deepcopy variant + grade_holistic flag + _normalize passthrough); 2 byte-identity tests lock the live prompt+tool. STEP 2 ✅ BUILT 6/18: scripts/eval_judge_enrich.py — `--size-only` NO-SPEND blast-radius pass (recompute has_direct_source DETERMINISTICALLY from stored grounded_text markers [SEC…/[Benzinga… vs [Web summary], segment out pre-W1 thin rows) + gated `--regrade` two-arm (BLIND vs ENRICHED) with AS-OF-date theme heat (mi_themes theme_date<=alert_date — NOT get_theme_membership which is lookahead) for operator labeling; build_judge_payload gained byte-neutral enrich kwargs; ep_grade_decision now logs has_direct_source (data-readiness for #337); recompute_has_direct_source pure helper + 6 tests, 910 suite green. SIZING RAN 6/18 (45d, no spend): 181/184 ASSESSABLE rows (98%) had a DIRECT source the live judge was shown 'no' for; 126 AT-RISK (direct + promote/material-lean). ⚠ RIGHT-SIZED (advisor 6/18): the 98% is the flag-wrongness rate, NOT the impact — the judge reads the corpus substance INCLUDING the dated marker ([SEC 8-K filed <date>…]), so it can establish source+freshness directly even with the meta-flag stuck at "no". The sizing data itself shows this: it graded HIGH/hold on nearly all 184 → largely ROBUST to the flag. EXPECT FEW has_direct_source flips in the re-grade. So this axis is likely LOW-IMPACT; if the batched re-grade confirms ~zero flips, that's the evidence to DROP has_direct_source from the #335 wire-in entirely (yield discipline working). The re-grade is still run (it measures theme-heat/structure/gap too) — has_direct_source just rides along, it doesn't justify the spend alone. NEXT (operator cost directive 6/18 — DON'T pay per-axis): the sizing pass cost $0 (no LLM). The `--regrade` (Opus spend) is DEFERRED and BATCHED into ONE consolidated re-grade at the pre-#335 checkpoint covering ALL pending payload enrichments at once (has_direct_source + theme-heat here + structure #330 + gap #331 + tape #299 + chart #267 as built) — re-judging the cohort ONCE, not once per axis. See #335. Live wire-in + axis_reads-on-alert ride #335. THEN ship the ADVISORY methodology-ordered composite (theme dominant ≥1.5–2× fundamentals per user_pradeep_catalyst_hierarchy; un-gated) by this ETA. Weight calibration stays forward-data-gated (registry review, now #-linked here); the load-bearing flip = #335. CHANGE_PROCESS + sign-off before any load-bearing flip. ▶ **GROUNDWORK RE-VERIFIED AT HEAD 6/24** (`docs/methodology/meta_rubric_groundwork_2026-06-24.md` — consolidates the 6/18 reconcile, current-at-HEAD, + frames the open within-Path-A composition decisions D1[theme-axis scoring mechanism: qualitative-read vs numeric-sub-score]→D7 for the operator's design pass). **KEY CLARIFICATION:** STEP-1 (6/18, fe6206a) was **FUNCTION-SIDE-ONLY** — `assemble_judge_inputs` ACCEPTS `theme_stage`/`theme_score` + renders them "when present," but the live CALL (`ep_detector.py:2829`) does NOT PASS them (verified 6/24: `theme_stage`/`theme_score` appear NOWHERE in ep_detector.py), so **the theme axis is still DARK to the judge** (only the `in_active_theme` bool reaches it). Same for `has_direct_source`/`revenue_stage`. So the call-side WIRE-IN (pass the axes the fn already accepts) = **#335** (grade-affecting, operator-gated) — NOT done by STEP-1; corrects any "theme axis is live" misread. NOT a launch blocker (current grade = the theme-heat-blind baseline + fail-open-to-floor; the theme axis enriches it POST-launch via #335). Reconcile/anchor (this task) = current-at-HEAD; composition + wire-in = operator's design pass + #335. ▶ **THEME-AXIS DIRECTION DECIDED 6/24 (operator + 2 advisor passes):** (b) engine = SoT for theme DETECTION (existence/membership/heat), NOT re-judged. Relevance ("is theme the DRIVER of this move?") attributed **STRUCTURALLY** — catalyst named-entities ∩ theme-cohort tickers/keywords (deterministic, traceable, auditable) — **NOT** the LLM-catalyst-axis (that + a judge-audit = the SAME LLM = CIRCULAR; measures self-consistency not correctness). Audited by an **INDEPENDENT mechanism** (judge's qualitative read OR co-movement/tape — must differ from the attributor). **ASYMMETRIC** — boost theme-as-driver, NEVER penalize themeless (doubly-justified: themeless may be an undiscovered-theme blind-spot, not a real absence). Pradeep ~1.5-2× rides on theme-as-driving-catalyst, decays to context otherwise. **FLIP (#335) GATE = grade-CORRECTNESS on a themeless-winner-INCLUSIVE labeled cohort, FULL STOP** — disagreement-rate is a review-load regulator + health gauge ONLY, NEVER a flip-gate. RATE vs COUNT: review-load = the COUNT (operator feels); health/gating = the RATE over sufficient N (small-N caveat — don't read low early counts as green). **STEP-0 (next, shadow):** wire theme-heat to the judge + log the structural attribution + 1 independent check beside it + start the themeless-winner-inclusive labeled cohort → let DATA size the weighting/monitor (scaffold, not cathedral). **CARVED OUT of v1 (someday-maybe):** the discovery-feeder (undiscovered-theme → narrative-lane #167) — confirmation-bias loop (judge→discovery→engine→boost→"validated"), adjudication brake fails at volume. ALL shadow + post-Phase-2 (#335); NOT today's clock. [ok:Track-2 Saturday deep-focus] [b2] [ok:composition anchor rides the #335 checkpoint (the axes accrue first)]
- #330 | 2026-07-18 | pending | TECHNICAL STRUCTURE as an EXPLICIT, CALIBRATED, TRACEABLE scored meta-rubric axis (#329 child; meta-rubric memory item 2; registry D2–D6 prose). NB precision (per ADR-0011): the JUDGE is NOT blind to structure — it already ingests gap_pct/pm_rvol/extension/vol_percentile/score_breakdown as RAW inputs and rubric clause 4 has it "modulate up or down" on structure QUALITATIVELY. AND the deterministic catalyst rubric (catalyst_rubric_runtime) scores ONLY fundamentals axes (revenue+accel/EPS/margin/beat/guidance) — NO structure axis. The gap = a scored structure axis with a calibrated weight + a LOGGED contribution (MAs 10/20/50/200 alignment, 52w-high distance, base shape, prior consolidation, round-number proximity), vs today's raw-signal-into-an-LLM. Data partly in mi_flag_candidates (base age, RS rank, COILED) + mi_daily_closes (MA distances). Gated behind #329 reconciliation spec. [ok:date-roll-0701] ▶ DESIGN DRAFTED 7/3 eve: **docs/decisions/0016-structure-axis-meta-rubric.md** (the 0015 pattern: boost-only, 3 components [Stage-2 200MA+52w · RMV base-tightness · extension state — ALL reusing existing primitives], STEP-0 = structure backfill onto the 452-row cohort THEN cross-tab, stacking cap deferred to #329). Saturday: sign with the 0015 table in the same sitting → backfill → shadow card. ▶ STEP-0 RUN 7/4 (docs/analysis/structure_axis_step0_2026-07-04.md): boost direction NOT contradicted at N≥30 — powered read (relaxed-history, N=386) modestly SUPPORTS: Stage2+tight 11.7%/64% vs no-Stage2 10.0%/56%; strict-252d variant coverage-starved (13-mo mi_daily_closes ceiling = a time-based watch item). → shadow build proceeds (queued behind #328/#335-eval slots). [b3] [ok:STEP-0 done; shadow build = Sunday card] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #331 | 2026-07-18 | pending | GAP-vs-STRUCTURE ALIGNMENT as an EXPLICIT scored meta-rubric axis (#329 child; meta-rubric memory item 3). Does the gap punch THROUGH resistance (institutional confirmation) or fade INTO a congestion zone? Same precision as #330: the judge sees raw gap_pct + structure and can reason about alignment qualitatively (ADR-0011 clause 4) — the gap is a CALIBRATED, traceable alignment score, distinct from raw gap magnitude (already in ep_score). Gated behind #329 spec + #330 structure primitives. [b3] [blocked:#329/#330 upstream still open - #330 shadow build queued post-sprint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #332 | 2026-07-25 | pending | METHODOLOGY-FIT / setup-class rubrics (#329 child; meta-rubric memory item 5) — different conviction rubrics for Pradeep small-cap-explosive vs Qullamaggie mature-leader-breakout vs episodic mid-cap-with-neglect. The long pole (needs the per-class composition designed); pulled in from 9/1 on the operator 6/18 accelerate directive, but kept behind #329/#330/#331 since it composes ON TOP of them.
- #333 | 2026-07-31 | pending | CATALYST DURABILITY forward axis (catalyst program; user_catalyst_depth 6/16 Pradeep tweet). The rubric ALREADY scores trailing acceleration (revenue YoY + decel/accel); the GAP is the FORWARD/PROJECTED leg — Pradeep durability = ≥2Q realized + ≥4Q PROJECTED high-revenue-growth (39%-class illustrative; the SHAPE is the point). Needs forward guidance/projection from STRUCTURED data → gated on the direct-sourcing backbone (#210/#211), not LLM prose. Distinct from #269 (revenue-over-EPS weighting of existing axes). [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #335 | 2026-07-18 | pending | PULLED FWD 6/28 (post-cutover deferral EXPIRED — launch was 6/22; eval runs this week, flip on operator sign-off — a REAL gate, not a parking date). ⚡ LOAD-BEARING FLIP of the meta-rubric composite into the LIVE (paper) EP grade — THE integration the operator actually wants ("improve EP trading", we're already trading EPs with the judge). The grade-affecting milestone: the #329 advisory composite becomes authoritative (theme #328 + catalyst-magnitude + catalyst_type, enriched by #330/#331 structure/gap as they land — incremental, flip the core first). EARLIEST = just post-6/22 by the established grade-affecting-wiring-post-cutover rule (#299/#320/#321 cluster — don't shift the trade set right before the live-money GO/NO-GO), then move fast. GATES (non-negotiable, fail-open-to-floor like ADR 0011): CHANGE_PROCESS + operator sign-off on the exact weighting + a DB toggle for instant revert + the BACKTEST = ⚡ ONE BATCHED RE-GRADE (operator cost directive 6/18: don't pay per-axis). Instead of a separate eval-spend run per axis, accumulate ALL pending payload enrichments (has_direct_source #329 + theme-heat #328/#329 + structure #330 + gap #331 + tape #299 + chart #267 as each is BUILT) and re-judge the cohort ONCE at this checkpoint via eval_judge_enrich --regrade — measures every axis's verdict-delta in a single paid run, operator labels the flips. (The $100+ Phase-B replay was 1307 rows; batching avoids re-paying that per-axis.) Clusters with the #320/#321/#299 post-launch grade-quality batch. NOTE: if the operator wants this PRE-6/22 instead, it overrides the trade-set-stability rule — flag for explicit sign-off (my rec: shadow pre-launch, flip immediately after). At the flip, the per-axis `axis_reads` decomposition surfaces on the alert + /why (the richer half of #336). [ok:flip decision = Saturday operator-together] ▶ $0-EVAL RUN 7/4 (docs/analysis/composite_vs_judge_eval_2026-07-04.md): gates OBJECTIVELY UNMET — N=22 thin funnel (38/390 alerts carry extractions); 64% disagreement ALL one-directional (composite-weak vs judge-HIGH; judge's keeps 11/12 positive 5d); the specced composite doesn't fully exist (theme axis = shadow as of TODAY, 0 accrued; type weights undefined). DISPOSITION: hold to the checkpoint — flip decision AFTER the axis shadows accrue + the ONE batched regrade eval (where #299's subset folds in, the batching principle). [b2] [ok:gates-unmet per the 7/4 eval; checkpoint ~7/18]
- #337 | 2026-08-05 | pending | MONTHLY judge-judgment review — aggregate "is the judge calling EPs right?" (operator 6/18, distinct from per-ticker /why #336). OPERATOR PRIORITY: "having all the data ready is key"; the review FORMAT is explicitly OPEN (operator unsure). DATA SUBSTRATE (advisor: the real question is the OUTCOME JOIN, not fields-on-a-row): judge verdict columns on mi_ep_alerts (judge_tier/direction/materiality/fire_axes/rubric_version + baseline_floor_tier) ⋈ mi_ep_scan_outcomes (fwd_5d/10d; ⚠ VERIFY the alert_date==scan_date join key lines up) ⋈ mi_live_trades (realized R) = the spine; ep_grade_decision now also logs has_direct_source (6/18). PROPOSED DESIGN (fold into the existing monthly_backward_check_sweep — reuse, NOT a new job; operator picks before build): (a) promote/demote/hold counts + floor-agreement rate; (b) judge-tier × forward-outcome cross-tab; (c) Unjustified-Demotion sweep — demotes that became winners (ADR 0011 named guard); (d) AKTS-class regression probes; (e) a SAMPLED set of promoted/flipped rows for OPERATOR labeling (go-live gate is operator labels, never self-score). ⭐ OPERATOR IDEA 6/18 (recorded): an LLM ADVISOR that reviews the judge's calls (like the dev-phase advisor here) — a meta-LLM reading judge decisions+outcomes to flag questionable/miscalibrated calls for operator attention; relates ensemble-divergence #301 (real-time 2nd-model verdict), questioner/investigator #212, precedent-retrieval #255 (memory project_cross_ticker_narrative_synthesis_gap holds the two-LLM-loop). Feasibility/usefulness TBD → evaluate as a candidate review MECHANISM (a richer variant of (e)). ✅ V1 BUILT 6/18 (operator: "propose one, draw from existing reviews, start + iterate"): scripts/monthly_judge_review.py — READ-ONLY standalone, SURFACES-not-prescribes (weekly-review lesson); direction-vs-floor counts + judge==floor agreement; by-direction & by-tier fwd-5d (win% LABELED drift-only per feedback_validate_metric_before_decision); ⚠️ Unjustified-Demotion sweep (demotes that ran ≥+5%/5d — ADR 0011 guard); operator-labeling sample pointer (never self-scores). Verified join scan_date==alert_date (eval_catalyst_materiality precedent). 5 tests, 915 suite green. REFINED + FOLDED 6/18 (operator "proceed"): logic moved to agents/market_intelligence/judge_review.py (importable; scripts/monthly_judge_review.py now a thin CLI re-export); ADDED realized P&L (fan-out-safe mi_live_trades subquery join) + has_direct_source footprint (reuses recompute_has_direct_source — surfaces the #329 defect's monthly trend until #335); WIRED into the monthly cron (quarterly_backward_check_sweep_job, independently-guarded so a review failure can't break the sweep) → auto-runs 1st-of-month 8am ET + Telegram. 12 tests, 916 suite green. VERIFY-LIVE = next month's 1st sweep fires the judge-review Telegram (can also run on-demand now). STILL OPEN (recorded, not built): the LLM-advisor review mechanism (operator idea — TBD feasibility) as a richer variant. ⭐ REVIEW→ACTION CHAIN (operator 6/18 "ensure effort yields results"): the review SURFACES (never prescribes — weekly-review lesson); each signal maps to a GATED action: (1) Unjustified-Demotion pattern → rubric amendment or axis fix (CHANGE_PROCESS+sign-off); (2) promote cohort NOT beating holds/floor → tighten promote criteria / find the bad-promote axis; (3) has_direct_source footprint DROPS after #335 → confirms the fix landed (verify-live); (4) direction/agreement drift across rubric_versions → regime/version-drift flag; (5) the operator-labeled sample = THE yield — it accrues the operator-labeled dataset that GATES every load-bearing axis flip (#335) + model-eval. YIELD DISCIPLINE: the review must drive ≥1 concrete gated decision per quarter or be trimmed (don't run effort that doesn't pay off). ⮕ ABSORBS #56/#113 (closed 6/20): the "is the rubric too generous / operator-vs-judge calibration" + EP-detection-calibration question those two tracked IS arm (e) here (operator-labeled sample) + the judge==floor agreement + by-tier outcome cross-tab — closed as SUBSUMED, not dropped. ⚠ INTEGRITY: those two closures (+ #60/#81 deleted in the 6/20 contentless-stub recovery) were AGENT-INITIATED under the filing-quality gate (only #134/#165 were operator-decided at the time) → surfaced for ratification + OPERATOR-RATIFIED 6/20 ("aligned"): closures STAND. Reopen any as a DISTINCT task only if it turns out to be detection-threshold work separate from the rubric — `git revert` restores the line + snapshot status. [ok:core-verified-live-0701-residual-weekend] ▶ FIRST LABELED SAMPLE BANKED 7/4 (docs/analysis/operator_labels_sitting_2026-07-04.md): Set-B 5/5 demotes RIGHT (incl. HQ +152% — attribution ≠ outcome held) + Set-A 3FP/1TP → the arm-(e) yield is real; the unjustified-demotion sweep read as HEALTHY not miscalibrated. [b3] [ok:first labeled sample BANKED 7/4 (5/5+3FP/1TP); remaining = the LLM-advisor-mechanism idea, post-sprint] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #338 | 2026-08-19 | pending | /simplify deferrals from the 6/18 judge-traceability review (#329/#336/#337) — 4 cleanup agents (reuse/simpl/efficiency/altitude). CHEAP fixes APPLIED 6/18: format_tier_transition reuse in judge_review (#253 single-copy contract); always-True tuple + hoisted-invariant-blind-payload in eval_judge_enrich; dropped dead _SQL cols (fwd_10d_pct/judge_rationale); closed-status FILTER on realized-P&L SUM (codebase convention); phantom-function docstring fix (corpus_provenance not classify_catalyst_sources); producer→consumer PINNING TEST (recompute_has_direct_source vs build_grounded_text markers). DEFERRED (structural, file-not-build): (A) /why's inline judge-trace block re-renders the alert's fields → extract shared format_judge_trace(row) for both /why + send_ep_alert (two renderers drift; couples #336); (B) verdict→r mirror omits judge_direction (briefing._judge_direction reconstructs it) → unify "apply verdict onto r" field-for-field with the DB write (ep_detector hot path, pre-existing); (C) deepen the marker-coupling past the pin: hoist [SEC/[Benzinga/[Web-summary prefixes to shared constants owned by build_grounded_text + an audit-JOIN agreement guard (recompute vs ep_grade_decision-logged structured value on #329+ rows); (D) extract get_theme_heat_asof(ticker,date) db helper (eval_judge_enrich + ep_selectivity_cohort:151 both inline it); (E) grade_engine_authority "judge"/"floor"/"fallback" bare literals → constants (low-pri, matches skip_reasons vocab); (F) extract _judge_authoritative(ep) (predicate now in 3 briefing helpers); (G) _MATERIAL_LEAN hardcodes catalyst_materiality's material+ tiers → reuse the vocab; (H) alert "direct-source ✓" on the judge fire-line can read as "judge used it" while the judge is BLIND to it until #335 — resolves when #335 wires it in (or reword sooner). All low-urgency hygiene; cleared as deliberate: kwarg-threading, deepcopy gate, aggregate/format split. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #269 | 2026-07-22 | in_progress | rubric v3 verify (AKTS forward outcome = first live promote-cohort point) + promote-cap eval harness [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #210 | 2026-07-22 | pending | catalyst-sourcing backbone umbrella (direct primary sources) [ok:date-roll-0701] [b3] [ok:sprint absorbed by the axis builds; sourcing next week] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #211 | 2026-07-22 | in_progress | news-gap discovery loop (unknown-rate KPI) — needs new scheduler job [ok:date-roll-0701] [b3] [ok:same] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #212 | 2026-07-29 | in_progress | questioner/investigator dialogic loop — productionize at larger cohort [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #230 | 2026-07-29 | pending | #212-as-sourcing-QA detector → feed #211 (low-precision triage) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #233 | 2026-07-29 | in_progress | Perplexity repositioning — KEEP + integrate (operator-adopted 6/17, NOT cut): (a) feed its independent GRADE into the JUDGE as a LABELED second-opinion/disagreement signal — judge currently reads its [Web summary] text but not the Claude-vs-Perplexity grade disagreement (grade-path INPUT change → CHANGE_PROCESS + shadow eval, judge is load-bearing); (b) reposition Perplexity as a labeled RECENCY source only (its live-web edge), judge-adjudicated vs primary (#210), and RETIRE the mechanical floor confidence_multiplier boost (with #320). Was "demote Perplexity/Tavily to labeled candidate (Wave C)" [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #235 | 2026-07-25 | pending | gap-discovery loop #211 integration (Wave E) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #215 | 2026-07-31 | pending | surgical prompt de-bias / OPTX-residual [b2]. ▶ #214 blocker CLEARED 6/29 (theme name-breadth rule shipped 48db3c0 + cooldowns verified healthy). REMAINING gate = a CLEAN grade cohort (the de-bias needs a debiased baseline to measure the OPTX-residual against) — rides on the grade-quality cluster settling (#335/#320/#321, ~7/3+). Re-evaluate when a clean cohort has accrued. [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #258 | 2026-08-08 | in_progress | ensure_schema() consolidation (fold 33 ALTERs) — branch+staging-validated, post-#277 [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #239 | 2026-09-01 | pending | ⚠ KEEP-GATED (eval 6/28): part (a) trailing-baseline dup is real BUT the deferral gate is UNMET — both consumers (verify_monday_firstfire, _wave_a_grade_inflation_check) are DISPOSABLE scripts; extract at the 3rd PERMANENT consumer. Part (b) _emit_shadow_row envelope = over-abstraction (provenance is intra-grade, tape/perplexity post-grade — unifying hides the context diff). Revisit (a) at a permanent consumer; (b) won't-do. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #265 | 2026-08-12 | pending | judge-flip-day review residuals (SQL-text SSoT, shared feeds list, WATCH cancel window) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #207 | 2026-08-01 | pending | model-eval governance quarterly review (data-gated)
- #197 | 2026-07-20 | in_progress | cap+1 game_changer slot SHADOW — promotion-gated N≥30 [b1] [ok:date-roll-0701]
- #274 | 2026-08-01 | pending | 2-member theme immortality fix (dissolve-on-flagged-pair, CHANGE_PROCESS) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #276 | 2026-07-21 | in_progress | W2 entry-mechanics program. ✅ STUDY #2 (stop geometry) DONE 6/18 (doc docs/analysis/w2_entry_study2_2026-06-18.md): added an offline stop_model knob (orb_low/atr_floor/atr_cap/day_low, threaded into sizing AND exit so R is comparable; 9 tests; harness c6b0029, deployed). RESULT on the retained judge-HIGH cohort (n=44; mi_ep_alerts pruned to 2026-03-16+ #341): WIDENING the stop monotonically HURTS in the trustworthy direction — baseline orb_low +1.40R/+$20.9k → atr_floor 0.5/1.0/1.5 +0.66/+0.48/+0.27R → day_low +0.14R/+$4.4k (stopout 59%→7% but R-multiples+$ collapse with the wider denominator). atr_cap +0.04R is noise (9% coverage, SUSPECT tighter direction, top-5-carried). VERDICT = **no live stop change; ORB-low optimal**; kill/scale bands need no re-derivation (winner IS orb_low). Both W2 levers now tested (study #1 OR-window+skip-wide-open dead/not-shippable; study #2 stop no-change) → RECOMMEND PAUSE the "tune the bracket" thread (regime-conditioned, NOT "edge proven absent": evidence is n=44 over ONE recent ~7-week window overlapping the tape the live stop already trades — regime-locality + near-circularity; re-open on a regime shift / kill-scale band review). Operator ack at launch checkpoint. Exit/management tune is the separate #306 (v1.1-W3, 7/01). [b1] [ok:#418 - operator ACK folds into the #425 declaration walk]

## Operational safety / hardening
- #439 | 2026-07-09 | pending | Operational safety / hardening - **coverage-drift: exclude integration-test COIDs from D2-HIGH alerts (EOD 7/6 noise finding)**. #184a correctly flagged 14 D2_untracked_order_high today (dedup capped to 3 Telegrams) - but they were leftover PAPER orders from scripts/_coverage_invariant_paper_exercise.py (ticker F, COID apollo_paper_integration_test_*, signal_type='integration_test'), NOT a real mirror gap; they self-resolved by 13:00. FIX: (a) coverage_drift treats an `apollo_paper_integration_test` / `_HARNESS_COID_PREFIX` COID as INFO (known test cruft), never a HIGH Telegram; (b) the paper-exercise scripts leave orders resting on cleanup-miss - audit their cancel-all cleanup step. Detector working AS DESIGNED (surfaced real untracked apollo orders); this is noise-reduction, not a bug. DoD: a test-COID order is INFO not HIGH; exercise scripts leave zero resting orders.
- #437 | 2026-07-09 | pending | Operational safety / hardening - **MECHANICAL closure of the 7/6 boot-crash class (advisor headline): extend infra/staging_restore_check.sh to run initialize_schema() / _seed_strategies_registry() against the ephemeral restored DB**. Today's #424 CheckViolationError + the preflight crash surfaced only at PROD boot because card tests mock the pool; the restore-check already spins up an ephemeral pgvector container + restores the dump - add a step that runs the app's schema-init + seed against it and fails the nightly check on ANY exception. This would have caught the constraint miss LAST NIGHT, mechanically, against the real schema - turning feedback_check_constraint_new_enum_value from prose (memory) into a GATE (the 'only gates hold' principle). DoD: staging_restore_check runs schema-init on the ephemeral DB, a deliberately-broken constraint fails the check, green on a clean schema.
- #435 | 2026-07-10 | pending | Operational safety / hardening - **/simplify 7/6 infra follow-ups (deploy-critical, deferred from the pass to a focused session w/ redeploy+validation)**: (a) service_watchdog.sh - the transition/dedup/recover state machine is now typed 3x (service loop + disk-warn + disk-critical) - extract ONE shared bash helper (the drift-irony: this file's own header says ops_lib.sh was extracted because helpers 'drifted within a day'); (b) docker-compose.prod.yml - the identical logging block is copy-pasted 6x -> a YAML anchor (x-logging: &default-logging); (c) replace Dockerfile.market 'COPY PLAN.md' with a compose bind-mount (/home/apollo/apollo_the_wise/PLAN.md:/app/PLAN.md:ro, like /app/logs) so the #426 blocking-count reflects the LIVE file (COPY snapshot goes stale on PLAN-only sessions that don't rebuild). DoD: bash -n + yaml-parse + docker compose config green, one deploy validates.
- #436 | 2026-07-11 | pending | Operational safety / hardening - **phantom pending_confirmation: prevention + self-heal (/simplify 7/6 altitude; the reaper is cleanup, not prevention)**: (a) ROOT CAUSE - why do magna53 entries stick in pending_confirmation with null entry_order_id and never get reaped (the ABSI/FCEL/SNX/ACAD class, 10-12d latent)? Diagnose + fix at the source (the entry-confirmation path). (b) SELF-HEAL - fold the reaper's broker-confirmed-absence check into the existing 15-min order_status_reconcile job (it already does broker-vs-DB comparison) so future phantoms self-clear on cadence instead of needing a human to notice cap starvation + manually run a script. Trade-state -> #151 discipline + operator-gated. Supersedes the inline #287 prose note.
- #434 | 2026-07-09 | pending | Operational safety / hardening - **Compact MEMORY.md index (38KB -> <17KB)**: it exceeds the 24.4KB read limit so some memories silently DON'T load into session context (standing issue all session). Trim each index row to a one-line hook (<150 chars); the full detail already lives in each topic file, so aggressive index trimming is safe. Merge/drop stale entries. DoD: MEMORY.md < 17KB, one line per entry, no memory content lost (topic files unchanged).
- #433 | 2026-07-08 | pending | Operational safety / hardening - FALSE naked-stop alert: retract the alarm when the retry succeeds (TRUST-CRITICAL; found live 7/6 WULF). The OTO entry bracket auto-attaches a stop leg; the 9:35 stop-refresh job then tries to place ANOTHER stop -> Alpaca APIError 40310000 insufficient-qty (heldfororders:40) -> fires the STOP ORDER FAILED / Position has NO stop protection + unprotected/remediation-4:05 alerts. The 09:35:04 retry SUCCEEDS (new stop placed, OTO leg canceled) but NO superseding stop-confirmed Telegram is sent -> operator reasonably believes the position is naked when it is fully protected. FIX (Telegram-only, no order logic): stop_update_retry_succeeded/stop_updated emit a resolved - stop LIVE @ price (order id) message that visually supersedes the alarm; suppress/downgrade the NO-protection alert while a retry is pending. Card + review. DoD: a simulated first-fail-then-retry-success emits exactly one net-reassuring sequence. >> DEPLOYED+VERIFIED-LIVE-DEPLOYED 7/6 (901b531, market-agent+execution green, running image carries the code). Advisor+Sonnet BOTH caught the gating side-filter bug pre-deploy (fixed: sell-side + qty coverage mirroring #128; WS-hot-path wait_for(8s)+raise_on_error fail-safe). WULF stop-out (-$32.80, stop 6640a645 FILLED @ 23.80 at 10:00 ET) VALIDATED the diagnosis - the stop was real + fired, the naked alarms were false. MESSAGING-behavior verify still pending a future real stop-update/cancel event (the deploy was ~11 ET, AFTER WULF's 09:35 alarms).
- #425 | 2026-07-21 | pending | Operational safety / hardening - **v1.0 DECLARATION WALK (#418 SS8) - the dated anti-idle trigger.** Operator walks v1-closeout-productization.md FL-1..8 against live evidence + signs; folds in the #276 ACK + #303 closeout. PULL EARLIER the moment #426's countdown shows all clocks green (earliest ~7/17). If this date passes un-walked, check_plan FAILS every commit - the plan cannot sit idle by construction.
- #423 | 2026-07-09 | pending | Operational safety / hardening - **S-D (#418 sweep): docs/ops/secrets_rotation.md** - per-credential rotation runbook (Alpaca paper+live pairs, Telegram token, Anthropic/Polygon/FMP/Perplexity keys): exact env vars, dual-account boot requirements, verification step per rotation. Docs-only; DoD = operator can rotate any credential from the doc alone (FL-5).
- #407 | 2026-08-08 | pending | **/promotetheme + anticipate /simplify cleanup (6/29; ALL no-money, collect-only — apply with the next theme-area change, not a standalone deploy).** 4-agent /simplify, 3 converged: (1) EXTRACT `_upsert_promoted_theme(conn, today, name, tickers, score, desc, days_active) -> bool` — `promote_candidate_by_name` (theme_engine.py:1515-1529) re-implements the BYTE-IDENTICAL guarded upsert of `promote_shadow_themes` (1432-1443) incl. the source-scoped ON CONFLICT + the str(res).endswith(' 1') tag-parse; a schema/guard change must move in lockstep or drift silently (NOT yet drifted). Extract the WRITE only; leave the RS-fetch/days_active compute per-caller (nightly batches, manual is N=1 — forcing one shape is premature). (2) drop the redundant `_rs` dict — the SQL already filters `WHERE ticker = ANY(tickers)` so `_vals = [r['rs_composite'] for r in _rs_rows if not None]`. (3) the 3 inline alpaca alert sites = KEEP inline (the established collector.py _polygon_get/_fmp_get convention) — swap the in-code reason from "raise-vs-[] asymmetry" (surmountable) to "explicit money-path clarity + minimal LOC". (4) the vestigial 4th `entries_fired` tuple element ('anticipate' constant) folds into #404 on the confirm_signal_at removal. Tests clean (29 new pass). [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #384 | 2026-07-16 | in_progress | **Re-enable GATE for X posting (disabled 6/26 via _X_POSTING_ENABLED=False).** (1) ✅ DONE 7/2: EP-tweet RVOL routed through _resolve_ep_rvol (local import per the circular-import note) + tests/test_twitter_ep_render.py (pm 12.3x renders, raw 0.01x can't leak) — all 3 surfaces now on the one helper. REMAINING (2): confirm the X account has credits (402 seen 6/26) + resolve the stale-SNX-tweet-during-verify-render mystery, THEN the operator flips _X_POSTING_ENABLED — do at the sprint with the operator (external-account state + a flip = operator's). Posting is OFF meanwhile, so zero exposure. [ok:code-half-done-7/2, flip is operator-gated] [b3] [ok:remaining = credits check + stale-tweet w/ operator] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> +SECURITY FLAG 7/6: the X_API_KEY/X_API_SECRET were printed to a LOCAL card transcript (docker compose config resolved .env during #421 YAML validation). Local-only exposure (not prod/committed), but ROTATE the X creds when re-enabling posting — fold the rotation into this task's credits check.
- #381 | 2026-09-01 | in_progress | **ELIMINATE the swallow-a-failure anti-pattern CODEBASE-WIDE (operator 6/25, ABSOLUTE: never ever eat a failure, that pattern must be completely gone). P1.** FMP-403 (#380) + 200MA-null (#371) + theme-shadow-0-rows (#173) are all ONE anti-pattern: a broad/bare except that swallows a real failure + falls back SILENTLY. COMPLETELY GONE = MECHANICAL: (1) a deploy + pre-commit GATE banning except-pass / bare-except / broad-except-silent-return (mirror preflight_datetime_hygiene), reviewed escape #loud-ok:reason for genuine control-flow; (2) INVENTORY every instance, classified A=swallows-real-failure / B=fail-safe-needs-alert / C=genuine-control-flow; (3) phased REMEDIATION by criticality (money/trade/grade/data paths first) -- each except RE-RAISES or ALERTS, never silent. THE RULE: fallback != silent. DoD: gate live + blocking + critical-path instances remediated. Inventory+gate-design launched 6/25; see memory feedback_systematic_silent_failure_guard. ▶ OPERATOR DECISIONS 6/25 (both = the recommended option): (1) FAILURE ALERTS = PING (Telegram) for terminal/important failures on money/grade paths (entry/exit/grade), LOG-only for high-frequency self-healing transients. (2) GATE SCOPE = live app dirs only (agents/core/channels/shared), NOT throwaway scripts. These drive the remediation; #381 unblocked once the theme-fix live-verify closes. ▶ GATE SHIPPED 6/27 (69816b9): `scripts/preflight_no_silent_failures.py` (AST, bans broad+silent except) + RATCHET baseline (174 sites/45 files = tracked debt, blocks NEW) + self-test (11) + deploy `[5k/7]` + pre-commit Gate 4 — advisor-reviewed. REMAINING = remediate the 174 baseline DOWN (the ratchet tracks the trend): the non-trade A's (grade ~25 / data ~30 / advisory ~105) are THIS task's ongoing work (log+audit via failure_policy.py decorators or `# loud-ok` for genuine control-flow; theme_engine's 11 wait on its live-verify); the 14 money/trade A's = #382 (separate verified+sign-off pass). DoD now: baseline → 0 (or all-`loud-ok` justified). ▶ BURNDOWN LIVE 7/3 (sprint Track 7): baseline 174→125 in two batches (b1: 17 sites twitter/briefing/collector/system_review · b2: 18-entry honest ledger SYNC + 14 sites incl. llm_health's OWN alert guards + kill_scale + trading_calendar; all B-logged/loud-ok'd, 0 behavior changes, suite green each). ≤100 = ~1 more batch (Sat/Sun); then the money/trade A's = #382. >> #418 D-8 (operator 7/5): v1.0 bar = money-paths clean (DONE via #382) + no-new-ones ratchet; baseline-zero re-homes as a DATED PHASE-2 milestone at #419. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #378 | 2026-07-08 | pending | **Cost-observability Phase 2 — THE TOTAL + THE ALARM.** Daily rollup of FULL operating cost = metered LLM (api_usage, post-#377) + the flat subscriptions (Polygon/FMP/Perplexity/Hetzner/Alpaca data) from a config -> $/day + $/month; a /cost on-demand board; + a Telegram WARN when over the operator-set threshold (monthly budget cap AND/OR a relative 2x-trailing-median daily anomaly). Needs #377 + operator threshold + subscription numbers. DoD: /cost board renders the full total + a synthetic over-spend fires the alert. ▶ OPERATOR 6/25: threshold = BOTH (rec accepted) — a monthly BUDGET CAP on the VARIABLE spend (Claude+Perplexity; the flat is fixed so no alarm on it) starting ~150 USD/mo (above the ~110 upper estimate + buffer; the meter refines it) + a 2x-trailing-30d-median DAILY anomaly. FLAT COSTS (operator): Hetzner 15 + Massive 33 + Alpaca 100 = 148 USD/mo (OPEN Q: does Massive include/replace Polygon+FMP, or are those separate lines? system pulls both). VARIABLE est ~40-110 USD/mo (Claude 30-80 + Perplexity 10-30, credit-based; the #377 meter replaces this estimate). FULL OPERATION est ~190-260 USD/mo. [b2] [ok:Sunday card — cost rollup + alarm] >> #418: absorbs sweep item S-C - also add a month-to-date spend line to the weekly review (deterministic appendix); v1.0 BLOCKING (FL-6). [ok:#418 glide-path re-date - absorbed S-C scope at the walk]
- #379 | 2026-07-29 | pending | **Cost-observability Phase 3 — THE WATCHDOG (operator 6/25: "indicate cost changes + hidden issues").** Per-caller cost-anomaly detection (a caller's $/day spikes vs its trailing median -> surfaces hidden leaks like the truncation-retry loop that doubled theme-discovery calls) + cost-reduction surfacing (Opus-where-Sonnet-fits, oversized prompts, runaway retry loops). Builds on #377/#378. DoD: a synthetic cost-spike fires + >=1 real reduction opportunity surfaced. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #363 | 2026-08-05 | pending | **/simplify fast-follow: dedup the live_tracker exit-state PREAMBLE shared by `run_partial_exits` (3:45) + `update_open_positions_live` (4:45).** 3-agent /simplify consensus (reuse+altitude+simplification, 6/23): the two jobs now share a BYTE-FOR-BYTE identical state-load preamble — the `SELECT...WHERE status='filled' AND remaining_shares>0`, the running_closes/exits JSON coerce, the `hard_stop = trade.get('hard_stop') or stop_price` fallback, the `remaining_shares<=0 or today<=alert_date` skip guard, the `get_index_history`+no_data branch, and the 8-key `state` dict. RISK = this codebase's recurring maintenance-divergence class — change the preamble in one and the other silently drifts, and one feeds the PARTIAL, the other the TRAIL (money-adjacent). FIX: extract `_load_exit_state(trade, today) -> (state, daily_bars) | None` + call from both; the decision call (`apply_daily_exit_step` + the per-job `skip_*` kwargs) stays PER-JOB — the validated trigger flow UNtouched. Deferred from the 6/23 /simplify because it edits the EXISTING `update_open_positions_live` (partly outside the reviewed diff) + is deployed-live → warrants its own careful build+test+deploy, not a tail-of-session churn. DoD: shared loader + both jobs consume it + tests green + deployed. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #348 | 2026-08-05 | pending | consolidate the mi_safeguard_state get/set + conditional-bump upsert into a generic db helper (deferred from the 6/19 /simplify reuse review). There are now 4 inline copies of the `SELECT state FROM mi_safeguard_state WHERE safeguard/account_mode` read and 2 of the `last_transition_at = CASE WHEN state<>EXCLUDED.state THEN NOW()...` conditional-bump upsert: broker/drawdown_breaker.py:351-369, kill_scale_bands.set_last_band, plus the always-bump variants db.py get/set_holistic_judge_enabled + get_manual_halt_state/set_trading_halted. Extract `get_safeguard_state(safeguard, account_mode)` + `set_safeguard_state(..., *, bump_on_change: bool)` into db.py and migrate all copies. POST-LAUNCH (touches the #345 manual-halt panic button + the live-money drawdown breaker → behavior-preserving but needs careful per-call verification + the band module already imports db inside fn bodies so no new circular-import). Low priority; the inline copies are idiomatic/consistent today, so this is cleanup not a bug. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #290 | 2026-07-09 | in_progress | dead_zone_reevaluation predicate FIXED 6/18 (referenced `dc_fwd.high`; mi_daily_closes has `high_price` → errored silently → never went ready; corrected + VERIFIED executes, count=155). Review is now READY → remaining = run its action_when_ready (re-run analyze_late_detection_v3 dead-zone ORB-extension precision on Hetzner; confirm <20% precision or close). Registry hygiene done same area 6/18: ninem_day2_{mechanical_vs_methodology_alignment, retire_for_consolidation} → status=done (decided by #327/#326) [ok:date-roll-0630] [b1]
- #343 | 2026-07-31 | in_progress | Chart-vision axis SHADOW (post-#267 eval, operator 6/18, $30 approved). #267 result: text framing dead (B−A=0), chart 2/2 operator-correct but thin (C−B=2/36). Judge is LOAD-BEARING on paper → promoting into _build_judge_prompt would feed paper EP grades; too thin at N=2. ✅ BUILT (this session, not yet deployed): new `agents/market_intelligence/chart_axis.py` SINGLE-SOURCES the candidate notes (CHART_AXIS_NOTE / _TEXT_ONLY, cut-paste byte-identical from the eval) + modal_stable + render_prior_day_chart + grade_b_c (B text-only vs C +chart, fresh ×3 each); eval_chart_judge.py now IMPORTS them (arm A stays inline) so the two surfaces can't drift. `_chart_axis_shadow_job` (17:50 ET mon-fri) grades today's HIGH+MODERATE (cap 8/day), emits `chart_axis_shadow_graded`/`_delta`/`_norender` audit (delta detail carries floor/b_modal/c_modal/direction/BOTH rationales/png_path); SHADOW invariant = audit-only writes, own client+sem, fail-open, off the 9:45 path. `_chart_axis_shadow_weekly_digest_job` (Sun 19:30) sends each delta's chart inline via new briefing.send_telegram_photo + running N vs 7/31 backstop. db helpers: get_prior_daily_ohlcv / get_chart_axis_shadow_cohort / _processed_tickers (idempotency) / _delta_count. Tests: tests/test_chart_axis.py (12 green incl. note pin). DECISION GATE (registry chart_vision_axis_shadow_decision): N≥10 deltas OR 2026-07-31 → promote-load-bearing (CHANGE_PROCESS, ADR 0011) vs hold. ✅ DEPLOYED + VERIFIED-LIVE 6/18 (commit 1ed704f, deploy.sh both, DEPLOY OK all 7 preflight; apollo-market recreated on HEAD; job partition kept 46 = +2 [chart_axis_shadow + _weekly_digest], neither removed). Read-only cohort+render probe vs real 6/18 data: 2 HIGH candidates (UUUU/SWBI), BOTH charts rendered (111KB/102KB) — cohort SQL + prior-day render proven live; the grade-with-chart path was already live-proven by the 6/18 eval (same chart_axis fns). ✅ ADVISOR-HARDENED 6/18 (commit c804d30, redeployed both DEPLOY OK): (Gap1, BLOCKER) delta PNGs were on ephemeral /app disk → wiped every deploy → Sunday digest would lose the chart to label; FIXED = digest RE-RENDERS from the audit row's ticker+alert_date (render is deterministic), job saves NO files. (Gap2) confirmed MODERATE persists in mi_ep_alerts (21d: 56 HIGH / 17 MOD ≈ 4.9/day → matches $30 economics). Split into date-param _run_chart_axis_shadow core. INTEGRATION SMOKE on real 6/18 cohort (UUUU/SWBI): 2 graded markers, 0 deltas → full render→grade B/C×3→modal→audit-write path proven live, 0 pollution (graded uncounted; only _delta counts). FIRST REAL (counted) FIRE = Mon 6/22 17:50 ET (Fri 6/19 = Juneteenth holiday → empty no-op). This row closes when the registry gate decides promote/hold. [ok:date-roll-0630] [b2] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #412 | 2026-07-12 | pending | 7/2 comprehensive-review findings batch (docs/analysis/code_review_findings_2026-07-02.md): 3 fixed same-day (F1 /regime double-desc · F2 HTF-shadow per-break try · F3 dead dedup) deploy 7/3 + verify; 12 filed F4-F15 — F4 scheduler imports scripts/_judge_replay_common (MUST land with/before #261 reorg or the #343 shadow silently dies) · F9 spend-tracker 16 silent-swallow pastes → one WARN wrapper · F10 DONE 7/5 (shared AsyncClient, loop-safe, F18 contract byte-identical; staging exercise = CLOSE-deploy verify) · F13 /themes markdown-400 no-fallback · F14 order_manager _*_out relay vars (trade-state, #151 discipline) · F5/F6/F7/F11/F12/F15 dedup/refactor per doc. MONEY-PATH F16-F23 (7/2 PM, per doc): ⚠ F22 #361 breakeven_active lost-update regression (BW 5/14 class — monotonic-write fix, careful path, HIGHEST of the batch) · F16 dead ambiguity-guard in _ensure_stop_coverage (duplicate-stop hazard) · F18 exec-routes 500→ExecutionUnreachable misclassification (active) · F17 confirm-flow topology wedge = decisive #364 evidence (dormant) · F19 dual-remediator race · F20 advisory-lock pool starvation · F23 drawdown tier alert one-shot-lossy · F21 kill/scale TOCTOU. DoD = F4+F9+F13 shipped; trade-state items (F22/F16/F18/F14/F10) paper-exercised-then-shipped per #151 discipline or explicitly re-dated; doc updated per-finding. ▶ 7/3 SPRINT SWEEP: F4(S1)·F5/F6(S4)·F9(S2)·F11/F15(S8)·F12(S7)·F13(S3)·F16·F18·F21(S12)·F22 ALL BUILT+MERGED (+ F1-F3 deployed AM). RESIDUAL = F10 ONLY (execution AsyncClient reuse — careful-tier, staging exercise); F7 + F14 SHIPPED 7/3 eve. ALSO 7/3 eve: the evening diff itself got a 2-agent correctness+quality re-review (1 gather fix + ema cross-refs applied; 7 money-lenses clean). ▶ /simplify DONE 7/3 eve (4 angles + applier: 7 groups merged 5d9ca24 — CachedGrade, coil-reason return, transport re-target, claim-primitive + drawdown TOCTOU fixed, corpus gather, test dedups, trivia; F16-SIBLING safety fix 9921259). REMAINING residuals: F7 (theme dup write) · F10 (AsyncClient reuse) · F14 (relay vars) · ✅ M&A-debounce DECIDED 7/4: LEAVE AS-IS (operator — zero staleness on a protective filter; revisit only on Polygon quota pressure). [b2] [ok:residual F10 + the d2-review G1/G2 leftovers = Sunday] >> METRICS-ENCODE + ADV-TRUNCATION DONE 7/6 (2d63e2b): insert_system_review double-encode fixed (plain dict/list via the jsonb codec, #177/#179 pattern) + 8 existing prod rows backfilled (all 11 now object/array) + Reviews-ready moved LLM->deterministic appendix (titles can't truncate). These were the last named residuals per the 7/5 findings; #412 = COMPLETE pending deploy-verify (F10 done 7/5, F1-F9/F11-F23 all shipped in the 7/3 sprint). [ok:metrics+ADV DONE/deployed/backfilled 7/6; ETA = the Sunday 7/12 weekly-review write-path verify-live]
- #414 | 2026-07-23 | pending | Entry-tuning for the stop-limit gap/no-trigger class (successor to the CLOSED alpaca_stop_trigger_reliability review, operator 7/4): widen the entry limit offset OR stop-MARKET entry with a chase cap — CHANGE_PROCESS (entry mechanics = methodology; N>=10 backtest per the review's own evidence base) + composes with #180's would-have-filled auto-classifier. DoD = a backtested proposal presented for operator sign-off (no unilateral ship). [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> +ROOT-CAUSE finding 7/6 (WULF live): entry OTO bracket auto-attaches a stop leg, then the SEPARATE 9:35 stop-refresh job places a 2nd stop -> self-conflict (insufficient-qty APIError, resolved by cancel-OTO-leg + keep-own-stop). Works but fragile + generates FALSE naked alarms (#433 = the UX half). Design half here: entry+stop should be ONE authority - decide OTO-leg-is-the-stop vs refresh-owns-it, one source; careful path. >> DESIGN folded into FABLE BLOCK #438 (operator 7/6): the single-stop-authority model (WULF root cause) + the gap-no-trigger backtest spec get designed there; card executes after sign-off.
- #416 | 2026-07-16 | pending | M&A-filter FP amendment (OPERATOR LABELS 7/4 — the Tier-2 sitting, Set A): of the 4 top material-miss suppressions, 3 labeled FALSE-POSITIVE (FRMI +25% & ONDS +23% via polygon_news; MMED +23% via keyword_in_text_1 — the crudest path; ONDS fired repeatedly across weeks) vs 1 TP (SUNE +216%, correctly suppressed). NEXT: pull the matched headlines for the 3 FPs → identify the match-mechanism failure (polygon_news false headline-match ×2 + raw-keyword ×1) → propose the amendment via CHANGE_PROCESS (filter changes = methodology; N-based, composes with #410's pin-guard + the mna_filter_direction_blindness 78%-unknown-path finding). DoD = amendment proposal with the FP mechanisms named, presented for operator sign-off. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #417 | 2026-07-08 | in_progress | CLAUDE.md structural trim (chronic 40k hard-ceiling pressure — hand-trimmed 4x this weekend; the standing sections have crept): one editing pass to get comfortably under the 36k warn threshold — compress the oldest standing sections into CHANGELOG/docs pointers WITHOUT losing load-bearing rules (THE LINE, gates, time-handling, deploy discipline stay verbatim). DoD = <36k and the recent-changes smoke test green. PASS-1 APPLIED 7/5 (39,975->37,783; zero fact loss, reviewed diff; smoke test green): dedup + doc-pointers where targets verified (ninem.md FULL parity, magna53_ep.md ORB window, CHANGELOG 5/13 outage). REMAINDER = the <36k gap needs DOC BACKFILL first (Dual-Account / EP-MAGNA53 / Entry Pipeline / Self-Audit L1-L3 / Stop-Leg / Theme Engine have no full-parity SSoT doc to point at - backfill targets listed in the 7/5 trim_accounting), then re-trim; do NOT guess-trim money-adjacent sections without backfill. [b1] [ok:pass-1 applied 7/5 - ETA advanced for the backfill+re-trim remainder]
- #415 | 2026-08-04 | pending | Allocator head-to-head telemetry (operator 7/4 disposition: HOLD shadow + add telemetry; docs/analysis/allocator_1b_comparison_2026-07-03.md §smallest-fix): log per-candidate (a) legacy-auto-entry-eligibility flag (real-contest filter), (b) next-ranked cascade candidate on downstream rejection, (c) slots_available at attempt-time cadence. Audit-only additions to unified_allocation_decided; re-review (registry earliest 8/4) when genuine contested days accrue. DoD = the three fields visible in the event JSON on a live decision day. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #340 | 2026-08-01 | pending | DQ delta-check (the more-correct long-term design, deferred from #339; operator chose self-heal for the rare-step case): alert on a >X% night-over-night DROP vs the trailing median rather than vs a frozen baseline — catches sudden real drops AND auto-adapts to intended step-changes without the deadlock. Scope: applies to both ingest + rs_engine + sector checks; pick X; keep a one-time transition-window alert so an intended step-down is NOTED once, not silent. [b2] [ok:no-project-push-today] ▶ 2ND INSTANCE 7/2: the #286 liquidity floor (6/15) stepped the scored universe 3.9k→2.5k and nightly_data_pull sat empty_result EVERY market day for 2+ wks (dead-red signal, nobody noticed) — frozen-floor recalibrated 3500→2200 same-day (scheduler.py), but this class recurs until the delta-check ships. [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #287 | 2026-07-07 | pending | partial-exit trade-state cleanup (FPS #183 stuck flag + 8-trade exits double-encoding) — operator-gated [b1] [ok:date-roll-0701] >> +FINDING 7/6 (surfaced by #184a coverage-drift day 1!): 4 STALE `pending_confirmation` phantom rows (ABSI 6/24, FCEL 6/24, SNX 6/25, ACAD 6/26; live mode, 0 shares, NO entry_order_id, never filled) are counted by get_open_position_count -> consuming 4 of 5 MAX_CONCURRENT_LIVE_POSITIONS slots (WULF took the last today; a 2nd live HIGH would be BLOCKED by phantoms). Reap in THIS operator-gated session: committed reaper for pending_confirmation w/ null entry_order_id aged >1 trading day (NOT docker-exec). No money at risk (no fills); it's a cap-accounting + hygiene leak. >> PHANTOM SUB-ITEM DONE 7/6 (VERIFIED-LIVE): reaper scripts/reap_stale_pending_confirmation.py (broker-confirmed, dry-run reviewed by operator, run in apollo-execution which holds live creds) reaped the 4 phantoms (ABSI/FCEL/SNX/ACAD 232/233/234/236 -> cancelled/phantom_reap_287); live cap freed 4 slots. Advisor+Sonnet reviewed. REMAINING #287 scope: the ORIGINAL FPS stuck-flag + 8-trade double-encoded exits (separate careful session). NOTE: the phantom-CREATION root cause (magna53 entries stuck in pending_confirmation w/ null entry_order_id, never reaped) needs a preventive fix too - the reaper is the cleanup, not the prevention.
- #280 | 2026-07-08 | pending | staging own paper account (before any market-hours staging use) [b1] [ok:date-roll-0701]
- #281 | 2026-08-15 | pending | staging copy-not-derive hardening (restore fork + compose/env drift) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #176 | 2026-08-15 | pending | SoT/roster drift tooling — create-time hook + scheduled ping (residual) [b2] [ok:date-roll-0701] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #216 | 2026-08-12 | pending | jsonb codec consolidation (root-fix of #179) — POST-launch by design (protects window) — POST-launch condition MET 6/28, eligible. [b2] [ok:dep-sweep-0630] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Apollo Trades dashboard (portfolio-app2)

- #194 | 2026-07-28 | pending | daily auto-export of BOTH snapshots (trades + themes) — needs deploy key [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #185 | 2026-07-28 | pending | corrupt-stop exclusion-count display fix [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Apollo Themes (portfolio-app2)

- #315 | 2026-07-31 | pending | RS-theme dashboard R-items (memory rs-theme-dash-backlog): R1 mobile/public-URL (Tailscale) · R2 bump chart · R3 canonicalization · R4 forward returns [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Fable planning reserve (operator 7/4 — Fable plans in max detail, Opus then executes purely)
- #438 | 2026-07-07 | pending | Fable planning reserve - **FABLE DESIGN BLOCK: exit/stop mechanics - the #1 expectancy lever (operator 7/6).** One execution-depth ADR (docs/decisions/00XX) covering BOTH halves of the entry/exit-mechanics leak (v1.1 diagnosis #2; management leak ~$10.5k > entry leak; MFE capture 18% vs the >50% bar): **(A) #306 STEP-2 winner-harvest sweep** - design the axis sweep + backtest-on-closed-cohort methodology for: giveback peak-lock (arm/floor params; the SMCI +12%->loss + PURR round-trip cases), trail-by-character (10 vs 20 vs 10->20MA handoff; the CRSR gave-back-85%-of-+52% case), partial size (33-50%). Output = RANKED parameterizations + overfit guardrails (eval_alpaca_skills discipline) on the N=10-winner+loser cohort; capture_pct is the KPI. **(B) #414 entry/stop authority + gap-no-trigger** - (i) the SINGLE-STOP-AUTHORITY model: OTO-leg-is-the-stop vs 9:35-refresh-owns-it (the WULF 7/6 false-alarm root cause - the OTO bracket + the refresh job BOTH place stops -> insufficient-qty conflict -> false naked alarms); decide + spec so entry+stop is ONE source. (ii) the stop-limit gap/no-trigger fix (SYRE -4.57R 7/6 class - widen limit offset vs stop-market+chase-cap) with the N>=10 backtest methodology. CONTRACT: pure-execution depth (schemas/params/gates/DoDs/tests, card-decomposed), zero open forks except operator-gated; THE LINE - any live change = trade-state -> CHANGE_PROCESS + #151 exercise + operator sign-off (this ADR MEASURES/DESIGNS, changes nothing live). Inputs ready: #306 STEP-0 doc + STEP-1 capture_pct KPI (live), the closed N=10 cohort, today's WULF/SYRE incident data (docs/analysis/183 + the #433/#414 commits). DoD = signed ADR + sign-off fork list, then Opus/Sonnet execute the sweep + the authority fix as cards. [ok:operator-filed 7/6, Fable-tomorrow]

_Directive: reserve remaining Fable capacity for these two planning deliverables (post-Sunday-CLOSE);
heavy execution goes to Sonnet cards or waits for Opus. Plans must be written to PURE-EXECUTION depth:
explicit specs, DoDs, acceptance criteria, sequencing — zero open design forks except explicitly
operator-gated ones._

- #418 | 2026-07-07 | in_progress | FABLE PLAN 1 — Apollo v1.0 close-out & productization plan: treat Apollo as a complete product SHIPPED TO CUSTOMER, done. Deliverable = docs/roadmap/v1-closeout-productization.md: (a) define the v1.0 finish line (what stands between today's state and “complete product, done”); (b) disposition the full open board into closeout-blocking vs post-1.0; (c) productization sweep — runbooks/DR/monitoring/onboarding/cost-governance/docs maturity; (d) every item carries acceptance criteria + sequence, written for pure-Opus execution. DoD = operator walks the plan and signs the finish line. >> PLAN WRITTEN 7/5 (docs/roadmap/v1-closeout-productization.md, d9ca057): FL-1..8 finish line + 104/104 disposition (21 BLOCKING / 78 PHASE-2 / 2 CLOSE / 3 DECISION) + sweep (S-A..S-D) + glide path (declare ~7/20-7/31). CLOSES at operator sign-off walk (SS8); dispositions apply to the board AT sign-off.

## Apollo v1.1 — fast-follow program (spec: docs/roadmap/apollo-v1.1-v2.0.md PART I)

_W1 selection-replay + kill/scale = #268 (done) + #275; W2 entry-mechanics = #276; W4 chart-vision = #267 (Launch project). Below = the waves not yet broken into tasks._
- #306 | 2026-07-16 | in_progress | v1.1-W3 exit & management: winner-harvest tune (partial size · trail-by-character · capture_pct KPI) + #91 time-stop refinement — trade-state, #151 harness discipline. STEP-0 DONE 7/4 eve (docs/analysis/w3_winner_harvest_step0_2026-07-04.md): N=10 partial-taken winners = 18% aggregate MFE capture ($5.9k of $32.8k peak; tier-one bar >50%) — 3 round-trippers (CRSR 15% / SMCI -27% closed-a-loss / PURR 0%); management leak > entry leak in $. SYRE -4.57R corroborates #414. STEP-1 DONE 7/5: capture_pct LIVE as weekly-review KPI (system_review deterministic appendix, SQL verified vs prod 10/$5,942/$32,775/18%; deploy pending). SCOPED 7/5 (doc §'The tune'): STEP-2 = axis-sweep backtest on the closed cohort (A giveback peak-lock [SMCI/PURR] · B 10/20MA trail-by-character [CRSR] · C partial size 33-50%) -> ranked parameterizations, no trade-state; STEP-3 = operator decision + CHANGE_PROCESS + #151 harness + paper-exercise before any live flip [b3] [ok:not-a-slip - STEP-1 shipped+verified 7/5, ETA advanced to the STEP-2 sweep] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5] >> STEP-2 design folded into FABLE BLOCK #438 (Tue 7/6-filed): the sweep spec + backtest methodology get designed there, then a card executes.
- #307 | 2026-07-24 | pending | v1.1-W5 experience-seed ritual: weekly operator-labeling cadence + precedent-retrieval shadow (with #219, #254, #255) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Apollo v2.0 — tier-one trader (horizons H1 Q3'26 · H2 Q4'26 · H3 2027 · spec: apollo-v1.1-v2.0.md PART II)

- #308 | 2026-08-15 | pending | P1 Experienced Judge (H1→H2): precedent corpus at scale + self-review→rubric-distillation loop + ensemble/uncertainty (#301 seed) + auto-growing probe library [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #309 | 2026-08-15 | pending | P2 Full Sight (H1→H2): multimodal (#267 daily→intraday→peer/SPY) + intraday narrative radar + tape features (#299) + negative-catalyst (#238) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #310 | 2026-08-22 | pending | P3 The Manager (H2): management judge mature (#300 shadow→load-bearing) + structure-stops via PIVOTS (docs/methodology/pivots-and-stock-character.md) + conviction sizing
- #311 | 2026-08-15 | pending | P4 Multi-Setup Book (H1→H2): per-setup judges + tightness→expansion graduation + 9M→flag pipeline + parabolic-short counter-regime book + regime-adaptive selection [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #312 | 2026-09-01 | pending | P5 Capital & Autonomy Ladder (H2→H3): unified_allocator live + formal shadow→paper→live ladder w/ auto-DEMOTION on breach + scaling/slippage telemetry
- #313 | 2026-08-15 | pending | P6 Institution-grade ops (H1→H2): #256 split (done) + replay-everything CI (#302) + cost governance (per-role LLM spend vs P&L) [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #314 | 2026-08-15 | pending | Trading-ideas detector book (P4 detail): TI1 parabolic-short · TI2 wick-fill (#283) · TI3 fishhook U&R · TI4 base+catalyst convergence · TI5 post-EP shape classifier · TI6 RMV (#54) — graduation gates in data_gated_reviews.yaml; detail in trading-ideas-backlog memory [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]

## Spec / detail docs (PLAN.md is the INDEX — every program above; detail + evidence live here, referenced)

_Launch DoD: docs/roadmap/launch-2026-06-22.md · v1.1/v2.0 map: docs/roadmap/apollo-v1.1-v2.0.md · ADRs: docs/decisions/*.md · evidence-gated reviews (weekly auto-surface): data_gated_reviews.yaml · setup SSoTs: docs/setups/*.md · methodology: docs/methodology/*.md · idea detail: trading-ideas-backlog memory. If a program isn't a task line above, it isn't planned — add it._

## Miscellaneous (no home project)
- #420 | 2026-07-09 | pending | External uptime pinger + kuma disposition (#256 W4 follow-up): the watchdog + restore-check + backup all run ON the prod host - whole-host death is silent. (a) Add a free external pinger (e.g. UptimeRobot) on the orchestrator /health via the nginx public URL or a dedicated ping endpoint - operator creates the account, I wire the endpoint; (b) operator decision: decommission the empty apollo-uptime (kuma) container from compose (never configured, 3mo idle) or keep for future dashboards. DoD: external check alive (test by stopping nginx briefly off-hours) + kuma decision executed.

- #121 | 2026-07-30 | in_progress | **Telegram formatting layer — consolidate + re-home DONE; remaining = legacy-Markdown→HTML surface migration.** ✅ consolidate-escapers was already done (a6c45a0 6/15 — there's ONE canonical `_md_escape`, no real dupes; the "≥3" premise was stale). ✅ 6/23: re-homed `_md_escape` + `_humanize_downgrade_reason` (+ its maps) + `_build_judge_delta_message` from scheduler.py → briefing.py (its natural home, owns send_telegram_message; PURE move, byte-identical, 1065 tests green; commit-not-deployed — behavior-preserving, rides the next deploy). REMAINING (#121-epic): migrate the remaining legacy-Markdown Telegram surfaces to the shared HTML layer (`shared/telegram_format`; d9c872d/4e483ee in progress). DoD: all operator-facing Telegram on the HTML layer. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
- #195 | 2026-07-08 | in_progress | SECURITY — rotate portfolio-app2 app_password (operator action) [b1] [ok:date-roll-0701]
- #334 | 2026-09-01 | pending | LOW-PRI theme-revive cooldown latch NOT YET WIRED (theme_engine.py:322, surfaced in 6/18 prose sweep): the one-way-latch arm is inert because no `revived_at` timestamp is persisted on mi_themes, so the only caller passes days_since_last_revive=None. Wire when revive goes live (persist revived_at, thread the delta). Filed not-dropped per the no-floating-prose directive; genuinely minor. [b1] [ok:#419 Phase-2 milestone re-date, operator-signed 7/5]
