# PLAN — the single source of truth (projects · tasks · ETAs)

**This file is THE plan.** Every task lives here, under a project, with an `ETA` date and a `status`.
The long-horizon plan (the 6/22 launch) lives here too — as dated tasks, not a separate doc. There is no
second plan surface: the calendar is phone reminders only, the harness #-task list is a session scratch
mirror, `CHANGELOG.md`/git hold history. If it's planned, it's a line here.

**Line format** (strict — parsed by `scripts/check_plan.py`):
`- #<id> | <YYYY-MM-DD ETA> | <status> | <title>`   ·  status ∈ pending · in_progress · blocked

**The two rituals operate ONLY on this file:**
- **OPEN** (every session): run `python scripts/check_plan.py --today` → it prints OVERDUE + due-today open
  tasks. That list IS the day's plan. State it before reacting to anything.
- **CLOSE** (every session): every open task's ETA must be ≥ today. Rebump any past-due one to a real
  future date (or close it). File every new item as a line here under a project. Then commit — the
  pre-commit gate (`check_plan.py`) REFUSES the commit if any open task lacks a project, an ETA, or has a
  past ETA, or if an open harness task is missing from this file. No gaps, mechanically.

_Last CLOSE: 2026-06-16._

---

## Launch — 6/22 GO/NO-GO (the long-horizon plan, as dated tasks · SSoT-spec: docs/roadmap/launch-2026-06-22.md)

> **DoD-7 TRIAGE SWEEP — 2026-06-18** (the "all projects deliberately dispositioned" gate). Every
> open task is now CLOSED / RUNWAY (≤6/22) / explicit POST-LAUNCH-with-reason. Spine GREEN: DoD-1
> measured+governed (#268+kill/scale signed), DoD-2 split live (#256), W1 replay (#268), W4
> chart-vision shadow (#343). Re-dispositioned drift: **#255** (W5 precedent-retrieval — was an
> accidental gate-rebump; now explicit post-launch, the W5 *labeling-ritual* bar is already met),
> **#226** (advisory + blocked → post-launch), **#291** (launch-critical reviews done; methodology
> batch → post-launch). Close-pending (done, awaiting snapshot refresh): **#284 #286 #270**.
> **COHORT DISPOSITION:** the ~46 tasks dated **2026-06-23** = the explicit POST-LAUNCH FAST-FOLLOW
> bucket (first working day after launch) — the v1.1 judge/catalyst tail, the catalyst-sourcing
> backbone (#210–#236), op-split residuals (#277–#281), SiP detection backlog, hygiene tail — NONE
> launch-blocking; the meta-rubric LOAD-BEARING flip is gated post-cutover by design (#335). The
> 6/19–6/22 runway = the launch-grade work (rehearsal #304, evidence pack #305, DoD-5 hygiene
> #237/#248/#260, DoD-6 dashboards #193/#185/#194 [#194 blocked on the operator deploy key]).
> Live-money GO-direction gates (#150/#151/#183/#184/#275) sit around the cutover, appropriately.

- #261 | 2026-06-26 | in_progress | scripts/ namespace reorg. SAFE SUBSET DONE 6/17 (5117ed1): 45 throwaway probes → scripts/probes/ (0 pipeline refs; 2 safety greps caught 19 false-throwaways = 4 imported shared modules + _270_* cluster + 4 code-referenced + YAML-cited review tools, all KEPT at root; operator's _reconcile left alone); 6 docs swept; 887 tests pass; no deploy needed. ⚠ REGRESSION FOUND + FIXED 6/20 (via #285's new guard test): the safe subset MIS-MOVED 5 ACTIVE monthly-sweep tools (_b53/_b54/_b77/_b88/_b92) into scripts/probes/ while their registration (quarterly_review.QUARTERLY_BACKWARD_CHECK_SCRIPTS) + their own docstrings still said scripts._bNN root → the 7/1 sweep would have 🔴'd all 5. Moved back to root (commit 2c518e4). ROOT CAUSE for the remaining reorg: the #261 safety greps checked YAML-cited tools but NOT Python-cited ones (a .py list) → when continuing the reorg, grep BOTH (incl quarterly_review.py's script list + any importlib/`python -m` refs); the new tests/test_quarterly_sweep_registration.py now backstops the sweep set. REMAINING (needs a focused block + per-file judgment): ops/evals split of the 94 real tools (touches deploy.sh/CI paths — careful sweep + verify-deploy) + delete-vs-keep the 45 untracked scratch scripts (incl non-_ WIP like analyze_/replay_/backfill_)
- #226 | 2026-06-30 | blocked | R3 Lane-2 narrative→mi_themes canonization (advisory; blocked_by #214 naming stability). DoD-7 TRIAGE 6/18: EXPLICIT POST-LAUNCH — it's BLOCKED on #214 AND advisory (no live grade authority), so it can't and needn't close pre-launch; moved off the 6/22 launch anchor to a real post-launch date.
- #266 | 2026-06-27 | in_progress | theme membership validation at BIRTH. SHIPPED 6/17 (operator-SIGNED): root-cause was discovery never running _validate_theme_membership (only :1754 Mon/Wed/Fri-rescore + :2371 assign-to-existing). Fix = _validate_new_themes_at_birth runs the SAME #213-tuned validator on discovered themes before _save_themes (after name-inheritance), emits theme_birth_validated; min-survivor guard keeps 2-member themes intact; 5 unit tests + CLAUDE.md SSoT same commit. VERIFY-LIVE 6/18 on the AUDIT ROWS (advisor catch): primary signal = theme_birth_validated rows after the next nightly theme run — the existing _theme_birth_validation_evidence.py probe CANNOT see birth strips (its JOIN gates on s.ticker = ANY(b.tickers) but birth-stripped tickers never enter mi_themes.tickers → false negative). Single-night 0 strips = no signal not failure. FOLLOW-ON (separate): identity-change half (re-validate at rename/description-revision); probe fix (drop the membership gate) before it enters the monthly sweep
- #299 | 2026-06-25 | blocked | P2 tape-features. BLOCKED ON FUNDING (operator decision). RIG DONE 6/17 — SLICE A+B shipped, eval rig built + machinery-verified (scripts/eval_tape_judge.py; binding finding: OR÷ATR available+ORB-tradeable for 79.1% of HIGHs; 11 tests; doc tape_judge_eval_299_2026-06-17.md), baked into prod image. FULL RUN DEFERRED 6/17 — requires FUNDING (~$170 full, or ~$50-90 directional first pass; ~3,420 Opus calls). TRADE-OFF of waiting: the judge keeps grading WITHOUT the tape axis (opening-range violence / premarket pace / liquidity) — an ENHANCEMENT we forgo, NOT a regression; no realized harm, the rig is turnkey whenever funded. NEW DATE 6/25 = decide-fund-and-run as the first step of post-launch tape-wiring, clustered with the #320/#321 post-launch grade-quality batch (grade-affecting WIRING is post-6/22 anyway). When funded: run → operator labels deltas (verify on theme/audit, ADR 0011 self-cert gate) → if majority-correct, CHANGE_PROCESS + sign-off → wire tape into live _judge_shadow
- #267 | 2026-06-27 | in_progress | (eval DONE + verified-live 6/18; the promote/hold DECISION now tracked by #343 + registry chart_vision_axis_shadow_decision at N≥10/7-31 — this row closes when that gate fires) judge chart-vision (W4). PART 1 BUILT+PUSHED 6/17 (47b2e14/72720e7): chart_render.py (mplfinance daily candles+10/20/50 MA+volume, point-in-time through PRIOR trading day — no alert-day candle; eyeballed), judge_transport multimodal image_png (byte-identical when None), grade_holistic threads image_png + chart_note candidate-axis (LIVE byte-identical), scripts/eval_chart_judge.py (TWO-SIDED with/without-chart delta surface, both-arms ×K noise floor, EMITS PNGs, cost print), build_clean_breakout_cohort.py (keep-side, loud-on-empty), deadcat_cohort.csv (66-row reject set), requirements mplfinance+matplotlib, 884 tests. Advisor-hardened (lookahead→prior-day, fidelity→mplfinance, ANCHORED via chart_note, Gap A emit-PNGs, Gap B two-sided). RUN 6/18 = operator-triggered (API spend) per runbook docs/analysis/chart_vision_eval_267_2026-06-18.md: deploy mplfinance → gen+CONFIRM keep cohort → BOTH --cohort flags → operator labels. Promotion of the axis to live _build_judge_prompt = CHANGE_PROCESS + sign-off (load-bearing, ADR 0011) — NOT done in part 1. ✅ EVAL RUN 6/18 (operator-approved, $6.72 actual / ~$7.5 total incl smoke — single-digit). Took fixing 3 latent bugs (eval never ran end-to-end): deadcat cohort STALE (2025 dates, mi_ep_alerts only 2026-03-16+ → 0 graded) → rebuilt 2026 dead-cats (#341); deadcat CSV not in image (#342); close_price→close SQL bug (fixed a47f067). Two-sided 18 reject + 18 keep, 3×. RESULT: **INSTRUCTION (B−A) = 0 deltas** (the text "consider technical structure" note buys NOTHING); **VISUAL (C−B) = 2 deltas, BOTH operator-labeled CORRECT 6/18** (CUE keep HIGH→MODERATE = correct catch of a wide/loose base the catalyst-only judge over-graded; ALOY keep MODERATE→HIGH = correct boost of a clean base); 0 reject-side deltas (baseline already low-graded the dead-cats). READ: the chart axis is PRECISE (2/2) + UNIQUELY ADDITIVE (text=0 → vision is the only lever) but LOW-FREQUENCY (2/36, N modest, ~19% judge noise). DECISION PENDING (operator, ADR 0011): promote vision via CHANGE_PROCESS (agent lean: ship-staged, text-only is proven dead) OR a larger confirmation run first. The 2/2-correct + text=0 = the first operator-validated evidence the chart axis adds real risk-management value (loose-base over-grade catch)
- #300 | 2026-06-27 | in_progress | P3 management-judge SHADOW (HOLD/PARTIAL_TAKE/TRAIL_TIGHTEN/FORCE_EXIT telemetry, zero authority). ADR 0014 (advisor-reviewed: R off entry−orb_low NOT trailed stop_price; no auto agree-bool=self-cert; snapshot-not-closes at 16:00; part1/part2 cut). PART 1 BUILT 6/17: mgmt_judge.py (assemble_mgmt_inputs/manage_holistic, bounded enum, fail-open, mirrors grade-judge) + mi_position_mgmt_decisions schema + insert_position_mgmt_decision writer + 11 tests (R-trap pinned). PART 2 ✅ BUILT 6/18: run_position_mgmt_judge() (mgmt_judge.py — DB-sourced open positions → live-snapshot price [snapshot_price helper: lastTrade.p→day.c→min.c, NEVER mi_daily_closes per the part-1 QURE stale-close artifact] → per-position manage_holistic → insert_position_mgmt_decision + digest line, per-position fail-open → position_mgmt_judge_null audit); thin _position_mgmt_judge_job (trading-day guard) registered 16:02 ET mon-fri (staggered 2min after the 16:00 entry-technique digest so two snapshot/LLM passes don't collide; still pre-17:00-pull), INTELLIGENCE_OWNED + job-classification guard green; format_mgmt_line/snapshot_price pure helpers + 4 tests, 921 suite green. EMPTY-SNAPSHOT GUARD added 6/18 (commit 1e0d874, advisor): get_snapshot_all→{} on error means every px None → judge grades blind → garbage batch; guard skips the whole pass + audits position_mgmt_judge_skipped (per-ticker None already skips+audits null). STILL DEFERRED (ADR 0014, conditional): time_stop_eligible (#91 peak-excursion) + 9M risk-basis (R safely None for 9M today since orb_low≠the prior-day-low basis — compute_position_metrics returns None). DEPLOYED + SMOKE-VERIFIED-LIVE 6/18 16:22 ET (deploy held during market hours per operator, run after the EOD jobs settled): deploy.sh both green (DEPLOY_EXIT=0, all 7 preflight gates, G6 skipped=closed), apollo-market recreated on HEAD (1e0d874), job retained on intelligence side (partition kept 44, position_mgmt_judge not in removed-27), no boot errors. SMOKE (run_position_mgmt_judge send=False, ~$1): 2 open positions graded — FPS HOLD +8.6R (R off entry−orb_low NOT the trailed stop@59.24-above-entry = the advisor trap, correct), QURE HOLD +0.7R @ live snapshot 46.29 (was a FALSE −8R/FORCE_EXIT in the part-1 stale-close smoke → snapshot_price fix CONFIRMED). Ground-truth: 2 rows in mi_position_mgmt_decisions (FPS 63.41 / QURE 46.29, opus-4-8, conf 0.70/0.66). REMAINING (the only open piece) = the first 16:02 ET CRON fire sends the SHADOW digest to Telegram (Fri 6/19; the job is registered+classified+trading-day-guarded, low risk) → then operator labeling of agree/disagree-with-mechanical begins accruing.
- #255 | 2026-07-08 | pending | judge precedent-retrieval v1 (kind=review) + periodic-review nudge. DoD-7 TRIAGE 6/18 (was an accidental 6/18→6/26 gate-rebump): EXPLICIT POST-LAUNCH decision. This is W5's "experience seed" build — but the W5 LAUNCH BAR ("labeling ritual running") is already MET + accruing live (chart-vision #343, mgmt-judge #300, judge-review #337 each emit operator-label streams). Precedent-retrieval has thin value until that corpus has VOLUME → correctly gated on corpus accrual, NOT launch-blocking. Clusters with the W5 ritual program #307/#219/#254 (7/08).
- #301 | 2026-06-25 | pending | P1 ensemble-divergence SHADOW (2nd-model verdict on HIGH-tier). DELIBERATELY DEFERRED to post-cutover (✅ operator-confirmed 6/19 "revisit after cutover"; named reason, not skipped): it adds a 2nd-model call in/near the grade path, and per the trade-set-stability discipline that gates #335 post-launch, new grade-path-adjacent code does NOT land pre-6/22 GO — even as shadow. Clusters with the post-launch grade-quality batch (#320/#321/#299/#335). Distinct from #233 (Perplexity grade AS a judge INPUT, CHANGE_PROCESS-gated) — #301 is a pure zero-authority divergence MONITOR (2nd model grades independently, divergence logged for operator visibility, no grade change); complementary, build as the shadow telemetry version. Ready to build post-cutover (design: integration at the HIGH-tier judge verdict; cheap 2nd model ~2-5 calls/day; telemetry table + digest section, consolidate-not-proliferate).
- #302 | 2026-06-27 | in_progress | P6 replay-regression v0. ✅ BUILT 6/19 (advisor-reshaped): produces the "P6 replay-regression weekly report" that data_gated_reviews.yaml::kill_scale_bands_quarterly_review input (b) + safeguards.md (lines 176/204) explicitly expect — surfaces the accruing LIVE R-distribution beside the #268b calibration envelope. agents/market_intelligence/replay_regression.py (compute_fingerprint per-trade stats + raw path counters; render_section; assess_regression reuses kill_scale_bands.assemble_band_inputs/current_losing_streak; run_replay_regression persists ONE replay_regression_snapshot audit row only when live trades exist) + CALIBRATION_ENVELOPE promoted to a single code constant in kill_scale_bands.py (was prose in 4 places) + folded as a SECTION of the Sunday weekly review (system_review.py, consolidate-not-proliferate) + on-demand scripts/replay_regression.py. ADVISOR RESHAPE 6/19 (the key correctness call): v0 SURFACES the comparison data, NO automated divergence verdict + NO Telegram — comparing a small accruing live cohort to the full-year n=399 envelope dimension-by-dimension is statistically invalid (path stats maxDD/streak can't be reached for months = false comfort; per-trade expectancy is noise at low N = false flags); restricted to sample-size-invariant per-trade stats with N shown; LIVE-only (paper's R-dist IS the IEX selection artifact); divergence framed "investigate/structural-or-decay" not "degrading". 8 tests, full suite 1011 green. ✅ DEPLOYED 6/19 (deploy.sh both, DEPLOY OK all preflight gates; G6 skipped=closed) + SMOKE-VERIFIED-LIVE: scripts/replay_regression.py ran in apollo-market against the live schema → "comparison begins at cutover (0 live closed trades)" + the calibration card from the new constant (confirms the module is in the running image + assemble_band_inputs reads live). REMAINING VERIFY-LIVE = 6/21 Sun weekly digest renders the section in Telegram; the snapshot rows + real numbers accrue as live closed trades land post-6/22. NOT a band/threshold change → no sign-off (surfaces-only). FOLLOW-ON (post-launch, gated on N): a valid divergence statistic once the live cohort supports one.
- #55  | 2026-08-01 | pending | revenue-stage threshold quarterly review (REVENUE_STAGE_MIN_USD). ✅ 6/19 RE-CHECK DONE (the scheduled 6/20 review, run a day early; read-only b50 backward-check in prod, N=26 in the $0 band ≥10): evidence does NOT support the $5M ratchet → KEEP REVENUE_STAGE_MIN_USD=0.01, NO code change. Reasoning (script's own decision matrix): the $0–5M clinical-stage band is positive-edge (+4.34% avg; tail winners ALOY +50.9%/QBTS +38.5%/ASPI +30.6%) so $5M over-blocks; the only clearly-bad band ($0: −7.71%/−12% median/27% win) is ALREADY excluded by the 0.01 pin. CORRECTED MECHANISM: the gate touches ONLY the earnings-day routine→strong BOOST (ep_detector.py:2020), not alert admission; None/no-yf-data fails SOFT to revenue-stage. Doc docs/analysis/revenue_stage_threshold_55_2026-06-19.md; audit-trail updated (data_gated_reviews b50_revenue_stage_verdict_audit_trail). ✅ OPERATOR RATIFIED 6/19 ("ok") — keep REVENUE_STAGE_MIN_USD=0.01, no code change (the recommendation matched current state). Next re-check rides the quarterly sweep (Aug 1). OPEN THREAD for then: the N/A/no-yf-data fail-soft (−17% cohort flows through boost-eligible) is a better lever than the $0–5M ratchet but needs a 2nd revenue source, not a threshold move.
- #303 | 2026-06-27 | in_progress | full-program advisor review + launch-readiness checklist + DR restore rehearsal. ✅ LAUNCH-READINESS VERIFICATION AUDIT DONE 6/19 (commit pending): traced every launch-critical safeguard claim in the #304 evidence pack to SOURCE (not prose) — panic button both gates highest-priority+fail-safe (live_tracker.py:105 + order_manager.py:186); day-1 equity guards all in _check_safeguards (LIVE_TRADING_ENABLED master kill :92, daily-loss :191, circuit-breaker :220, tiered drawdown :240); creds boot-block incl the 2026-05-13 outage class (phase=live under ENABLE_LIVE_MODE=false → BOOT BLOCKED, agent.py:7037+); kill/scale band job registered 16:13 ET INTELLIGENCE_OWNED + unclassified-job boot guard (scheduler.py:185,4434); #349 DR fix in restore.sh:383 (execution then both); Monday verify scripts all present. NO new gaps (the same audit class that caught #349) → folded a "code-verified 6/19" §1 note into the evidence pack (consolidate-not-proliferate). ✅ SSoT-CONSISTENCY SUB-AUDIT 6/19 (safeguards.md the operator had open): every threshold in safeguards.md MATCHES constants.py (MAX_CONCURRENT_LIVE_POSITIONS=5, CIRCUIT_BREAKER_CONSEC_LOSSES=10 [deprecated], DAILY_LOSS_LIMIT_PCT=0.02, DRAWDOWN_PEAK_WINDOW_DAYS=30, MIN_SNAPSHOT_HISTORY_DAYS=7) + the kill/scale bands + 16:13 band job + /pause gate all match code. ONE drift FOUND+FIXED: CLAUDE.md's always-loaded "Paper Trading" blurb was STALE (said "max 4 positions, 5-loss circuit breaker" — code is 5 / 10-deprecated); corrected + added a safeguards.md SSoT pointer so it can't re-drift. ✅ DR REHEARSAL DONE 6/20 (#349 closed — PASSED on the split topology). ✅ MONDAY GO RUNBOOK BUILT 6/20 (docs/roadmap/monday_go_runbook.md, cross-linked from evidence-pack §5): the go-live procedure dry-run-validated against HEAD — each step traced to code (creds boot-block agent.py:6871/7037; phase=live routing constants.py:153; position_size_multiplier floor+recompute entry_pipeline.py:357-371; preflight live path; /pause #345) + exact commands/SQL + confirm + rollback. KEY GOTCHA surfaced: the flip is NOT one switch — live_real_enabled/position_size_multiplier/max_concurrent_positions are SQL-only (no /strategy command; promote only advances phase + is eligibility-gated), and the live creds must be in .env BEFORE the deploy or the boot-block fires. REMAINING: (a) a true cross-codebase advisor review is still only partially covered (the runbook validated the GO path specifically; advisor() sees only the transcript) — fold the broad sweep into the Monday GO walkthrough with the operator. This task is effectively complete pending that walkthrough.
- #304 | 2026-06-27 | in_progress | launch rehearsal (staging→prod) + GO/NO-GO evidence pack + docs/CHANGELOG. ✅ EVIDENCE PACK ASSEMBLED + ADVISOR-CORRECTED 6/19 (accelerated from Sun): docs/roadmap/go-no-go-evidence-2026-06-22.md — single operator-facing GO/NO-GO surface: gate composite (P0s cleared · GATE2 ratified · GATE3 GO-supportive START-SMALL · gate5 signed · #345 panic ✅ · #275 bands ✅ · #349 DR rehearsal PASSED 6/20) + #268 Phase B economics + #268b signed criteria + residual-risks + mechanical GO steps + NO-GO path + Monday checklist. ⚖️ ADVISOR DONE-CHECK 6/19 caught + FIXED two correctness issues (commit 2869228): (1) BLOCKING — the pack PRE-CLOSED the operator's HARD #344 catalyst gate ("does not block the GO"); per the CLAUDE.md HARD-gate rule the agent must NOT classify it satisfied. Reframed the #344 row (🔴 HARD GATE — operator's open call), Bottom line, residual-risk #1, NO-GO triggers as the operator's OPEN question: does shadow-now + the Mon/Tue flip satisfy the gate, or was the live flip required pre-GO? (operator's 6/19 BFLY-real-EP label re-pointed the gate onto catalyst-correctness). (2) GATE-3 numbers now trace to PRIMARY output (sip_replay_r_cohort.py re-run 6/19), three cohort slices reconciled: raw paper-IEX N=21 (−5.64R), SIP REAL-only N=23 (−0.45R), synth-CANCELLED N=15 (+1.183R); +2.18R selection delta; crosses 0 at drop-top-4. SURFACES not prescribes (#305 is the operator's call). REMAINING (#349 rehearsal PASSED 6/20): docs/CHANGELOG launch-notes + operator review of the pack.
- #305 | 2026-06-24 | pending | 🚀 6/22 GO/NO-GO + EXECUTE the MAGNA53 real-money cutover. ▶ RUNBOOK = docs/roadmap/monday_go_runbook.md (turnkey, code-traced sequence). ▶ AUTO-ENTRY WIRED 6/20 (operator-signed, on main → rides the GO deploy): live+live_real_enabled=True now AUTO-ENTERS real money (entry_pipeline._should_auto_enter) — NOT a manual proposal; SSoT in safeguards.md/magna53_ep.md 6/20 change-logs. ▶ FINAL CONFIG (operator-reasoned 6/20): phase=live · live_real_enabled=true · position_size_multiplier=1.0 (FULL 1% risk/trade ~$50) · max_concurrent_positions=NULL (global 5, NO tight cap — broad participation for low-WR, #197) · START-SMALL = the $5,000 account itself. ▶ FUNDING GATE (Section F, HARD): account CREATED 6/21, ACH INITIATED Sat night 6/20 → settles ~Wed 6/24+ (gate on the F4 ACTUAL read, not the estimate). real fills need SETTLED live buying power ≥ start size — sizing reads live equity, so $0 → safe size_too_small no-op. ▶ TWO-PHASE (funding settles mid-week not Mon — runbook restructured 6/21): PHASE 1 Mon 6/22 = VALIDATE+STAGE at live_real_enabled=FALSE (#346 clean → no-filter audit → wire ALPACA_LIVE_*+ENABLE_LIVE_MODE → SQL phase=live+live_real_enabled=FALSE+mult=1.0+cap=NULL → deploy EXECUTION-FIRST then both → preflight mode=live PASS → /status live block renders [BP $0 EXPECTED]+not blocked → /pause+/resume; NO real money, validates the live-creds-boot/preflight class). PHASE 2 the day F4 funds SETTLE (~Wed 6/24+) = ARM REAL MONEY (confirm /status+dashboard BP≥$5k settled+not blocked → SQL live_real_enabled=TRUE, no redeploy → re-audit magna53 ONLY live+t → /pause+/resume → first auto-entry STOP-LEG watch). FINAL armed config = phase=live·live_real_enabled=true·mult=1.0·cap=NULL on the $5k account (the Phase-2 end state). ▶ ADVISOR 6/21 code-verified the runbook + caught 2 LAUNCH-BLOCKERS (both fixed in runbook): (1) P1.2 — prod .env has ONLY legacy ALPACA_API_KEY, but ENABLE_LIVE_MODE=true hard-requires CANONICAL ALPACA_PAPER_* and returns BEFORE the legacy remap → legacy-only BOOT-BLOCKS (agent.py:6883-6893); FIX = add canonical ALPACA_PAPER_* (copy legacy values) + ALPACA_LIVE_* before the deploy. (2) P2.1 — strategy registry is a process-wide invalidate-only cache (registry.py:50,90), so a raw SQL live_real_enabled=true flip is a SILENT no-op until a RESTART; FIX = deploy.sh execution-first then both after the Phase-2 flip. /pause confirmed cache-independent (fresh DB read db.py:2351) = the reliable instant kill. ▶ GATES: Monday verifies clean (#346 #344 shadow, #275 band job, scan wall-time) + operator GO call; NO-GO under signed rules = launch-complete, not a failure. ▶ VERIFY-LIVE = first MAGNA53 auto-entry (live submit + per_strategy_sizing_applied full-1% + STOP LEG attached + AUTO-ENTERED Telegram); no stop leg → /pause + investigate. ▶ LAUNCH DAY 6/22: ✅ PHASE 1 DONE+GREEN — .env verified (4 cred names), SQL flip (magna53 live / live_real_enabled=FALSE staged-paper, only one live), deploy GREEN (DEPLOY OK, preflight magna53 mode=live PASS, all 3 containers fresh). #346 gate CLEAN (46 enrich rows premarket, re-poll once/ticker, 1/46 changed=FRVO routine→strong = CORRECT lift on the real 6/22 NVIDIA/PNNL partnership; premarket-guard held, 0 in-window). ⚠ LAUNCH-BLOCKER #3 found+fixed LIVE: deploy order is EXECUTION-FIRST then both (both's preflight routes the creds gate→execution which both doesn't recreate → both-first FAILS on ALPACA_LIVE_API_KEY; matches #349 DR execution-first). Runbook P1.4 corrected. Also fixed _344_shadow_verify date-type crash (3a41dfe). REMAINING: operator /status (BP $0 expected) + /pause; then PHASE 2 mid-week on funds-settle. ⚠ PARTIALS (CORRECTED 6/22 — agent overstepped, RETRACTED): partial exits are paused ONLY as a TEMPORARY bug-mitigation (`_PARTIAL_EXIT_PAUSED=True`, bbe95a0) while #151 is fixed — this is **NOT a methodology change**. Partials are the TESTED sell discipline (16/23 committed in paper, months of validation); the #151 fix RESTORES them. **Timing works in our favor**: real money = Phase 2 ~Wed 6/24+ (funds-settle), the #151 fix is 6/23 → partials get fixed + proven BEFORE any real-money trade. The agent had unilaterally written "Phase 2 runs full-exits, no partials" — that was an UNAUTHORIZED sell-discipline change and is retracted. The launch posture IF partials somehow aren't reliable by funds-settle (gate Phase 2 vs run with the never-naked net) is the OPERATOR's decision, NOT the agent's to pre-decide. ▶ **ACH LANDED 6/22** (early — ~2d ahead of the ~Wed est): Section F funding gate now **MET**. **Operator decision 6/22: HOLD the Phase-2 arm to the PLANNED sequence** — NOT arming early despite available funds. The #151 partials fix (6/23) gets fixed + proven FIRST, THEN arm. So funds are no longer the gate — **#151 is.** (Before arming, still confirm `/status`+dashboard show SETTLED BP ≥ $5k — "landed" ≠ always immediately settled/withdrawable.)

## Live-money cutover — MAGNA53 (the GO-direction gates)

- #150 | 2026-06-25 | in_progress | Alpaca stop-trigger / share-reservation race — explicit sell-retry fix. CONFIRMED SHIPPED 6/18: `order_manager._is_share_reservation_lag()` + bounded retry (held_for_orders / "insufficient qty available") present + mature in HEAD since the FTRE 2026-05-09 incident. Remaining (verify-live) = observe a clean retry on the NEXT real share-reservation-lag event (event-driven, can't force) → then close. Live-money-promotion gate.
- #151 | 2026-06-23 | in_progress | **partial-exit hardening — the pending_replace-race FIX. LAUNCH-CRITICAL: gates Phase 2 live partials (this path would risk REAL money on the first live magna53 partial).** ✅ PAUSED LIVE 6/22 (operator "pause and fix"): commit bbe95a0, `deploy.sh execution`, `_PARTIAL_EXIT_PAUSED=True` guard at the top of `execute_partial_exit` (order_manager.py) — EVERY trigger (auto live_tracker + force `/partialnow`) no-ops BEFORE any stop touch → position keeps FULL stop+size (strictly safer); audits `partial_exit_paused`; test pins return-False+no-broker-touch. PAUSE VERIFY-LIVE = next market-open partial step → `partial_exit_paused` audit + no partial taken. ▶ ROOT CAUSE (order_manager.py:1457): the verify-shares-free poll is only 3s (12×0.25s); Alpaca leaves the OLD stop in `pending_replace` longer → `qty_available` stays 0 → sell ABORTS → the retry RE-REPLACES into the SAME race → LOOPS, never selling, leaving the reduced stop under-covering the position. Evidence: 7 failures (4 aborted + 3 sell_failed) + **2 rollback_failed** since 5/27 (IBM 5/27, RCAT 6/01, FPS 6/04, QURE 6/22) — the prior "N=7 clean cycles, not failures" framing was WRONG. ▶ FIX: (1) on retry, detect an ALREADY-reduced stop → SKIP the re-replace, go straight to the sell (breaks the loop); (2) replace the fixed 3s poll with cancel-the-stuck-old-stop OR an adaptive wait for `pending_replace` to clear; (3) make abort/rollback ALWAYS restore the full stop (the 2 rollback_failed = under-protected). MUST integration-test vs paper Alpaca (the 5/27 IBM mass-close cascade was a hasty fix to THIS path — feedback_no_trade_state_fix_without_integration) + VALIDATE on a live partial at MARKET OPEN before flipping `_PARTIAL_EXIT_PAUSED=False`. DoD: a live partial completes clean (replace→sell→matched stop) + breaker/rollback verified; the pause flips off in the SAME validated commit. SSoT docs/decisions/0009. ▶ CORRECTED 6/22 (operator's LIVE Alpaca paste = ground truth, supersedes the inferred cause above): QURE has BOTH stops LIVE — `200@42.50 pending_replace` (orig, Jun 18) + `134@42.50 new` (today's partial 16:45). So the position is OVER-covered (200+134 stops on 200 sh) = SAFE/protected, **NOT naked** (the "66 naked" inference was WRONG — the old stop didn't disappear, it's stuck-but-live). REAL cause: Alpaca leaves the old stop stuck in `pending_replace` **INDEFINITELY** (hours, not merely >3s) → BOTH stops reserve the shares → `qty_available=0` → the sell can NEVER get free shares → loop. ⇒ the loop-breaker (skip-replace) is INSUFFICIENT alone — the stuck old stop still reserves; the fix MUST **clear/cancel the stuck `pending_replace` stop** to free the partial qty. Open question the paper-Alpaca harness must answer FIRST: is a `pending_replace` order cancelable, or how else do you clear it? FPS(109)/SYRE(93) are clean single stops — only QURE is messed up, and it's over-covered = no naked risk anywhere ⇒ ZERO urgency; fix RIGHT in the harness session, do NOT ship a half-fix. ▶▶ APPROACH — OPERATOR-APPROVED 6/22 ("do whatever you need to fix it tmr, don't forget it") — DURABLE, supersedes the point-fix FIX list above: STOP point-fixing (5+ prior "fixes", each for ONE mode, each broke on the next Alpaca edge: IBM cancel-race / RCAT sub-penny / FPS verify / QURE pending_replace — the operator is RIGHTLY skeptical a 6th point-fix lasts). The 3-part durable scope: **(1) NEVER-NAKED invariant DECOUPLED from the partial succeeding** — `sync_positions` (the independent reconciler) GUARANTEES re-protection whenever stop-qty < position-qty, so ANY partial failure = "no profit trimmed", NEVER "naked"; safety must NOT depend on the multi-step replace→verify→sell→rollback dance being perfect. THIS is the safety-critical piece. **(2) paper-Alpaca FAILURE-MATRIX harness** — reproduce ALL known modes (cancel-race · sub-penny · stuck pending_replace · share-reservation lag · sell-fail-rollback) and run EVERY change against the WHOLE matrix (prior fixes "worked then broke" because each was tested vs only the latest mode). **(3) CONVERGE not loop** — after N attempts give up with a clear alert, never re-replace into the same race. HONEST TARGET = never-naked (guaranteed) + fails-gracefully-no-loops + validated-vs-full-matrix; explicitly NOT "perfect forever". **LAUNCH TIMING (CORRECTED 6/22 — agent OVERSTEPPED, retracted): partials are the TESTED sell discipline, NOT to be removed; the pause is temporary bug-mitigation, this fix RESTORES them.** The agent had unilaterally written "Phase 2 runs full-exits, NO partials" — an UNAUTHORIZED sell-discipline change, retracted. Timing favors us: real money = Phase 2 ~Wed 6/24+ (funds-settle) > the 6/23 fix → partials fixed + proven BEFORE any real-money trade. IF (only if) partials aren't reliable by funds-settle, the launch posture (gate Phase 2 vs run with the never-naked net) is the OPERATOR's call — never the agent's. DoD: matrix harness green across ALL modes + never-naked invariant verified (kill a partial mid-flight → reconciler re-protects, no naked window) + a clean live partial at market open; ONLY then flip `_PARTIAL_EXIT_PAUSED=False`. ▶ **LIVE EVIDENCE 6/23 (FPS #183, PAPER) — 2 NEW failure-matrix modes:** a partial STOP FILL desync'd the DB. The trailed stop @59.40 was HIT 6/22 16:45 ET, filled **81/109**, canceled with **28** left — but the DB never recorded the partial fill (`mi_live_trades.remaining_shares` stayed 109 vs broker 28). Today the Day-2 stop-refresh tried a 109-sh stop @59.40 on a 28-sh position with price **58.35 BELOW 59.40** → rejected ("stop must be < current price") → the old stop was already cancelled → **NAKED**; the 109-sh fallback hit 109>28 avail → failed. (Operator flattening the 28 manually on the paper dashboard — completes the already-fired stop.) **MODE 1 — partial-stop-fill not reconciled:** broker qty < DB qty after a partial stop trigger ⇒ the never-naked invariant MUST compute `target` from BROKER-truth qty (post the :2523 qty-sync), never the stale DB. **MODE 2 — stop-price-above-market (breach):** when `intended stop_price > current price`, a sell-stop is rejected ⇒ the invariant must DETECT the breach + ALERT + CONVERGE (not loop, not silently re-place above market); the breach-EXIT (market-sell the remainder) is the OPERATOR's/strategy's call, NOT the invariant's. Both added to the harness matrix. ▶ **BUILT + PARENT-REVIEWED 6/23 (committed, BEHIND THE PAUSE, NOT DEPLOYED):** `_ensure_stop_coverage` + a coverage loop in `sync_positions` (after orphan/adopt) + 11 unit tests (CASE1 no-op · CASE2 re-protect · idempotency · single-stop · mode-COID both branches · breach→alert+converge · over-cover no-op · multi-stop ambiguous · integration locks broker-truth qty 28-not-109) — full suite **1054 green**. Every line read: matches the advisor's net-of-pending single-idempotent-stop spec; broker-truth qty (rebuilt from intact `alpaca_positions`, `qty` not `qty_available`); replace-not-add; breach→one alert+converge (no retry/auto-exit/set_stop). ▶ **REMAINING UN-PAUSE GATE (before flipping `_PARTIAL_EXIT_PAUSED=False` — NOT a deploy blocker):** verify `execute_partial_exit` records the pending-exit (visible to `get_pending_exit_qty`) BEFORE it reduces the stop — else the reconciler firing in the sub-second Step1→Step2 window sees pending=0/stop=134/broker=200 → target=200 → over-protects → cascade when the 66 sell fills. Safe to DEPLOY now BECAUSE partials are paused (no in-flight partials to race); this gate is un-pause-only. ▶ **DEPLOY (operator-gated):** `deploy.sh execution` (execution-side, `feedback_deploy_both_excludes_execution`) + a paper-Alpaca integration exercise of the invariant first (discipline: no trade-state fix without a paper-broker exercise; the unit tests are mocked). ▶ **DEPLOYED-LIVE 6/23 (operator-authorized):** never-naked invariant LIVE in apollo-execution — `deploy.sh both`→`execution` (both DEPLOY OK + all 10 preflight gates; apollo-execution `Up` fresh + healthy; `_ensure_stop_coverage`/`_is_stop_above_market` import in the RUNNING container, verified). Git was a clean FF (prod 0-ahead/26-behind — an earlier "divergence" read was a FALSE alarm). It now runs every `sync_positions` cadence: re-protects under-covered positions; on the FPS price-through-stop class it breach-alerts + converges (the safety net the old reconciler lacked). **VALIDATED** by the G6 preflight paper-Alpaca exercise (placed a paper stop + replaced it UP 1→2 against real paper Alpaca, end-to-end, both deploys) + 1054 unit tests + line-by-line review. `_PARTIAL_EXIT_PAUSED` still `True` (partials NOT un-paused). ▶ **#151 OPEN QUESTION ANSWERED (G6 6/23): a `pending_replace` stop is NOT cancelable WHILE pending** ("order pending replacement" / "already in replaced state" — twice in G6); it becomes cancelable only after the replace settles terminal. ⇒ the converge-not-loop rationale: never try to cancel a stuck `pending_replace`; let it settle, the never-naked net re-protects. ▶ End-to-end paper harness `scripts/_coverage_invariant_paper_exercise.py` BUILT (local, uncommitted, NEVER RUN — review before running; for the un-pause validation). ▶ REMAINING un-pause gate unchanged: the mid-partial pending-vs-stop-reduce ordering race. ▶ **ORDERING RACE CONFIRMED REAL (investigation 6/23):** `execute_partial_exit` reduces the stop FIRST (replace ~:1238, set_stop ~:1259) and records the pending sell LAST (mi_live_orders insert ~:1543) → a **~6s window where `get_pending_exit_qty`=0 while the stop is reduced** → the now-LIVE never-naked reconciler sees under-cover → UPSIZES → then the sell fills → OVER-cover → **the IBM 5/27 cascade class** (the exact thing the advisor warned of). NO `asyncio.Lock` serializes the reconciler vs the partial (both run in apollo-execution). **PAUSED = SAFE NOW (no in-flight partials → window can't open); HARD blocker to UN-PAUSE.** FIX before un-pause: (1) record the pending-exit BEFORE the stop-reduce (an early marker `get_pending_exit_qty` counts) OR a serializing lock; (2) converge-hardening — DELETE the rollback block (~:1576–1598) + the abort stop-nulling (~:1620): never cancel a `pending_replace` (G6-confirmed un-cancelable), let the net re-protect; keep the 2-attempt replace cap + the breaker. **UN-PAUSE NOT SAFELY ACHIEVABLE 6/23** (the fix needs build+review+harness, AND the DoD's clean ORGANIC live partial at market-open is market-dependent — can't force one). ⇒ go-live-TODAY posture (full-exits + the live net, vs hold) = the OPERATOR's call. ▶ **CADENCE VERIFIED 6/23 (the linchpin):** the `_ensure_stop_coverage`/`sync_positions` re-protect runs ONLY at **4:05 PM (eod_cleanup) + 9:00 PM (evening_backstop) + boot** (scheduler.py:1205/1560); the 15-min `order_status_reconcile` (#123) is **audit-only — NO re-protect**. So the never-naked net is an **EOD backstop, NOT ~15-min** (earlier agent mis-statement to operator — corrected). ⇒ for un-pausing partials, the immediate-re-protect-on-abort MUST cover **ALL 3 abort paths** (sell-fail [DONE], old-stop-not-live ~:1419, verify-stop-dead ~:1487) — the slow net can't promptly backstop a mid-day abort. **UN-PAUSE GATES (all required, advisor-confirmed):** (a) the real-PG cross-connection test green vs a real DB (the SKIPPED topology-proof `tests/test_advisory_lock_real_pg.py`, `APOLLO_TEST_DSN`); (b) a paper partial on the DEPLOYED fix; (c) immediate-re-protect extended to all 3 abort paths; (d) verify a failed sell leaves NO lingering pending-exit row (else `target=broker−pending` under-covers); + operator sign-off + (ideally) an organic live partial. ⇒ **UN-PAUSE NOT ACHIEVABLE TODAY.** The EOD cadence also gives the full-exits posture a mid-day-stop-failure→naked-until-EOD residual (consider a more-frequent coverage check as a near-term strengthening). Fix BUILT+COMMITTED decd521 (PG advisory lock + converge + sell-fail re-protect), behind the pause. ▶ **VALIDATED END-TO-END 6/23 (operator-authorized paper run, `scripts/_partial_exit_paper_validation.py`):** PASSED both vs the REAL paper broker — TEST A: clean partial trimmed 6→4, single live stop covering 4 (no dup), sell filled; TEST B: FORCED sell-failure → post-lock re-protect restored stop 4→6 (broker truth), **ZERO naked, ZERO sold**, ONE honest "ABORTED…🛡 Coverage repaired" telegram (no "rollback"), **ZERO cancel_order** (never touches pending_replace). Cleanup verified (F flat, 0 residue). ⇒ **ALL UN-PAUSE GATES MET:** (a) cross-process advisory lock PROVEN on the real prod DB (2-conn test); (b) full partial flow validated on paper (this run); (c) immediate re-protect on ALL 3 abort paths — TEST B confirms the abort re-protects IMMEDIATELY in-process (not waiting for the EOD net); (d) no lingering pending row. + 1064 unit tests + line-by-line review + advisor. Code DEPLOYED-LIVE in apollo-execution (477ee39), still `_PARTIAL_EXIT_PAUSED=True`. **REMAINING = OPERATOR SIGN-OFF on the `_PARTIAL_EXIT_PAUSED=False` flip (THE LINE — never the agent's).** On sign-off: set the flag False in source + deploy both→execution → partials LIVE on PAPER (real-money partials ride Phase 2 arm, separate + operator's call). ▶ **✅ UN-PAUSED + VERIFIED-LIVE 6/23 (operator sign-off, commit 09c46b1):** `_PARTIAL_EXIT_PAUSED=False` confirmed in the RUNNING apollo-execution container (deploy both→execution clean, container Up fresh + healthy). Partials RE-ENABLED on the paper account; real-money partials ride the separate Phase 2 arm. The months-long pending_replace-race saga is closed at the root (NOT a 6th point-fix). **REMAINING VERIFY-LIVE:** watch the FIRST ORGANIC paper partial fire clean in production (a real Day-2+ position hitting its trim target) — the production confirmation; the forced-paper harness already proved the code path. Then #151 fully closes. ▶ **BREAKER VERIFIED CLOSED 6/23 (advisor's second-gate check):** the paper validation used `force=True` (bypasses the breaker); the production scheduler path is `force=False` (checks it). Read the LIVE value: `_consecutive_partial_exit_failures()=1 < threshold 3` → breaker CLOSED → the production path will NOT skip → organic partials WILL fire. "Live" confirmed on BOTH gates (pause=False + breaker closed). (Success-aware + 7d-windowed, so the pre-pause failures don't all count.)
- #183 | 2026-07-15 | in_progress | ORB classifier IEX/window mislabel
- #184 | 2026-06-24 | in_progress | broker-authoritative trade-state mirror (ADR 0008) — increments 2/3 (inc.1 write-side regression fence SHIPPED 6/6, audit_trade_state_demotions.py). (2) read-only DB↔broker coverage-drift detector (observe-only, no mutation) + ingest untracked broker orders (the a41e7c6a mirror gap) + fix /syncnow ("Unknown command" despite full wiring); (3) guarded auto-correction (broker-wins, gated by the #137 degraded/empty-read guard — never the first increment). DoD: coverage-drift alerts live + mirror complete + /syncnow works → cutover prerequisite. SSoT docs/decisions/0008-trade-state-broker-source-of-truth.md
- #225 | 2026-06-24 | pending | resolve the 3 genuine trade-state demotion residuals — the except-enclosed stop_order_id→NULL / status→closed demotions that audit_trade_state_demotions.py flagged WITHOUT a `# broker-confirmed:` tag (the non-#151 ones; was "L584/905/1575" but line numbers drift → find via `python scripts/audit_trade_state_demotions.py`). Make each broker-confirmed-and-tagged or fix it, THEN wire that audit as the blocking deploy gate. DoD: 0 untagged residuals + gate active in deploy.sh. SSoT ADR 0008 increment-1 build-status (#184 sibling).
- #275 | 2026-06-27 | in_progress | kill/scale band digest evaluation + band-transition alerts + override awareness. ✅ BUILD COMPLETE 6/19 (verify-live post-launch). FOUNDATION SHIPPED 6/19: agents/market_intelligence/kill_scale_bands.py — pure evaluator (trailing-20/-40 expectancy, current losing streak [r≤0 conv], cumulative R → SCALE/HOLD/REDUCE/KILL; drawdown-BLOCK equity guard binds day-1; 20-trade sample floor on strategy-health bands) reading the SIGNED #268b thresholds from safeguards.md. 12 boundary tests PROVE the bands stay silent through the healthy +0.95R-year envelope (worst t20 −1.03→REDUCE-not-KILL, maxDD −24R→not-KILL, p5 −0.63→HOLD-not-REDUCE, 15-streak HOLD/16-streak REDUCE, <20 floor). DATA-SOURCE LAYER SHIPPED 6/19 (read-only, VALIDATED against live schema): assemble_band_inputs (canonical realized R = total_pnl/risk_dollars over status=closed methodology trades [pnl_attribution IS NULL], chronological — lockstep w/ #291 GATE-3 sip_replay_r_cohort.py; persisted drawdown tier from mi_safeguard_state NOT a live Alpaca call per feedback-scheduler-aggregators-db-sourced; equity_above_start = earliest-vs-latest live snapshot) + get_active_override (kill_scale_override audit rows, cleared-aware) + format_band_line + scripts/evaluate_kill_scale_bands.py (on-demand surface, the SSoT interim). Live-DB dry-run: paper n=30 (cum −13.22R, t20 −0.46R, dd=REDUCE, 31 snaps) resolves; live n=0 → HOLD below floor (pre-launch baseline). WIRING SHIPPED 6/19: (a) Sunday weekly-digest band SECTION (system_review.band_digest_section — surfaces verdict+numbers+override, never prescribes); (b) band-TRANSITION Telegram alert via a DAILY 16:13 ET EOD job (kill_scale_band_eval, INTELLIGENCE_OWNED, right after the 16:12 equity snapshot when inputs are fresh — chose daily-EOD over an execution-side per-close hook: mirrors the drawdown-tier alert cadence + the band inputs only refresh daily at the snapshot + avoids the #256 exec seam; persisted last-band via mi_safeguard_state safeguard='kill_scale_band', deduped, ∅→HOLD baseline suppressed); (c) override read+write (record_override/clear_override → kill_scale_override / _cleared audit rows; scripts/set_kill_scale_override.py set|clear surface; reader annotates verdict). 13 tests green + full suite 1000 green; SSoT safeguards.md updated (impl + override mechanism). VERIFY-LIVE: 6/21 Sun weekly digest renders the band section + 6/22 the 16:13 job fires clean (machinery); the band itself = meaningful as live closed trades accrue post-launch (pre-launch HOLD below 20-trade floor). FOLLOW-UP (optional, not blocking): Telegram natural-language "override kill/scale: ..." capture (deferred — execute_task routing is frozen #260; script + dev-session write cover it). NOT a threshold change (bands signed #268b) → no new sign-off; SSoT alerting layer only.
- #345 | 2026-06-27 | in_progress | ✅ CORE DONE+VERIFIED-LIVE 6/19 (only go-live full-exercise remains, ETA→launch). 🔴 LAUNCH GATE (operator 6/19) — one-command GLOBAL real-money trading PAUSE. ✅ BUILT 6/19 (advisor-reviewed design + done-check): db.get_manual_halt_state/set_trading_halted (mi_safeguard_state, mirrors judge toggle, fail-SAFE→"unreadable"); _check_safeguards halt gate (live-path only, highest priority, BLOCK_TRADING_PAUSED / distinct INFRA_HALT_STATE_UNREADABLE on read-fail); /pause+/resume handler (read-back-and-report actual state) wired 3-place (handler+exact-match routing [no startswith, #260]+dispatch dict+2 BotCommands); skip_reasons + safeguards.md SSoT updated. 8 tests (tests/test_trading_pause.py), full suite 968 green. ADVISOR DONE-CHECK 6/19: (#1 COVERAGE) closed — added a 2nd halt gate at the submit_entry CHOKEPOINT (order_manager.py, fail-safe, pre-atomic-claim) so the panic button also covers the telegram_confirm proposal-confirm path that bypasses _check_safeguards; Day-1 re-entry is DISABLED (R3 env off) so latent-only — re-add a gate there IF R3 ever re-enabled. 10 tests, suite 970 green. ✅ DEPLOYED + VERIFIED-LIVE 6/19: deploy.sh both then execution (parity preflight [5f/7] caught a missing CommandHandler registration → fixed 460349e; both DEPLOY OK, G6 skipped=closed). apollo-execution recreated (Up ~1min) + apollo-market (Up 3min), both healthy. CROSS-SERVICE verify: set flag on apollo-market (command side) → apollo-execution gate read it → `(False, 'block:trading_paused', 0.0)`; /resume → off; clean end state (halt OFF). Fail-safe path incidentally exercised (one-shot cold-pool race → "unreadable" → correctly blocked; production persistent loop unaffected). CORE control DONE. (Real-money block fully exercisable only once ENABLE_LIVE_MODE=true at go-live.) ✅ (#2 RESOLVED — operator 6/19: ADD CANCEL) /pause now ALSO cancels resting unfilled real-money entry brackets: added account_mode filter to order_manager.cancel_unfilled_entries + exposed it cross-service (execution_client facade + _CROSS_FNS/_SLOW_COMMAND_FNS + execution_routes _EXEC_HANDLERS, route↔client parity test green) + /pause handler calls it (account_mode='live', reason='manual /pause') after halt-on, reports count cancelled; cancel-fail is surfaced (halt stays ON, 10:00 cleanup backstops). Reuses the proven 10:00/EOD cancel path (not new trade-state mechanics). Suite 970 green. ✅ DEPLOYED + VERIFIED-LIVE 6/19: both+execution DEPLOY OK, apollo-execution recreated (Up healthy); cross-service cancel facade call from apollo-market (intelligence) → http → apollo-execution returned count=0 (no resting orders) = wiring confirmed end-to-end. #345 CORE COMPLETE (pause + cancel, fail-safe, both deployed + verified). Full real-money exercise (actual block + actual resting-order cancel) at go-live when ENABLE_LIVE_MODE=true — inherent pre-cutover caveat, not #345 incompleteness. Today there is only per-strategy `/strategy disable <id>` (instant, DB+cache-invalidate, read per-entry) — no single switch to halt ALL new real-money entries at runtime (LIVE_TRADING_ENABLED=false is boot-read → needs a restart, too slow). BUILD: a runtime halt flag in the DB (reuse mi_safeguard_state, e.g. row ('manual_halt','live'); DB-sourced per-entry, NOT module state — feedback_scheduler_aggregators_db_sourced) + a gate in entry_pipeline.py real-money path (when halted AND account_mode=live + live_real_enabled → block, new skip_reasons.py `block:trading_paused`, audit orb_blocked + Telegram) + Telegram `/pause`+`/resume` (3-PLACE rule: agent.py handler + dispatch dict + telegram.py BotCommand; both report current state). Scope = blocks NEW real-money entries only; paper/shadow telemetry unaffected; does NOT flatten open positions (they keep resting broker stops — a separate flatten-all is NOT this). Tests (halt set→real-money blocked, clear→passes, paper unaffected) + integration (gate fires before submit). DEPLOY = market-agent + orchestrator + EXECUTION (the gate runs execution-side; commands run intelligence/orch — feedback_deploy_both_excludes_execution). Build alongside #344 on 6/19 (both entry-path, one tested 3-service deploy). HARD GATE: do NOT flip magna53 live_real_enabled=TRUE until this is live+verified.
- #316 | 2026-07-15 | pending | PDT / SEC-FINRA Rule 4210 — confirm ALPACA's rollout (not Fidelity's) + relax BLOCK_PDT_LOCKOUT via CHANGE_PROCESS (memory pdt_rule_4210_change_2026; revisit at cutover)

## Family A — consolidation plays post a runup (ADR 0013 · reset of #270)

- #270 | 2026-07-07 | in_progress | Phase 1 BUILT + VERIFIED 6/17 (new mi_anticipation_consolidation table + evaluate_consolidation + select_consolidation_keys carry-forward + 13 tests, all SHADOW). Live probe GREEN: COO IN on pure gate (1.153), proposer/confirmer agree, carry-forward absorbs the real 7/71 anchor drift. UN-PAUSED 6/17 (decision-4 fast-follow): consolidation_readiness job registered (17:35 ET mon-fri) + classified in INTELLIGENCE_OWNED_JOB_IDS + /anticipation board rewired (lifecycle→consolidation), 836 tests green. VERIFY-LIVE 6/18: confirm mi_anticipation_consolidation rows written after first 17:35 ET run + no dup non-aged rows per ticker (carry-forward) via mi_job_runs + confirm the newly-COILED digest renders the UNIFIED /anticipation row format (de.format_consolidation_row single-source, deployed 5599dbc — operator-flagged drift fix 6/17). ✅ VERIFY-LIVE DONE 6/18: (1) rows written — live /anticipation board render (via the app path) showed ~23 consolidation rows (6 coiled + 17 post-runup), all DISTINCT tickers; (2) no-dup carry-forward — the un-pause gate test_carry_forward_absorbs_real_615_to_616_drift is green on the real 6/15→6/16 drift snapshots (the authoritative check; mi_job_runs psql cross-check is operator-gated + redundant given the test + distinct-ticker render); (3) unified digest — the daily Family-A digest now renders via format_consolidation_row (plain-words consolidation this session). Phase 1 verified-live; Phase 2 = #289.
- #327 | 2026-07-07 | in_progress | **#270 Step 3 — consolidation entry-watch + shadow-settle + 9M-universe feed (critical path for #326).** ✅ PHASE A (the #326 DIRECTIONAL READ) DONE 6/18 — replay-first (live shadow can't reach decision-grade N by 7/7). Symmetric two-arm replay over the 121-row mi_9m_day2_candidates cohort (Day-2 ORB vs consolidation FIRST5-break, same SETTLE_RULE/forward window, full-universe expectancy, both-ways universe, MFE-free via anticipation.simulate). scripts/_327_replay.py + _327_pull_minute.py + _327_dump.py; doc docs/analysis/ninem_consolidation_vs_day2_replay_327_2026-06-18.md (advisor-hardened: within-name cut + split claims). VERDICT: **Claim A (SOLID) — Day-2 ORB earns NO robust edge** (filled median −0.24R, +3.2R total all 3 outliers, −1.4R on the 17 consolidation names) → carries the #326 RETIRE half; **Claim B (PROMISING, selection-inflated → Phase-B-gated) — consolidation better** (+2.00R median, 82% win, broad). Mechanism = stop tightness (first5-low vs wide prior-day-low). ✅ PHASE B — OFFLINE caveat-#1 correction DONE 6/18 (advisor-reconciled): extended _327_replay.py (consolidation_scan exposes every post-coil day w/ as-of base_high; Phase-A confirmed-close numbers byte-identical) + wider 266-day minute pull → re-timed Arm-2 to the FIRST intraday OR-break a live watcher takes. ⚠ MATERIAL FINDING (changes the #326 replacement narrative): **Claim B's +2.00R was an ENTRY-MODEL ARTIFACT, not a live edge** — inflated jointly by (1) daily-close selection (20/35 names break intraday on an EARLIER, weaker-close day; filled median +2.00R→−1.00R re-timed) AND (2) a 2%-sub-base OR tolerance (only 7/17 Phase-A "fills" had OR5-high actually ABOVE the base). De-rated, consolidation (+3.7R loose / +5.0R genuine, both outlier-carried) drops into the SAME break-even bucket as Day-2 ORB (+3.2R) → the QUANTITATIVE case for "consolidation is the strong replacement" is GONE. HARNESS LIMIT (explicit): every Arm-2 variant is an OR-break-NEAR-base proxy, NOT the live all-day volume-confirmed #94 watcher (blind to mid-session base breaks) — so it CANNOT settle whether the real watcher has an edge; no single de-rate number is faithful. **Claim A (retire Day-2 ORB) UNCHANGED/solid** (Arm-1 has no base-break gate). RESOLUTION = the live forward shadow with the REAL #94 detector is now the ONLY instrument that can answer the live-edge question → measure-before-wire JUSTIFIES (not moots) the live wiring. ✅ ENTRY-SIGNAL STUDY DONE 6/18 (operator "what signals the best entry"): scripts/_327_entry_signal.py — parameterized coil-readiness (rmv+range+vol gate × consecutive-day COUNT × stop), stop-matched curves not an argmax cell. FINDING: **PERSISTENCE (N consecutive tight days), NOT rmv DEPTH, times the entry** — N=3 lifts capture 32%→44% + median uncapped MFE +0.8R→+2.1R, STOP-MATCHED + robust across the rmv/range/vol knobs (Δ+9–12pp); VANISHES under over-tight level gates (range≤4% Δ0). Robust REGION: range≤5–7%, vol≤1.0×, rmv≤30–40, N=2–3. ✅ FORWARD SHADOW WIRED 6/18 (operator "wire it, we have potential edge, let's not wait"): new mi_consolidation_entry_shadow table + anticipation.entry_signal_at (the SINGLE-SOURCE signal — the offline sweep delegates is_tight to it, pinned by tests/test_consolidation_entry_signal.py, 8 tests) + entry-watch folded into _consolidation_readiness_job (17:35 ET, one shadow row per OPEN coil, open-dedup pins first-fire, 9m/family_a origin tag, point-in-time) + Telegram digest. Stop recorded coiled_low (validated default) + structural_low (alt) for later settlement. ALL SHADOW (zero execution). ✅ VERIFY-LIVE DONE 6/18 (deploy b8245ec, both): write-probe proved the insert path (insert✓ open-dedup✓ CHECK✓ self-cleaned) + the real job wrote **18 sane backfill rows** (all N=3, rmv≤28, range 1.8–6.0%, vol≤0.98×, both stops, HLIT correctly tagged [9m]) + a 2nd run held at 18 (live open-dedup confirmed). Container fresh on HEAD; job 17:35 ET mon-fri (un-paused) → tomorrow's run is the first scheduled fire. ✅ SETTLEMENT MACHINERY BUILT 6/18 (operator "build the machinery now, turnkey before data lands"): anticipation.entry_bet_outcome (the validated asymmetric bet — capture/stop/open + uncapped fwd_mfe_r under coiled_low, 12-bar horizon; the offline sweep's bet_outcome now delegates to it, pinned) + settle_entry_shadow (abstains <12 fwd bars, else outcome+fwd_mfe_r+realized_r; realized_r REUSES the tested settle_row +1R/+3R day-5 harvest) + db settle/fetch/summary helpers + _consolidation_entry_settle_job (17:40 ET mon-fri, registered + INTELLIGENCE_OWNED) + tests/test_consolidation_entry_settle.py (11 tests). SURVIVORSHIP FIX (advisor 6/18): settle on DEFINITIVE outcome (capture/stop final at any bar count; only 'open' waits the full window) so a stop/capture-then-HALT name isn't orphaned (would silently bias capture%); the residual halt-while-open is surfaced via get_consolidation_entry_shadow_summary.open_overdue_n (orphan canary, never silent). Turnkey: settles automatically once a row reaches the 12-bar window (~first settles ~2026-07-07). ✅ VERIFY-LIVE DONE 6/18 (deploy 575b412): all 4 first-run SQL paths clean on real Postgres — settle-probe (UPDATE write-back + double-settle guard + open-dedup-free), run-settle (get_settleable + clean 0-settle), summary SQL (READOUT "18 open / 0 OVERDUE / 0 settled", by-origin splits clean, medians null until ripe). 17:40 ET job registered/fresh. ✅ SURFACE CONSOLIDATION DONE 6/18 (operator "consolidate, don't add a new command every time" + plain-words readability; AskUserQuestion: Family-A-only + plain words): instead of adding /readout, FOLDED the whole lifecycle into ONE board + ONE digest (6 surfaces → 2). /anticipation now shows edge-header (the readout) → 🎯 entry fired → 🪙 coiling → 👁 post-runup → 📐 settled; the 3 separate daily pushes (🪙🎯📐) collapse into ONE unified Family-A digest by folding settlement INLINE into _consolidation_readiness_job (removed the standalone settle job + its INTELLIGENCE_OWNED id → _run_entry_shadow_settlement helper). format_consolidation_row rewritten to plain words (+70% · coiling 14d · very tight · tightening↓) + new format_entry_fired_row/format_entry_settled_row (single-source, all pinned). RULE going forward: new lifecycle stages = sections, never new commands. ✅ VERIFY-LIVE DONE 6/18 (deploy 12d8aff): /anticipation board rendered off the 18 live rows — edge header + 🎯 entry-fired (12, HLIT tagged 9M, stop %s) + 🪙 coiling (tightness word) + 👁 post-runup (runup + "Nd since peak", tightness word omitted — a live-render readability fix, the rmv-axis would contradict the not-coiled state) + 📐 settled (0). ⏭ REMAINING: nothing until data accrues → OBSERVE-ONLY, first settles ~7/7 answer the #326 live-edge → if confirmed, the shadow→PAPER graduation (register + wire entry → phase=paper, in PARALLEL with live magna53) = #353. Execution-side/split-adjacent
- #289 | 2026-06-26 | pending | folds into Phase 2 (shared universe+coil) — general-anticipation selection/ranking layer
- #353 | 2026-07-08 | pending | Consolidation entry → PAPER graduation (the shadow→paper rung of the promotion ladder; runs IN PARALLEL with magna53 LIVE — per-strategy phase routing, `_check_safeguards` per-mode isolated so a paper strategy can NEVER touch real $). ▶ GATE: graduate ONLY if #327's ~7/7 first-settlements show a real consolidation edge (the #326 live-edge question, on the REAL #94 watcher not the replay proxy); no edge → stay observe-only shadow, do NOT graduate. ▶ BUILD: (1) register consolidation in `mi_strategies` (family=family_a · signal_type · outcomes_table · promotion_model) — it is NOT a registered strategy today (absent from /status); (2) wire its entry into `entry_pipeline.submit_trade_entry` via a consolidation `spec_builder` (stop=coiled_low = the shadow's validated default; per-strategy sizing) — the entry+settle mechanics exist in shadow (`anticipation.entry_signal_at` + `entry_bet_outcome`/`settle_entry_shadow`), so this promotes the shadow RECORDER to a REAL paper-fill path, not a shadow row; (3) `phase=paper` → routes to the PAPER Alpaca account (real fills, fake $); magna53 stays untouched on live. ▶ VALIDATE: accrue closed paper trades → the promotion check (median R / win rate — the magna53 ladder) + digest readout. ▶ PROMOTE paper→live later = a SEPARATE Phase-2-style flip with its own gate (NOT this task). ▶ BONUS: a paper strategy beside live magna53 also yields a real-time IEX-vs-SIP selection-gap control (the GATE-3 thesis). DoD: consolidation registered + `phase=paper` + accruing REAL paper fills with a promotion-check readout (not a shadow row). Refs #327 (shadow edge), #289 (selection layer), #326 (retire-9m-for-consolidation).
- #354 | 2026-06-27 | pending | MERGE flag_continuation INTO the consolidation play (Family A) — it is NOT a separate strategy, it IS the **Confirm-flag entry (b)** (base-high breakout); operator-directed 6/22 ("flag_continuation = one and the same as the consolidation play"). Code-verified diff (Explore 6/22, file:line cited): same runup-anchored family + SHARED tightness primitives (`_compute_rmv`/`_compute_fresh_tightening` imported by anticipation.py — no dup). DIFFERENCES = flag_continuation carries stale/stricter params; reconcile to the consolidation play's SIGNED model: (1) UNDERCUT — flag_detector.py:513 INVALIDATES on undercut; consolidation ALLOWS it (ADR 0013 §1, operator's veto of undercut-as-gate) → re-gate INVALIDATED→WATCH_UR so undercut routes to entry (c) U&R, not rejection; (2) RUNUP 50%/60d → 15%/10d (Pradeep signed; 50% misses COO@15%); (3) ANCHOR pivot-walk → runup-peak-close; (4) TIGHTNESS ratio gates → volatility-relative RMV/ATR. KEEP: 0.4% tight-close as ranking-not-gate (operator veto), $20M/day universe, the shared primitives. COMBINE: flag's breakout logic becomes Confirm entry (b); retire flag_continuation as a standalone mi_strategies row → ONE strategy = consolidation play w/ 3 entries (Anticipate/Confirm/U&R). ⚠ LOAD-BEARING: flag_detector drives the live /flags board → detection-criterion change = CHANGE_PROCESS (docs/setups SSoT) + N≥10 backtest + operator sign-off (the undercut re-gate = ADR 0013 deferred Phase 4 WATCH_UR). GATED on the #327 ~7/7 edge read (don't merge before the play proves edge). DoD: one strategy, flag_continuation absorbed, undercut→U&R, gates reconciled to the signed model, /flags unbroken. Refs #327/#353/#326, ADR 0013. ▶ GROUNDED 6/22 (read ADR 0013 + CHANGE_PROCESS + flag_continuation.md exists) — CORRECTIONS: (1) this merge IS ADR 0013's already-DESIGNED Phases 2–4 (Family A = one universe + 3 entry modes; §1 "they share ONE universe + ONE coil detection, undercut ALLOWED") — NOT a one-deploy param-swap. (2) the "Confirm" entry is `_flag_break_scan_job` (#94 INTRADAY break) per §4 Phase 3; flag_continuation (5:25 PM EOD) is the REDUNDANT standalone whose role is SUBSUMED + retired, NOT "entry b". (3) the undercut→WATCH_UR fix is gated BY THE ADR (§4 Phase 4: DECOUPLED, SEQUENCE LAST, own sign-off + N≥10 backtest, SSoT flag_continuation.md same commit). (4) reconcile to anticipation.py's SIGNED §2 universe (`db.get_anticipation_universe`, 15%/10d/runup-peak/RMV-ATR) — do NOT edit flag_detector's 50%/60d in place. (5) NOT gated on 7/7 (that gates TRADE-graduation #353); the merge build is its own shadow track. EXECUTE as a focused build (Phase 2 shared universe+coil → Phase 3 the 3 entries → Phase 4 undercut [gated]), after-close deploys, NOT launch day. ▶ BUILT 6/22 (Phase 2-3 SHADOW · commit befb41e · NOT deployed — rides Phase 2): `anticipation.confirm_signal_at` (new PURE breakout detector on the §2 universe via daily bars — deliberately NOT the live #94 job/mi_flag_breaks, which would gate Confirm by the flag 50%/60d cohort = #270 phantom in miniature) + `mi_consolidation_entry_shadow.entry_mode` column (DEFAULT 'anticipate' back-tags existing rows) + the dedup FIX the planner caught (pre-#354 2-col open-index/ON-CONFLICT silently DROPPED the 2nd mode on the same coil → now 3-col ticker/anchor/entry_mode) + `by_mode` readout (ADR §1 never-blend) + `_consolidation_readiness_job` fires BOTH modes into the one lifecycle. 5 tests, full suite 1042 green. ISOLATION verified (anticipation.py pure; entry_pipeline + ep_detector do NOT import it). ✅ DEPLOYED 6/22 after-close (16:50 ET, bundled w/ the mgmt-format deploy · `deploy.sh market-agent` DEPLOY OK · all preflight gates green, G6 skipped=closed · apollo-market fresh · entry_mode migration + the open_mode 3-col index VERIFIED LIVE in pg_indexes; old 2-col index dropped · execution/orch untouched). ▶ VERIFY-LIVE = tonight's 17:35 ET consolidation job writes the first `entry_mode='confirm'` rows + the `by_mode` readout populates (check mi_consolidation_entry_shadow next session). REMAINING (still gated): Phase 4 undercut→WATCH_UR (sign-off + N≥10 backtest); then retire the standalone flag_continuation scan once the play subsumes its role. ▶▶ **SUPERSEDED 6/22 — THE OPERATOR SPLIT (the merge premise was WRONG; un-merge instead).** The flag→consolidation merge built a Confirm/breakout entry on a **structurally-broken** consolidation detector — anchor measured the runup INSIDE the base (STM-class excluded) + the entry fired on quiet DECLINES (BTU/UFO/DRUG) with NO holds/consolidation gate at all. Full diagnosis: `docs/analysis/consolidation_runup_defect_2026-06-22.md`. **Operator decision 6/22 — clean SPLIT + rename:** (1) **HTF (High Tight Flag)** = the former flag/flag_continuation → its OWN setup (big runup → tight base → enter on the BREAKOUT); operator details later (own #-task when they land). (2) **Anticipation** = the former "consolidation play" (#270/Family A) → SIMPLIFIED to STRICTLY the Pradeep tight-day ANTICIPATE entry (enter IN the coil, before the break); DROP the Confirm/breakout merge (#354 befb41e) + U&R. (3) **Sugar Babies** = a stock CONDITION (recurring 9M), NOT a setup → likely a confluence point on a setup, TBD/revisit. DEFINITION = the captured Pradeep blueprint (`operator_shared_notes.md`: 6/16 thread + 6/22 Gemini cross-ref): runup 15%/10d → HOLDS (Upper-Third ≤20–30% retrace + One-Strike ≤1×4% breakdown, **volatility-relative**) → series of tight days (volatility-relative, NR-day) → anticipate. REBUILD PROGRESS 6/22 (SHADOW, committed: `_consolidation_acceptance_test.py` + `_anticipation_holds_measure.py` + `_anticipation_shortlist.py` + fixtures): anchor refined to the recent runup-leg peak (3–20d base, not the global max); holds gate **data-grounded** on the 6/15 Pradeep cohort (declines 2–5 breakdowns/23–34% retrace vs tight names 0–1/≤11%); confirmed the gate must be volatility-relative (HYLN). ⏸ **PAUSED for operator labels** on the 303-name shortlist (`docs/analysis/anticipation_shortlist_to_label_2026-06-22.md`) → calibrate the volatility-relative thresholds → wire into the detector (shadow, sign-off + ADR-0013 provenance rule) → re-validate. The #354 merge text ABOVE is superseded by this split. ▶ **ROADMAP (all 3 setups + next steps + WHY): `docs/roadmap/family_a_setups_split_2026-06-22.md`** — #354 here = SETUP 1 (Anticipation). #356/#357 = the other two setups; #358 = the systemic provenance fix.
- #356 | 2026-06-27 | pending | **HTF (High Tight Flag) — SETUP 2 of the 6/22 split.** The former flag/flag_continuation detector becomes its OWN setup (big runup → tight base → enter on the confirmed BREAKOUT). `flag_detector.py` is LIVE + load-bearing (`/flags`, #94 intraday break, digest) + UNCHANGED this session (clean). ✅ **SPEC GROUNDED 6/22** (`operator_shared_notes.md` HTF blueprint — O'Neil/Minervini/Qullamaggie, operator-shared): flagpole **90–100%+/4–8wk** (`C≥1.9×C₄₀` or `High₄₀≥1.9×Low₄₀`); flag **≤10–25% pullback near the 40d high**, 3–5wk, vol dry-up + right-side tightening; trend above 10/20/50 MA; catalyst-backed; **entry = breakout above the flag high on ≥150% ADV** (buy-stop-limit 5–10¢ above the pivot = tightest right-side candle high); stop below the tightest-day low / 10–20 EMA, max-loss 5–8%; manage = scale 33–50% at 3–5d → breakeven → trail 10/20 EMA. NEXT (BUILD, can quick-start): **(0) KEEP the existing 5-stage state machine** (unqualified→WATCH→TIGHTENING→COILED→TRIGGERED — operator 6/22 "I like how it shows which stage a stock is at in the flag"); reconcile only the CRITERIA underneath, NOT the staging/display. (1) reconcile `flag_detector`'s UNSOURCED `50%/60d` → the sourced **90%/40d + ≤25% pullback** (CHANGE_PROCESS + N≥10 backtest + sign-off, load-bearing `/flags`); (2) add the breakout-entry + EMA-trail management; (3) rename flag→HTF. DoD: HTF detector matches the sourced spec + `/flags` unbroken + sign-off. Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`.
- #357 | 2026-06-27 | pending | **Persistent Sugar Babies — role decision (the 3rd STREAM of the split — a CONDITION, NOT a Family-A setup).** Recurring-9M stock CONDITION (≥3 9M EOD prints/180d, `mi_sugar_babies_cohort`); the surface overlays the flag/HTF stage on the 9M cohort. Operator 6/22: any setup can include a sugar-baby or not → likely an additional CONFLUENCE/SCORE input on a stock already in a setup, NOT a standalone play. NEXT: (1) operator decides the role (confluence/score input vs standalone watchlist); (2) re-frame the surface (cohort = Family-B/EP universe; stage overlay → HTF). DoD: role decided + surface re-framed (or kept observational). Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`.
- #358 | 2026-06-27 | pending | **Enforce ADR 0013's provenance rule — flag any live gate value with NO source citation** (advisor 6/22, the SYSTEMIC root). The detector silently DIVERGED from a signed methodology (`is_entry_tight`'s absolute range contradicted the operator's signed 6/16 volatility-relative conclusion for weeks, uncaught) because NOTHING checks code-against-captured-methodology. Build a check (test or deploy gate) that fails when a cohort-shaping gate constant lacks a source citation in `operator_shared_notes.md` / the ADR (ADR 0013's own provenance rule, currently UNENFORCED). Scope: Family-A/detection gates first. DoD: the check fails on an unsourced gate value + is wired into CI/pre-commit. Roadmap `docs/roadmap/family_a_setups_split_2026-06-22.md`.
- #297 | 2026-06-23 | pending | FAMILY B EP rework — inherits the gap-anchored replay()/evaluate_candidate + mi_anticipation_lifecycle + ARCHIVE/clean its phantom rows (decoupled from #270 Phase 1). 9M-ENTRY retirement (Day-2 ORB → consolidation) is the dated cut-over decision #326 (9/15)
- #283 | 2026-06-23 | pending | wick_fill promotion eval (shadow→live), gated behind Family A

## Stocks in Play — detection / setups / entries / 9M / flags

- #326 | 2026-06-27 | in_progress | **DECISION SIGNED 2026-06-18 (operator): RETIRE 9M Day-2 ORB → shadow** (pulled in from 7/7 on the #327 Claim-A read; option A/deprecate in ninem.md). CHANGE_PROCESS done (ninem.md change-log, REVERSAL of 5/31 documented). (1) ✅ FLIP EXECUTED 2026-06-18 (`/strategy 9m_day2 demote` → DB-confirmed phase=shadow, enabled=t). REMAINING (= verified-live): (2) verify next 9:31 ET `_9m_day2_orb_job` resolves shadow / no paper submit (tomorrow); (3) #327 Phase B consolidation shadow stood up as the replacement (so the cohort isn't dark). Original task text below. — 9M Day-2 ORB entry → RETIRE for consolidation entries (CUT-OVER DECISION, operator vision). MOVE FAST (operator 6/17: weeks not months; all SHADOW/PAPER = no real $ at risk in the swap). CRITICAL-PATH BUILD = #327 (the consolidation entry-watch + shadow-settle + 9M-universe feed → Day-2 ORB & consolidation run PARALLEL on the same names; reuses #270 recorder + #94 flag-break + #270 harvest, all LIVE/BUILT — integration not from-scratch). DECIDE ~7/7, PULL EARLIER on a clear DIRECTIONAL read (operator judgment, NOT a rigid N) → retire 9m_day2 to shadow (sign-off + CHANGE_PROCESS — that gate stays). COUPLING (only real-$ caution): BLOCKS the 9M Day-2 → live-money promotion (Q3); acceleration is shadow-only. Registry ninem_day2_retire_for_consolidation. **6/18 DIRECTIONAL READ IN (#327 Phase A): supports RETIRE (Claim A — Day-2 ORB no robust edge, solid) + consolidation as replacement (Claim B — promising, Phase-B-shadow-gated). 2 OPERATOR DECISIONS surfaced: (1) the GO/timing on retire; (2) SCOPE — consolidation fires on only ~⅓ of 9M names, so retiring Day-2 narrows the entered universe to ⅓; decide whether the other ⅔ (no consolidation setup) keep an entry path or go unentered. See the doc's "Read for #326".**
- #168 | 2026-06-23 | in_progress | shadow-detector quality/actionability filter for LIVE-ping graduation (evidence-gated)
- #167 | 2026-06-23 | in_progress | Lane-2 narrative theme detector (shadow) — ongoing
- #170 | 2026-07-15 | in_progress | EP cooldown re-setup admission: the ~60d cooldown over-suppresses legit re-setups (~7-10d separates extended-continuation from re-setup) → shorten or add a fresh-catalyst/re-based override. (Scope recovered 6/20; shadow classifier LIVE ep_detector.py:1345-1358 fail-open; live-flip ship gated on realized-R + CHANGE_PROCESS, review data_gated_reviews #170.)
- #115 | 2026-06-23 | pending | #94/#95/#96 intraday SHADOW-detector BOILERPLATE dedup (/simplify deferral 2026-05-24, recovered from CHANGELOG; was gated "after first-fire" — detectors have fired). The 3 mirror detectors (flag-break/support-test/MA-pullback) copy the same scan→persist→dedup scaffold. DoD: extract ONE shared helper, all 3 call it, behaviour-preserving, tests green. Pairs with #116 (caches) / #117 (constants).
- #116 | 2026-06-23 | pending | #94/#95/#96 intraday SHADOW-detector CACHE dedup (/simplify deferral 2026-05-24, recovered from CHANGELOG). The 3 mirror detectors keep near-identical per-detector caches. DoD: consolidate into one cache layer / shared util, behaviour-preserving, tests green. Pairs with #115 (boilerplate) / #117 (constants).
- #146 | 2026-07-15 | pending | Flag detector: allow direct TIGHTENING→TRIGGERED (drop/weaken the COILED prerequisite) on close>base_high_close + volume confirm — gated on N≥10 backtest. (Scope recovered 6/20 from ADR 0005 + flag_continuation.md; verified NOT shipped: flag_detector.py:626-635 still requires coiled_today/was_coiled_recent; intraday detector works around it.)
- #178 | 2026-07-15 | pending | Merge /setup + /why into ONE filter-reason observability command (entry-execution-observability thread, relates #150/#171). (Scope recovered 6/20 from PORTFOLIO.md/ab38d85; verified NOT shipped: _handle_setup_query agent.py:3148 + _handle_why_query :5357 still separate.)

## Judge & catalyst — v1.1 program

- #317 | 2026-06-27 | in_progress | EP-alert `catalyst` field contradicts the judge. ROOT CAUSE LOCALIZED 6/18: `ep["catalyst"] = news_summary[:500]` (ep_detector.py:2248/2282) = the Perplexity DISCOVERY narrative, a DIFFERENT text from `claude_analysis` (the judge's grounded rationale shown in italics). 6/18 reconfirm (SWBI): judge graded HIGH on the 8-K earnings, but the "Catalyst:" line read "headline-light newsflow… positioning/flow move rather than a clean catalyst" (contradiction) AND was cut off mid-sentence ("1) Sector and…" = the 300-char _truncate_sentence). IMPLEMENTED 6/18 (option A SUPPRESS, operator-chosen): threaded `has_direct_source` onto the ep alert payload (ep_detector result dict) + send_ep_alert suppresses the "Catalyst:" discovery line when has_direct_source AND claude_analysis present (the grounded catalyst is the italic; safe default keeps the line otherwise — never zero catalyst). Compile + 100 alert/grade tests green. LIMITATION/follow-on: has_direct_source is NOT in the _catalyst_cache tuple (4 pack sites), so a cached MODERATE→HIGH re-grade loses it → catalyst line kept (safe default, not a regression); add to the cache tuple for full coverage = follow-on. NEEDS market-agent DEPLOY + VERIFY-LIVE on the next direct-source HIGH (catalyst line absent, no contradiction). Pre-6/22.
- #320 | 2026-06-25 | pending | DEFERRED post-launch (operator 6/17, plan docs/analysis/grade_quality_deferred_320_321_plan_2026-06-17.md). Stale confidence_multiplier after a floor downgrade. GRADE-AFFECTING — reset can flip floor_tier + the score<50 PRE-JUDGE skip. WAIT = don't shift trade set pre-6/22; data exists (grade audit). Validate: how many carried the stale boost, how many drop below threshold, did the judge want them (pre-judge gating loss). COUPLED to #233 (display-only Perplexity supersedes this). Backtest wk of 6/23, decide by 6/25. COST OF DELAY: occasional false-positive alerts (small). 
- #321 | 2026-06-25 | pending | DEFERRED post-launch (operator 6/17, same plan doc). Missing prior-year YoY = SHARED ROOT of rubric=None AND the spurious strong→routine downgrade (LZB 6/17). Wire deterministic yfinance YoY (already shadow-recovered, #149) into rubric + downgrade gate. WAIT = don't shift trade set pre-6/22; data exists. Validate: coverage, flip-rate, correctness. UNKNOWN TO ANSWER FIRST: is yfinance reliable enough to be load-bearing? (spot-check error rate). Backtest wk of 6/23, decide by 6/25. COST OF DELAY: every earnings day we keep wrongly downgrading genuinely-strong gappers = MISSED TRADES — the larger, recurring cost
- #322 | 2026-07-01 | pending | theme-detection gap (memory theme_detection_two_lane_architecture): judge inferred JBL's AI-infra theme that neither Lane-1 clustering nor Lane-2 narrative tracks — real coverage gap, not a label. Feed into P2/P4 narrative radar (#309/#311)
- #328 | 2026-06-24 | in_progress | THEME AS A SCORED RUBRIC INPUT (#329 child — the theme axis of the meta-rubric) (operator 6/18, emphatic — "absolutely critical; Pradeep's theme is the #1 catalyst driver; not having this filed was a disastrous miss"). TODAY theme is DISPLAY-only metadata (get_theme_membership on the EP alert) + the judge weighs it QUALITATIVELY in the holistic grade (#249/#233) — it is NOT a scored axis in the catalyst rubric (catalyst_rubric_runtime score_ep_with_rubric). Meta-rubric vision (user_meta_rubric_architecture + user_pradeep_catalyst_hierarchy: theme=#1, > policy/shortage/sales): theme stage+score should ADJUST the composite/gate (Accelerating→boost, Mainstream sustain, Nascent partial; standalone→no theme credit). GRADE-AFFECTING, judge/floor are load-bearing → CHANGE_PROCESS + operator sign-off on the mapping + N≥10 backtest before any live flip; safe shadow eval first. SOFT-PREREQ #325 (coverage must be trustworthy or we'd score on a blind-spot-ridden signal) + couples to #233 (judge theme-weight) + #322 (detection gap). FIRST DELIVERABLE SHIPPED 6/18: EP alert distinguishes BLIND-SPOT vs STANDS-ALONE for missing themes (briefing.py — see #319). NEXT: design spec (gate-shift vs composite-score input; how blind-spot/standalone feed it; stage→weight table) — scope by ETA.
- #329 | 2026-06-24 | in_progress | META-RUBRIC composition (Phase 5) — ANCHOR the orphaned registry review + reconcile-first (operator 6/18 sweep; the parent of #328/#330/#331/#332). ⚡ ACCELERATED (operator 6/18: "rubric is critical to EPs — we're already trading EPs with the judge live; integrate earlier the better"). ALIGNMENT to the launch spine: the ADVISORY/SHADOW composite is UN-GATED (touches no live grade) → build it NOW, pre-6/22, in parallel with launch rehearsal; the LOAD-BEARING FLIP is grade-affecting → post-6/22 by the established trade-set-stability rule (#299/#320/#321 cluster), tracked as #335. So aggression lands on the shadow build (this task) + a fast post-launch flip (#335), NOT on destabilizing the cutover. NOT "we have no composition layer" — the live holistic judge ALREADY composes theme/catalyst/narrative qualitatively (#249/#240). The real gap: (a) theme/structure/gap-alignment aren't EXPLICIT, calibrated, traceable inputs (judge weighs them implicitly/inconsistently), and (b) the registry review `phase5_meta_rubric_calibration` (data_gated_reviews.yaml: threshold=30, earliest 2026-09-08) had NO PLAN #-task/ETA — yet its OWN text records operator 6/04 DE-GATING it to "ship advisory NOW" (the N≥30 gate only governs weight CALIBRATION, not the advisory build). So the advisory composite was un-gated but never moved for lack of a task = this sweep's headline miss. FIRST DELIVERABLE ✅ DONE 6/18: RECONCILIATION SPEC `docs/analysis/meta_rubric_reconciliation_329_2026-06-18.md` (ground-truth read of grade_holistic/_RUBRIC v3/assemble_judge_inputs + det. rubric + floor). KEY FINDINGS: the judge IS the composition layer but (a) theme STAGE/SCORE is computed + shown on the alert yet NOT passed to the judge (only the bool in_active_theme) — theme heat is invisible to the judge; (b) structure context (MA/resistance/base) isn't passed so clause-5 gap-alignment is unjudgeable; (c) no per-axis traceability. ARCH ✅ DECIDED 6/18 (operator): PATH A = enrich the ONE judge (pass the inputs it's blind to + add `axis_reads` per-axis traceability to the grade_ep schema; calibration rides the operator-labeled eval). Path B's separate logistic numeric composite stays the forward-gated registry cross-check (NOT a 2nd authority). So #330/#331/#328 build JUDGE AXIS INPUTS, not a separate composite. ⚠ NEW FINDING (call-site verify, ep_detector.py:2431): the live judge call does NOT thread `has_direct_source` OR `revenue_stage` (nor theme stage/score) → the prompt renders both as always-"no". Rubric v3 clause 1 LEANS on has_direct_source (=false→prefer-floor skepticism) → the judge applies freshness skepticism UNIVERSALLY + can't credit a fresh SEC-sourced 8-K = LIKELY LIVE GRADE-AFFECTING DEFECT (has_direct_source is computed — threaded for #317 today — just not passed). Judge is LOAD-BEARING (toggle ON 6/10) so the live payload edit is grade-affecting → build+measure on the EVAL ARM, flip via #335. Priority: has_direct_source > theme stage/score > structure. SSoT target when #330/#331 land = NEW docs/setups/meta_rubric.md + own SSoT per signal (catalyst_rubric.md SSoT confirms fundamentals-only + "add signals as separate inputs, each own SSoT"). STEP 1 ✅ SHIPPED 6/18 (fe6206a, live byte-identical): theme_stage/theme_score plumbed into assemble_judge_inputs + rendered on the in_active_theme line when present; eval-only `axis_reads` per-axis traceability (`_judge_tool(include_axis_reads)` deepcopy variant + grade_holistic flag + _normalize passthrough); 2 byte-identity tests lock the live prompt+tool. STEP 2 ✅ BUILT 6/18: scripts/eval_judge_enrich.py — `--size-only` NO-SPEND blast-radius pass (recompute has_direct_source DETERMINISTICALLY from stored grounded_text markers [SEC…/[Benzinga… vs [Web summary], segment out pre-W1 thin rows) + gated `--regrade` two-arm (BLIND vs ENRICHED) with AS-OF-date theme heat (mi_themes theme_date<=alert_date — NOT get_theme_membership which is lookahead) for operator labeling; build_judge_payload gained byte-neutral enrich kwargs; ep_grade_decision now logs has_direct_source (data-readiness for #337); recompute_has_direct_source pure helper + 6 tests, 910 suite green. SIZING RAN 6/18 (45d, no spend): 181/184 ASSESSABLE rows (98%) had a DIRECT source the live judge was shown 'no' for; 126 AT-RISK (direct + promote/material-lean). ⚠ RIGHT-SIZED (advisor 6/18): the 98% is the flag-wrongness rate, NOT the impact — the judge reads the corpus substance INCLUDING the dated marker ([SEC 8-K filed <date>…]), so it can establish source+freshness directly even with the meta-flag stuck at "no". The sizing data itself shows this: it graded HIGH/hold on nearly all 184 → largely ROBUST to the flag. EXPECT FEW has_direct_source flips in the re-grade. So this axis is likely LOW-IMPACT; if the batched re-grade confirms ~zero flips, that's the evidence to DROP has_direct_source from the #335 wire-in entirely (yield discipline working). The re-grade is still run (it measures theme-heat/structure/gap too) — has_direct_source just rides along, it doesn't justify the spend alone. NEXT (operator cost directive 6/18 — DON'T pay per-axis): the sizing pass cost $0 (no LLM). The `--regrade` (Opus spend) is DEFERRED and BATCHED into ONE consolidated re-grade at the pre-#335 checkpoint covering ALL pending payload enrichments at once (has_direct_source + theme-heat here + structure #330 + gap #331 + tape #299 + chart #267 as built) — re-judging the cohort ONCE, not once per axis. See #335. Live wire-in + axis_reads-on-alert ride #335. THEN ship the ADVISORY methodology-ordered composite (theme dominant ≥1.5–2× fundamentals per user_pradeep_catalyst_hierarchy; un-gated) by this ETA. Weight calibration stays forward-data-gated (registry review, now #-linked here); the load-bearing flip = #335. CHANGE_PROCESS + sign-off before any load-bearing flip.
- #330 | 2026-06-30 | pending | TECHNICAL STRUCTURE as an EXPLICIT, CALIBRATED, TRACEABLE scored meta-rubric axis (#329 child; meta-rubric memory item 2; registry D2–D6 prose). NB precision (per ADR-0011): the JUDGE is NOT blind to structure — it already ingests gap_pct/pm_rvol/extension/vol_percentile/score_breakdown as RAW inputs and rubric clause 4 has it "modulate up or down" on structure QUALITATIVELY. AND the deterministic catalyst rubric (catalyst_rubric_runtime) scores ONLY fundamentals axes (revenue+accel/EPS/margin/beat/guidance) — NO structure axis. The gap = a scored structure axis with a calibrated weight + a LOGGED contribution (MAs 10/20/50/200 alignment, 52w-high distance, base shape, prior consolidation, round-number proximity), vs today's raw-signal-into-an-LLM. Data partly in mi_flag_candidates (base age, RS rank, COILED) + mi_daily_closes (MA distances). Gated behind #329 reconciliation spec.
- #331 | 2026-07-02 | pending | GAP-vs-STRUCTURE ALIGNMENT as an EXPLICIT scored meta-rubric axis (#329 child; meta-rubric memory item 3). Does the gap punch THROUGH resistance (institutional confirmation) or fade INTO a congestion zone? Same precision as #330: the judge sees raw gap_pct + structure and can reason about alignment qualitatively (ADR-0011 clause 4) — the gap is a CALIBRATED, traceable alignment score, distinct from raw gap magnitude (already in ep_score). Gated behind #329 spec + #330 structure primitives.
- #332 | 2026-07-25 | pending | METHODOLOGY-FIT / setup-class rubrics (#329 child; meta-rubric memory item 5) — different conviction rubrics for Pradeep small-cap-explosive vs Qullamaggie mature-leader-breakout vs episodic mid-cap-with-neglect. The long pole (needs the per-class composition designed); pulled in from 9/1 on the operator 6/18 accelerate directive, but kept behind #329/#330/#331 since it composes ON TOP of them.
- #333 | 2026-06-27 | pending | CATALYST DURABILITY forward axis (catalyst program; user_catalyst_depth 6/16 Pradeep tweet). The rubric ALREADY scores trailing acceleration (revenue YoY + decel/accel); the GAP is the FORWARD/PROJECTED leg — Pradeep durability = ≥2Q realized + ≥4Q PROJECTED high-revenue-growth (39%-class illustrative; the SHAPE is the point). Needs forward guidance/projection from STRUCTURED data → gated on the direct-sourcing backbone (#210/#211), not LLM prose. Distinct from #269 (revenue-over-EPS weighting of existing axes).
- #335 | 2026-07-03 | pending | ⚡ LOAD-BEARING FLIP of the meta-rubric composite into the LIVE (paper) EP grade — THE integration the operator actually wants ("improve EP trading", we're already trading EPs with the judge). The grade-affecting milestone: the #329 advisory composite becomes authoritative (theme #328 + catalyst-magnitude + catalyst_type, enriched by #330/#331 structure/gap as they land — incremental, flip the core first). EARLIEST = just post-6/22 by the established grade-affecting-wiring-post-cutover rule (#299/#320/#321 cluster — don't shift the trade set right before the live-money GO/NO-GO), then move fast. GATES (non-negotiable, fail-open-to-floor like ADR 0011): CHANGE_PROCESS + operator sign-off on the exact weighting + a DB toggle for instant revert + the BACKTEST = ⚡ ONE BATCHED RE-GRADE (operator cost directive 6/18: don't pay per-axis). Instead of a separate eval-spend run per axis, accumulate ALL pending payload enrichments (has_direct_source #329 + theme-heat #328/#329 + structure #330 + gap #331 + tape #299 + chart #267 as each is BUILT) and re-judge the cohort ONCE at this checkpoint via eval_judge_enrich --regrade — measures every axis's verdict-delta in a single paid run, operator labels the flips. (The $100+ Phase-B replay was 1307 rows; batching avoids re-paying that per-axis.) Clusters with the #320/#321/#299 post-launch grade-quality batch. NOTE: if the operator wants this PRE-6/22 instead, it overrides the trade-set-stability rule — flag for explicit sign-off (my rec: shadow pre-launch, flip immediately after). At the flip, the per-axis `axis_reads` decomposition surfaces on the alert + /why (the richer half of #336).
- #336 | 2026-06-27 | in_progress | EP-ALERT + /why judge-decision TRACEABILITY (operator 6/18: "make the rubric + judge inputs traceable on the alert succinctly so I can tell WHY the rating + if it aligns + for periodic reviews"). DISPLAY-ONLY (no grade change), SHIPPED 6/18: (a) the alert italic now LEADS with the JUDGE's own rationale when authoritative — it was showing the FLOOR's claude_analysis under an authoritative judge (#319 coherence class); (b) materiality_tier + direct-source ✓ folded onto the fire line as the at-a-glance "does this align?" divergence cues (consolidated, NOT a 5th block); (c) /why now surfaces the judge decision trace (tier · direction-vs-floor · materiality · axes · rubric_version · rationale) = the periodic-review surface per ADR-0011 (reuses /why, didn't rebuild); (d) judge_rationale + judge_materiality_tier plumbed onto the in-memory alert dict (ep_detector post-write-success, DB-first like fire_axes). Pure helpers resolve_why_text/format_judge_trace_suffix + 5 tests, 904 suite green. The PER-AXIS axis_reads decomposition (theme lit→promote because X) rides #335 (gated — eliciting it changes the live tool def). DEPLOYED 6/18 (deploy.sh both, e3e74c1, DEPLOY OK). VERIFY-LIVE = next HIGH alert (judge rationale in italic; materiality/source on fire line) + /why shows the judge trace. /why is the PER-TICKER surface; the AGGREGATE monthly review = #337.
- #337 | 2026-07-01 | pending | MONTHLY judge-judgment review — aggregate "is the judge calling EPs right?" (operator 6/18, distinct from per-ticker /why #336). OPERATOR PRIORITY: "having all the data ready is key"; the review FORMAT is explicitly OPEN (operator unsure). DATA SUBSTRATE (advisor: the real question is the OUTCOME JOIN, not fields-on-a-row): judge verdict columns on mi_ep_alerts (judge_tier/direction/materiality/fire_axes/rubric_version + baseline_floor_tier) ⋈ mi_ep_scan_outcomes (fwd_5d/10d; ⚠ VERIFY the alert_date==scan_date join key lines up) ⋈ mi_live_trades (realized R) = the spine; ep_grade_decision now also logs has_direct_source (6/18). PROPOSED DESIGN (fold into the existing monthly_backward_check_sweep — reuse, NOT a new job; operator picks before build): (a) promote/demote/hold counts + floor-agreement rate; (b) judge-tier × forward-outcome cross-tab; (c) Unjustified-Demotion sweep — demotes that became winners (ADR 0011 named guard); (d) AKTS-class regression probes; (e) a SAMPLED set of promoted/flipped rows for OPERATOR labeling (go-live gate is operator labels, never self-score). ⭐ OPERATOR IDEA 6/18 (recorded): an LLM ADVISOR that reviews the judge's calls (like the dev-phase advisor here) — a meta-LLM reading judge decisions+outcomes to flag questionable/miscalibrated calls for operator attention; relates ensemble-divergence #301 (real-time 2nd-model verdict), questioner/investigator #212, precedent-retrieval #255 (memory project_cross_ticker_narrative_synthesis_gap holds the two-LLM-loop). Feasibility/usefulness TBD → evaluate as a candidate review MECHANISM (a richer variant of (e)). ✅ V1 BUILT 6/18 (operator: "propose one, draw from existing reviews, start + iterate"): scripts/monthly_judge_review.py — READ-ONLY standalone, SURFACES-not-prescribes (weekly-review lesson); direction-vs-floor counts + judge==floor agreement; by-direction & by-tier fwd-5d (win% LABELED drift-only per feedback_validate_metric_before_decision); ⚠️ Unjustified-Demotion sweep (demotes that ran ≥+5%/5d — ADR 0011 guard); operator-labeling sample pointer (never self-scores). Verified join scan_date==alert_date (eval_catalyst_materiality precedent). 5 tests, 915 suite green. REFINED + FOLDED 6/18 (operator "proceed"): logic moved to agents/market_intelligence/judge_review.py (importable; scripts/monthly_judge_review.py now a thin CLI re-export); ADDED realized P&L (fan-out-safe mi_live_trades subquery join) + has_direct_source footprint (reuses recompute_has_direct_source — surfaces the #329 defect's monthly trend until #335); WIRED into the monthly cron (quarterly_backward_check_sweep_job, independently-guarded so a review failure can't break the sweep) → auto-runs 1st-of-month 8am ET + Telegram. 12 tests, 916 suite green. VERIFY-LIVE = next month's 1st sweep fires the judge-review Telegram (can also run on-demand now). STILL OPEN (recorded, not built): the LLM-advisor review mechanism (operator idea — TBD feasibility) as a richer variant. ⭐ REVIEW→ACTION CHAIN (operator 6/18 "ensure effort yields results"): the review SURFACES (never prescribes — weekly-review lesson); each signal maps to a GATED action: (1) Unjustified-Demotion pattern → rubric amendment or axis fix (CHANGE_PROCESS+sign-off); (2) promote cohort NOT beating holds/floor → tighten promote criteria / find the bad-promote axis; (3) has_direct_source footprint DROPS after #335 → confirms the fix landed (verify-live); (4) direction/agreement drift across rubric_versions → regime/version-drift flag; (5) the operator-labeled sample = THE yield — it accrues the operator-labeled dataset that GATES every load-bearing axis flip (#335) + model-eval. YIELD DISCIPLINE: the review must drive ≥1 concrete gated decision per quarter or be trimmed (don't run effort that doesn't pay off). ⮕ ABSORBS #56/#113 (closed 6/20): the "is the rubric too generous / operator-vs-judge calibration" + EP-detection-calibration question those two tracked IS arm (e) here (operator-labeled sample) + the judge==floor agreement + by-tier outcome cross-tab — closed as SUBSUMED, not dropped. ⚠ INTEGRITY: those two closures (+ #60/#81 deleted in the 6/20 contentless-stub recovery) were AGENT-INITIATED under the filing-quality gate (only #134/#165 were operator-decided at the time) → surfaced for ratification + OPERATOR-RATIFIED 6/20 ("aligned"): closures STAND. Reopen any as a DISTINCT task only if it turns out to be detection-threshold work separate from the rubric — `git revert` restores the line + snapshot status.
- #338 | 2026-07-15 | pending | /simplify deferrals from the 6/18 judge-traceability review (#329/#336/#337) — 4 cleanup agents (reuse/simpl/efficiency/altitude). CHEAP fixes APPLIED 6/18: format_tier_transition reuse in judge_review (#253 single-copy contract); always-True tuple + hoisted-invariant-blind-payload in eval_judge_enrich; dropped dead _SQL cols (fwd_10d_pct/judge_rationale); closed-status FILTER on realized-P&L SUM (codebase convention); phantom-function docstring fix (corpus_provenance not classify_catalyst_sources); producer→consumer PINNING TEST (recompute_has_direct_source vs build_grounded_text markers). DEFERRED (structural, file-not-build): (A) /why's inline judge-trace block re-renders the alert's fields → extract shared format_judge_trace(row) for both /why + send_ep_alert (two renderers drift; couples #336); (B) verdict→r mirror omits judge_direction (briefing._judge_direction reconstructs it) → unify "apply verdict onto r" field-for-field with the DB write (ep_detector hot path, pre-existing); (C) deepen the marker-coupling past the pin: hoist [SEC/[Benzinga/[Web-summary prefixes to shared constants owned by build_grounded_text + an audit-JOIN agreement guard (recompute vs ep_grade_decision-logged structured value on #329+ rows); (D) extract get_theme_heat_asof(ticker,date) db helper (eval_judge_enrich + ep_selectivity_cohort:151 both inline it); (E) grade_engine_authority "judge"/"floor"/"fallback" bare literals → constants (low-pri, matches skip_reasons vocab); (F) extract _judge_authoritative(ep) (predicate now in 3 briefing helpers); (G) _MATERIAL_LEAN hardcodes catalyst_materiality's material+ tiers → reuse the vocab; (H) alert "direct-source ✓" on the judge fire-line can read as "judge used it" while the judge is BLIND to it until #335 — resolves when #335 wires it in (or reword sooner). All low-urgency hygiene; cleared as deliberate: kwarg-threading, deepcopy gate, aggregate/format split.
- #269 | 2026-06-27 | in_progress | rubric v3 verify (AKTS forward outcome = first live promote-cohort point) + promote-cap eval harness
- #210 | 2026-06-23 | pending | catalyst-sourcing backbone umbrella (direct primary sources)
- #211 | 2026-06-23 | in_progress | news-gap discovery loop (unknown-rate KPI) — needs new scheduler job
- #344 | 2026-06-27 | in_progress | ✅ REPLAY-VALIDATED — cache-staleness fix is NOT launch-blocking (the 6/22 gate evidence). Harness scripts/_344_late_source_replay.py + doc docs/analysis/late_source_replay_344_2026-06-19.md (read-only, lookahead-free). FUNNEL (12d, all post-Wave-B data): 21 web-only grades (deduped) → 2 had a direct source land in the ORB window → 0 re-graded higher → 0 new fires. LOAD-BEARING = the ENUMERATION (stage-2 = a COMPLETE census via production's OWN source funcs, web-independent → immune to the no-web caveat) + the hand-read of both cases: addressable surface is just **2 of 21**, and both in-window sources are weak (product hype / dilution). CORROBORATION A = restart re-grades (a restart clears _catalyst_cache → a later same-day provenance row = a real production web-inclusive re-grade): 0 of **12 eligible** flipped false→true, 0 tier increases (timing-limited — samples restart re-grades only). CORROBORATION B = 0 same-day pre-grade sources (no live #210 gap). BFLY + BTQ both grade `routine` on their ACTUAL direct source (BFLY = product/prototype PR, no financials; BTQ = ATM dilution — agent reads correct-reject, operator confirms). ⚖️ OPERATOR LABEL LANDED 6/19 (HARD gate): BFLY 6/18 IS a real EP — `routine` is WRONG (operator corrected the grade reasoning: partner IS named = Midjourney; contract value DOES exist = $74M/5yr in a prior 8-K; Gemini concurs textbook-EP; captured verbatim in operator_shared_notes.md). So the gate RE-POINTS off the cache fix onto catalyst-CORRECTNESS (corpus-completeness). Cache surgery = DEFERRED-not-built (0/12-eligible production evidence + 2/21 enumeration). ROOT CAUSE scoped read-only (probe _344_bfly_corpus_scope.py): BFLY graded on a HEADLINE-ONLY corpus — the 6/18 Benzinga PR body=0c (a headline flash; NOT recovered even by include_content) and the grounded corpus lacked the PRIOR material-agreement context (2025-11-18 8-K item 1.01 = the Midjourney Co-Dev & Licensing Agreement; 2026-02-26 8-K = $6.8M Q4 partnership revenue). DECOMPOSED 2 corpus gaps: (a) get_alpaca_news omits include_content=True → discards full Benzinga bodies generally (recovers 0c→~3-7k for MOST items, but NOT BFLY's headline flash); (b) no prior item-1.01-8K/revenue-attribution enrichment → the load-bearing lever for BFLY. FIX (general, not BFLY-shaped, advisor 6/19): always include the ticker's recent item-1.01 8-K + recent revenue attribution in the grounded corpus (= #210 direct-primary-source direction). GATE METRIC = NET catalyst-correctness across the cohort (distribution shift vs baseline + false-positive direction — NOT winner-rescue/recovery, which recovers BFLY by construction). ENRICHMENT EVAL BUILT + RUN 6/19 (_344_late_source_replay.py --enrich): findings — (1) BFLY's grade-time corpus was EMPTY (PR came 8:12, after the 7:00 grade); the 8:12 PR is headline-only (body=0c even w/ include_content) so a cache re-poll on it ALSO stays routine; (2) the $74M deal IS on file (2025-11-18 8-K item-1.01 Midjourney Co-Dev/License: $15M one-time + $10M/yr + $9M milestones; 2026-02-26 8-K = $6.8M Q4 partnership rev) but needs SUBSTANTIVE exhibit slicing (raw text is iXBRL-cover boilerplate); (3) with proper slicing BFLY lifts routine→strong BUT for the WRONG reason — grader mis-dated the 7-wk-old Apr-30 earnings 8-K as "today" = a DATE-CONFUSION INFLATION artifact, exactly the net-correctness risk. OPERATOR CHOSE (6/19) to SPRINT the full fix. BUILT + DEPLOYED as a web-inclusive SHADOW (advisor: the offline no-web cohort can't measure web×enrichment double-counting → only a live web-inclusive shadow gives true net-correctness; AEHR proved baseline≠production). Components: (a) collector.get_alpaca_news include_content param; (b) ep_detector.assemble_grade_corpus = SSoT corpus builder (date-anchor + age-labeled prior item-1.01/2.02 context — the replay now imports it, no validate-one-ship-another); (c) ep_grade_enrich_shadow (uncached, once/tkr/day: current vs enriched, web-inclusive); (d) ep_repoll_shadow (cached path: re-grade ONCE when a new primary-subject source appears after a routine grade in the ORB window — the BFLY mechanism; should_repoll_shadow pure fn + unit tests); flag ENRICH_SHADOW_ENABLED. TWO-CASE validated (BFLY routine→strong citing today's news sized by the $74M deal; BTQ ATM-dilution canary STAYS routine). Offline cohort (60): only BFLY-rise + AEHR-restore + CASY gc→strong(conservative, still fires), 0 inflation. ⚠️ SUPERSEDED 6/19: that offline-60 was measured PRE-truncation-fix (prior-agreement context was sliced off for any long-today-news name by the grounded_text[:6000] bug, advisor-caught during #238) — do NOT cite "0 inflation across 60" as fresh flip evidence; the live shadow (now fixed code, today capped 6000 + enriched at 12000) supersedes it. BFLY itself unaffected (short 0c-body corpus, never truncated → routine→strong stands). FLIP (make enriched the live grade) GATED on Monday+ shadow data (physics: needs live-market grades) + net-correctness + CHANGE_PROCESS + operator sign-off. SHADOW IS PREMARKET-ONLY (<9:30 ET) — advisor 6/19: it's synchronous on run_ep_scan (order-submission path), so guarded OFF the 9:30-10:00 ORB entry window (BFLY's PR was 8:12 premarket → still covered); SEC fetch bounded max_filings=8 + reused on re-poll. ▶ MONDAY VERIFY-LIVE: scripts/_344_shadow_verify.py reads ep_grade_enrich_shadow + ep_repoll_shadow (net-correctness distribution + CHANGES to label + re-poll thrash check + latency p95) — confirm rows write premarket, re-poll fires once (restart can dupe — check uptime), latency OK; AND confirm SCAN WALL-TIME unchanged vs pre-#344 baseline (the real entry-path risk). SSoT change-log = docs/setups/magna53_ep.md (2026-06-19). Docs late_source_replay_344_2026-06-19.md. History in git + missed_ep_bfly_2026-06-18.md. ✅ HARD GATE OPERATOR-RESOLVED 6/19: "shadow plus Monday flip is good" → shadow-now + the Mon/Tue flip (#347) satisfies the catalyst gate for the 6/22 GO, CONDITIONAL on a clean Monday #346 shadow verify (does NOT waive the #347 CHANGE_PROCESS + sign-off on the flip itself).
- #347 | 2026-06-24 | pending | #344 FLIP-DECISION (aggressive ETA — feedback-shadow-needs-dated-flip-eta; no open-ended shadow). After ~2 trading days of shadow data (Mon 6/22 #346 verify + Tue 6/23): `_344_shadow_verify.py --since 3` → IF enriched LIFTS true catalysts AND INFLATES nothing (net-correctness, web-inclusive) AND re-poll fires once cleanly AND scan wall-time OK → CHANGE_PROCESS + operator sign-off → FLIP enriched to the LIVE grade (assemble_grade_corpus becomes the live corpus + the cache re-poll actually re-grades live, not shadow; magna53_ep.md change-log + reversion ENRICH_SHADOW_ENABLED=false). ⚠️ FLIP MUST CARRY THE WINDOW (advisor 6/19): the live grade path today calls `_classify_catalyst_claude(..., max_chars=6000 default)`; the validated shadow grades at `max_chars=_GRADE_ENRICH_MAX_CHARS`(12000). Swapping ONLY the corpus builder and leaving the default 6000 re-introduces the truncation bug (the appended dilution/agreement context slices off) → the live grade won't reproduce the measured shadow. The flip MUST pass `max_chars=_GRADE_ENRICH_MAX_CHARS` on the live grade call. IF thin/inflating → iterate the rubric or re-bump (don't let it drift open-ended). This makes BFLY-class names fire live. Gates #272 catalyst-correctness. ALSO carries the #238 dilution feed (CLOSED 6/19 — build + offline canary done): the flip read must ALSO confirm the dilution block's SUPPRESSION value on live `has_dilution` rows (a catalyst-less pump-into-dilution correctly held routine) — unexercised in the 6/19 offline cohort (all 6 dilution hits had real catalysts). SAFETY direction PASSED with the block CONFIRMED IN-WINDOW (after the advisor-caught truncation fix — today's-news capped 6000 + enriched graded at 12000; the live #344 shadow was ALSO silently truncating prior-agreement context for long-today names, now fixed): 4 names/6 alerts, 0 wrong-suppression incl. the false-positive-risk WDC; the one change (ELVN game_changer→strong) is a conservative temper that still fires + matches the live grade. Doc docs/analysis/dilution_canary_238_2026-06-19.md.
- #212 | 2026-06-23 | in_progress | questioner/investigator dialogic loop — productionize at larger cohort
- #230 | 2026-06-23 | pending | #212-as-sourcing-QA detector → feed #211 (low-precision triage)
- #233 | 2026-06-23 | in_progress | Perplexity repositioning — KEEP + integrate (operator-adopted 6/17, NOT cut): (a) feed its independent GRADE into the JUDGE as a LABELED second-opinion/disagreement signal — judge currently reads its [Web summary] text but not the Claude-vs-Perplexity grade disagreement (grade-path INPUT change → CHANGE_PROCESS + shadow eval, judge is load-bearing); (b) reposition Perplexity as a labeled RECENCY source only (its live-web edge), judge-adjudicated vs primary (#210), and RETIRE the mechanical floor confidence_multiplier boost (with #320). Was "demote Perplexity/Tavily to labeled candidate (Wave C)"
- #235 | 2026-06-23 | pending | gap-discovery loop #211 integration (Wave E)
- #236 | 2026-06-23 | pending | corpus_provenance as byproduct of build_grounded_text (DRY refactor)
- #214 | 2026-06-27 | in_progress | theme auto-naming breadth rule — verify (cooldowns back to ~3/day)
- #215 | 2026-06-23 | pending | surgical prompt de-bias / OPTX-residual (blocked_by #214 + clean cohort)
- #258 | 2026-07-15 | in_progress | ensure_schema() consolidation (fold 33 ALTERs) — branch+staging-validated, post-#277
- #259 | 2026-06-23 | in_progress | failure-policy decorators (@advisory_fail_open / @trade_state_fail_loud)
- #237 | 2026-07-15 | pending | shared scheduled_eod_digest helper (the ~6-job 16:xx digest family)
- #239 | 2026-07-15 | pending | Wave-A trailing-baseline dedup + _emit_shadow_row envelope (/simplify deferrals)
- #265 | 2026-06-23 | pending | judge-flip-day review residuals (SQL-text SSoT, shared feeds list, WATCH cancel window)
- #207 | 2026-08-01 | pending | model-eval governance quarterly review (data-gated)
- #197 | 2026-06-23 | in_progress | cap+1 game_changer slot SHADOW — promotion-gated N≥30
- #192 | 2026-06-23 | pending | deferred-findings-to-task discipline follow-up
- #272 | 2026-06-27 | in_progress | LAUNCH 6/22 umbrella (SSoT docs/roadmap/launch-2026-06-22.md). #344 gate ✅ SHADOW-VALIDATED 6/19 (cache-staleness fix is NOT launch-blocking — Check A 0/23 production flips; doc late_source_replay_344_2026-06-19.md) → no longer blocks GO on building a cache fix; remaining #344 dependency = operator's BFLY/BTQ catalyst-correctness label (routine-correct → gate satisfied; should-be-higher → re-point gate at a materiality-rubric task). ✅ #344 HARD GATE OPERATOR-RESOLVED 6/19: "shadow plus Monday flip is good" — shadow-now + the Mon/Tue live flip (#347) SATISFIES the catalyst gate; CONDITIONAL only on Monday's clean #346 shadow verify (dirty read → NO-GO trigger). #349 DR: operator decided NOT to gate GO on the rehearsal (runs separately, next couple days). So 6/22 GO/NO-GO now rides only on Monday's mechanical verifies + START-SMALL sizing.
- #274 | 2026-07-15 | pending | 2-member theme immortality fix (dissolve-on-flagged-pair, CHANGE_PROCESS)
- #276 | 2026-07-15 | in_progress | W2 entry-mechanics program. ✅ STUDY #2 (stop geometry) DONE 6/18 (doc docs/analysis/w2_entry_study2_2026-06-18.md): added an offline stop_model knob (orb_low/atr_floor/atr_cap/day_low, threaded into sizing AND exit so R is comparable; 9 tests; harness c6b0029, deployed). RESULT on the retained judge-HIGH cohort (n=44; mi_ep_alerts pruned to 2026-03-16+ #341): WIDENING the stop monotonically HURTS in the trustworthy direction — baseline orb_low +1.40R/+$20.9k → atr_floor 0.5/1.0/1.5 +0.66/+0.48/+0.27R → day_low +0.14R/+$4.4k (stopout 59%→7% but R-multiples+$ collapse with the wider denominator). atr_cap +0.04R is noise (9% coverage, SUSPECT tighter direction, top-5-carried). VERDICT = **no live stop change; ORB-low optimal**; kill/scale bands need no re-derivation (winner IS orb_low). Both W2 levers now tested (study #1 OR-window+skip-wide-open dead/not-shippable; study #2 stop no-change) → RECOMMEND PAUSE the "tune the bracket" thread (regime-conditioned, NOT "edge proven absent": evidence is n=44 over ONE recent ~7-week window overlapping the tape the live stop already trades — regime-locality + near-circularity; re-open on a regime shift / kill-scale band review). Operator ack at launch checkpoint. Exit/management tune is the separate #306 (v1.1-W3, 7/01).

## Operational safety / hardening
- #352 | 2026-06-23 | in_progress | VERIFY-LIVE the L2 persistence dedup (commit 63b116d, monitoring-only). After the Mon 6/22 17:30 ET nightly audit: confirm a still-band-3 theme_count_active logged an L3 row carrying `persistent_l2_downgrade=true` (NOT a repeat L2 Telegram); and that any genuine spike/collapse/high-variance breach still fired L2. DoD: one clean Monday nightly showing fire-once-then-quiet on the slow-drift class. SSoT tests/test_l2_persistence_dedup.py. (From the 6/21 weekly-review a+b dig.) ❌ **6/22 VERIFY FAILED** (operator asked "why does this fire every day despite fixes"): the 17:30 nightly fired theme_count_active **L2 again** (23 vs median 42.5), and `to_band` is **BLANK on EVERY L2 row 6/12→6/22** incl tonight's post-deploy row (verified `(detail::jsonb)->>'to_band'` in mi_audit_log; the L3 rows 6/08/6/11 DO have to_band 1/2). So `_last_band_for` finds no recorded band → can't dedup → fires daily. The dedup is **NOT writing to_band on the L2 branch** — root-cause it (deployed-but-not-on-this-path vs a code bug in the L2 detail assembly) before re-trusting #352. ▶ **ALSO (from the #286 finding, 6/22): the alert fires partly on a STALE baseline.** The median 42.5 predates BOTH the #286 6/16 universe step-change (3,950→2,450, operator-signed liquidity floor — a legitimate structural drop, NOT an anomaly) AND the #325 discovery recovery. So even a working dedup won't fully quiet it — the detector compares against a pre-#286 baseline that no longer exists. TWO real fixes here: (1) write `to_band` on the L2 branch (the dedup); (2) rebase / make the baseline step-change-aware so a signed structural shift (like #286) doesn't read as a 30-day anomaly (ties to #325's "baseline-discontinuity-aware" note for the lagging metric). ▶ **FOLD-IN from the 6/22 /simplify** (bank when you touch this code for fix #1, behavior-preserving): drop the dead `band` param from `_persistent_l2_downgrade` (its only caller is inside `if band==3`, so the `band!=3` guard is dead); extract a shared `_is_stable_metric(mad,p50)` predicate (the stable-tight-MAD clause is re-derived in BOTH `_is_slow_drift` and `_persistent_l2_downgrade`); fix the pinned `warming` constant (always False in that branch). KEEP `_persistent_l2_downgrade` itself — the /simplify altitude pass confirmed it's correctly targeted (the persistence axis, not a bolt-on).
- #348 | 2026-07-15 | pending | consolidate the mi_safeguard_state get/set + conditional-bump upsert into a generic db helper (deferred from the 6/19 /simplify reuse review). There are now 4 inline copies of the `SELECT state FROM mi_safeguard_state WHERE safeguard/account_mode` read and 2 of the `last_transition_at = CASE WHEN state<>EXCLUDED.state THEN NOW()...` conditional-bump upsert: broker/drawdown_breaker.py:351-369, kill_scale_bands.set_last_band, plus the always-bump variants db.py get/set_holistic_judge_enabled + get_manual_halt_state/set_trading_halted. Extract `get_safeguard_state(safeguard, account_mode)` + `set_safeguard_state(..., *, bump_on_change: bool)` into db.py and migrate all copies. POST-LAUNCH (touches the #345 manual-halt panic button + the live-money drawdown breaker → behavior-preserving but needs careful per-call verification + the band module already imports db inside fn bodies so no new circular-import). Low priority; the inline copies are idiomatic/consistent today, so this is cleanup not a bug.
- #323 | 2026-06-27 | in_progress | L1 high_ep_no_terminal_state (LZB) — DEPLOYED LIVE 6/17 (bfa4b50+64a0c12; coercion of date-over-wire at _insert_skipped_trade + de-silenced skip-write except → skip_row_write_error audit+Telegram). All 3 containers recreated. Verify-live = next out-of-ORB HIGH writes its skip row (event-driven, now self-announcing)
- #325 | 2026-06-23 | in_progress | theme_count_active decline (true 42→15 active in 16d; L2 fired 6/12/15/16/17, each dismissed "benign"). DIAGNOSED 6/17: NOT regime (28/40 top RS-100 leaders UNCOVERED, all w/ descriptions once mi_ticker_overrides merged), NOT desc-filter, NOT cooldown-flood (~24 cooled) — a real coverage/birth malfunction (0 births/16d; theme_discovered 0× in 40h). Collapse is in discovery-LLM→_score_new_theme→_validate_new_themes_at_birth but stage counts were logs-only (die on recreate→4 blind dismissals). SHIPPED+DEPLOYED telemetry `theme_engine_funnel` (d1afc69, theme_engine.py) = durable per-run funnel. VERIFY 6/18 ✅ FUNNEL READ (17:02 ET run): {uncovered:21, velocity:30, turners:6, elite_covered:18, discovery_called:true, new_raw_llm:0, survived:0}. STAGE PINNED = the DISCOVERY LLM itself returns 0 raw (NOT description-strip [disproved: only 3 missing→fetched], NOT a crash [no discovery_error]). Container logs nail the mechanism: `Theme discovery: model stopped without calling report_themes — forcing a final report` → forced report came back EMPTY → `0 new themes discovered`. = the #173 "shadow-death" class RECURRING despite the force_report guard (theme_engine.py:3072 loop, THEME_MODEL Sonnet, max_tokens=1500, tool_choice auto→forced). Two candidate ROOT CAUSES, not yet distinguished (deterministic repro owed before any fix, #325 "NOT blind"): (a) max_tokens=1500 too low (assignment LLM next door uses 4000) → model reasons in text, truncates before the tool call → silent stop, then forced report has no room to commit = BUG/just-fix; (b) prompt's heavy anti-false-positive framing → model reports empty when forced = CRITERION (sign-off gated). git history shows NO recent change to this block → the 16d drought correlates with something else (universe/prompt growth? model?). ADVISOR-REVISED 6/18 (the fork is CONFOUNDED, not XOR — (a) is the prerequisite; you can't see conservatism through a truncation confound): (1) bumping max_tokens 1500→4000 is INFRA CAPACITY not a criterion change → SHIPPED UNGATED (theme_engine.py:_discover_new_themes, the disposition line untouched); (2) added DURABLE per-call telemetry `theme_discovery_llm_call` (stop_reason + output_tokens + force_report + n_tool_uses) — the funnel was logs-only and DIED on the container recreate from today's deploys (couldn't grep the 17:02 discovery logs = the lesson, now fixed). VERIFY-ATTEMPT 6/19 → DEFERRED to 6/22 (could NOT run — 6/19 is Juneteenth, NYSE closed): nightly_data_pull ran 17:00 ET but EARLY-RETURNS on a non-trading-day (scheduler.py:249 `if not market_status.is_trading_day` → return at :254) BEFORE the theme step → 0 theme_engine_funnel + 0 theme_discovery_llm_call rows today (success in mi_job_runs = the clean holiday-skip, NOT a telemetry bug; confirmed deterministically). The fix IS LIVE IN THE RUNNING IMAGE (verified in-container: max_tokens=4000 theme_engine.py:2123 + theme_discovery_llm_call emit :3101). theme_discovery_llm_call = NONE EVER → the last real run (6/18 17:02, new_raw_llm=0) PREDATES the telemetry deploy → **the max_tokens fix has never been exercised by a theme run yet**; the 6/18 0-raw is a PRE-fix data point. Did NOT force an off-cycle run (the heavy-blind-mutation caution holds). FIRST VALID TEST = Monday 6/22 ~17:00 ET nightly_data_pull. NEXT (ETA 6/22, after that run): READ the audit, rows in `iteration` order (auto then forced). DISCRIMINATOR — **PRIMARY signal = new_raw>0** (theme_engine_funnel): if discovery now returns themes, CAPACITY was the cause = (a) confirmed + fixed + coverage recovering, don't even need stop_reason. ONLY if still 0: stop_reason='max_tokens' → 4000 STILL truncates (prompt grew bigger than thought → trim the prompt, escalate); 'end_turn' + 0 tool_uses + forced returns themes:[] → (b) ISOLATED → the disposition-line ("return zero when in doubt") question goes to the operator (sign-off-gated); repeated consult_advisor then giving up = a THIRD mode (advisor-loop), neither branch. Corroborating: max_tokens=1500 was always there yet discovery worked at 42 themes → something crossed a threshold ~16d ago, almost certainly the prompt grew (more uncovered/velocity stocks → more reasoning → overruns 1500); 4000 buys headroom + the stop_reason telemetry catches the next overrun. (Synthetic-clean-set repro SKIPPED — a short prompt won't reproduce the truncation a long production prompt causes → false "1500 is fine".) NOTE: the PLAN's earlier "merge mi_ticker_overrides descriptions" hypothesis is SUPERSEDED (descriptions aren't the failing stage). Also still owed: fix lagging _today_active_themes metric (30 vs true 15, pre-#214 shape; baseline-discontinuity-aware). Probe: scripts/_theme_count_anomaly_probe.py. CONCRETE LIVE EXAMPLE 6/18 (from the UUUU EP alert): the "Rare Earth Magnets & Critical Battery Materials" theme is ACTIVE (Mainstream) yet UUUU (Energy Fuels, uranium/rare-earth, EP HIGH 6/18) is NOT a member, and that theme shrank 4→2 members 6/11→6/17 — live proof of the membership/assignment collapse (not just births). Downstream: EP alerts show "judge-inferred/not-tracked" instead of the real theme (the #319 reframe). ✅ **6/22 VERIFY — PRIMARY DISCRIMINATOR POSITIVE → (a) CAPACITY CONFIRMED**: tonight's 17:04 theme_engine_funnel = **new_raw_llm:4, new_themes_survived:4** (vs 0 for 16 days) → the max_tokens 1500→4000 fix WORKED; discovery births RESTORED; the count will recover. (uncovered:12, velocity:30, e.g. survived "Crude &…".) CAVEATS to watch: (1) ONE run — confirm births continue the next nightlies before closing; (2) one iteration (17:06) STILL hit stop_reason=max_tokens=4000 + force_report + heavy consult_advisor (5 calls across 2 iterations) → discovery is token-hungry at the new ceiling (may need prompt-trim or fewer advisor calls); (3) uncovered fell 21→12 vs 6/18 — consistent with the **#286 $10M liquidity floor (operator-signed dd4eeb4 6/15, first effect 6/16 — confirmed 6/22, INTENDED)** which LEGITIMATELY shrank the universe ~38% and the candidate pool with it → a structural (intended) lower ceiling on theme count, not a bug; (4) still owed: the membership/assignment collapse (UUUU) + the lagging _today_active_themes metric. Discovery-births root cause = CLOSED-positive; #325 stays open for the continuation watch + those 4 items.
- #288 | 2026-06-23 | in_progress | Prong B idle-review escalation — REDEPLOY DONE 6/17 (both-deploy; a6c45a0 ⊆ deployed HEAD, boot applied new schema, container healthy). Remaining verify-live: clean /reviews + escalation render on next idle-review fire
- #290 | 2026-06-27 | in_progress | dead_zone_reevaluation predicate FIXED 6/18 (referenced `dc_fwd.high`; mi_daily_closes has `high_price` → errored silently → never went ready; corrected + VERIFIED executes, count=155). Review is now READY → remaining = run its action_when_ready (re-run analyze_late_detection_v3 dead-zone ORB-extension precision on Hetzner; confirm <20% precision or close). Registry hygiene done same area 6/18: ninem_day2_{mechanical_vs_methodology_alignment, retire_for_consolidation} → status=done (decided by #327/#326)
- #291 | 2026-06-26 | in_progress | triage the idle data-gated reviews (run/defer/ratify each; incl #54). DoD-7 TRIAGE 6/18: the LAUNCH-CRITICAL subset is DONE (see below — P0s/GATE2/GATE3/gate5); the REMAINING methodology-review batch (rmv #54, theme-narrative-blindness, drawdown-breaker promotion, rel-volume-floor, cooldown-resetup, nbis-calibration, …) is EXPLICIT POST-LAUNCH — evidence/methodology reviews, none launch-blocking; ETA→6/26 as the post-launch review session. TRIAGE DONE 6/18 (operator-approved prod read, check_pending_reviews): **27 genuinely READY** (predicate met + date passed), **0 errored** — idle by backlog, not by neglect. LAUNCH-CRITICAL subset (6/22 is 4d out) = sequence FIRST: P0 alpaca_stop_trigger_reliability, P0 orb_entry_stuck_pending_new, paper_r_expectancy_validation (GATE 3 live), ftre_partial_trail_verification (GATE 2 live), gate5_adel_deliverables_status_check, gate5h_value_invariant_proposal. Methodology batch: rmv_phase2_evaluation (#54), theme_engine_narrative_blindness, drawdown_breaker_promotion, rel_volume_large_cap_floor_evidence, ep_cooldown_resetup_admission, nbis_rubric_calibration_gap, silent_failure_taxonomy_audit, +others. Each needs ground-truth (no bulk-ratify). = a focused multi-review session, NOT one pass. ✅ BOTH P0s TRIAGED 6/18 (operator chose launch-critical P0s; verified neither is an open cutover blocker): (1) orb_entry_stuck_pending_new — CLOSED 5/28 (APScheduler 10:00 misfire, watchdog shipped); verified read = **0 post-fix recurrences** (all 10 predicate hits are ≤5/26: RDW + 9 April orders the #123 reconcile swept 5/27 = drift, not stalls). FOUND + FIXED a launch bug: the 90d predicate false-counted pre-fix history → at 6/22 the cutover gate would have FALSELY read ≥1 (RDW still in-window) and blocked a paper→live flip. Floored the predicate at the 5/28 fix date (post-fix-only recurrence detector; reads 0 verified) — committed/deployed. (2) alpaca_stop_trigger_reliability — already DOWNGRADED P0→P2 6/03 (not a blocker; mechanism = #182 paper-IEX entry-side miss, re-escalate ONLY on an exit-side activation failure, none observed); its broad predicate (any unfilled ORB in 30d) is a P2 MONITOR by design → correctly left pending, no edit. ✅ GATE 3 (paper_r_expectancy) CHARACTERIZED 6/18: N=21 closed paper trades since 5/12 (pnl_attribution NULL), **cum −5.64R, 33% WR (7/21), −$1,862** = RED on raw paper. BUT this is the KNOWN-BIASED paper-IEX cohort (IEX misses the fast clean breakouts → drops winners + IEX-priced; the filled winners underran: PURR 0.00, FTRE +0.14, IBM +0.48 vs losers ~−1R). Per the operator's hard gate (memory paper_iex_vs_live_sip_gate_adjustment + project_gate3_sip_selection_finding, #223 = +2.27R SIP selection delta), the DECISION number is the SIP-RECONSTRUCTED cohort (#180/#182), NOT raw paper-IEX → the SIP reconstruction is the gating dependency for the 6/22 go/no-go. RAW gate = not green; real eval = SIP-pending. ✅ GATE 2 (ftre_partial_trail) CHARACTERIZED 6/18: the partial-trail path WAS exercised (5 paper partial-then-close trades: BW/PURR/IBM/CRSR/RCAT). 6 naked_position_detected events since 5/10, ALL detected+auto-remediated ("sync_positions will remediate" — defense-in-depth working). Mapped: 3 = the KNOWN IBM 5/27-28 docker-exec cascade (separate, postmortem'd), 2 = FPS (NOT partial-trail trades), **1 = RCAT 6/01 — DURING a qualifying partial-trail trade** = exactly the race GATE 2 guards. ✅ RCAT 6/01 ROOT-CAUSED 6/18 → GATE 2 VERIFIED (recommend green, awaiting operator ratify — safeguard gate, not agent-unilateral): the RCAT naked event was the FIX WORKING, not a race it missed. Timeline: partial_exit_started → replacement stop hit a TRANSIENT APIError → code correctly ABORTED (not proceed-uncovered) → naked_position_detected cleared stop_order_id defensively → +7s stop_ack_broker_covered confirmed broker had 1529/1529 covered = NO actual naked exposure (atomic replace failed → original stop intact; partial never executed, success=False) → operator retried 17:15, clean fill +$1,175.79. Defense-in-depth handled a transient broker APIError exactly right = OPPOSITE of the FTRE incident → 0 partial-trail-race misses → fix verified. Root-cause documented in the registry entry; status→done on operator ratify. LAUNCH PICTURE (4d out): P0s cleared + GATE 2 VERIFIED; **GATE 3 (SIP reconstruction) is now the ONE real open go/no-go dependency** (raw paper −5.64R is IEX-biased; the SIP cohort is the decision number). ✅ gate5_adel CHARACTERIZED 6/18: all CODE deliverables shipped+verified — A naked-position remediation (live, seen working in the RCAT case + order_manager/trade_stream), B/C/G already shipped (G = deploy gate [5c/7]), D stuck_fill_watchdog (_stuck_fill_watchdog_job scheduler:1254, registered), E schema column-type regression (test_db_update_prepare.py + deploy gate [5b/7]); only F = operator post-mortem sign-off pending. **FINAL LAUNCH SCOREBOARD (6/18, 4d out): P0s ✅cleared · GATE 2 ✅verified (ratify) · gate5 ✅(F sign-off) · GATE 3 ✅ RE-RUN 6/18 (sip_replay_r_cohort.py, current grown cohort) = **GO-supportive (reduced-size), verdict HOLDS**: selection delta **+2.18R** (synth-CANCELLED +1.18 vs synth-FILLED −1.00 same-exit; was +2.27R), gap_through=0 (26 cancelled→15 would_have_filled = 100% reachable), REAL-only N=23/−0.45 E[R] confirms the raw −5.64R is the IEX selection ARTIFACT not a broken edge; edge concentrated (drop-top-3 survives, crosses 0 at 4) → START-SMALL; 6/6 winners convincing fills. Doc refreshed. Decision-grade DIRECTION (not a pinned live E[R]); reliability gates separate.** Net: ALL launch gates now GO-supportive/verified; launch readiness gates on START-SMALL sizing + operator sign-offs (GATE-2 ratify, gate5-F, the GO/size call). #291 launch-critical subset = DONE. ✅ OPERATOR SIGN-OFFS 6/19: GATE-2 (partial-trail) RATIFIED green · gate5-F (IBM 5/27 post-mortem) SIGNED · #54 (RMV phase-2 review) RATIFIED closed. REMAINING launch sign-offs = the 6/22 GO/size call (decision; does NOT auto-flip to real money — see below) + the NEW #344 live-flip (after shadow-validation, CHANGE_PROCESS). Current state confirmed 6/19: magna53 phase=paper, live_real_enabled=f, multiplier 1.0, cap NULL; ENABLE_LIVE_MODE=false (live account not yet wired) — so go-live is a deliberate staged config+deploy, not a switch the GO decision throws.
- #341 | 2026-06-23 | pending | #267 chart-vision eval infra — REJECT cohort staleness + a builder. deadcat_cohort.csv is STALE: 2025 dates, mi_ep_alerts only retains 2026-03-16+ (those tickers absent) → 0 graded. 6/18 rebuilt ad-hoc from 2026 data (worst HIGH alerts fwd_5d≤−8%, reject_2026.csv in-container). PERMANENT FIX = a build_deadcat_cohort.py sibling to build_clean_breakout_cohort.py (DB-window-aware, regenerates the adversarial reject set from retained data) so the eval is turnkey + can't silently go stale. (The close_price→close eval SQL bug already fixed a47f067.)
- #343 | 2026-06-27 | in_progress | Chart-vision axis SHADOW (post-#267 eval, operator 6/18, $30 approved). #267 result: text framing dead (B−A=0), chart 2/2 operator-correct but thin (C−B=2/36). Judge is LOAD-BEARING on paper → promoting into _build_judge_prompt would feed paper EP grades; too thin at N=2. ✅ BUILT (this session, not yet deployed): new `agents/market_intelligence/chart_axis.py` SINGLE-SOURCES the candidate notes (CHART_AXIS_NOTE / _TEXT_ONLY, cut-paste byte-identical from the eval) + modal_stable + render_prior_day_chart + grade_b_c (B text-only vs C +chart, fresh ×3 each); eval_chart_judge.py now IMPORTS them (arm A stays inline) so the two surfaces can't drift. `_chart_axis_shadow_job` (17:50 ET mon-fri) grades today's HIGH+MODERATE (cap 8/day), emits `chart_axis_shadow_graded`/`_delta`/`_norender` audit (delta detail carries floor/b_modal/c_modal/direction/BOTH rationales/png_path); SHADOW invariant = audit-only writes, own client+sem, fail-open, off the 9:45 path. `_chart_axis_shadow_weekly_digest_job` (Sun 19:30) sends each delta's chart inline via new briefing.send_telegram_photo + running N vs 7/31 backstop. db helpers: get_prior_daily_ohlcv / get_chart_axis_shadow_cohort / _processed_tickers (idempotency) / _delta_count. Tests: tests/test_chart_axis.py (12 green incl. note pin). DECISION GATE (registry chart_vision_axis_shadow_decision): N≥10 deltas OR 2026-07-31 → promote-load-bearing (CHANGE_PROCESS, ADR 0011) vs hold. ✅ DEPLOYED + VERIFIED-LIVE 6/18 (commit 1ed704f, deploy.sh both, DEPLOY OK all 7 preflight; apollo-market recreated on HEAD; job partition kept 46 = +2 [chart_axis_shadow + _weekly_digest], neither removed). Read-only cohort+render probe vs real 6/18 data: 2 HIGH candidates (UUUU/SWBI), BOTH charts rendered (111KB/102KB) — cohort SQL + prior-day render proven live; the grade-with-chart path was already live-proven by the 6/18 eval (same chart_axis fns). ✅ ADVISOR-HARDENED 6/18 (commit c804d30, redeployed both DEPLOY OK): (Gap1, BLOCKER) delta PNGs were on ephemeral /app disk → wiped every deploy → Sunday digest would lose the chart to label; FIXED = digest RE-RENDERS from the audit row's ticker+alert_date (render is deterministic), job saves NO files. (Gap2) confirmed MODERATE persists in mi_ep_alerts (21d: 56 HIGH / 17 MOD ≈ 4.9/day → matches $30 economics). Split into date-param _run_chart_axis_shadow core. INTEGRATION SMOKE on real 6/18 cohort (UUUU/SWBI): 2 graded markers, 0 deltas → full render→grade B/C×3→modal→audit-write path proven live, 0 pollution (graded uncounted; only _delta counts). FIRST REAL (counted) FIRE = Mon 6/22 17:50 ET (Fri 6/19 = Juneteenth holiday → empty no-op). This row closes when the registry gate decides promote/hold.
- #342 | 2026-06-23 | pending | Deploy/image gap — repo-root eval CSVs (deadcat_cohort.csv etc.) are NOT copied into the apollo-market image (Dockerfile COPYs agents/ + scripts/, not root CSVs) → /app/deadcat_cohort.csv was missing; worked around 6/18 via docker cp. FIX = move the eval cohorts under scripts/ (covered by the COPY) OR add the root CSVs to the Dockerfile. Prevents the next "file not in container" surprise.
- #340 | 2026-07-01 | pending | DQ delta-check (the more-correct long-term design, deferred from #339; operator chose self-heal for the rare-step case): alert on a >X% night-over-night DROP vs the trailing median rather than vs a frozen baseline — catches sudden real drops AND auto-adapts to intended step-changes without the deadlock. Scope: applies to both ingest + rs_engine + sector checks; pick X; keep a one-time transition-window alert so an intended step-down is NOTED once, not silent.
- #273 | 2026-07-15 | in_progress | LLM credit-exhaustion alerting — decorator-hook sweep + spend telemetry + low-balance warning
- #287 | 2026-06-23 | pending | partial-exit trade-state cleanup (FPS #183 stuck flag + 8-trade exits double-encoding) — operator-gated
- #278 | 2026-06-23 | pending | deploy.sh preflight role-aware (intelligence false-fail), low-urgency
- #279 | 2026-06-23 | pending | W2 split /simplify deferrals (derive _EXEC_HANDLERS; bidirectional partition guard; dedup feed resolver)
- #280 | 2026-06-23 | pending | staging own paper account (before any market-hours staging use)
- #281 | 2026-06-23 | pending | staging copy-not-derive hardening (restore fork + compose/env drift)
- #256 | 2026-06-23 | in_progress | #256 W4 closure (2-svc DR runbook · per-service uptime · nightly staging-restore)
- #176 | 2026-06-23 | pending | SoT/roster drift tooling — create-time hook + scheduled ping (residual)
- #216 | 2026-06-23 | pending | jsonb codec consolidation (root-fix of #179) — POST-launch by design (protects window)
- #172 | 2026-06-23 | pending | partial-exit /simplify refactor (blocked behind #151 architectural split)

## Apollo Trades dashboard (portfolio-app2)

- #194 | 2026-07-15 | pending | daily auto-export of BOTH snapshots (trades + themes) — needs deploy key
- #185 | 2026-07-15 | pending | corrupt-stop exclusion-count display fix

## Apollo Themes (portfolio-app2)

- #193 | 2026-07-15 | in_progress | RS theme rank-evolution tab (snapshot freshness rides #194)
- #315 | 2026-07-15 | pending | RS-theme dashboard R-items (memory rs-theme-dash-backlog): R1 mobile/public-URL (Tailscale) · R2 bump chart · R3 canonicalization · R4 forward returns

## Apollo v1.1 — fast-follow program (spec: docs/roadmap/apollo-v1.1-v2.0.md PART I)

_W1 selection-replay + kill/scale = #268 (done) + #275; W2 entry-mechanics = #276; W4 chart-vision = #267 (Launch project). Below = the waves not yet broken into tasks._
- #306 | 2026-07-01 | pending | v1.1-W3 exit & management: winner-harvest tune (partial size · trail-by-character · capture_pct KPI) + #91 time-stop refinement — trade-state, #151 harness discipline
- #307 | 2026-07-08 | pending | v1.1-W5 experience-seed ritual: weekly operator-labeling cadence + precedent-retrieval shadow (with #219, #254, #255)
- #219 | 2026-07-08 | pending | weekly verified-miss review → RITUAL (systematic missed-winner labels feed W5)
- #254 | 2026-07-08 | pending | ground-truth label corpus (the axis-3 "accumulated experience" substrate)

## Apollo v2.0 — tier-one trader (horizons H1 Q3'26 · H2 Q4'26 · H3 2027 · spec: apollo-v1.1-v2.0.md PART II)

- #308 | 2026-08-01 | pending | P1 Experienced Judge (H1→H2): precedent corpus at scale + self-review→rubric-distillation loop + ensemble/uncertainty (#301 seed) + auto-growing probe library
- #309 | 2026-08-01 | pending | P2 Full Sight (H1→H2): multimodal (#267 daily→intraday→peer/SPY) + intraday narrative radar + tape features (#299) + negative-catalyst (#238)
- #310 | 2026-10-01 | pending | P3 The Manager (H2): management judge mature (#300 shadow→load-bearing) + structure-stops via PIVOTS (docs/methodology/pivots-and-stock-character.md) + conviction sizing
- #311 | 2026-08-01 | pending | P4 Multi-Setup Book (H1→H2): per-setup judges + tightness→expansion graduation + 9M→flag pipeline + parabolic-short counter-regime book + regime-adaptive selection
- #312 | 2026-10-01 | pending | P5 Capital & Autonomy Ladder (H2→H3): unified_allocator live + formal shadow→paper→live ladder w/ auto-DEMOTION on breach + scaling/slippage telemetry
- #313 | 2026-08-01 | pending | P6 Institution-grade ops (H1→H2): #256 split (done) + replay-everything CI (#302) + cost governance (per-role LLM spend vs P&L)
- #314 | 2026-07-15 | pending | Trading-ideas detector book (P4 detail): TI1 parabolic-short · TI2 wick-fill (#283) · TI3 fishhook U&R · TI4 base+catalyst convergence · TI5 post-EP shape classifier · TI6 RMV (#54) — graduation gates in data_gated_reviews.yaml; detail in trading-ideas-backlog memory

## Spec / detail docs (PLAN.md is the INDEX — every program above; detail + evidence live here, referenced)

_Launch DoD: docs/roadmap/launch-2026-06-22.md · v1.1/v2.0 map: docs/roadmap/apollo-v1.1-v2.0.md · ADRs: docs/decisions/*.md · evidence-gated reviews (weekly auto-surface): data_gated_reviews.yaml · setup SSoTs: docs/setups/*.md · methodology: docs/methodology/*.md · idea detail: trading-ideas-backlog memory. If a program isn't a task line above, it isn't planned — add it._

## Miscellaneous (no home project)

- #121 | 2026-06-23 | in_progress | shared Telegram formatting layer (consolidate ≥3 escapers, re-home off scheduler.py)
- #195 | 2026-06-23 | in_progress | SECURITY — rotate portfolio-app2 app_password (operator action)
- #334 | 2026-07-15 | pending | LOW-PRI theme-revive cooldown latch NOT YET WIRED (theme_engine.py:322, surfaced in 6/18 prose sweep): the one-way-latch arm is inert because no `revived_at` timestamp is persisted on mi_themes, so the only caller passes days_since_last_revive=None. Wire when revive goes live (persist revived_at, thread the delta). Filed not-dropped per the no-floating-prose directive; genuinely minor.
- #248 | 2026-07-15 | pending | regime engine: post-pullback hysteresis + spy_vs_200ma NULL fix + stale docstring
- #318 | 2026-06-27 | in_progress | morning-brief economic-calendar. ✅ ET FIX WORKED (6/18 brief showed "12:30 PM ET" etc.). BUT operator caught a NEW bug 6/18: calendar showed bare TIMES with NO event names (content-less) — the ET-hardening over-indexed the prompt on time-format. FIXED 6/18: prompt now requires '<time> ET — <event name>' per line + a parsing guard drops content-less bare-time lines (regex-tested). ✅ DEPLOYED 6/18 (deploy.sh both → DEPLOY OK, both containers recreated). VERIFY at 6/19 9 AM brief: named events shown.
