# PLAN — the single source of truth (projects · tasks · ETAs)

**This file is THE plan.** Every task lives here, under a project, with an `ETA` date and a `status`.
The long-horizon plan (the 6/22 launch) lives here too — as dated tasks, not a separate doc. There is no
second plan surface: the calendar is phone reminders only, the harness #-task list is a session scratch
mirror, `CHANGELOG.md`/git hold history. If it's planned, it's a line here.

**Line format** (strict — parsed by `scripts/check_plan.py`):
`- #<id> | <YYYY-MM-DD ETA> | <status> | <title>`   ·  status ∈ pending · in_progress · blocked

**The two rituals operate ONLY on this file:**
- **OPEN** (every session): run `python scripts/check_plan.py --today` → it prints OVERDUE + due-today open
  tasks. That list IS the day's plan. State it before reacting to anything.
- **CLOSE** (every session): every open task's ETA must be ≥ today. Rebump any past-due one to a real
  future date (or close it). File every new item as a line here under a project. Then commit — the
  pre-commit gate (`check_plan.py`) REFUSES the commit if any open task lacks a project, an ETA, or has a
  past ETA, or if an open harness task is missing from this file. No gaps, mechanically.

_Last CLOSE: 2026-06-16._

---

## Launch — 6/22 GO/NO-GO (the long-horizon plan, as dated tasks · SSoT-spec: docs/roadmap/launch-2026-06-22.md)

- #261 | 2026-06-20 | in_progress | scripts/ namespace reorg. SAFE SUBSET DONE 6/17 (5117ed1): 45 throwaway probes → scripts/probes/ (0 pipeline refs; 2 safety greps caught 19 false-throwaways = 4 imported shared modules + _270_* cluster + 4 code-referenced + YAML-cited review tools, all KEPT at root; operator's _reconcile left alone); 6 docs swept; 887 tests pass; no deploy needed. REMAINING (needs a focused block + per-file judgment): ops/evals split of the 94 real tools (touches deploy.sh/CI paths — careful sweep + verify-deploy) + delete-vs-keep the 45 untracked scratch scripts (incl non-_ WIP like analyze_/replay_/backfill_)
- #226 | 2026-06-22 | pending | R3 Lane-2 narrative→mi_themes canonization (advisory; blocked_by #214 naming stability) — rebumped 6/17→6/22 (launch anchor; still blocked)
- #266 | 2026-06-18 | in_progress | theme membership validation at BIRTH. SHIPPED 6/17 (operator-SIGNED): root-cause was discovery never running _validate_theme_membership (only :1754 Mon/Wed/Fri-rescore + :2371 assign-to-existing). Fix = _validate_new_themes_at_birth runs the SAME #213-tuned validator on discovered themes before _save_themes (after name-inheritance), emits theme_birth_validated; min-survivor guard keeps 2-member themes intact; 5 unit tests + CLAUDE.md SSoT same commit. VERIFY-LIVE 6/18 on the AUDIT ROWS (advisor catch): primary signal = theme_birth_validated rows after the next nightly theme run — the existing _theme_birth_validation_evidence.py probe CANNOT see birth strips (its JOIN gates on s.ticker = ANY(b.tickers) but birth-stripped tickers never enter mi_themes.tickers → false negative). Single-night 0 strips = no signal not failure. FOLLOW-ON (separate): identity-change half (re-validate at rename/description-revision); probe fix (drop the membership gate) before it enters the monthly sweep
- #299 | 2026-06-25 | blocked | P2 tape-features. BLOCKED ON FUNDING (operator decision). RIG DONE 6/17 — SLICE A+B shipped, eval rig built + machinery-verified (scripts/eval_tape_judge.py; binding finding: OR÷ATR available+ORB-tradeable for 79.1% of HIGHs; 11 tests; doc tape_judge_eval_299_2026-06-17.md), baked into prod image. FULL RUN DEFERRED 6/17 — requires FUNDING (~$170 full, or ~$50-90 directional first pass; ~3,420 Opus calls). TRADE-OFF of waiting: the judge keeps grading WITHOUT the tape axis (opening-range violence / premarket pace / liquidity) — an ENHANCEMENT we forgo, NOT a regression; no realized harm, the rig is turnkey whenever funded. NEW DATE 6/25 = decide-fund-and-run as the first step of post-launch tape-wiring, clustered with the #320/#321 post-launch grade-quality batch (grade-affecting WIRING is post-6/22 anyway). When funded: run → operator labels deltas (verify on theme/audit, ADR 0011 self-cert gate) → if majority-correct, CHANGE_PROCESS + sign-off → wire tape into live _judge_shadow
- #267 | 2026-06-18 | in_progress | judge chart-vision (W4). PART 1 BUILT+PUSHED 6/17 (47b2e14/72720e7): chart_render.py (mplfinance daily candles+10/20/50 MA+volume, point-in-time through PRIOR trading day — no alert-day candle; eyeballed), judge_transport multimodal image_png (byte-identical when None), grade_holistic threads image_png + chart_note candidate-axis (LIVE byte-identical), scripts/eval_chart_judge.py (TWO-SIDED with/without-chart delta surface, both-arms ×K noise floor, EMITS PNGs, cost print), build_clean_breakout_cohort.py (keep-side, loud-on-empty), deadcat_cohort.csv (66-row reject set), requirements mplfinance+matplotlib, 884 tests. Advisor-hardened (lookahead→prior-day, fidelity→mplfinance, ANCHORED via chart_note, Gap A emit-PNGs, Gap B two-sided). RUN 6/18 = operator-triggered (API spend) per runbook docs/analysis/chart_vision_eval_267_2026-06-18.md: deploy mplfinance → gen+CONFIRM keep cohort → BOTH --cohort flags → operator labels. Promotion of the axis to live _build_judge_prompt = CHANGE_PROCESS + sign-off (load-bearing, ADR 0011) — NOT done in part 1
- #300 | 2026-06-19 | in_progress | P3 management-judge SHADOW (HOLD/PARTIAL_TAKE/TRAIL_TIGHTEN/FORCE_EXIT telemetry, zero authority). ADR 0014 (advisor-reviewed: R off entry−orb_low NOT trailed stop_price; no auto agree-bool=self-cert; snapshot-not-closes at 16:00; part1/part2 cut). PART 1 BUILT 6/17: mgmt_judge.py (assemble_mgmt_inputs/manage_holistic, bounded enum, fail-open, mirrors grade-judge) + mi_position_mgmt_decisions schema + insert_position_mgmt_decision writer + 11 tests (R-trap pinned). PART 2 ✅ BUILT 6/18: run_position_mgmt_judge() (mgmt_judge.py — DB-sourced open positions → live-snapshot price [snapshot_price helper: lastTrade.p→day.c→min.c, NEVER mi_daily_closes per the part-1 QURE stale-close artifact] → per-position manage_holistic → insert_position_mgmt_decision + digest line, per-position fail-open → position_mgmt_judge_null audit); thin _position_mgmt_judge_job (trading-day guard) registered 16:02 ET mon-fri (staggered 2min after the 16:00 entry-technique digest so two snapshot/LLM passes don't collide; still pre-17:00-pull), INTELLIGENCE_OWNED + job-classification guard green; format_mgmt_line/snapshot_price pure helpers + 4 tests, 921 suite green. EMPTY-SNAPSHOT GUARD added 6/18 (commit 1e0d874, advisor): get_snapshot_all→{} on error means every px None → judge grades blind → garbage batch; guard skips the whole pass + audits position_mgmt_judge_skipped (per-ticker None already skips+audits null). STILL DEFERRED (ADR 0014, conditional): time_stop_eligible (#91 peak-excursion) + 9M risk-basis (R safely None for 9M today since orb_low≠the prior-day-low basis — compute_position_metrics returns None). DEPLOYED + SMOKE-VERIFIED-LIVE 6/18 16:22 ET (deploy held during market hours per operator, run after the EOD jobs settled): deploy.sh both green (DEPLOY_EXIT=0, all 7 preflight gates, G6 skipped=closed), apollo-market recreated on HEAD (1e0d874), job retained on intelligence side (partition kept 44, position_mgmt_judge not in removed-27), no boot errors. SMOKE (run_position_mgmt_judge send=False, ~$1): 2 open positions graded — FPS HOLD +8.6R (R off entry−orb_low NOT the trailed stop@59.24-above-entry = the advisor trap, correct), QURE HOLD +0.7R @ live snapshot 46.29 (was a FALSE −8R/FORCE_EXIT in the part-1 stale-close smoke → snapshot_price fix CONFIRMED). Ground-truth: 2 rows in mi_position_mgmt_decisions (FPS 63.41 / QURE 46.29, opus-4-8, conf 0.70/0.66). REMAINING (the only open piece) = the first 16:02 ET CRON fire sends the SHADOW digest to Telegram (Fri 6/19; the job is registered+classified+trading-day-guarded, low risk) → then operator labeling of agree/disagree-with-mechanical begins accruing.
- #255 | 2026-06-18 | pending | judge precedent-retrieval v1 (kind=review) + periodic-review nudge
- #238 | 2026-06-19 | pending | EDGAR negative-catalyst (dilution S-3/424B5/8-K-3.02) sourcing
- #301 | 2026-06-19 | pending | P1 ensemble-divergence SHADOW (2nd-model verdict on HIGH-tier)
- #302 | 2026-06-20 | pending | P6 replay-regression v0 (weekly scheduled selection-replay report)
- #55  | 2026-06-20 | pending | revenue-stage threshold quarterly review (REVENUE_STAGE_MIN_USD)
- #303 | 2026-06-20 | pending | full-program advisor review + launch-readiness checklist + DR restore rehearsal
- #304 | 2026-06-21 | pending | launch rehearsal (staging→prod) + GO/NO-GO evidence pack + docs/CHANGELOG
- #305 | 2026-06-22 | pending | 🚀 6/22 GO/NO-GO decision with operator (NO-GO under signed rules = launch-complete)

## Live-money cutover — MAGNA53 (the GO-direction gates)

- #150 | 2026-06-25 | in_progress | Alpaca stop-trigger / share-reservation race — explicit sell-retry fix. CONFIRMED SHIPPED 6/18: `order_manager._is_share_reservation_lag()` + bounded retry (held_for_orders / "insufficient qty available") present + mature in HEAD since the FTRE 2026-05-09 incident. Remaining (verify-live) = observe a clean retry on the NEXT real share-reservation-lag event (event-driven, can't force) → then close. Live-money-promotion gate.
- #151 | 2026-06-19 | in_progress | partial-exit hardening N=7 clean cycles (stalled on quiet flow, not failures)
- #183 | 2026-06-19 | in_progress | ORB classifier IEX/window mislabel
- #184 | 2026-06-20 | in_progress | broker-authoritative trade-state mirror (ADR 0008) increments 2/3
- #225 | 2026-06-20 | pending | resolve 3 demotion residuals (order_manager L584/905/1575) + blocking deploy gate
- #275 | 2026-06-19 | pending | kill/scale band digest evaluation + band-transition alerts + override awareness
- #316 | 2026-06-22 | pending | PDT / SEC-FINRA Rule 4210 — confirm ALPACA's rollout (not Fidelity's) + relax BLOCK_PDT_LOCKOUT via CHANGE_PROCESS (memory pdt_rule_4210_change_2026; revisit at cutover)

## Family A — consolidation plays post a runup (ADR 0013 · reset of #270)

- #270 | 2026-06-20 | in_progress | Phase 1 BUILT + VERIFIED 6/17 (new mi_anticipation_consolidation table + evaluate_consolidation + select_consolidation_keys carry-forward + 13 tests, all SHADOW). Live probe GREEN: COO IN on pure gate (1.153), proposer/confirmer agree, carry-forward absorbs the real 7/71 anchor drift. UN-PAUSED 6/17 (decision-4 fast-follow): consolidation_readiness job registered (17:35 ET mon-fri) + classified in INTELLIGENCE_OWNED_JOB_IDS + /anticipation board rewired (lifecycle→consolidation), 836 tests green. VERIFY-LIVE 6/18: confirm mi_anticipation_consolidation rows written after first 17:35 ET run + no dup non-aged rows per ticker (carry-forward) via mi_job_runs + confirm the newly-COILED digest renders the UNIFIED /anticipation row format (de.format_consolidation_row single-source, deployed 5599dbc — operator-flagged drift fix 6/17). ✅ VERIFY-LIVE DONE 6/18: (1) rows written — live /anticipation board render (via the app path) showed ~23 consolidation rows (6 coiled + 17 post-runup), all DISTINCT tickers; (2) no-dup carry-forward — the un-pause gate test_carry_forward_absorbs_real_615_to_616_drift is green on the real 6/15→6/16 drift snapshots (the authoritative check; mi_job_runs psql cross-check is operator-gated + redundant given the test + distinct-ticker render); (3) unified digest — the daily Family-A digest now renders via format_consolidation_row (plain-words consolidation this session). Phase 1 verified-live; Phase 2 = #289.
- #327 | 2026-06-20 | in_progress | **#270 Step 3 — consolidation entry-watch + shadow-settle + 9M-universe feed (critical path for #326).** ✅ PHASE A (the #326 DIRECTIONAL READ) DONE 6/18 — replay-first (live shadow can't reach decision-grade N by 7/7). Symmetric two-arm replay over the 121-row mi_9m_day2_candidates cohort (Day-2 ORB vs consolidation FIRST5-break, same SETTLE_RULE/forward window, full-universe expectancy, both-ways universe, MFE-free via anticipation.simulate). scripts/_327_replay.py + _327_pull_minute.py + _327_dump.py; doc docs/analysis/ninem_consolidation_vs_day2_replay_327_2026-06-18.md (advisor-hardened: within-name cut + split claims). VERDICT: **Claim A (SOLID) — Day-2 ORB earns NO robust edge** (filled median −0.24R, +3.2R total all 3 outliers, −1.4R on the 17 consolidation names) → carries the #326 RETIRE half; **Claim B (PROMISING, selection-inflated → Phase-B-gated) — consolidation better** (+2.00R median, 82% win, broad). Mechanism = stop tightness (first5-low vs wide prior-day-low). ✅ PHASE B — OFFLINE caveat-#1 correction DONE 6/18 (advisor-reconciled): extended _327_replay.py (consolidation_scan exposes every post-coil day w/ as-of base_high; Phase-A confirmed-close numbers byte-identical) + wider 266-day minute pull → re-timed Arm-2 to the FIRST intraday OR-break a live watcher takes. ⚠ MATERIAL FINDING (changes the #326 replacement narrative): **Claim B's +2.00R was an ENTRY-MODEL ARTIFACT, not a live edge** — inflated jointly by (1) daily-close selection (20/35 names break intraday on an EARLIER, weaker-close day; filled median +2.00R→−1.00R re-timed) AND (2) a 2%-sub-base OR tolerance (only 7/17 Phase-A "fills" had OR5-high actually ABOVE the base). De-rated, consolidation (+3.7R loose / +5.0R genuine, both outlier-carried) drops into the SAME break-even bucket as Day-2 ORB (+3.2R) → the QUANTITATIVE case for "consolidation is the strong replacement" is GONE. HARNESS LIMIT (explicit): every Arm-2 variant is an OR-break-NEAR-base proxy, NOT the live all-day volume-confirmed #94 watcher (blind to mid-session base breaks) — so it CANNOT settle whether the real watcher has an edge; no single de-rate number is faithful. **Claim A (retire Day-2 ORB) UNCHANGED/solid** (Arm-1 has no base-break gate). RESOLUTION = the live forward shadow with the REAL #94 detector is now the ONLY instrument that can answer the live-edge question → measure-before-wire JUSTIFIES (not moots) the live wiring. ✅ ENTRY-SIGNAL STUDY DONE 6/18 (operator "what signals the best entry"): scripts/_327_entry_signal.py — parameterized coil-readiness (rmv+range+vol gate × consecutive-day COUNT × stop), stop-matched curves not an argmax cell. FINDING: **PERSISTENCE (N consecutive tight days), NOT rmv DEPTH, times the entry** — N=3 lifts capture 32%→44% + median uncapped MFE +0.8R→+2.1R, STOP-MATCHED + robust across the rmv/range/vol knobs (Δ+9–12pp); VANISHES under over-tight level gates (range≤4% Δ0). Robust REGION: range≤5–7%, vol≤1.0×, rmv≤30–40, N=2–3. ✅ FORWARD SHADOW WIRED 6/18 (operator "wire it, we have potential edge, let's not wait"): new mi_consolidation_entry_shadow table + anticipation.entry_signal_at (the SINGLE-SOURCE signal — the offline sweep delegates is_tight to it, pinned by tests/test_consolidation_entry_signal.py, 8 tests) + entry-watch folded into _consolidation_readiness_job (17:35 ET, one shadow row per OPEN coil, open-dedup pins first-fire, 9m/family_a origin tag, point-in-time) + Telegram digest. Stop recorded coiled_low (validated default) + structural_low (alt) for later settlement. ALL SHADOW (zero execution). ✅ VERIFY-LIVE DONE 6/18 (deploy b8245ec, both): write-probe proved the insert path (insert✓ open-dedup✓ CHECK✓ self-cleaned) + the real job wrote **18 sane backfill rows** (all N=3, rmv≤28, range 1.8–6.0%, vol≤0.98×, both stops, HLIT correctly tagged [9m]) + a 2nd run held at 18 (live open-dedup confirmed). Container fresh on HEAD; job 17:35 ET mon-fri (un-paused) → tomorrow's run is the first scheduled fire. ✅ SETTLEMENT MACHINERY BUILT 6/18 (operator "build the machinery now, turnkey before data lands"): anticipation.entry_bet_outcome (the validated asymmetric bet — capture/stop/open + uncapped fwd_mfe_r under coiled_low, 12-bar horizon; the offline sweep's bet_outcome now delegates to it, pinned) + settle_entry_shadow (abstains <12 fwd bars, else outcome+fwd_mfe_r+realized_r; realized_r REUSES the tested settle_row +1R/+3R day-5 harvest) + db settle/fetch/summary helpers + _consolidation_entry_settle_job (17:40 ET mon-fri, registered + INTELLIGENCE_OWNED) + tests/test_consolidation_entry_settle.py (11 tests). SURVIVORSHIP FIX (advisor 6/18): settle on DEFINITIVE outcome (capture/stop final at any bar count; only 'open' waits the full window) so a stop/capture-then-HALT name isn't orphaned (would silently bias capture%); the residual halt-while-open is surfaced via get_consolidation_entry_shadow_summary.open_overdue_n (orphan canary, never silent). Turnkey: settles automatically once a row reaches the 12-bar window (~first settles ~2026-07-07). ✅ VERIFY-LIVE DONE 6/18 (deploy 575b412): all 4 first-run SQL paths clean on real Postgres — settle-probe (UPDATE write-back + double-settle guard + open-dedup-free), run-settle (get_settleable + clean 0-settle), summary SQL (READOUT "18 open / 0 OVERDUE / 0 settled", by-origin splits clean, medians null until ripe). 17:40 ET job registered/fresh. ✅ SURFACE CONSOLIDATION DONE 6/18 (operator "consolidate, don't add a new command every time" + plain-words readability; AskUserQuestion: Family-A-only + plain words): instead of adding /readout, FOLDED the whole lifecycle into ONE board + ONE digest (6 surfaces → 2). /anticipation now shows edge-header (the readout) → 🎯 entry fired → 🪙 coiling → 👁 post-runup → 📐 settled; the 3 separate daily pushes (🪙🎯📐) collapse into ONE unified Family-A digest by folding settlement INLINE into _consolidation_readiness_job (removed the standalone settle job + its INTELLIGENCE_OWNED id → _run_entry_shadow_settlement helper). format_consolidation_row rewritten to plain words (+70% · coiling 14d · very tight · tightening↓) + new format_entry_fired_row/format_entry_settled_row (single-source, all pinned). RULE going forward: new lifecycle stages = sections, never new commands. ✅ VERIFY-LIVE DONE 6/18 (deploy 12d8aff): /anticipation board rendered off the 18 live rows — edge header + 🎯 entry-fired (12, HLIT tagged 9M, stop %s) + 🪙 coiling (tightness word) + 👁 post-runup (runup + "Nd since peak", tightness word omitted — a live-render readability fix, the rmv-axis would contradict the not-coiled state) + 📐 settled (0). ⏭ REMAINING: nothing until data accrues → OBSERVE-ONLY, first settles ~7/7 answer the #326 live-edge. Execution-side/split-adjacent
- #289 | 2026-06-18 | pending | folds into Phase 2 (shared universe+coil) — general-anticipation selection/ranking layer
- #297 | 2026-06-23 | pending | FAMILY B EP rework — inherits the gap-anchored replay()/evaluate_candidate + mi_anticipation_lifecycle + ARCHIVE/clean its phantom rows (decoupled from #270 Phase 1). 9M-ENTRY retirement (Day-2 ORB → consolidation) is the dated cut-over decision #326 (9/15)
- #283 | 2026-06-23 | pending | wick_fill promotion eval (shadow→live), gated behind Family A

## Stocks in Play — detection / setups / entries / 9M / flags

- #326 | 2026-06-20 | in_progress | **DECISION SIGNED 2026-06-18 (operator): RETIRE 9M Day-2 ORB → shadow** (pulled in from 7/7 on the #327 Claim-A read; option A/deprecate in ninem.md). CHANGE_PROCESS done (ninem.md change-log, REVERSAL of 5/31 documented). (1) ✅ FLIP EXECUTED 2026-06-18 (`/strategy 9m_day2 demote` → DB-confirmed phase=shadow, enabled=t). REMAINING (= verified-live): (2) verify next 9:31 ET `_9m_day2_orb_job` resolves shadow / no paper submit (tomorrow); (3) #327 Phase B consolidation shadow stood up as the replacement (so the cohort isn't dark). Original task text below. — 9M Day-2 ORB entry → RETIRE for consolidation entries (CUT-OVER DECISION, operator vision). MOVE FAST (operator 6/17: weeks not months; all SHADOW/PAPER = no real $ at risk in the swap). CRITICAL-PATH BUILD = #327 (the consolidation entry-watch + shadow-settle + 9M-universe feed → Day-2 ORB & consolidation run PARALLEL on the same names; reuses #270 recorder + #94 flag-break + #270 harvest, all LIVE/BUILT — integration not from-scratch). DECIDE ~7/7, PULL EARLIER on a clear DIRECTIONAL read (operator judgment, NOT a rigid N) → retire 9m_day2 to shadow (sign-off + CHANGE_PROCESS — that gate stays). COUPLING (only real-$ caution): BLOCKS the 9M Day-2 → live-money promotion (Q3); acceleration is shadow-only. Registry ninem_day2_retire_for_consolidation. **6/18 DIRECTIONAL READ IN (#327 Phase A): supports RETIRE (Claim A — Day-2 ORB no robust edge, solid) + consolidation as replacement (Claim B — promising, Phase-B-shadow-gated). 2 OPERATOR DECISIONS surfaced: (1) the GO/timing on retire; (2) SCOPE — consolidation fires on only ~⅓ of 9M names, so retiring Day-2 narrows the entered universe to ⅓; decide whether the other ⅔ (no consolidation setup) keep an entry path or go unentered. See the doc's "Read for #326".**
- #286 | 2026-06-20 | in_progress | RS liquidity floor — EVIDENCE GATHERED 6/18 (operator-approved prod read): the $10M floor WORKS (0 names <$10M scored). The issue = thin names in the $10–20M band ranking high — 395 names in-band, 10 in RS top-100 (OCC #4 $12.7M, QMCO #8, HPP, TRT, ELMT, ARQQ, ATLC, URGN, PRCH, INV). KEY CONTEXT: the entry detectors have their OWN higher liquidity gates (9M $50M dvol, EP own) → these thin names get RS-scored for context but are NOT traded → a floor raise is LEADERBOARD/signal hygiene (keeps junk out of briefings + theme-discovery inputs), NOT a trade-safety fix. ✅ DECISION 6/18 (operator): KEEP $10M — no change. Rationale: the floor works (0 below), and the thin-$10–20M-names-ranking-high is COSMETIC (entry detectors gate at $50M+ so they don't trade); a raise would lose legit small-cap momentum from the RS context for only leaderboard cleanliness. RESOLVED — no code change. (Close-pending snapshot refresh.)
- #271 | 2026-06-19 | pending | breadth-extreme detector wiring (analysis done; feed calculate_breadth_full + /regime cells)
- #168 | 2026-06-23 | in_progress | shadow-detector quality/actionability filter for LIVE-ping graduation (evidence-gated)
- #167 | 2026-06-23 | in_progress | Lane-2 narrative theme detector (shadow) — ongoing
- #170 | 2026-06-23 | in_progress | (Stocks-in-Play backlog item — confirm scope at triage)
- #56  | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #60  | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #65  | 2026-06-23 | pending | per-strategy sizing/cap follow-ups
- #81  | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #97  | 2026-06-23 | in_progress | (SiP backlog — confirm scope/title at triage)
- #113 | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #115 | 2026-06-23 | pending | /simplify deferral (filed #115)
- #116 | 2026-06-23 | pending | /simplify deferral (filed #116)
- #134 | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #146 | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)
- #178 | 2026-06-23 | pending | (SiP backlog — confirm scope/title at triage)

## Judge & catalyst — v1.1 program

- #284 | 2026-06-18 | in_progress | M&A acquirer-direction title-leak fix — awaiting operator sign-off (HARD-gate list) + shadow-validate
- #285 | 2026-06-19 | pending | graduate M&A accuracy review into monthly backward-check sweep (auto-run + Telegram)
- #317 | 2026-06-19 | in_progress | EP-alert `catalyst` field contradicts the judge. ROOT CAUSE LOCALIZED 6/18: `ep["catalyst"] = news_summary[:500]` (ep_detector.py:2248/2282) = the Perplexity DISCOVERY narrative, a DIFFERENT text from `claude_analysis` (the judge's grounded rationale shown in italics). 6/18 reconfirm (SWBI): judge graded HIGH on the 8-K earnings, but the "Catalyst:" line read "headline-light newsflow… positioning/flow move rather than a clean catalyst" (contradiction) AND was cut off mid-sentence ("1) Sector and…" = the 300-char _truncate_sentence). IMPLEMENTED 6/18 (option A SUPPRESS, operator-chosen): threaded `has_direct_source` onto the ep alert payload (ep_detector result dict) + send_ep_alert suppresses the "Catalyst:" discovery line when has_direct_source AND claude_analysis present (the grounded catalyst is the italic; safe default keeps the line otherwise — never zero catalyst). Compile + 100 alert/grade tests green. LIMITATION/follow-on: has_direct_source is NOT in the _catalyst_cache tuple (4 pack sites), so a cached MODERATE→HIGH re-grade loses it → catalyst line kept (safe default, not a regression); add to the cache tuple for full coverage = follow-on. NEEDS market-agent DEPLOY + VERIFY-LIVE on the next direct-source HIGH (catalyst line absent, no contradiction). Pre-6/22.
- #319 | 2026-06-19 | in_progress | EP-alert grade-coherence (judge load-bearing). 6/17 fixes DEPLOYED + 6/18 VERIFIED on live alerts (operator-confirmed: headline resolves to judge verdict; grade-provenance Floor·Perplexity·Judge coherent — SWBI "strong(Claude)·routine Pplx ✗differs·Judge HIGH←authoritative", UUUU "game changer·✓agree·Judge HIGH"). NEW finding 6/18: the judge-inferred THEME line shows only a generic "judge-inferred (not in a tracked cohort)" string (briefing.py:2331) and NEVER the actual inferred theme NAME (UUUU's rare-earth/uranium/defense-policy is in claude_analysis but not surfaced) — because the judge emits NO inferred-theme field. REFRAMED 6/18 (operator catch): the engine ALREADY surfaces real themes — get_theme_membership reads mi_themes; the judge already weighs theme in the grade (#249). UUUU's "judge-inferred/not-tracked" is a #325 COVERAGE SYMPTOM, not a missing feature: the "Rare Earth Magnets & Critical Battery Materials" theme is ACTIVE (Mainstream) but UUUU was never assigned to it (theme shrank 4→2 members 6/11→6/17). So DO NOT build a judge-inferred-theme-name band-aid — it would MASK #325. The real fix = #325 (membership/coverage). #319's shippable alert fix today is the #317 catalyst-suppress (A). 6/18 ALSO SHIPPED (operator-directed): the EP-alert theme line now EXPLICITLY distinguishes BLIND-SPOT (judge inferred a theme/narrative but the engine tracks NO cohort → flag as possible coverage gap, #325) vs STANDS-ALONE (no engine cohort AND no judge-inferred theme → genuinely a single-name move) — was a bare "Theme: —" that conflated the two (briefing.py send_ep_alert). Separate latent gap (theme as a SCORED rubric INPUT) NOW FILED as #328. (SWBI "Theme: — stands alone" is CORRECT — catalyst axis only, judge lit no theme.)
- #320 | 2026-06-25 | pending | DEFERRED post-launch (operator 6/17, plan docs/analysis/grade_quality_deferred_320_321_plan_2026-06-17.md). Stale confidence_multiplier after a floor downgrade. GRADE-AFFECTING — reset can flip floor_tier + the score<50 PRE-JUDGE skip. WAIT = don't shift trade set pre-6/22; data exists (grade audit). Validate: how many carried the stale boost, how many drop below threshold, did the judge want them (pre-judge gating loss). COUPLED to #233 (display-only Perplexity supersedes this). Backtest wk of 6/23, decide by 6/25. COST OF DELAY: occasional false-positive alerts (small). 
- #321 | 2026-06-25 | pending | DEFERRED post-launch (operator 6/17, same plan doc). Missing prior-year YoY = SHARED ROOT of rubric=None AND the spurious strong→routine downgrade (LZB 6/17). Wire deterministic yfinance YoY (already shadow-recovered, #149) into rubric + downgrade gate. WAIT = don't shift trade set pre-6/22; data exists. Validate: coverage, flip-rate, correctness. UNKNOWN TO ANSWER FIRST: is yfinance reliable enough to be load-bearing? (spot-check error rate). Backtest wk of 6/23, decide by 6/25. COST OF DELAY: every earnings day we keep wrongly downgrading genuinely-strong gappers = MISSED TRADES — the larger, recurring cost
- #322 | 2026-07-01 | pending | theme-detection gap (memory theme_detection_two_lane_architecture): judge inferred JBL's AI-infra theme that neither Lane-1 clustering nor Lane-2 narrative tracks — real coverage gap, not a label. Feed into P2/P4 narrative radar (#309/#311)
- #328 | 2026-06-24 | in_progress | THEME AS A SCORED RUBRIC INPUT (#329 child — the theme axis of the meta-rubric) (operator 6/18, emphatic — "absolutely critical; Pradeep's theme is the #1 catalyst driver; not having this filed was a disastrous miss"). TODAY theme is DISPLAY-only metadata (get_theme_membership on the EP alert) + the judge weighs it QUALITATIVELY in the holistic grade (#249/#233) — it is NOT a scored axis in the catalyst rubric (catalyst_rubric_runtime score_ep_with_rubric). Meta-rubric vision (user_meta_rubric_architecture + user_pradeep_catalyst_hierarchy: theme=#1, > policy/shortage/sales): theme stage+score should ADJUST the composite/gate (Accelerating→boost, Mainstream sustain, Nascent partial; standalone→no theme credit). GRADE-AFFECTING, judge/floor are load-bearing → CHANGE_PROCESS + operator sign-off on the mapping + N≥10 backtest before any live flip; safe shadow eval first. SOFT-PREREQ #325 (coverage must be trustworthy or we'd score on a blind-spot-ridden signal) + couples to #233 (judge theme-weight) + #322 (detection gap). FIRST DELIVERABLE SHIPPED 6/18: EP alert distinguishes BLIND-SPOT vs STANDS-ALONE for missing themes (briefing.py — see #319). NEXT: design spec (gate-shift vs composite-score input; how blind-spot/standalone feed it; stage→weight table) — scope by ETA.
- #329 | 2026-06-24 | in_progress | META-RUBRIC composition (Phase 5) — ANCHOR the orphaned registry review + reconcile-first (operator 6/18 sweep; the parent of #328/#330/#331/#332). ⚡ ACCELERATED (operator 6/18: "rubric is critical to EPs — we're already trading EPs with the judge live; integrate earlier the better"). ALIGNMENT to the launch spine: the ADVISORY/SHADOW composite is UN-GATED (touches no live grade) → build it NOW, pre-6/22, in parallel with launch rehearsal; the LOAD-BEARING FLIP is grade-affecting → post-6/22 by the established trade-set-stability rule (#299/#320/#321 cluster), tracked as #335. So aggression lands on the shadow build (this task) + a fast post-launch flip (#335), NOT on destabilizing the cutover. NOT "we have no composition layer" — the live holistic judge ALREADY composes theme/catalyst/narrative qualitatively (#249/#240). The real gap: (a) theme/structure/gap-alignment aren't EXPLICIT, calibrated, traceable inputs (judge weighs them implicitly/inconsistently), and (b) the registry review `phase5_meta_rubric_calibration` (data_gated_reviews.yaml: threshold=30, earliest 2026-09-08) had NO PLAN #-task/ETA — yet its OWN text records operator 6/04 DE-GATING it to "ship advisory NOW" (the N≥30 gate only governs weight CALIBRATION, not the advisory build). So the advisory composite was un-gated but never moved for lack of a task = this sweep's headline miss. FIRST DELIVERABLE ✅ DONE 6/18: RECONCILIATION SPEC `docs/analysis/meta_rubric_reconciliation_329_2026-06-18.md` (ground-truth read of grade_holistic/_RUBRIC v3/assemble_judge_inputs + det. rubric + floor). KEY FINDINGS: the judge IS the composition layer but (a) theme STAGE/SCORE is computed + shown on the alert yet NOT passed to the judge (only the bool in_active_theme) — theme heat is invisible to the judge; (b) structure context (MA/resistance/base) isn't passed so clause-5 gap-alignment is unjudgeable; (c) no per-axis traceability. ARCH ✅ DECIDED 6/18 (operator): PATH A = enrich the ONE judge (pass the inputs it's blind to + add `axis_reads` per-axis traceability to the grade_ep schema; calibration rides the operator-labeled eval). Path B's separate logistic numeric composite stays the forward-gated registry cross-check (NOT a 2nd authority). So #330/#331/#328 build JUDGE AXIS INPUTS, not a separate composite. ⚠ NEW FINDING (call-site verify, ep_detector.py:2431): the live judge call does NOT thread `has_direct_source` OR `revenue_stage` (nor theme stage/score) → the prompt renders both as always-"no". Rubric v3 clause 1 LEANS on has_direct_source (=false→prefer-floor skepticism) → the judge applies freshness skepticism UNIVERSALLY + can't credit a fresh SEC-sourced 8-K = LIKELY LIVE GRADE-AFFECTING DEFECT (has_direct_source is computed — threaded for #317 today — just not passed). Judge is LOAD-BEARING (toggle ON 6/10) so the live payload edit is grade-affecting → build+measure on the EVAL ARM, flip via #335. Priority: has_direct_source > theme stage/score > structure. SSoT target when #330/#331 land = NEW docs/setups/meta_rubric.md + own SSoT per signal (catalyst_rubric.md SSoT confirms fundamentals-only + "add signals as separate inputs, each own SSoT"). STEP 1 ✅ SHIPPED 6/18 (fe6206a, live byte-identical): theme_stage/theme_score plumbed into assemble_judge_inputs + rendered on the in_active_theme line when present; eval-only `axis_reads` per-axis traceability (`_judge_tool(include_axis_reads)` deepcopy variant + grade_holistic flag + _normalize passthrough); 2 byte-identity tests lock the live prompt+tool. STEP 2 ✅ BUILT 6/18: scripts/eval_judge_enrich.py — `--size-only` NO-SPEND blast-radius pass (recompute has_direct_source DETERMINISTICALLY from stored grounded_text markers [SEC…/[Benzinga… vs [Web summary], segment out pre-W1 thin rows) + gated `--regrade` two-arm (BLIND vs ENRICHED) with AS-OF-date theme heat (mi_themes theme_date<=alert_date — NOT get_theme_membership which is lookahead) for operator labeling; build_judge_payload gained byte-neutral enrich kwargs; ep_grade_decision now logs has_direct_source (data-readiness for #337); recompute_has_direct_source pure helper + 6 tests, 910 suite green. SIZING RAN 6/18 (45d, no spend): 181/184 ASSESSABLE rows (98%) had a DIRECT source the live judge was shown 'no' for; 126 AT-RISK (direct + promote/material-lean). ⚠ RIGHT-SIZED (advisor 6/18): the 98% is the flag-wrongness rate, NOT the impact — the judge reads the corpus substance INCLUDING the dated marker ([SEC 8-K filed <date>…]), so it can establish source+freshness directly even with the meta-flag stuck at "no". The sizing data itself shows this: it graded HIGH/hold on nearly all 184 → largely ROBUST to the flag. EXPECT FEW has_direct_source flips in the re-grade. So this axis is likely LOW-IMPACT; if the batched re-grade confirms ~zero flips, that's the evidence to DROP has_direct_source from the #335 wire-in entirely (yield discipline working). The re-grade is still run (it measures theme-heat/structure/gap too) — has_direct_source just rides along, it doesn't justify the spend alone. NEXT (operator cost directive 6/18 — DON'T pay per-axis): the sizing pass cost $0 (no LLM). The `--regrade` (Opus spend) is DEFERRED and BATCHED into ONE consolidated re-grade at the pre-#335 checkpoint covering ALL pending payload enrichments at once (has_direct_source + theme-heat here + structure #330 + gap #331 + tape #299 + chart #267 as built) — re-judging the cohort ONCE, not once per axis. See #335. Live wire-in + axis_reads-on-alert ride #335. THEN ship the ADVISORY methodology-ordered composite (theme dominant ≥1.5–2× fundamentals per user_pradeep_catalyst_hierarchy; un-gated) by this ETA. Weight calibration stays forward-data-gated (registry review, now #-linked here); the load-bearing flip = #335. CHANGE_PROCESS + sign-off before any load-bearing flip.
- #330 | 2026-06-30 | pending | TECHNICAL STRUCTURE as an EXPLICIT, CALIBRATED, TRACEABLE scored meta-rubric axis (#329 child; meta-rubric memory item 2; registry D2–D6 prose). NB precision (per ADR-0011): the JUDGE is NOT blind to structure — it already ingests gap_pct/pm_rvol/extension/vol_percentile/score_breakdown as RAW inputs and rubric clause 4 has it "modulate up or down" on structure QUALITATIVELY. AND the deterministic catalyst rubric (catalyst_rubric_runtime) scores ONLY fundamentals axes (revenue+accel/EPS/margin/beat/guidance) — NO structure axis. The gap = a scored structure axis with a calibrated weight + a LOGGED contribution (MAs 10/20/50/200 alignment, 52w-high distance, base shape, prior consolidation, round-number proximity), vs today's raw-signal-into-an-LLM. Data partly in mi_flag_candidates (base age, RS rank, COILED) + mi_daily_closes (MA distances). Gated behind #329 reconciliation spec.
- #331 | 2026-07-02 | pending | GAP-vs-STRUCTURE ALIGNMENT as an EXPLICIT scored meta-rubric axis (#329 child; meta-rubric memory item 3). Does the gap punch THROUGH resistance (institutional confirmation) or fade INTO a congestion zone? Same precision as #330: the judge sees raw gap_pct + structure and can reason about alignment qualitatively (ADR-0011 clause 4) — the gap is a CALIBRATED, traceable alignment score, distinct from raw gap magnitude (already in ep_score). Gated behind #329 spec + #330 structure primitives.
- #332 | 2026-07-25 | pending | METHODOLOGY-FIT / setup-class rubrics (#329 child; meta-rubric memory item 5) — different conviction rubrics for Pradeep small-cap-explosive vs Qullamaggie mature-leader-breakout vs episodic mid-cap-with-neglect. The long pole (needs the per-class composition designed); pulled in from 9/1 on the operator 6/18 accelerate directive, but kept behind #329/#330/#331 since it composes ON TOP of them.
- #333 | 2026-06-27 | pending | CATALYST DURABILITY forward axis (catalyst program; user_catalyst_depth 6/16 Pradeep tweet). The rubric ALREADY scores trailing acceleration (revenue YoY + decel/accel); the GAP is the FORWARD/PROJECTED leg — Pradeep durability = ≥2Q realized + ≥4Q PROJECTED high-revenue-growth (39%-class illustrative; the SHAPE is the point). Needs forward guidance/projection from STRUCTURED data → gated on the direct-sourcing backbone (#210/#211), not LLM prose. Distinct from #269 (revenue-over-EPS weighting of existing axes).
- #335 | 2026-07-03 | pending | ⚡ LOAD-BEARING FLIP of the meta-rubric composite into the LIVE (paper) EP grade — THE integration the operator actually wants ("improve EP trading", we're already trading EPs with the judge). The grade-affecting milestone: the #329 advisory composite becomes authoritative (theme #328 + catalyst-magnitude + catalyst_type, enriched by #330/#331 structure/gap as they land — incremental, flip the core first). EARLIEST = just post-6/22 by the established grade-affecting-wiring-post-cutover rule (#299/#320/#321 cluster — don't shift the trade set right before the live-money GO/NO-GO), then move fast. GATES (non-negotiable, fail-open-to-floor like ADR 0011): CHANGE_PROCESS + operator sign-off on the exact weighting + a DB toggle for instant revert + the BACKTEST = ⚡ ONE BATCHED RE-GRADE (operator cost directive 6/18: don't pay per-axis). Instead of a separate eval-spend run per axis, accumulate ALL pending payload enrichments (has_direct_source #329 + theme-heat #328/#329 + structure #330 + gap #331 + tape #299 + chart #267 as each is BUILT) and re-judge the cohort ONCE at this checkpoint via eval_judge_enrich --regrade — measures every axis's verdict-delta in a single paid run, operator labels the flips. (The $100+ Phase-B replay was 1307 rows; batching avoids re-paying that per-axis.) Clusters with the #320/#321/#299 post-launch grade-quality batch. NOTE: if the operator wants this PRE-6/22 instead, it overrides the trade-set-stability rule — flag for explicit sign-off (my rec: shadow pre-launch, flip immediately after). At the flip, the per-axis `axis_reads` decomposition surfaces on the alert + /why (the richer half of #336).
- #336 | 2026-06-19 | in_progress | EP-ALERT + /why judge-decision TRACEABILITY (operator 6/18: "make the rubric + judge inputs traceable on the alert succinctly so I can tell WHY the rating + if it aligns + for periodic reviews"). DISPLAY-ONLY (no grade change), SHIPPED 6/18: (a) the alert italic now LEADS with the JUDGE's own rationale when authoritative — it was showing the FLOOR's claude_analysis under an authoritative judge (#319 coherence class); (b) materiality_tier + direct-source ✓ folded onto the fire line as the at-a-glance "does this align?" divergence cues (consolidated, NOT a 5th block); (c) /why now surfaces the judge decision trace (tier · direction-vs-floor · materiality · axes · rubric_version · rationale) = the periodic-review surface per ADR-0011 (reuses /why, didn't rebuild); (d) judge_rationale + judge_materiality_tier plumbed onto the in-memory alert dict (ep_detector post-write-success, DB-first like fire_axes). Pure helpers resolve_why_text/format_judge_trace_suffix + 5 tests, 904 suite green. The PER-AXIS axis_reads decomposition (theme lit→promote because X) rides #335 (gated — eliciting it changes the live tool def). DEPLOYED 6/18 (deploy.sh both, e3e74c1, DEPLOY OK). VERIFY-LIVE = next HIGH alert (judge rationale in italic; materiality/source on fire line) + /why shows the judge trace. /why is the PER-TICKER surface; the AGGREGATE monthly review = #337.
- #337 | 2026-07-01 | pending | MONTHLY judge-judgment review — aggregate "is the judge calling EPs right?" (operator 6/18, distinct from per-ticker /why #336). OPERATOR PRIORITY: "having all the data ready is key"; the review FORMAT is explicitly OPEN (operator unsure). DATA SUBSTRATE (advisor: the real question is the OUTCOME JOIN, not fields-on-a-row): judge verdict columns on mi_ep_alerts (judge_tier/direction/materiality/fire_axes/rubric_version + baseline_floor_tier) ⋈ mi_ep_scan_outcomes (fwd_5d/10d; ⚠ VERIFY the alert_date==scan_date join key lines up) ⋈ mi_live_trades (realized R) = the spine; ep_grade_decision now also logs has_direct_source (6/18). PROPOSED DESIGN (fold into the existing monthly_backward_check_sweep — reuse, NOT a new job; operator picks before build): (a) promote/demote/hold counts + floor-agreement rate; (b) judge-tier × forward-outcome cross-tab; (c) Unjustified-Demotion sweep — demotes that became winners (ADR 0011 named guard); (d) AKTS-class regression probes; (e) a SAMPLED set of promoted/flipped rows for OPERATOR labeling (go-live gate is operator labels, never self-score). ⭐ OPERATOR IDEA 6/18 (recorded): an LLM ADVISOR that reviews the judge's calls (like the dev-phase advisor here) — a meta-LLM reading judge decisions+outcomes to flag questionable/miscalibrated calls for operator attention; relates ensemble-divergence #301 (real-time 2nd-model verdict), questioner/investigator #212, precedent-retrieval #255 (memory project_cross_ticker_narrative_synthesis_gap holds the two-LLM-loop). Feasibility/usefulness TBD → evaluate as a candidate review MECHANISM (a richer variant of (e)). ✅ V1 BUILT 6/18 (operator: "propose one, draw from existing reviews, start + iterate"): scripts/monthly_judge_review.py — READ-ONLY standalone, SURFACES-not-prescribes (weekly-review lesson); direction-vs-floor counts + judge==floor agreement; by-direction & by-tier fwd-5d (win% LABELED drift-only per feedback_validate_metric_before_decision); ⚠️ Unjustified-Demotion sweep (demotes that ran ≥+5%/5d — ADR 0011 guard); operator-labeling sample pointer (never self-scores). Verified join scan_date==alert_date (eval_catalyst_materiality precedent). 5 tests, 915 suite green. REFINED + FOLDED 6/18 (operator "proceed"): logic moved to agents/market_intelligence/judge_review.py (importable; scripts/monthly_judge_review.py now a thin CLI re-export); ADDED realized P&L (fan-out-safe mi_live_trades subquery join) + has_direct_source footprint (reuses recompute_has_direct_source — surfaces the #329 defect's monthly trend until #335); WIRED into the monthly cron (quarterly_backward_check_sweep_job, independently-guarded so a review failure can't break the sweep) → auto-runs 1st-of-month 8am ET + Telegram. 12 tests, 916 suite green. VERIFY-LIVE = next month's 1st sweep fires the judge-review Telegram (can also run on-demand now). STILL OPEN (recorded, not built): the LLM-advisor review mechanism (operator idea — TBD feasibility) as a richer variant. ⭐ REVIEW→ACTION CHAIN (operator 6/18 "ensure effort yields results"): the review SURFACES (never prescribes — weekly-review lesson); each signal maps to a GATED action: (1) Unjustified-Demotion pattern → rubric amendment or axis fix (CHANGE_PROCESS+sign-off); (2) promote cohort NOT beating holds/floor → tighten promote criteria / find the bad-promote axis; (3) has_direct_source footprint DROPS after #335 → confirms the fix landed (verify-live); (4) direction/agreement drift across rubric_versions → regime/version-drift flag; (5) the operator-labeled sample = THE yield — it accrues the operator-labeled dataset that GATES every load-bearing axis flip (#335) + model-eval. YIELD DISCIPLINE: the review must drive ≥1 concrete gated decision per quarter or be trimmed (don't run effort that doesn't pay off).
- #338 | 2026-07-15 | pending | /simplify deferrals from the 6/18 judge-traceability review (#329/#336/#337) — 4 cleanup agents (reuse/simpl/efficiency/altitude). CHEAP fixes APPLIED 6/18: format_tier_transition reuse in judge_review (#253 single-copy contract); always-True tuple + hoisted-invariant-blind-payload in eval_judge_enrich; dropped dead _SQL cols (fwd_10d_pct/judge_rationale); closed-status FILTER on realized-P&L SUM (codebase convention); phantom-function docstring fix (corpus_provenance not classify_catalyst_sources); producer→consumer PINNING TEST (recompute_has_direct_source vs build_grounded_text markers). DEFERRED (structural, file-not-build): (A) /why's inline judge-trace block re-renders the alert's fields → extract shared format_judge_trace(row) for both /why + send_ep_alert (two renderers drift; couples #336); (B) verdict→r mirror omits judge_direction (briefing._judge_direction reconstructs it) → unify "apply verdict onto r" field-for-field with the DB write (ep_detector hot path, pre-existing); (C) deepen the marker-coupling past the pin: hoist [SEC/[Benzinga/[Web-summary prefixes to shared constants owned by build_grounded_text + an audit-JOIN agreement guard (recompute vs ep_grade_decision-logged structured value on #329+ rows); (D) extract get_theme_heat_asof(ticker,date) db helper (eval_judge_enrich + ep_selectivity_cohort:151 both inline it); (E) grade_engine_authority "judge"/"floor"/"fallback" bare literals → constants (low-pri, matches skip_reasons vocab); (F) extract _judge_authoritative(ep) (predicate now in 3 briefing helpers); (G) _MATERIAL_LEAN hardcodes catalyst_materiality's material+ tiers → reuse the vocab; (H) alert "direct-source ✓" on the judge fire-line can read as "judge used it" while the judge is BLIND to it until #335 — resolves when #335 wires it in (or reword sooner). All low-urgency hygiene; cleared as deliberate: kwarg-threading, deepcopy gate, aggregate/format split.
- #269 | 2026-06-19 | in_progress | rubric v3 verify (AKTS forward outcome = first live promote-cohort point) + promote-cap eval harness
- #210 | 2026-06-23 | pending | catalyst-sourcing backbone umbrella (direct primary sources)
- #211 | 2026-06-23 | in_progress | news-gap discovery loop (unknown-rate KPI) — needs new scheduler job
- #212 | 2026-06-23 | in_progress | questioner/investigator dialogic loop — productionize at larger cohort
- #230 | 2026-06-23 | pending | #212-as-sourcing-QA detector → feed #211 (low-precision triage)
- #233 | 2026-06-23 | in_progress | Perplexity repositioning — KEEP + integrate (operator-adopted 6/17, NOT cut): (a) feed its independent GRADE into the JUDGE as a LABELED second-opinion/disagreement signal — judge currently reads its [Web summary] text but not the Claude-vs-Perplexity grade disagreement (grade-path INPUT change → CHANGE_PROCESS + shadow eval, judge is load-bearing); (b) reposition Perplexity as a labeled RECENCY source only (its live-web edge), judge-adjudicated vs primary (#210), and RETIRE the mechanical floor confidence_multiplier boost (with #320). Was "demote Perplexity/Tavily to labeled candidate (Wave C)"
- #235 | 2026-06-23 | pending | gap-discovery loop #211 integration (Wave E)
- #236 | 2026-06-23 | pending | corpus_provenance as byproduct of build_grounded_text (DRY refactor)
- #214 | 2026-06-19 | in_progress | theme auto-naming breadth rule — verify (cooldowns back to ~3/day)
- #215 | 2026-06-23 | pending | surgical prompt de-bias / OPTX-residual (blocked_by #214 + clean cohort)
- #217 | 2026-06-23 | in_progress | theme validation query hygiene — verify deploy
- #258 | 2026-06-19 | in_progress | ensure_schema() consolidation (fold 33 ALTERs) — branch+staging-validated, post-#277
- #257 | 2026-06-23 | in_progress | central model registry verify ([5i/7] + CI + advisor logs 4-8)
- #259 | 2026-06-23 | in_progress | failure-policy decorators (@advisory_fail_open / @trade_state_fail_loud)
- #260 | 2026-06-19 | in_progress | freeze execute_task keyword cascade (routing regression test + merge ticker skip-sets)
- #237 | 2026-06-19 | pending | shared scheduled_eod_digest helper (the ~6-job 16:xx digest family)
- #239 | 2026-06-19 | pending | Wave-A trailing-baseline dedup + _emit_shadow_row envelope (/simplify deferrals)
- #265 | 2026-06-23 | pending | judge-flip-day review residuals (SQL-text SSoT, shared feeds list, WATCH cancel window)
- #264 | 2026-06-23 | in_progress | news-source-quality drift: min-N floor + composition context (false-alarm fix)
- #207 | 2026-08-01 | pending | model-eval governance quarterly review (data-gated)
- #197 | 2026-06-23 | in_progress | cap+1 game_changer slot SHADOW — promotion-gated N≥30
- #149 | 2026-06-23 | in_progress | (judge/catalyst quality item — confirm scope at triage)
- #165 | 2026-06-23 | pending | (judge/catalyst item — confirm scope at triage)
- #191 | 2026-06-23 | pending | (catalyst-sourcing item — confirm scope at triage)
- #192 | 2026-06-23 | pending | deferred-findings-to-task discipline follow-up
- #272 | 2026-06-22 | in_progress | LAUNCH 6/22 umbrella (SSoT docs/roadmap/launch-2026-06-22.md)
- #274 | 2026-06-19 | pending | 2-member theme immortality fix (dissolve-on-flagged-pair, CHANGE_PROCESS)
- #276 | 2026-06-19 | in_progress | W2 entry-mechanics program (Mon stop-geometry study next)

## Operational safety / hardening

- #323 | 2026-06-19 | in_progress | L1 high_ep_no_terminal_state (LZB) — DEPLOYED LIVE 6/17 (bfa4b50+64a0c12; coercion of date-over-wire at _insert_skipped_trade + de-silenced skip-write except → skip_row_write_error audit+Telegram). All 3 containers recreated. Verify-live = next out-of-ORB HIGH writes its skip row (event-driven, now self-announcing)
- #325 | 2026-06-19 | in_progress | theme_count_active decline (true 42→15 active in 16d; L2 fired 6/12/15/16/17, each dismissed "benign"). DIAGNOSED 6/17: NOT regime (28/40 top RS-100 leaders UNCOVERED, all w/ descriptions once mi_ticker_overrides merged), NOT desc-filter, NOT cooldown-flood (~24 cooled) — a real coverage/birth malfunction (0 births/16d; theme_discovered 0× in 40h). Collapse is in discovery-LLM→_score_new_theme→_validate_new_themes_at_birth but stage counts were logs-only (die on recreate→4 blind dismissals). SHIPPED+DEPLOYED telemetry `theme_engine_funnel` (d1afc69, theme_engine.py) = durable per-run funnel. VERIFY 6/18 ✅ FUNNEL READ (17:02 ET run): {uncovered:21, velocity:30, turners:6, elite_covered:18, discovery_called:true, new_raw_llm:0, survived:0}. STAGE PINNED = the DISCOVERY LLM itself returns 0 raw (NOT description-strip [disproved: only 3 missing→fetched], NOT a crash [no discovery_error]). Container logs nail the mechanism: `Theme discovery: model stopped without calling report_themes — forcing a final report` → forced report came back EMPTY → `0 new themes discovered`. = the #173 "shadow-death" class RECURRING despite the force_report guard (theme_engine.py:3072 loop, THEME_MODEL Sonnet, max_tokens=1500, tool_choice auto→forced). Two candidate ROOT CAUSES, not yet distinguished (deterministic repro owed before any fix, #325 "NOT blind"): (a) max_tokens=1500 too low (assignment LLM next door uses 4000) → model reasons in text, truncates before the tool call → silent stop, then forced report has no room to commit = BUG/just-fix; (b) prompt's heavy anti-false-positive framing → model reports empty when forced = CRITERION (sign-off gated). git history shows NO recent change to this block → the 16d drought correlates with something else (universe/prompt growth? model?). ADVISOR-REVISED 6/18 (the fork is CONFOUNDED, not XOR — (a) is the prerequisite; you can't see conservatism through a truncation confound): (1) bumping max_tokens 1500→4000 is INFRA CAPACITY not a criterion change → SHIPPED UNGATED (theme_engine.py:_discover_new_themes, the disposition line untouched); (2) added DURABLE per-call telemetry `theme_discovery_llm_call` (stop_reason + output_tokens + force_report + n_tool_uses) — the funnel was logs-only and DIED on the container recreate from today's deploys (couldn't grep the 17:02 discovery logs = the lesson, now fixed). NEXT (ETA 6/19, after the scheduled 5 PM ET theme run — NOT forced off-cycle: a full run re-scores every theme via Perplexity + persists = a heavy blind mutation, advisor 6/18; the durable telemetry captures it for free): READ the audit, rows in `iteration` order (auto then forced). DISCRIMINATOR — **PRIMARY signal = new_raw>0** (theme_engine_funnel): if discovery now returns themes, CAPACITY was the cause = (a) confirmed + fixed + coverage recovering, don't even need stop_reason. ONLY if still 0: stop_reason='max_tokens' → 4000 STILL truncates (prompt grew bigger than thought → trim the prompt, escalate); 'end_turn' + 0 tool_uses + forced returns themes:[] → (b) ISOLATED → the disposition-line ("return zero when in doubt") question goes to the operator (sign-off-gated); repeated consult_advisor then giving up = a THIRD mode (advisor-loop), neither branch. Corroborating: max_tokens=1500 was always there yet discovery worked at 42 themes → something crossed a threshold ~16d ago, almost certainly the prompt grew (more uncovered/velocity stocks → more reasoning → overruns 1500); 4000 buys headroom + the stop_reason telemetry catches the next overrun. (Synthetic-clean-set repro SKIPPED — a short prompt won't reproduce the truncation a long production prompt causes → false "1500 is fine".) NOTE: the PLAN's earlier "merge mi_ticker_overrides descriptions" hypothesis is SUPERSEDED (descriptions aren't the failing stage). Also still owed: fix lagging _today_active_themes metric (30 vs true 15, pre-#214 shape; baseline-discontinuity-aware). Probe: scripts/_theme_count_anomaly_probe.py. CONCRETE LIVE EXAMPLE 6/18 (from the UUUU EP alert): the "Rare Earth Magnets & Critical Battery Materials" theme is ACTIVE (Mainstream) yet UUUU (Energy Fuels, uranium/rare-earth, EP HIGH 6/18) is NOT a member, and that theme shrank 4→2 members 6/11→6/17 — live proof of the membership/assignment collapse (not just births). Downstream: EP alerts show "judge-inferred/not-tracked" instead of the real theme (the #319 reframe).
- #324 | 2026-06-23 | pending | deploy.sh `both` excludes apollo-execution — make complete or add a drift guard (execution-side broker code needs a 2nd deploy.sh execution; caught on the LZB fix; memory feedback_deploy_both_excludes_execution; relates #279/#256)
- #288 | 2026-06-19 | in_progress | Prong B idle-review escalation — REDEPLOY DONE 6/17 (both-deploy; a6c45a0 ⊆ deployed HEAD, boot applied new schema, container healthy). Remaining verify-live: clean /reviews + escalation render on next idle-review fire
- #290 | 2026-06-19 | in_progress | dead_zone_reevaluation predicate FIXED 6/18 (referenced `dc_fwd.high`; mi_daily_closes has `high_price` → errored silently → never went ready; corrected + VERIFIED executes, count=155). Review is now READY → remaining = run its action_when_ready (re-run analyze_late_detection_v3 dead-zone ORB-extension precision on Hetzner; confirm <20% precision or close). Registry hygiene done same area 6/18: ninem_day2_{mechanical_vs_methodology_alignment, retire_for_consolidation} → status=done (decided by #327/#326)
- #291 | 2026-06-20 | in_progress | triage the idle data-gated reviews (run/defer/ratify each; incl #54). TRIAGE DONE 6/18 (operator-approved prod read, check_pending_reviews): **27 genuinely READY** (predicate met + date passed), **0 errored** — idle by backlog, not by neglect. LAUNCH-CRITICAL subset (6/22 is 4d out) = sequence FIRST: P0 alpaca_stop_trigger_reliability, P0 orb_entry_stuck_pending_new, paper_r_expectancy_validation (GATE 3 live), ftre_partial_trail_verification (GATE 2 live), gate5_adel_deliverables_status_check, gate5h_value_invariant_proposal. Methodology batch: rmv_phase2_evaluation (#54), theme_engine_narrative_blindness, drawdown_breaker_promotion, rel_volume_large_cap_floor_evidence, ep_cooldown_resetup_admission, nbis_rubric_calibration_gap, silent_failure_taxonomy_audit, +others. Each needs ground-truth (no bulk-ratify). = a focused multi-review session, NOT one pass. ✅ BOTH P0s TRIAGED 6/18 (operator chose launch-critical P0s; verified neither is an open cutover blocker): (1) orb_entry_stuck_pending_new — CLOSED 5/28 (APScheduler 10:00 misfire, watchdog shipped); verified read = **0 post-fix recurrences** (all 10 predicate hits are ≤5/26: RDW + 9 April orders the #123 reconcile swept 5/27 = drift, not stalls). FOUND + FIXED a launch bug: the 90d predicate false-counted pre-fix history → at 6/22 the cutover gate would have FALSELY read ≥1 (RDW still in-window) and blocked a paper→live flip. Floored the predicate at the 5/28 fix date (post-fix-only recurrence detector; reads 0 verified) — committed/deployed. (2) alpaca_stop_trigger_reliability — already DOWNGRADED P0→P2 6/03 (not a blocker; mechanism = #182 paper-IEX entry-side miss, re-escalate ONLY on an exit-side activation failure, none observed); its broad predicate (any unfilled ORB in 30d) is a P2 MONITOR by design → correctly left pending, no edit. REMAINING launch-gates to verify next: paper_r_expectancy_validation (GATE 3), ftre_partial_trail_verification (GATE 2), gate5_adel_deliverables_status_check.
- #339 | 2026-06-19 | in_progress | DQ baseline self-heal (operator-flagged 6/18 "RS engine: 2454 scored (expected ~3955)" warning). DIAGNOSED ground-truth: NOT a data-pull failure (ingest healthy 12,135 daily closes) and NOT a bug — it's the INTENDED #286 $10M liquidity floor (commit dd4eeb4, deployed 6/15, first run 6/16) excluding illiquid names from the SCORED universe by design. Confirmed: 6/15 pre-floor scored 3925 of which 1512 were <$10M dollar-vol; 6/18 post-floor scored 2424, 0 below $10M, min exactly $10.0M → 3925−1512=2413≈2424. The WARNING is a stale-baseline FALSE ALARM with a DEADLOCK: get_data_quality_expected averaged the last 5 SUCCESSFUL (passed=TRUE) runs, but every post-floor run FAILS → none updates the baseline → it's frozen at the pre-floor 3955 and fires nightly forever (alert-fatigue risk masks a real future drop). FIX SHIPPED 6/18 (db.py get_data_quality_expected): MEDIAN of the last 5 runs regardless of pass/fail — self-heals to the new normal, robust to the mixed transition window + a lone anomalous night; a real sudden drop still alerts for the transition window. ✅ VERIFIED-LIVE 6/18 (deploy 2692bdd, both, DEPLOY OK): get_data_quality_expected now returns 2454 (median last-5) not the frozen 3955; re-ran the 6/18 check → value 2454 vs expected 2454 → passed=True → get_quality_warnings(6/18)=(none), today's false alarm CLEARED. Remaining (belt-and-suspenders): the 6/19 nightly RS run passes at the new normal. Operator chose self-heal (#1) over the delta-check (#340) — "rare update" so the simpler window-recovery fits.
- #340 | 2026-07-01 | pending | DQ delta-check (the more-correct long-term design, deferred from #339; operator chose self-heal for the rare-step case): alert on a >X% night-over-night DROP vs the trailing median rather than vs a frozen baseline — catches sudden real drops AND auto-adapts to intended step-changes without the deadlock. Scope: applies to both ingest + rs_engine + sector checks; pick X; keep a one-time transition-window alert so an intended step-down is NOTED once, not silent.
- #273 | 2026-06-19 | in_progress | LLM credit-exhaustion alerting — decorator-hook sweep + spend telemetry + low-balance warning
- #287 | 2026-06-23 | pending | partial-exit trade-state cleanup (FPS #183 stuck flag + 8-trade exits double-encoding) — operator-gated
- #277 | 2026-06-23 | pending | #256 split go-live: ✅ http-ORB handoff VERIFIED CLEAN 6/16 (RXT/NTLA); close after a 2nd HIGH-day confirm
- #278 | 2026-06-23 | pending | deploy.sh preflight role-aware (intelligence false-fail), low-urgency
- #279 | 2026-06-23 | pending | W2 split /simplify deferrals (derive _EXEC_HANDLERS; bidirectional partition guard; dedup feed resolver)
- #280 | 2026-06-23 | pending | staging own paper account (before any market-hours staging use)
- #281 | 2026-06-23 | pending | staging copy-not-derive hardening (restore fork + compose/env drift)
- #256 | 2026-06-23 | in_progress | #256 W4 closure (2-svc DR runbook · per-service uptime · nightly staging-restore)
- #176 | 2026-06-23 | pending | SoT/roster drift tooling — create-time hook + scheduled ping (residual)
- #216 | 2026-06-23 | pending | jsonb codec consolidation (root-fix of #179) — POST-launch by design (protects window)
- #172 | 2026-06-23 | pending | partial-exit /simplify refactor (blocked behind #151 architectural split)
- #152 | 2026-06-23 | pending | (op-safety item — confirm scope at triage)

## Apollo Trades dashboard (portfolio-app2)

- #194 | 2026-06-19 | pending | daily auto-export of BOTH snapshots (trades + themes) — needs deploy key
- #185 | 2026-06-19 | pending | corrupt-stop exclusion-count display fix

## Apollo Themes (portfolio-app2)

- #193 | 2026-06-19 | in_progress | RS theme rank-evolution tab (snapshot freshness rides #194)
- #315 | 2026-07-15 | pending | RS-theme dashboard R-items (memory rs-theme-dash-backlog): R1 mobile/public-URL (Tailscale) · R2 bump chart · R3 canonicalization · R4 forward returns

## Apollo v1.1 — fast-follow program (spec: docs/roadmap/apollo-v1.1-v2.0.md PART I)

_W1 selection-replay + kill/scale = #268 (done) + #275; W2 entry-mechanics = #276; W4 chart-vision = #267 (Launch project). Below = the waves not yet broken into tasks._
- #306 | 2026-07-01 | pending | v1.1-W3 exit & management: winner-harvest tune (partial size · trail-by-character · capture_pct KPI) + #91 time-stop refinement — trade-state, #151 harness discipline
- #307 | 2026-07-08 | pending | v1.1-W5 experience-seed ritual: weekly operator-labeling cadence + precedent-retrieval shadow (with #219, #254, #255)
- #219 | 2026-07-08 | pending | weekly verified-miss review → RITUAL (systematic missed-winner labels feed W5)
- #254 | 2026-07-08 | pending | ground-truth label corpus (the axis-3 "accumulated experience" substrate)

## Apollo v2.0 — tier-one trader (horizons H1 Q3'26 · H2 Q4'26 · H3 2027 · spec: apollo-v1.1-v2.0.md PART II)

- #308 | 2026-08-01 | pending | P1 Experienced Judge (H1→H2): precedent corpus at scale + self-review→rubric-distillation loop + ensemble/uncertainty (#301 seed) + auto-growing probe library
- #309 | 2026-08-01 | pending | P2 Full Sight (H1→H2): multimodal (#267 daily→intraday→peer/SPY) + intraday narrative radar + tape features (#299) + negative-catalyst (#238)
- #310 | 2026-10-01 | pending | P3 The Manager (H2): management judge mature (#300 shadow→load-bearing) + structure-stops via PIVOTS (docs/methodology/pivots-and-stock-character.md) + conviction sizing
- #311 | 2026-08-01 | pending | P4 Multi-Setup Book (H1→H2): per-setup judges + tightness→expansion graduation + 9M→flag pipeline + parabolic-short counter-regime book + regime-adaptive selection
- #312 | 2026-10-01 | pending | P5 Capital & Autonomy Ladder (H2→H3): unified_allocator live + formal shadow→paper→live ladder w/ auto-DEMOTION on breach + scaling/slippage telemetry
- #313 | 2026-08-01 | pending | P6 Institution-grade ops (H1→H2): #256 split (done) + replay-everything CI (#302) + cost governance (per-role LLM spend vs P&L)
- #314 | 2026-07-15 | pending | Trading-ideas detector book (P4 detail): TI1 parabolic-short · TI2 wick-fill (#283) · TI3 fishhook U&R · TI4 base+catalyst convergence · TI5 post-EP shape classifier · TI6 RMV (#54) — graduation gates in data_gated_reviews.yaml; detail in trading-ideas-backlog memory

## Spec / detail docs (PLAN.md is the INDEX — every program above; detail + evidence live here, referenced)

_Launch DoD: docs/roadmap/launch-2026-06-22.md · v1.1/v2.0 map: docs/roadmap/apollo-v1.1-v2.0.md · ADRs: docs/decisions/*.md · evidence-gated reviews (weekly auto-surface): data_gated_reviews.yaml · setup SSoTs: docs/setups/*.md · methodology: docs/methodology/*.md · idea detail: trading-ideas-backlog memory. If a program isn't a task line above, it isn't planned — add it._

## Miscellaneous (no home project)

- #121 | 2026-06-23 | in_progress | shared Telegram formatting layer (consolidate ≥3 escapers, re-home off scheduler.py)
- #195 | 2026-06-23 | in_progress | SECURITY — rotate portfolio-app2 app_password (operator action)
- #246 | 2026-06-23 | pending | theme_engine isinstance(anthropic.APIError) TypeError in local test env
- #334 | 2026-07-15 | pending | LOW-PRI theme-revive cooldown latch NOT YET WIRED (theme_engine.py:322, surfaced in 6/18 prose sweep): the one-way-latch arm is inert because no `revived_at` timestamp is persisted on mi_themes, so the only caller passes days_since_last_revive=None. Wire when revive goes live (persist revived_at, thread the delta). Filed not-dropped per the no-floating-prose directive; genuinely minor.
- #248 | 2026-06-19 | pending | regime engine: post-pullback hysteresis + spy_vs_200ma NULL fix + stale docstring
- #318 | 2026-06-19 | in_progress | morning-brief economic-calendar. ✅ ET FIX WORKED (6/18 brief showed "12:30 PM ET" etc.). BUT operator caught a NEW bug 6/18: calendar showed bare TIMES with NO event names (content-less) — the ET-hardening over-indexed the prompt on time-format. FIXED 6/18: prompt now requires '<time> ET — <event name>' per line + a parsing guard drops content-less bare-time lines (regex-tested). ✅ DEPLOYED 6/18 (deploy.sh both → DEPLOY OK, both containers recreated). VERIFY at 6/19 9 AM brief: named events shown.
