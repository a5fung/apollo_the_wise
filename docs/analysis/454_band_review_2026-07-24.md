# #454 — Kill/scale band review, pulled forward (quarterly-review inputs b/c/d + the §5 regime fork)

**Date:** 2026-07-24 (PT) · **Status:** RECOMMENDATION FOR OPERATOR SIGN-OFF — changes NOTHING live.
**Card spec:** `docs/analysis/454_band_review_card_spec_2026-07-24.md` · **Bands under review:**
`kill_scale_bands.py` signed thresholds (#268b, operator-signed 2026-06-12, `docs/setups/safeguards.md`).
**Every number below is from read-only prod SELECTs run 2026-07-24 ~21:20 PT (post-close), or the named
scripts — not from prior-doc prose.** Two stale prior-doc claims found; flagged in §7.

> **✅ INDEPENDENTLY RE-VERIFIED (Opus, 2026-07-24, after the card returned).** This doc was produced by
> a Fable card; per the operating model its load-bearing claims were re-run against prod before it was
> allowed to stand as citable evidence. Both §7 contradictions CONFIRMED:
> - surviving Phase-B `historical_scan` rows = **85** (2026-04-20 → 05-04) — down from the 107 the
>   2026-07-17 doc recorded. The calibration evidence really did decay further in one week.
> - live closed trades = **9**, spanning 2026-07-06 → 07-24 (18 calendar days ≈ 15 trading days) —
>   consistent with the card's n=20 ≈ **2026-08-20** projection, and with its point that the card
>   spec's "~3 months to n=20" was stale.
>
> ⚠ The **0-for-9 / −7.50R / 9-of-9-non-Bull** finding is the operator's separate action item; it is
> NOT an input to the KEEP verdict (n=9 is below the retune bar in both directions).

## 1. TL;DR

**KEEP the signed bands unchanged — no retune (no evidence reachable that clears CHANGE_PROCESS rule 1),
and do NOT park the review: run §5 option (a) EXTENDED through the current non-Bull episode (operator
go-ahead needed — write-side) before the bands mechanically activate at n=20, projected ~2026-08-20.**

## 2. The n=9 block (stated up front)

**There are exactly 9 closed live trades** (verified: the band-input query below). The strategy-health
bands are silent below the 20-trade sample floor (`_SAMPLE_FLOOR = 20`); only equity guards bind today.
**Nothing in this review derives or retunes any band threshold from those 9 rows** — n=9 against an
n=399 calibration is noise dressed as evidence (CHANGE_PROCESS rule 1, N≥10; the parabolic
ship→revert→restore scar). The register's own `action_when_ready` ("re-derive the band statistics …
over the live cohort") is **blocked by the same constraint** until n≥20.

**Unblock condition:** 20 closed live trades. At the measured July cadence (9 closes in 15 trading
days, 7/06→7/24, ≈0.6/trading-day; fills accelerating: 3 → 2 → 5 per week), the 11 remaining closes
arrive in ~18 trading days ⇒ **≈2026-08-20 (±1 week)**. ⚠ The card spec's "~3 months at the live
rate" is stale — it predates the July fill acceleration (§7).

### 2a. Input (a) — the 9 rows, descriptively (NO inference)

Query (the exact band-input definition, `kill_scale_bands.assemble_band_inputs`):
```sql
SELECT ticker, alert_date, signal_type, regime, closed_at::date, total_pnl, risk_dollars,
       total_pnl/risk_dollars AS r
FROM mi_live_trades
WHERE status='closed' AND account_mode='live' AND pnl_attribution IS NULL
  AND risk_dollars IS NOT NULL AND risk_dollars <> 0
ORDER BY alert_date, id;
```

| # | Ticker | Alert | Regime (at alert) | Closed | R |
|---|---|---|---|---|---|
| 1 | WULF | 07-06 | Choppy | 07-06 | −0.696 |
| 2 | CRCL | 07-10 | Choppy | 07-10 | −0.806 |
| 3 | WDFC | 07-10 | Choppy | 07-10 | −0.800 |
| 4 | TSEM | 07-14 | Correcting | 07-14 | −1.047 |
| 5 | MANE | 07-15 | Choppy | 07-16 | −0.106 |
| 6 | HUT | 07-20 | Correcting | 07-20 | −0.981 |
| 7 | NVCR | 07-23 | Correcting | 07-24 | −1.070 |
| 8 | THC | 07-24 | Correcting | 07-24 | −0.808 |
| 9 | WKC | 07-24 | Correcting | 07-24 | −1.181 |

Descriptive only: n=9, 0 wins, current streak 9, cum **−7.50R** (−$180.16 at start-small sizing,
risk ≈$20–47/trade), mean −0.833R, median −0.808R, range −1.181..−0.106. All magna53. Three closes
exceeded the −1R full stop (slippage beyond stop). One open position (SMCI, filled 7/22). Exclusion
check: 0 closed-live rows excluded by `pnl_attribution`/`risk_dollars` — the cohort is complete.

**Regime composition of the cohort — the load-bearing descriptive fact:** by at-alert-time labels,
**9 of 9 trades are non-Bull** (4 Choppy, 5 Correcting); by same-day EOD `mi_market_regime` labels,
8 of 9 (7/06 EOD prints Bull; the trade row carries the prior nightly's Choppy — capture-timing, both
frames agree the cohort is essentially all non-Bull). The live system is trading **exclusively in the
regimes the #268b envelope does not price** (`454_regime_stratified_envelope_2026-07-17.md`: ~95%
Bull exposure, single non-Bull episode).

## 3. Findings — inputs (b), (c), (d)

### 3a. Input (b) — P6 replay-regression snapshots: accruing live dist vs the calibration

Source: `mi_audit_log` `event_type='replay_regression_snapshot'` (writer:
`replay_regression.run_replay_regression`, Sunday weekly review; persists only when n>0):
```sql
SELECT created_at, detail FROM mi_audit_log
WHERE event_type='replay_regression_snapshot' ORDER BY created_at;
```
Two rows exist (first live close was 7/06, so 6/21–7/05 Sundays correctly wrote nothing):

| Sunday | n | expectancy | win | maxDD (running) | streak |
|---|---|---|---|---|---|
| 2026-07-12 | 3 | −0.77R | 0% | −2.3R | 3 |
| 2026-07-19 | 5 | −0.69R | 0% | −3.5R | 5 |
| (recompute today, same formula) | 9 | −0.83R | 0% | −7.5R | 9 |

Both persisted snapshots **recompute exactly** from the per-trade series above (−0.7673, −0.6910) —
the pipeline is verified, not trusted. Direction of drift: the accruing live distribution sits far
below the calibration card (exp +0.95R, win 30%) and is worsening — but (i) n≤9 (per-trade expectancy
at this N is noise given the 62%-stop/13%-≥+3R calibration shape — the module's own no-verdict
rationale), (ii) **the entire live window is the non-Bull tape the envelope does not price**
(single-regime honesty — inherited by every number in this row), and (iii) the structural confounds
stand (47% recall, IEX-1min-ORB calibration vs live SIP fills, Lane-2 dark pre-6/26). #302 v0
surfaces-only by design; **no divergence verdict is statistically available yet**, and none is issued
here.

### 3b. Input (c) — override log

```sql
SELECT event_type, created_at, summary, detail FROM mi_audit_log
WHERE event_type LIKE 'kill_scale%' ORDER BY created_at;
```
**Zero `kill_scale_override` rows. Zero `kill_scale_override_cleared`. Zero eval errors.** The only
row ever is the 6/19 `∅ → HOLD` baseline transition; `mi_safeguard_state` confirms the band has been
HOLD since 2026-06-19 with no transition. The safeguards.md change-log (grepped, all entries since
6/12) contains **no override entry** either — both override surfaces agree. The 16:13 ET
`kill_scale_band_eval` job is registered (`scheduler.py:5290`).

**Reading:** no repeated-overrides-in-one-direction signal exists — but this is *absence of evidence*,
not evidence the bands are well-set: the bands have never bound (HOLD below the sample floor since
signing), so there has been nothing to override.

### 3c. Input (d) — demote-side watch-metric (judge-demoted cohort forward performance)

Phase-B reference: judge-demoted floor-HIGHs n=94 averaged **+1.01R** forward (demotes NOT reliably
saves — `selection_replay_268_phaseB.md`, table verified in-file). Live analog (no simulated forward
R exists for untraded demotes — see §6; the reachable metric is `mi_ep_scan_outcomes.fwd_5d_pct`,
**% not R, drift-dominated, directional-only** per `judge_review.py`'s metric-honesty rule):

```sql
SELECT a.judge_direction, COUNT(*), COUNT(o.fwd_5d_pct) AS settled,
       AVG(o.fwd_5d_pct), PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY o.fwd_5d_pct),
       COUNT(*) FILTER (WHERE o.fwd_5d_pct > 0),
       COUNT(*) FILTER (WHERE o.fwd_5d_pct >= 5.0)
FROM mi_ep_alerts a
LEFT JOIN mi_ep_scan_outcomes o ON o.ticker=a.ticker AND o.scan_date=a.alert_date
WHERE a.grade_engine_authority='judge' AND COALESCE(a.source,'live')='live'
  AND a.judge_direction IS NOT NULL
GROUP BY a.judge_direction;
```
Judge era live-source (2026-06-11 → 2026-07-24, 58 judge-driven alerts):

| Direction | n | settled | mean fwd5d | median | positive | ≥ +5% |
|---|---|---|---|---|---|---|
| demote | 13 | 11 | **+19.3%** | +4.7% | 10/11 | 5 |
| hold | 35 | 31 | +12.0% | +6.0% | 29/31 | 18 |
| promote | 10 | 6 | +12.3% | +10.4% | 6/6 | 4 |

Demoted **floor-HIGHs** specifically (the exact Phase-B analog): n=7, 5 settled — mean fwd5d
**+33.2%** (median +2.6%; 5/5 positive; dominated by HQ 6/15 +152.5%; others: AEHR +7.5, KYMR +2.2,
WDFC +0.9, PRG +2.6).

**Verdict on (d): the signal has HELD, not inverted** — live demotes are still not reliably saves
(demoted names outperform holds on mean fwd5d, same direction as Phase B's +1.01R). Caveats inline:
n is tiny, the metric is % (not R) and drift-saturated, and the window is majority non-Bull tape.
Per CHANGE_PROCESS rule 3, no individual demote is classified right/wrong here — the operator labels
calls via `/why` + the monthly judge review; this is the aggregate watch-metric only.

## 4. What the bands will mechanically read at activation (planning arithmetic — NOT calibration evidence)

Pure arithmetic on the evaluator (`evaluate_kill_scale_bands`), stated so the operator is not
surprised ~4 weeks out; it is **not** evidence about band correctness:

- At n=20, trailing-20 = mean of all 20 trades. First reading hits **REDUCE (≤ −0.70R)** iff the next
  11 trades average ≤ −0.59R. The current 9-trade mean is −0.83R — *if the current tape continues,
  REDUCE at activation is the modal outcome.* KILL (≤ −1.05R) needs the next 11 to average ≤ −1.23R
  (systematic worse-than-full-stop slippage — 3 of 9 have exceeded −1R, but a sustained −1.23 mean is
  a different animal). Cum-R KILL (−30R) is far (−7.5R today).
- Regime context at that moment: since the calibration window closed (5/04), the tape is 34 Bull /
  13 Choppy / 10 Correcting weekdays, with 7/16→7/24 solidly Correcting — the first activated t20
  window will be **non-Bull-heavy, judged against Bull-calibrated thresholds**. Per the 7/17 analysis
  verdict: a REDUCE/KILL breach in a non-Bull tape should be read with the prior that it can be a
  regime effect, not (only) strategy decay. That interpretive fork belongs to the operator — §5b/§5c.

## 5. Recommendation on the §5 regime-conditionality fork + open forks

**Fork (primary, the card's job): §5 of `454_regime_stratified_envelope_2026-07-17.md` — (a) $0-LLM
partial re-cut / (b) full paid re-run / (c) prospective accrual.**
**Rec: (a), EXTENDED through the present, before ~8/20 — with (c) continuing by default; (b) stays
gated on (a)'s result.**

Reasoning:
1. Something material changed since §5 was written: **a second, independent non-Bull episode now
   exists** (June–July Choppy/Correcting, 23 non-Bull weekdays and counting — vs the calibration
   window's single 3-week crash cluster). Option (a) as originally specced (same 12-month window)
   would measure a regime effect from ONE episode with zero replication; extending the scan window
   through the present (2025-06-09 → today) adds the second episode **at the same $0 LLM cost** —
   scan + simulate are deterministic. This converts (a) from "weak by construction" to genuinely
   informative.
2. There is a clock: the bands activate ≈8/20, plausibly straight into REDUCE (§4). (c) alone leaves
   the operator ruling on that breach with no regime prior; (a)-extended delivers the raw-outcome
   regime stratification before the decision arrives.
3. (c) costs nothing and is already running (the #302 snapshots + the trade rows' regime column) —
   it is not a fork *against* (a), it is the default that continues regardless.
4. (b) stays unrecommended per [[rigor-before-paid-eval-spend]]: pay for the judge-cohort re-cut only
   if (a) shows a raw-outcome regime effect worth pricing.

Constraints on (a), acknowledged: **it is write-side** (re-inserts `mi_ep_alerts
source='historical_scan'` rows + Polygon load) — needs its own operator go-ahead, not executed under
this card. Two riders (fork 5.d/5.e below): durable persistence of the outcome series, and awareness
that the idempotent `delete_historical_alerts` will clear the last 85 surviving Phase-B rows.

**Open forks for the operator (fork + 1-line rec):**

- **5.a §5 fork — (a) vs (b) vs (c):** rec **(a)-extended + (c) default**; (b) only if (a) shows an
  effect.
- **5.b Band-activation stance (~8/20):** pre-commit NOW how a first-reading REDUCE in a non-Bull tape
  will be read (regime-prior + (a)'s evidence → deliberate override-or-comply, logged via the existing
  `kill_scale_override` mechanism) vs decide in the moment — rec **pre-commit now**, same
  policy-before-drawdown spirit as the bands themselves.
- **5.c Annotate safeguards.md:** add a change-log entry recording the Bull-conditionality caveat on
  the signed bands (docs-only, no threshold change) — rec **yes**, at sign-off of this review, so the
  caveat travels with the bands rather than living in analysis docs.
- **5.d Persistence rider on any (a) run:** require the per-trade outcome series be persisted durably
  (DB table or checked-in CSV) in the same run — rec **require it** (the §2 archaeology must not
  repeat; the surviving evidence is still decaying, §7).
- **5.e Register hygiene:** the 8/01 quarterly entry's `action_when_ready` ("re-derive band stats over
  the live cohort") is n-blocked until ~8/20 — rec **re-point the 8/01 session at this doc** and set
  the re-derivation to the n=20 event, not the calendar date.

## 6. What this card could not reach (named, no silent gaps)

1. **The exact per-trade R series behind the envelope** — still unreachable (re-verified: the
   surviving in-window `historical_scan` population has decayed further, §7). No per-regime envelope
   re-cut is possible read-only.
2. **Forward R (in R units) for the demote cohort** — untraded demotes have no simulated ORB outcome;
   only `fwd_5d/10d_pct` exists. The Phase-B +1.01R vs live +19.3%/+33.2% comparison is directional
   only, never magnitude.
3. **Positive confirmation the Sunday digest job ran on 6/21, 6/28, 7/5** — no snapshot rows are
   *expected* (n=0 skip is by design) and no `kill_scale_band_eval_error` rows exist, but no
   affirmative run-log was checked.
4. **Option (a) itself** — write-side, recommended only (hard constraint 1).
5. **Paper-mode band/regression surfaces** — out of scope by design (LIVE-only, the GATE-3 feed-bias
   rationale).

## 7. Contradictions found vs prior docs (verify-against-primary rule)

1. **Card spec: "The bands are silent below 20 (~3 months at the live rate)."** Stale. Measured
   cadence (7/06→7/24: 9 closes / 15 trading days; fills 3→2→5 per week) puts n=20 at
   **≈2026-08-20 (±1 week)** — ~4 weeks, not ~3 months. The estimate predates the July fill
   acceleration. This matters: it moves the (a)-before-activation deadline forward.
2. **`454_regime_stratified_envelope_2026-07-17.md` §2: "107 of 1,307 in-window rows survive
   (2026-04-13..2026-05-04)."** Now **85 rows (2026-04-20..2026-05-04)** — a further ~20% of the
   surviving Phase-B evidence deleted in the week since that doc (a later replay's idempotent clear).
   Strengthens fork 5.d: the archaeology decays while we wait.
3. Verified UNCHANGED (no contradiction): the calibration-window regime composition (222 Bull / 11
   Crisis / 2 Correcting / 1 Choppy weekdays — exact match to the 7/17 doc §3a); the "9 closed live
   trades" count (exactly 9 at 7/24 close, +1 open SMCI); both persisted #302 snapshots recompute
   exactly from the trade rows; Phase-B demote table (+1.01R, n=94) as quoted.
