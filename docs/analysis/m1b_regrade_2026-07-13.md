# M1-b batched regrade — delta table for the 7/18 sitting (#335)

**Run:** `eval_judge_enrich --regrade --days 30 --limit 8 --replicates 3` on prod
2026-07-13 (operator-authorized ~$5 / 32 Opus calls). Blind arm ×3 (noise floor) vs one
ENRICHED arm (has_direct_source=True + as-of theme heat). Harness bug fixed same run
(`_REGRADE_SQL` — the cohort query had omitted the columns `build_judge_payload` reads;
regression-pinned in `tests/test_eval_judge_enrich_regrade.py`).

## Verdict flips (blind stable across K, enrich changed it) — 2, for OPERATOR labeling

| alert_date | ticker | flip | theme-heat | driver (judge rationale, verbatim head) |
|---|---|---|---|---|
| 2026-06-15 | HYMC | MODERATE→none | Fading / 1.58 | "The only fresh SEC filing is a routine EVP employment agreement (item 5.02) with zero financial materiality — not a HIGH-tier driver. The web summary attributes…" |
| 2026-06-15 | MMYT | MODERATE→none | no-theme | "The only 'catalyst' is a Benzinga earnings-call transcript headline with zero reported numbers — no revenue, no EPS, no guidance…" |

**Noise (blind verdict itself unstable across the 3 replicates) — 0.** Both flips are real
enrich effects, not judge run-to-run variance.

## ⚠️ MECHANISM MISMATCH — read this before labeling (the headline finding)

**M1-b tests a DIFFERENT mechanism than the one `composite_authority` flips live.** What goes
live at M1-d = the **deterministic** `theme_axis_credit` (the signed, boost-only stage→credit
table) composed onto the tier by `compose_final_tier` — exactly what `mi_theme_axis_shadow`
logs. This regrade instead re-grades with theme heat **inside the judge's prompt** (the #329
payload-enrichment path), which `compose_final_tier` never runs. So labeling HYMC/MMYT
validates the **#329 prompt-enrichment direction**, **NOT** the deterministic composition that
flips live. **This $5 run produced ZERO evidence on the live M1-d mechanism.**

## Interpretation (agent read — NOT a label; the operator labels correctness at the sitting)

- **Both flips are DEMOTIONS, and the attribution is CONFOUNDED.** The enriched arm flipped
  `has_direct_source=True` **and** threaded theme heat in the **same** payload — the two
  enrichments moved together, so a flip cannot be cleanly attributed to one. They read as
  **catalyst-quality / source-driven** demotions (HYMC = a routine 5.02 EVP employment
  agreement; MMYT = a numberless Benzinga headline), but HYMC's Fading-theme text in the prompt
  could also have shifted the judge's qualitative read (even though the *deterministic* table
  gives Fading 0 credit). Both flips look reasonable, but which enrichment drove them is not
  isolable from this run.
- **ZERO theme-axis BOOST flips** — none of the 8 rows carried Accelerating / Nascent-near-miss
  / Mainstream-tiebreak membership (Accelerating-theme EP alerts are rare; ~88% of HIGHs are
  themeless, so a random 8-row window catches none). Consistent with, but not a substitute for,
  the mechanism-mismatch point above.

## The fork for the sitting — an OPERATOR ruling (methodology → THE LINE)

The evidence that speaks to the live M1-d mechanism is the **deterministic
`mi_theme_axis_shadow` (461 rows)** — it logs every case where `theme_axis_credit` would have
moved a tier, i.e. the composed-tier mechanism itself. This regrade does **not** corroborate
that mechanism (see the mismatch above).

**The ruling is the operator's (I do not score sufficiency):** does the 461-row deterministic
theme shadow alone carry enough theme-composition evidence to sign M1-d at the sitting? Two
honest facts to weigh:
- The authorized $5 run produced **no** evidence on the mechanism that flips live — only on the
  separate #329 prompt-enrichment direction.
- **A larger *random* regrade does NOT fix this** — more rows ≈ more themeless rows (88%), still
  the wrong mechanism. The ONLY way to get judge-side theme-boost evidence is an
  **Accelerating-membership-TARGETED cohort** (the harness has no such filter today — it would
  need a small change), and even then it tests the judge-prompt path, not `compose_final_tier`.
