# M1-b batched regrade — delta table for the 7/18 sitting (#335)

**Run:** `eval_judge_enrich --regrade --days 30 --limit 8 --replicates 3` on prod
2026-07-13 (operator-authorized ~$5 / 32 Opus calls). Blind arm ×3 (noise floor) vs one
ENRICHED arm (has_direct_source=True + as-of theme heat). Harness bug fixed same run
(`_REGRADE_SQL` — the cohort query had omitted the columns `build_judge_payload` reads;
regression-pinned in `tests/test_eval_judge_enrich_regrade.py`).

## Verdict flips (blind stable across K, enrich changed it) — 2, for OPERATOR labeling

| alert_date | ticker | flip | theme-heat | driver (judge rationale, verbatim head) |
|---|---|---|---|---|
| 2026-06-15 | HYMC | MODERATE→none | Fading / 1.58 | "The only fresh SEC filing is a routine EVP employment agreement (item 5.02) with zero financial materiality — not a HIGH-tier driver. The web summary attributes…" |
| 2026-06-15 | MMYT | MODERATE→none | no-theme | "The only 'catalyst' is a Benzinga earnings-call transcript headline with zero reported numbers — no revenue, no EPS, no guidance…" |

**Noise (blind verdict itself unstable across the 3 replicates) — 0.** Both flips are real
enrich effects, not judge run-to-run variance.

## Interpretation (agent read — NOT a label; the operator labels correctness at the sitting)

- **Both flips are DEMOTIONS driven by `has_direct_source`, not by the theme axis.** HYMC's
  Fading theme contributes 0 credit (theme_axis_credit is boost-only; Fading→0); its demotion
  is the judge, once told it's looking at the actual filing, seeing a routine 5.02 employment
  agreement. MMYT is themeless — a pure `has_direct_source` flip on a numberless Benzinga
  headline. So this cohort corroborates the **#329 has_direct_source axis** (the judge sharpens
  its skepticism on weak catalysts when the source flag is threaded), which is the *right*
  direction if the operator judges these two demotions correct.
- **⚠️ ZERO theme-axis BOOST flips in this cohort — the M1-d mechanism is essentially unexercised
  here.** The theme-axis credit only fires on Accelerating (+1) / Nascent-near-miss / Mainstream-
  tiebreak membership; none of the 8 --limit rows carried such a membership (Accelerating-theme
  EP alerts are rare, and an 8-row 30-day window didn't catch one). So this $5 run is strong
  evidence on the has_direct_source enrichment and **thin-to-silent on theme composition itself**.

## The fork for the sitting (theme-composition evidence)

The primary M1-d evidence remains the **deterministic `mi_theme_axis_shadow` (461 rows)** —
it directly logs every case where `theme_axis_credit` would have moved a tier, which is exactly
the composed-tier mechanism. This regrade is the judge-side corroboration of the *enrichment
direction*, not the theme-boost magnitude.

**Decision (operator, at or before the sitting):** is (this $5 run's has_direct_source
corroboration + the 461-row deterministic theme shadow) sufficient theme-composition evidence
to sign M1-d, OR fund a **larger, theme-targeted regrade** (~$10–15, --limit 20–30 or an
Accelerating-membership-filtered cohort) to see the judge actually compose Accelerating-theme
names UP? Recommendation: the deterministic shadow already carries the boost evidence the
regrade can't cheaply surface; sign on the shadow + this corroboration unless the operator
wants the judge-side boost demonstrated explicitly.
