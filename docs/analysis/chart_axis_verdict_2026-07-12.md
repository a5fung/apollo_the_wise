# Chart-vision axis — interim verdict from the LIVE #343 shadow (Block 4 T6, 2026-07-12)

**Question (T6 / #267-W4):** does the judge grade better *seeing the chart*? The planned fresh
eval turned out unnecessary — **the #343 shadow has been running the exact paired experiment
live since 6/22** (arm B text-only vs arm C +point-in-time chart, ×3 modal-stability, EOD on
every EP HIGH+MODERATE).

## The data (all 23 graded events, 6/18–7/10)

- **21/23: B ≡ C, perfectly modal-stable, identical verdicts.** The chart changed nothing.
- **2/23 divergent — both INSTABILITY, not signal**: KYMR 6/25 (B=MODERATE×3, C=HIGH,HIGH,MODERATE
  — the chart nudged toward promotion but couldn't stabilize) · QDEL 6/29 (B mixed none/MODERATE,
  C=none×3 — if anything the chart made the *demote* more decisive).
- **`chart_axis_shadow_delta` rows: 0** — by design (a delta requires stable-AND-different), so
  the labeling queue never populated. NOT a broken job (23 graded events prove it runs).
- Side-observation: the TEXT judge does all the moving — 4 floor-MODERATEs (DFTX, SYRE, AVAV,
  CRCL) were promoted to HIGH by **both arms identically**. Verdict-level judgment is saturated
  by the text corpus on this cohort.

## The read

On the live cohort (mostly clear-cut calls), **the chart adds no verdict-level signal at N=23**.
This does NOT yet test the hard cohort — names where the chart *should* matter (extended/
parabolic structure under a strong print). That test is now SPECCED as the review's follow-up:
the **FALSE_BOOST ±chart probe** — the 9 real false-boost names (mined corpus, cached raws →
payloads, real charts, 2 arms ×3, ~$8): if arm C demotes the boosts the tape rejected, the axis
earns a narrow demote-role; if not, hold/kill with evidence on both easy and hard cohorts.

## Dispositions

- The `chart_vision_axis_shadow_decision` review is correctly armed (OR-date backstop fires
  7/31 even at 0 deltas — the zero-delta branch + the probe spec now live in its action).
- M1 pack (7/18): unchanged — chart wire-ins stay parked (this strengthens that parking).
- No new task: the probe rides the 7/31 review fire.
