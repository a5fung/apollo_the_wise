# Build prep — +2R resting profit trigger (#508)

**Operator SIGNED OFF 2026-08-01. Build starts 2:00 PM ET.**
This file is the pre-build state: what is ready, what is decided, what is still unknown, and the
sequence. Read before writing code.

---

## Answer to "is everything ready to build?" — YES as of 2026-08-01 12:05 ET

The one open item (order structure) was settled by the operator's own suggestion — see DESIGN SETTLED
below. Nothing now blocks the 2:00 PM start.

**READY (built and pushed today, no sign-off needed, already on main):**
- `docs/setups/exit_discipline.md` — the SSoT, with the deployed behaviour, the measured record, and
  the operator's day-5 ruling.
- **Profit-take decision notification** in `run_partial_exits` — announces ticker · day · close · R ·
  shares · new stop, before the sell, self-catching. AST-tested (my first version referenced three
  unbound names and would have silently never fired).
- **`first_live_winner`** review — predicate verified against prod, returns 0.
- **`exit_regime_separability`** (T2) — verified, returns 1 of 4.

**SETTLED — the order structure.** It was the only blocker; the operator resolved it (below).

---

## The order-structure decision

**Gemini's instruction was: split the entry into two orders** — 1/3 with a +2R target, 2/3 without.
**That cannot be recorded in this system.** `mi_live_trades` carries
`UNIQUE (ticker, alert_date)` (index `mi_live_trades_ticker_alert_date_key`, verified against prod),
so two entry rows for one ticker on one day are impossible without a schema migration. And if they
were possible, `count_open_positions` counts rows in `OPEN_POSITION_STATUSES`, so a single ticker
would consume **two of the five** position slots — silently tightening the cap.

Gemini's own text is also self-contradictory here: it says Alpaca reduces the linked stop
automatically so we need not patch quantities, then says we must split to avoid a quantity mismatch.
If the first is true the split is unnecessary.

### ✅ DESIGN SETTLED — operator's, 2026-08-01: *"can't we cancel stop, replace with less shares
### then sell?"*

**Yes — and it dissolves the problem rather than working around it.** That IS the sequence
`execute_partial_exit` already performs: reduce the stop first (under a per-trade DB advisory lock so
the reconciler cannot fight it), verify, then sell. **No resting order, therefore no window where the
stop over-covers the position, therefore the unknown below is moot.**

**The hook already exists.** `track_position_extremes` runs **every 5 minutes, 09:30-16:00 ET,
mon-fri**, and for every open position it **fetches real-time Alpaca MINUTE BARS** and upserts the
full path into `mi_intraday_bars` (`scheduler.py`, `_track_open_position_extremes_job`). So the
intraday price the exit path was missing is *already being collected on the exact cadence we need*.

**The change becomes small:** in that job, if any minute high in the in-hold window ≥
`entry + 2 × risk` and `not partial_taken` → call the existing `execute_partial_exit`. No new order
type, no schema change, no cap interaction, no unique-key problem, no new failure mode on the money
path.

**Detection is NOT lossy** — because the job reads minute BARS, a spike between polls is still seen.
Only the FILL price differs: we sell at the prevailing price when noticed, not at the exact +2R touch.

**Measured on the one live trade with both a real excursion and full minute data (QBTS):**
entry 18.17, risk 0.42, +2R target 19.01. First touch **09:40 (high 19.14 = 2.30R)**. At the next
5-minute poll price was **19.13 = 2.29R** — i.e. the poll would have filled *better* than the 2.00R a
resting limit would have taken, because price kept rising. The cost is two-sided, not one-sided: a
spike-and-revert would fill worse.

⚠ **Consequence for the signed anticipated-effect: +0.47R was modelled on limit-at-level fills and is
now an ESTIMATE, not the expected value of what we are building.** Step 1 below re-scores it under the
actual mechanism.

### Superseded design (kept for the record) — resting limit order

**One entry, one bracket (unchanged). After the entry fills, place a resting LIMIT SELL for 1/3 at
`entry + 2 × risk_per_share`. On its fill, reduce the stop to the remaining 2/3 and move it to
breakeven.**

Why this and not a bracket take-profit leg:
- It reuses `finalize_partial_exit()` (`order_manager.py:2191`), which already commits partial state
  on the real WebSocket fill, under the per-trade advisory lock, idempotent on `order_id`.
- It leaves the entry path, the position cap, the dedup key and #500's price-aware entry untouched.
- One trade row, one position slot. No migration.

⚠ **Confirmed NOT paused**: `_PARTIAL_EXIT_PAUSED = False` (`order_manager.py:1398`). The #151 hard
pause was lifted; the machinery is live. (Its docstring still says "PAUSED" — stale comment, worth
fixing in this build.)

### The one real unknown — the over-covered stop window

`execute_partial_exit` deliberately **reduces the stop FIRST, then sells**, holding a DB advisory lock
across the whole sequence, so the position is never unprotected and the reconciler cannot fight it.

**A resting limit inverts that**: the broker fills the sell *first*, and only then do we learn about
it (WebSocket) and replace the stop. Between those two events the stop covers **more shares than are
held**.

**This is the thing to answer before writing code, and it cannot be answered from the repo:**
1. Does Alpaca reject, auto-reduce, or leave an over-covering stop after a partial position
   reduction?
2. If it leaves it, what happens if that stop then triggers — does it try to sell shares we do not
   have, and does that reject the whole order?

**How to answer it: a paper smoke test, before any live code.** Place a bracket, fill it, rest a
limit for 1/3, let it fill, and observe the stop leg's state. That is ~20 minutes and it decides the
design. `alpaca_client.replace_order()` (`:447`) exists for the follow-up replace.

---

## Build sequence — REVISED for the settled design

1. ✅ **DONE 2026-08-01 — re-scored under the 5-minute-poll fill. THE SIGNED NUMBER SURVIVES.**

   First the data had to be made adequate: only **1 of 12** live trades had fill-day minute coverage,
   so the model would have rested on a single trade. Backfilled `mi_intraday_bars` from **Polygon**
   (6,892 RTH bars across the 12 live trades' in-hold windows) — deliberately NOT via Alpaca, because
   the intelligence container blanks its Alpaca credentials on purpose ("creds isolation" in
   docker-compose.prod.yml; the keys live only on apollo-execution). Coverage went **1 → 9 of 12**
   live, 5 → 13 of 43 overall. Telemetry table only; no trade state touched.

   Then scored both fill models **on the same trades**:

   | rule | n | LIMIT fill (as signed) | 5-MIN POLL (as building) | delta |
   |---|---|---|---|---|
   | 1/3 at +2R | 11 | −0.56 | −0.56 | **−0.00** |
   | 1/3 at +3R | 11 | −0.65 | −0.65 | **+0.00** |
   | 1/3 at +1R | 11 | −0.53 | −0.55 | −0.02 |
   | 1/3 at 1 ADR | 11 | −0.45 | −0.49 | −0.04 |

   *(actual, same 11 trades: −0.99)*

   **The mechanism change costs at most 0.04R, and nothing at +2R or +3R.** The concern that drove
   this step — that a poll would miss the intraday spike a resting limit catches — does not
   materialise, because detection reads minute BARS: the spike is still seen, only the fill moment
   moves. **+2R under the real mechanism is worth +0.43R against actual on these 11 trades**, against
   +0.47R signed on 12 under the idealised fill.

   ▶ **Gate cleared. Proceed to step 2.**
2. **Add the trigger to `track_position_extremes`** — it already holds the minute bars; the addition
   is a threshold test plus the existing `execute_partial_exit` call.
3. **~~Handle a resting-order fill~~ — NOT NEEDED.** The existing stop-reduce→verify→sell sequence
   and `finalize_partial_exit` cover it unchanged.
4. **Remove the time-based partial** from `exit_logic.py` behind a flag, so reversion is config not
   code. Update `docs/setups/exit_discipline.md` in the SAME commit (CHANGE_PROCESS r6).
5. **Tests**: fires at +2R and not before · breakeven arms after · no double-partial with the daily
   job · stop quantity correct after fill · 9M Day-2 path untouched · reversion flag restores the old
   behaviour.
6. ✅ **DONE — CHANGE_PROCESS r1 satisfied, plus a check r1 does not ask for.**
   ⚠ First a correction to my own earlier wording: **there is no "#151 harness."** That phrase was
   mine, carried over from other cards; #151 is the partial-exit race-fix task. r1's actual
   requirement is *"N≥10 historical samples evaluated"* — satisfied by the replay (N=36 magna53, 34
   candidate rules, every figure independently recomputed twice, re-scored under the real poll fill).

   **But the replay validated the RULE, not the CODE.** New probe
   `scripts/probes/_508_trigger_parity.py` applies the shipped predicate — `MAX(in-hold bar high) ≥
   entry + R × risk`, partial not already taken — to the historical cohort and compares trade-for-
   trade against the replay. The predicate is deliberately RE-IMPLEMENTED there, not imported:
   importing would prove only that a function equals itself.

   **Result: 11 agree, 0 diverge, 1 unmeasurable.** The shipped code reproduces the replay's
   decisions exactly.

   ⚠ **The one unmeasurable is MANE, and it favours the change.** The shipped predicate fires on it
   (peak +7.92R vs a 121.98 target); the replay's poll model could not score it for lack of a
   measurable poll fill. So the trigger fires on **4 of 12** live trades while the measured +0.43R
   rests on **3** — and the excluded one is the cohort's biggest runner. Including it would move the
   number up, not down.
7. **Deploy `market-agent` AND `execution`** — `broker/exit_logic.py` and `broker/order_manager.py`
   are both in `exec_loaded_modules.txt`. Read the delta first. Off-hours.
8. **Verify-live** on the first qualifying trade against the pre-committed reversion triggers.

## Tooling note — Alpaca's backtest skill (installed 2026-08-01)

`~/.claude/skills/alpaca-trading-backtest/` (Apache-2.0, from `alpacahq/alpaca-skills`). Formalises a
strategy spec, fetches bars via the Alpaca CLI, runs a local simulation, and emits a reproducible run
folder with a buy-and-hold benchmark.

⚠ **NOT the tool for this build, and the reason matters.** It re-simulates entries from bars. Our
`_508_exit_rule_replay.py` starts from the **43 actual recorded trades** — real fills, real stops,
real exits — and asks what a different exit would have done to them. Swapping known fill prices for
modelled ones would weaken the evidence CHANGE_PROCESS is asking for, not strengthen it.

**Where it IS the right tool: #503** — whether the ENTRY works at all over history, benchmarked. That
is Monday's session, not today's build.

✅ **Alpaca CLI 0.0.13 now installed** (`~/.local/bin/alpaca`), from the official release binary with
its SHA-256 verified against the published checksums before unpacking — neither Homebrew nor Go is on
this machine, so the two documented routes would each have meant installing a toolchain for one
3.5 MB binary. ⏳ Not authenticated: `alpaca profile login` is the operator's to run. ⚠ Note the CLI
is **not data-only** — `alpaca order` / `position` / `option` can trade. Keep the profile on PAPER.

## Pre-committed reversion triggers (from the signed proposal)

1. Partial fires, remainder stopped at breakeven, trade then runs ≥+4R same session — one occurrence
   is a review, two is a revert.
2. Next 10 closed live trades worse than the same 10 replayed under the incumbent.
3. Reversion is a config flag, full stop — **simpler than the signed proposal stated.** That caveat
   ("resting orders on open positions must also be cancelled") applied to the superseded design.
   With the settled design there are no resting orders to cancel: flipping the flag stops the trigger
   firing and the day-3 rule resumes. Nothing on an open position needs unwinding.

## What this build is NOT expected to do

Make the strategy profitable. The shadow ORB control — same alerts, same gates, no broker — shows
**zero winners across bull AND correcting months**. This reduces the size of losses. The upstream
question (why live trades die in 1.5 days) is #503 and is untouched by this.
