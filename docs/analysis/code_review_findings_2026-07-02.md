# Comprehensive code review — 8226c3f..HEAD (6/12 → 7/1), findings

**Run 2026-07-02.** Range: 539 commits, 351 files, +92k/−1.3k (Python core 265 files, +33k).
Method after the full fan-out hit the session limit: recovered the completed finder's 15 candidates
from the workflow journal (already paid for), verified them inline, + ONE scoped agent for the
money-path correctness gap (broker/ diff — results appended below when it lands).
Umbrella task: **#412** (fix batch rides the 7/3 sprint deploy window).

## Fixed same-day (committed 7/2, deploy 7/3)
| # | Where | What |
|---|---|---|
| F1 | agent.py:4716 | `/regime` printed the grouped-why description TWICE (the section formatter embeds it since 6/24; the handler still appended it). Dropped the append. |
| F2 | flag_detector.py:1622 | HTF breakout shadow recorder wrapped the whole per-tick loop in one try — one bad break silently dropped shadow rows for every later break that tick (#370 class; this dataset gates the future paper decision). Now per-break try. |
| F3 | agent.py:4932 | Dead re-dedup loop over `get_shadow_theme_candidates` (query is already `DISTINCT ON (name)`), with a wrong ordering comment. Removed. |

## Filed → #412 (verify at fix time; line numbers are 7/2)
| # | Where | Finding | Priority |
|---|---|---|---|
| F4 | scheduler.py:3212 | Prod job `_run_chart_axis_shadow` imports from `scripts/_judge_replay_common` — breaks when #261 (sprint) reorganizes scripts/; the #343 shadow would silently stop accruing (theme-shadow-0-rows class). Re-home the 3 helpers under `agents/market_intelligence/` (same move judge_review.py made). **Must land with/before #261.** | HIGH |
| F5 | ep_detector.py:1662 | #344 re-poll precheck fetches FULL news bodies (`include_content=True`) every premarket tick per routine ticker just to count primary-subject items. Light call for the count; bodies only on trigger (once/day). | MED |
| F6 | ep_detector.py:~1656+~1801 | Enrichment shadow + re-poll shadow are two ~70-line near-copies (same SEC fetch → corpus → classify pipeline, duplicated premarket-window expr). Extract `_build_enriched_corpus` before the next #344 iteration diverges them. | MED |
| F7 | theme_engine.py:1468 | `promote_candidate_by_name` (operator /promotetheme) duplicates `promote_shadow_themes`' guarded INSERT…ON CONFLICT verbatim — two hand-synced copies writing live `mi_themes`. Shared `_upsert_promoted_theme`. | MED |
| F8 | live_tracker.py:629 | Finder claimed the 3:45 partial job duplicates the 4:45 scaffold → **verified DELIBERATE + documented** (decision fn is shared SSoT; write ownership explicit per BW 5/14 protocol). Optional `_load_exit_state` helper only. | LOW |
| F9 | spend_tracker.py | #377 cost meter pasted as `try/except: pass` at ~16 call sites — a tracker failure silences ALL spend telemetry (the exact May-2026 outage class it was built to fix). One `log_anthropic_call_safe` wrapper that WARNs. | MED |
| F10 | execution_client.py:113 | `_http_call` builds a fresh `httpx.AsyncClient` (new pool + TLS handshake) per facade call incl. `trigger_orb_entry` on the ORB window. Module-level shared client. **Trade transport → careful path** (staging exercise, not a hot fix). | MED |
| F11 | flag_detector.py:77 | `_htf_settle_from_bars` re-implements `anticipation.entry_bet_outcome`'s capture/stop/abstain semantics — lockstep-divergence risk makes Family-A vs HTF capture% incomparable. Generalize the tested primitive (entry-price override + include-entry-bar flag). | MED |
| F12 | db.py:6686 | `_dd` ISO→date coercion redefined in 4 functions + 1 inline copy in live_tracker (the LZB 6/13 class was one missed site). One module-level `_coerce_date`. | LOW |
| F13 | channels/telegram.py:1586 | 6+ copies of the AgentRequest→POST /task→reply boilerplate; the copies already diverge — `/themes <arg>` lacks the plain-text retry on Markdown-400 that `/ideas` has (underscore-heavy theme name → hard error). One `_post_market_task` helper. | MED |
| F14 | order_manager.py:1729 | `execute_partial_exit` copies 5 vars into `_*_out` relay names after the advisory-lock block — pure renaming layer in the most safety-critical function; a future in-lock edit that misses the relay line creates a real mismatch. Use the original names. **Trade-state → #151 discipline** (paper exercise before relying on cron). | MED |
| F15 | flag_detector.py:174 | `_FLAG_DEPTH_MIN` (the load-bearing ≤25% HTF depth gate) is stranded mid-file glued to a SHADOW-only helper, ~120 lines from its sibling gate constants. Move to the constants block. | LOW |

## Money-path correctness pass (broker/ diff, single agent)

| # | Where | Finding | Priority |
|---|---|---|---|
| F16 | order_manager.py:2696-2702 + alpaca_client.py:532-538 | **`_ensure_stop_coverage`'s ambiguity guard is dead code.** The `except → return None ("couldn't read broker — defer")` can never fire: `get_open_orders` catches ALL exceptions internally and returns `[]` (with a #370 alert). A transient orders-read failure is therefore indistinguishable from "no live stop" → the coverage loop proceeds to PLACE a stop while the real one may still be live → duplicate sell-stops → oversell hazard; an ACCEPTED duplicate then permanently wedges future repair via the `>1 stops → ambiguous skip` branch (:2706). Backstop = Alpaca's share-reservation rejection only. FIX = make ambiguity representable (exceptions-out read for this caller, or a `None`-on-failure sentinel distinct from `[]`). **Trade-state → careful path (#151 discipline).** Verified inline 7/2. | HIGH |
| F17 | agent.py:626-635 → execution_client.py:350 → telegram_confirm.py | **Staged-proposal Confirm flow is broken under the live HTTP-split topology — this is WHY the #364 buttons "never worked."** `/broker/callback` registers unconditionally on the intelligence container; `handle_confirm_callback` is a documented LOCAL passthrough (deferred from HTTP, "W2 step 5" — never designed); `handle_callback` flips `status→'confirmed'` BEFORE `submit_entry`, which fails on the creds-less container → trade wedged at 'confirmed', no order, no stop, no retry (re-press hits the pending-confirmation guard), logger-only failure. DORMANT today (no strategy stages proposals — magna53 auto-enters). **Resolution = the #364 operator call: removal kills the path; if staged mode is ever revived, the routing must be designed first + the flip-before-submit ordering fixed.** | HIGH (dormant) |
| F18 | execution_routes.py:58-68 + execution_client.py:113-127 | **Execution-side real failures are misclassified as "unreachable."** `_exec_call` has no try/except → any genuine exception inside `trigger_orb_entry`/`execute_partial_exit` (Alpaca rejection, DB error) becomes a generic FastAPI 500 → the client wraps it as `ExecutionUnreachable`, discarding the original reason — directly contradicting the module's own invariant (lines 23-25: unreachable must stay distinct from answered-with-error). ACTIVE now (`EXECUTION_MODE=http` in prod): a real order-submission failure during the ORB window reads as a network blip, steering the operator's response wrong. FIX = catch in `_exec_call` → structured error payload (type+message) → client re-raises the original class distinctly. **Careful path (execution transport; staging exercise).** Verified inline 7/2. | HIGH |
| F19 | order_manager.py:1449-1474/:1734-1803 vs :3008-3066 | **Two unsynchronized stop-remediators can race**: the #151 abort-path re-protect (advisory-locked) vs the pre-existing `_sync_positions_for_mode` orphan loop (NO lock) — in different processes under the HTTP split. Both can observe "no live stop" for the same trade before either writes → duplicate stop → repair then permanently declined by the ambiguous branch. Low frequency (sync 2×/day, aborts rare) but newly possible since the split. FIX direction = orphan loop takes the same per-trade advisory try-lock. Careful path. | MED |
| F20 | order_manager.py:1102-1131 + db.py:112 | **`_trade_advisory_lock` holds a dedicated pool connection across multi-second broker I/O** against `max_size=5`; nested `pool.acquire()` calls run inside its scope. 2+ concurrent partials (or partial + coverage try-locks) can self-starve the pool → stall on the real-money exit path. Not possible pre-diff (per-statement acquires only). FIX direction = shrink the connection hold to the lock acquire/release, or a dedicated non-pool connection. Careful path. | MED |
| F21 | kill_scale_bands.py:255-273 | TOCTOU on band-transition dedup: unlocked read→write lets concurrent scheduled + on-demand evaluations both see `transitioned=True` → duplicate Telegram/audit. Advisory-only module — no money impact. | LOW |
| F22 | live_tracker.py:~606-614 + order_manager.py:1853-1860 | **#361 regressed the BW 5/14 protection on `breakeven_active` — lost-update can permanently clobber the breakeven floor after a partial.** Pre-#361, the partial-day UPDATE deliberately OMITTED `breakeven_active`; post-#361 the 4:45 job writes it unconditionally as a passthrough of its job-START read (with `skip_partial_decision=True`, `apply_daily_exit_step` never mutates it). If `finalize_partial_exit` (the only `TRUE`-writer) lands between the job's opening SELECT and that ticker's UPDATE — delayed WS fill (the 9 PM backstop exists precisely for late events) or `/partialnow` near 4:45 — the job writes stale FALSE. NO self-heal: `partial_taken=TRUE` survives → the partial branch never re-fires → the flag stays FALSE for the trade's life. Dollar impact real: the partial's stop-replace is qty-only (keeps the OLD stop price); the breakeven raise happens ONLY via the 4:45 `update_stop` with the breakeven-floored effective stop → remaining ~2/3 keeps its below-entry stop; on a fade, exit at hard_stop instead of breakeven. FIX direction: make the write monotonic (`SET breakeven_active = breakeven_active OR $3` — the flag never legitimately goes TRUE→FALSE) or drop it from this UPDATE and derive from `partial_taken`. **Trade-state → careful path (#151 discipline). Mechanism confirmed; likelihood MED (needs the delayed-fill window), impact HIGH.** | HIGH |
| F23 | drawdown_breaker.py:437-461 | The new tier-transition Telegram is **one-shot-lossy**: `send_telegram_message` returns False (never raises), the return is unchecked, and the alert fires on transition only — a Telegram outage at the transition moment permanently loses the REDUCE/BLOCK sizing alert (regressing to the exact 6/05 silent-REDUCE behavior this change was built to fix; the audit row remains, which was the original insufficient surface). FIX = check the return + persist an alert-pending retry (same class as the kill/scale state-persisted-before-alert pattern). | MED |

**Coverage map (money path — COMPLETE):** order_manager / entry_pipeline / alpaca_client / execution_client / execution_routes / kill_scale_bands — reviewed (agent + inline spot-verify). trade_stream + exit_logic — no changes in range. live_tracker (+295) — reviewed twice: inline pass called it clean; the sub-review then found **F22** (the cross-process lost-update — a reminder that comment-discipline review ≠ concurrency review). drawdown_breaker (+26) — F23. gap_through_telemetry (+16) — clean (label sync; also revealed the sprint doc still said P0 for `alpaca_stop_trigger_reliability`, ratified P2 on 6/03 → fixed). telegram_confirm (+39) — audited clean (the Markdown-retry double-send is cosmetic only; the atomic `WHERE status='pending_confirmation'` guard prevents duplicate orders); the F17 topology wedge stands separately.
