# Lane-1 sitting pack — every fork pre-argued (Fable, built 2026-07-11 eve for the Sun 7/13 sitting)

> **✅ SITTING HELD 2026-07-12 (ran a day early) — FULL DOCKET RULED; this doc is now the MINUTES.**
> **Bundle A: SIGNED wholesale (all 7).** **B1 #395: NO-GO on real entries + the shadow KEEPS
> OBSERVING** (operator chose observe over the kill rec — re-surface only on a sign-flip or
> operator call). **B2 0026-D2: PARKED** (behind the Block-4 T3 faithful harness; D1+D3 signed).
> **B3 #452: SHADOW-COUNT NOW** (build this week; theme-membership family key; promote-to-blocking
> after ~2 clean weeks = operator fork). **Bundle C: RATIFIED all** (incl. #416 proceed; #394
> auto-resolved = methodology signed, tuning waits on shadow-N). Rulings recorded on every source
> task line (commit of 2026-07-12); #170 + #395 + #459 CLOSED as completed decisions; the
> `ep_cooldown_resetup_admission` gated review resolved. Unblocked build queue: see the CLOSE
> commit + HANDOFF §3.

**Pre-read (10 min):** `edge_dossier_451` → `premortem_450` §decision-surface → this pack.
**Format:** every queued fork = the call · the strongest case AGAINST the rec · why the rec
survives · evidence pointer. Grouped for ruling speed: **Bundle A** = sign-wholesale (each rec
is evidence-backed and reversible-or-gated) · **Bundle B** = the 3 real deliberations · **Bundle
C** = ratify-and-move. Rulings land on the source task lines.

**If you rule only five things:** B1 (#395 kill), B2 (0026-D2 park), B3 (#452 scope),
A1 (0025 sign → unblocks the merge-arm cards), A6 (0030/0031 sign → unblocks this week's
robustness map before the 7/18 M1).

---

## Bundle A — sign-wholesale (recs with their strongest counters, pre-argued)

**A1 · ADR 0025 F1–F3 (theme merge arm) — SIGN.**
Against: one non-deterministic Haiku replay; a wrong merge later could collapse a real
distinction. Survives: the replay PASSED with both legit-kill anchors safe + driver-based
precision on all 25 distincts; C3 hardens (temp-0/retry) and the T2b golden corpus (14 pairs,
hard anchors zero-tolerance) now GATES the build; no-money surface; 30d distinct-cooldown bounds
blast radius. F1 keep-anchor-name · F2 pair-scope as designed · F3 ship Arm A independently = yes.
*Evidence: `274_merge_replay_table` (✓PASS) · `theme_merge_corpus_v1.json`.*

**A2 · ADR 0027 F1–F2 (Family-B lifecycle) — SIGN.**
Against: adds machinery while Family-B entry evidence is thin. Survives: it IS the
evidence-generation machinery (#326 needs its readout by 9/15); observational-only; pure
inheritance of the proven Family-A shapes; the R9 `cooldown_suppressed` tag closes the #170
bypass. F1 seed-scope + F2 seed-gate-bypass = per the ADR's recs.
*Evidence: ADR 0027 · T4 register R3/R9.*

**A3 · ADR 0028 F1–F3 (salience profiles) — SIGN THE FRAME (P0 visibility only).**
Against: profile machinery before per-class N exists = overfit scaffolding. Survives: F1's whole
point is REFUSING three rubrics (the overfit machine); P0 is deterministic classification +
visibility, fail-to-baseline, no grade change; P1 calibration stays gated on corpus arithmetic.
F2 taxonomy as defined · F3 momentum-penalty = test-in-P1.
*Evidence: ADR 0028 §1 · T4 register R6 (gate-tuple extension recorded).*

**A4 · ADR 0029 F1–F3 (entry-bracket mechanics) — SIGN; ship D1 (C2) on the next execution deploy.**
Against: touching live bracket mechanics for a fragility that hasn't fired recently. Survives:
D1 is a correctness consolidation two other designs SEQUENCE BEHIND (0031's live fork; R8
precedence); F1's D2 winner is deliberately evidence-decided (C1's table), not pre-picked; F3
2% chase-ceiling is a design bound, not a tune.
*Evidence: ADR 0029 · T4 register R8.*

**A5 · #357 role — SIGN CONFLUENCE (staged): Stage-1 badge NOW, Stage-2 credit at N≥15 via the 0028 classifier.**
Against: 11 member-alerts is tiny. Survives: the staging IS the answer to small N — the badge is
zero-money visibility; the credit waits for the gate and routes through the classifier (register
R11), with watchlist-only as the encoded fallback.
*Evidence: `357_sugar_step0_table` (80% vs 40% across 9 distinct names).*

**A6 · Tonight's designs: 0030 F1–F3 + 0031 F1–F3 + T2b/T2c forks — SIGN.**
Against (0030): golden verdicts encode judgment; a wrong golden trains the wrong behavior.
Survives: every golden carries its rationale inline; positive controls + D08 protect the
over-skepticism direction; the corpus only grows, never shrinks-to-pass; gate hard-FAIL has a
signed-waiver valve. Against (0031): two arms = double build. Survives: the arms are the two
computable readings of your own methodology doc; the shadow (not a debate) picks; abstention is
first-class; the sequencing gate is encoded hard. T2b's M10-MERGE/M14-DISTINCT are the two
direction-testing calls — each carries its rationale. T2c metrics are observe-only L2 surfaces.
**Signing A6 unblocks the 0030-C1/C3 build THIS WEEK — the robustness map must precede 7/18.**
*Evidence: ADR 0030 · ADR 0031 · `block3_t2_llm_surface_hardening`.*

**A7 · Premortem items #454 (calibration honesty) + #455 (intraday drawdown alert) + #456 (residuals scheduling) — SIGN AS FILED.**
Against: build-queue pressure. Survives: #454 makes the next 15 live trades diagnostic (cheap,
observational); #455 closes the six-naked-hours gap with an alert, not an action; #456 is
scheduling only.
*Evidence: `premortem_450` R3/R4/R6.*

## Bundle B — the three real deliberations

**B1 · #395 — coil-finder → real entries: NO-GO, and KILL vs keep-observing.**
- *Steelman GO/observe:* N=34 is modest; momentum systems live with −1 medians; three +2R target
  hits prove the watcher mechanics work; the sunk build is real.
- *Counter (why NO-GO):* the shape is wrong, not just the mean — 18% win with the tail pointing
  DOWN (−13.98R gap-through) is the *inverse* of the tail-carried edge this book runs on; and the
  offline Phase-B re-timing PREDICTED this result — consistent evidence, not variance. Keeping a
  measured-losing setup live-adjacent invites re-litigating it monthly.
- **Rec: NO-GO + formally kill the real-entry path.** Keep the shadow ONLY if you name a specific
  parameter hypothesis tonight (none is on the table); otherwise stop the shadow too and free the
  attention. Consequences auto-cascade: #353 stays gated · #394 tuning closes as moot · 0026 is
  machinery-only (register R2).
- *Evidence: `395_go_nogo_evidence` · dossier §5.*

**B2 · 0026-D2 (drop the COILED prerequisite) — PARK (do not sign, do not build).**
- *Steelman build-anyway:* the 9/92 stuck-in-TIGHTENING cohort is real; direct-trigger names
  show BIGGER fwd-max (+3.47R median upper bound) — there may be winners the gate blocks.
- *Counter (why park):* tonight's probe reversed the design's premise — the incumbent gate is
  **tail-positive (+0.78R mean, +14.9R total)**; and the replay that would justify the change is
  only 4/21-faithful, so there is NO rule-grade evidence FOR it. Loosening a working gate on a
  19%-faithful instrument is exactly the overfit CHANGE_PROCESS exists to stop.
- **Rec: park D2 behind a faithful direct-trigger harness (a filed card, built only if the
  fwd-max signal keeps tempting us). Sign D1 + D3 tonight; they don't depend on D2.**
- *Evidence: `146_triggered_gate_backtest_table` · register R1.*

**B3 · #452 — the family-slot cap (premortem R1, the TOP risk): scope the build.**
- *Steelman defer:* the correlated book hasn't burned us yet; theme data is noisy (78-theme
  fragmentation just got a merge arm signed tonight).
- *Counter (why build now):* R1 is the pre-mortem's largest single loss path (5 slots = 1 theme
  in 5 costumes); the cap is observational-then-blocking (same ladder every safeguard used); and
  the register already solved its two design traps (`exposure_family` keyed on post-0025
  membership · `db.OPEN_POSITION_STATUSES` — R5).
- **Rec: build #452 this week as SHADOW-COUNT + alert first** (log when a would-be entry lifts
  same-family exposure above 2), flip to blocking after the count proves clean for ~2 weeks —
  the drawdown-breaker promotion pattern. Fork for you: the v1 family key = active-theme
  membership (rec) vs sector.
- *Evidence: `premortem_450` R1 · register R5.*

## Bundle C — ratify and move

- **C1 · #170 cooldown re-setup — ratify NO-GO** (below the live book; anti-selective; 6/01
  didn't replicate). A fresh-CATALYST discriminator is a *redesign* — file only if wanted.
- **C2 · #448 b6 — ratify the no-LLM re-score path** (deterministic re-derive + FMP quarters);
  the crosstab runs in its own 7/16 session with the fidelity plan as specced.
- **C3 · #394 — closes as MOOT if B1 = kill** (nothing left to tune); if observe, sign the
  methodology doc as-is and revisit at shadow-N.
- **C4 · #416 M&A amendment (due 7/16)** — rule from its own doc at the sitting; not re-argued
  here (its evidence didn't change tonight).
- **C5 · R5-at-M1 preconditions — ratify:** the T2c drift band (specced) + the 0030 robustness
  map (C3 card) are the #335 flip preconditions; both build this week. Detail: the M1 pack.

---
*Everything above lands its ruling on the source task line; the cards each ruling unblocks are
already queued on those lines. Built by Fable 7/11 eve so the sitting is ruling-speed.*
