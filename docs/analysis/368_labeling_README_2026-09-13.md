# The 44 rows — one question, three possible answers

**Sheet: `368_labeling_sheet_2026-09-13.tsv`** (opens in Numbers/Excel). Fill the **LABEL**
column. Nothing else needs to change. **~20 minutes** at ~25s a row. Resumable — partial is fine,
and re-running the seeder never overwrites a row you have already answered.

## The question — the same one for all 44

Every row is a themed EP alert. For each:

> **Was the theme actually the driver of this move?**

| LABEL | means |
|---|---|
| `y` | yes — the theme drove it |
| `n` | no — we credited a theme that was not the driver |
| `?` | genuinely cannot tell |

Use **NOTE** when the answer needs a word (it is where your *"crypto miners are converting into
AI infra plays"* note came from, and that one reframed the whole finding).

## What your answers decide

**How big the theme boost should be** — the one open number in the meta-rubric. The boost only
ever *adds* to a themed name and never penalises a themeless one, so this half of the cohort is
the half that decision rests on.

- These 44 are the false-positive side: *did we credit a theme that was not really there?*
- You have already labelled 59 themed rows (71 yes / 18 no / 1 unsure overall). These are the
  ones enrolled since, so the sample describes **today's engine**.

## Two things worth knowing before you start

- **Every row is from the current engine** — alerts 2026-08-04 to 09-08. The earlier cohort was
  scrapped because it described an engine state we no longer run, and labelling that would have
  been your time spent on a system that does not exist.
- **44 rows gives ±11 points.** That is workable for a magnitude we will revisit; it is not
  precision. Getting to ±8 means waiting roughly three more months for rows to accrue, which is
  why the ask is this size rather than larger.

## The other half, deliberately not sent

The themeless winners (*"was there a theme here we missed?"*) accrue at about one a day, so
tightening that side means waiting ~44 days. That half guards a directional guarantee — never
penalise a themeless name — rather than a number, so it does not block this decision.

Send the filled sheet back and I ingest it the same turn.
