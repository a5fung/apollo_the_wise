# APOLLO — THE DONE-DONE MAP (Fable strategic reassessment, 2026-07-12 eve)

**The question asked:** where are we relative to *done done, 100% complete*; the path; the
blockers; how to accelerate. **The honest headline: Apollo's BUILD is ~85–90% complete for
everything the next two milestones need — the remaining path is EVIDENCE-BOUND, not
build-bound.** The 7/12 gap-hunt already proved the design shelf is finished ("the program is
execution-shaped"); this weekend finished the instrumentation. What stands between today and
"done" is mostly *accrual*: clean soak days, live trades, shadow settlements — plus a short
list of specific pulls that compress the calendar.

---

## §1 What "done done" actually means — five metered levels

"100%" for a trading system is an OPERATING STATE, not a feature list. Proposed ladder (each
level metered; the operator signs each):

| Level | Definition | The meter |
|---|---|---|
| **D1 — v1.0 DECLARED** | The institution is complete + hardened: full live loop, mechanical safety, ops autonomy, mirror completeness, docs-only recovery | FL-1..FL-8 green + the §8 signature (`v1-closeout-productization.md`) |
| **D2 — EDGE PROVEN (or killed)** | The strategy question answered on real money | The **#268b kill/scale bands** verdict at N≥20 live MAGNA53 trades: scale, hold, or kill — any of the three IS "done" for D2 |
| **D3 — THE BOOK** | Not one regime's edge: ≥2 more setups through their own graduation gates + regime_matrix live | HTF (#397) · 9M-Day2 · consolidation Confirm/U&R each at its own N-gate; 0020 §5 matrix signed |
| **D4 — TIER-ONE METERED** | The five tier-one criteria each measurably green | judge precision on labels · conviction+regime sizing live · **MFE capture >50%** (today 18%) · regime adaptation w/o re-tuning panic · the review loop demonstrably compounding (rubric versions advancing on evidence) |
| **D5 — DONE-DONE** | The institution RUNS ITSELF, sustained | **One full quarter**: operator role = supervision only (sittings/sign-offs, ≤30 min/day) · zero manual trade-state repairs · costs inside the envelope · capital scaled to the operator's target · every change still CHANGE_PROCESS'd |

**Current honest percentages:** D1 ≈ **90%** · D2 ≈ **15%** (N=3/20; all instrumentation live)
· D3 ≈ **40%** (fully designed, shadows accruing, nothing graduated) · D4 ≈ **35%** (designed
100%, capture 18/50, autonomy rung 2/5) · D5 ≈ **5%** (the ops streak is the seed).

## §2 Where we are — the ground truth (7/12 eve)

- **Live**: MAGNA53 real money since 6/30 · N=3 closed / −$71 (below noise) · equity ~$4.9k ·
  prod healthy · five verified money-path deploys today alone.
- **FL scorecard**: FL-3 ✅ (7 green nights done) · FL-8 ✅ (4th Sunday completed TODAY) ·
  FL-2 ~85% (max-positions got a LIVE exercise 7/6 via the phantoms; a few drills remain) ·
  FL-4 built-and-dark (ingest R1 paper-proven; promotion gated) · FL-5 partial (#417 pass-1
  done; setup-doc reconcile = a walk item) · FL-6 = one small card (spend data already exists
  via #377) · FL-7 = mostly market-gated verify-lives · **FL-1 = 3/10 and THE pacing item.**
- **The edge**: a monitored hypothesis (dossier) — selection creates R, tails carry it; the
  next 15 live trades are diagnostic (#454 signed). Nothing yet contradicts it; nothing yet
  proves it. That is exactly what D2 exists to answer.
- **The design shelf**: ADRs 0011–0031 — complete. The judge is measured (36/36), gated
  ([5m/7], proven on a live model-swap), and drift-monitored (T2c, accruing from Monday).

## §3 The critical path (what actually gates what)

```
                       ┌─ FL-1 soak (10 clean days) ──────────┐
 TODAY ─┬─ FL-4 ingest promotion (dry_run → live_r1) ─────────┼─→ D1 DECLARE (~7/22–24 if pulled)
        ├─ FL-6 cost card · FL-2 last drills · FL-5 reconcile ┘
        │
        ├─ live N accrual (≈1.5–2/wk) ────────────────────────→ D2 bands verdict (N≥20 ≈ mid-Sept)
        │      └─ capture% program in parallel: giveback F1 (8/06) → M4 exit-tune (7/25) → 0031 readout (8/09)
        │
        ├─ M1 judge authority (7/18, fully armed) ─┐
        ├─ HTF #397 GO/NO-GO (7/18, LOW-flow note) ┼──────────→ D3 book (H2) → D4 tier-one (Q4)
        ├─ 0026 machinery + Confirm/U&R shadows ───┘
        │
        └─ M5 (9/1): P5 allocator armed · #381 zero ──────────→ D4 → D5 = a sustained QUARTER (≈ Q1 2027)
```

**The rate limiters, ranked:**
1. **Live N accrual (gates D2)** — ~1.5–2 trades/wk at MAGNA53's selectivity in this regime.
   *Not accelerable by building.* The ONLY honest accelerants: time, and (for the BOOK's
   aggregate edge, not MAGNA53's verdict) more setups graduating. ⚠ Explicitly REJECTED as a
   lever: widening selection/sizing to accrue N faster — that changes the strategy being
   measured (THE LINE + it invalidates the bands).
2. **The soak clock (gates D1)** — repairs reset it. Today's hardening (finalizer locks, the
   /pause hole, watchdogs, janitor design) exists precisely to keep the next 10 days
   repair-free. The best soak accelerator is the work already deployed.
3. **Shadow settlements (gate D3/D4 readouts)** — giveback + pivot arms need live
   round-trippers; HTF needs breaks. Flow-bound; reviews are date-armed so nothing stalls.
4. **Operator decision bandwidth** — the real human constraint. The packs pattern (Lane-1,
   M1) is the fix: every sitting turnkey, every fork pre-argued. Institutionalize it.

## §4 Blockers — the specific list

**Hard (calendar-moving):**
- **B1 · FL-1 needs a RULING**: do committed, dry-run-reviewed repair scripts (7/6 reap, 7/7
  jsonb cleanup) reset the soak? *Strict reading: yes → clock restarted 7/8 → 3/10 → soak
  completes ~7/22.* The fork is the operator's; the strict ruling is recommended (they were
  repairs — the system needed hands). **Decide at the walk-pack review so the declare date is
  real.**
- **B2 · FL-4 promotion is date-parked, not blocked**: the 7/25 review is a BACKSTOP. Nothing
  prevents flipping `dry_run` Monday; ≥3 clean R1 proposals + sign-off → `live_r1` ≈ 7/17 →
  5 quiet days → FL-4 green ≈ 7/24, INSIDE the soak window.
- **B3 · D2 is math**: N≥20 at current flow = mid-September. No honest shortcut exists.

**Soft (queued, not calendar-critical):** FL-6 card · FL-2 remaining drills (daily-loss
synthetic, boot preflight evidence) · FL-5 reconcile sweep · #416 draft (7/16) · #448 crosstab
(7/16) · 0025 C1–C3 build · #464/#465 hardening (post-D1 fine) · the week's verify-lives.

## §5 Acceleration levers (each with cost/risk/rec)

| # | Lever | Effect | Risk | Rec |
|---|---|---|---|---|
| A1 | **Flip ingest `dry_run` Monday** (operator, 1 min) | FL-4 green ~7/24 vs ~8/1 | zero (propose-only) | **DO** |
| A2 | **Rule the soak-reset question + fix the declare date (~7/22)** | D1 date becomes real, not a window | none | **DO** |
| A3 | **Pre-write the #425 walk pack** (tonight, Fable) | the declaration walk = ruling-speed | none | **DONE tonight** |
| A4 | **FL-6 cost card this week** (Sonnet, ~half-day) | removes the last build FL | none | **DO** |
| A5 | **HTF #397 GO-to-paper at 7/18 IF N supports** | D3 starts accruing its first non-MAGNA lane | low-flow expectation (T3: ~4/3mo) — a GO may still be right, with honest flow expectations | operator at 7/18 |
| A6 | Build 0026 machinery + 0027 lifecycle (the week's cards) | more shadow lanes → D3 evidence | none (shadow) | queue as planned |
| A7 | ~~Widen selection / add regimes to accrue N faster~~ | — | invalidates D2's measurement + THE LINE | **REJECTED — recorded so it never creeps in** |

## §6 The last-Fable deliverables (tonight)

1. **This map** — the shared definition of done + the path. Review at the walk.
2. **The #425 declaration-walk pack** (`docs/roadmap/v1_declaration_walk_pack.md`) — FL-by-FL
   evidence pointers, the two forks (B1 soak ruling · B2 ingest pull) pre-argued, the sign
   sheet. The walk becomes an hour, not a day.

**And the honest close:** after tonight there is genuinely nothing left that needs Fable — the
designs are signed, the instruments are live, the packs are written. From here Apollo gets
"done" by *running*: clean days, real trades, settled shadows, and the operator ruling at the
gates the system now sets for itself. **D1 in ~10 days · D2 by mid-September · D3/D4 across
Q4 · D5 = the quarter after the system stops needing us daily.**
