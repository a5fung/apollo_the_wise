### 2026-08-11 — account-mode literal gate [5o/7]
- SQL mode/phase literals need `mode-ok:`; nightly sweep replays them on any phase change / dormant pinned book, once ever. SSoT `dual_account.md`.

# Apollo the Wise — Change Log

Compressed historical change log. **Live operational reference + recent prose entries (~2 weeks) live in `CLAUDE.md`.** Entries graduate here once they age out of the recent window.

Each entry is a one-liner: `topic — key change & lesson`. Full prose lives in git history at the listed commits — `git log --grep="<topic>"` or `git blame` for code-level context.

When consulted: investigating "why did we change X?", design reviews, retrospectives. Not loaded by default into agent context.

---


### 2026-08-09 — measure the guard before building it (graduated from CLAUDE.md 2026-08-11)

- Specced a delegation gate, measured 37 session-days, did NOT build it (complaint days sit inside
  the normal range — best achievable precision 33%). Routing declaration + advisory ledger instead;
  why it can't be gated: commit `f578a54`.


### 2026-08-03 — the week's lesson: correct code, wrong surface (graduated from CLAUDE.md 2026-08-08)

- **Four defects that week shared one shape** — the code was right and the thing carrying it was
  not: an insert naming a renamed constraint (2 days silent), a board keying off a field its payload
  never supplied (inert from day one), a gate living only on one machine (`.claude/` gitignored),
  and a sweep whose varied setting never reached the code (91 days).
- **So verify the SURFACE** — the rendered message, the deployed image, the actual row. A passing
  test on fabricated input proves nothing about production.
- **A guard that always fires is not a guard.** Three broad checks were built and thrown away that
  week after measuring their false-positive rate.

### 2026-07-24/25 — v1.0 DECLARED · ATAI deal-pin (#502) · breaker live-path (#454 p3) · operating model
- v1.0 shipped 7/24 (§8 signed, all 8 FL gates green) — the blocking/launch lens retires, the board becomes the #419 Phase-2 program. **#502**: a cash-deal pin is mechanically indistinguishable from a coil — the tightness that scores it COILED *is* the pin; both M&A layers structurally could not reach a FRESH pin, so a two-axis conjunction (5d band ≤2.5% AND ≥5× vol spike) shipped — 11 suppressed / 393 preserved over 405 rows, HUM (+25.7%) free. Rejected + documented: reusing the EP classifier verdict (its store is 1-true/1-junk — ACLS graded `mna` while its own summary said "no catalysts found", then ran +16%). **#454 p3**: drawdown-breaker live-path exercise, 37/37, ZERO writes; set-tier-then-restore was rejected because REDUCE returns ok=True@0.5×, so a crash would silently half-size live entries. **Lessons**: a filter list is the operator's call (CHANGE_PROCESS r3) — state findings, not rulings; and 3 harness "failures" were wrong expectations, the code was right.

### 2026-07-14 — Theme ecosystems (ADR 0032) Phase 1 · Marios-litmus PASS · /themes v2
- Theme ecosystem read-model (no-money, Fable-built/Opus-verified): a curated 20-bucket taxonomy (`theme_ecosystems.yaml`, operator-signed; AI DISTRIBUTED across E-AISEMI/E-AIINFRA/E-SAAS+E-CYBR) + `mi_theme_ecosystems` Haiku-at-birth mapping + a breadth-weighted boosted score + a hierarchical `/themes` v2 board. Litmus vs Marios (cyber-dominant): Apollo DETECTED perfectly (cohort RS 75→89) but had SURFACED it as 1 buried line of 63; the board now ranks E-CYBR #1. **Lesson ([[verify-operator-facing-surface]]): "verify-live green" checked DB data, not the Telegram render — `/themes` routed orchestrator-side to a handler the ecosystem render was never wired into. Verify the operator-facing SURFACE.**

### 2026-07-12/13 — session growth gate · 2 Fable red-teams · 6-fork rulings pack
- Growth gate (`check_plan` HARD): a session may not END with more open tasks than the PT-day began; `--today` pins the baseline, operator `--carryover` is the only escape (after a month of fake burndown, 99→116). Fable via Agent-model-override ran 2 red-teams (composition + v1-readiness) — all code-verified, 3 fixes deployed same-night. 6-fork rulings pack approved wholesale: soak STRICT · FL-4 synthetic criterion + dry_run · 0025 slice→MERGE prompt v2 · giveback = close-below DECISION-LINE · RED-3 sizing clamp · #416 signed. **Lesson: verify premium-model output vs code — 1 of 6 REDs was over-rated (the NULL was a deliberate fail-safe).**

### 2026-07-16 — FIRST REAL-MONEY FILL chain VERIFIED (#413 closed) · #150 reservation-race retry verified-live
- MANE 7/15 (live, $119.34×8): ORB bracket → entry filled → stop-refresh hit the EXACT share-reservation race (`held_for_orders:8, available:0`, old stop leg named) → `_is_share_reservation_lag` bounded retry placed the stop 3s later (bc100ddb) → old leg canceled clean → held overnight protected → stop filled 7/16 open, −$2.40, exits row written. Zero naked-stop alarms, WS+reconcile agreed; TSEM 7/14 = 2nd clean instance. Closes #413 (first-fill chain watch) AND #150 (the FTRE-class retry, event-gated since 5/09 — observed clean on real money). Lesson: the two rarest-event gates both cleared on one trade — check event-gated tasks against EVERY live fill.

### 2026-07-16 — THEME_SUBTHEME_ARM flipped · the big-push night (graduated from CLAUDE.md 2026-07-31)
- **#471 flip executed** (operator-named, Fable-double-checked): `theme_subtheme_arm=on` 17:15 ET; first armed run Fri nightly. **#413+#150 closed on ONE trade** — MANE 7/15 (live) verified the first-fill chain AND the reservation-race retry — lesson: **check event-gated tasks against every live fill** (#269's AKTS event had fired unnoticed too). **B6 + #468 converge**: the EP score's 70-79 band is anti-predictive (60-69 is the BEST) → the lever is score composition, not thresholds → meta-rubric block. **#476 root cause** = the March `biotech max_themes=0` sector-cap (silent Pass-2 drop + nightly shadow-promote resurrection) — Option A ruled, replay-calibrated to per-family cap ≤6. **#477** = one bare `_` in `(ingest dry_run)` flipped the brief chunk's entity parity (Telegram blames a far-downstream offset). Push night: 3 closes + 8 builds (109→106); Friday staged via `decision_digest_2026-07-17.md`. Lesson: prod mutations need operator-NAMED actions (classifier) — batch asks into digests.

### 2026-07-09 — F1 giveback shadow deployed · #290 closed · #439 part-b
- ADR 0023 F1: `giveback_shadow.py` (+6%/60% peak-lock) deployed both money containers as log-only SHADOW on the live book (THE LINE) + `giveback_shadow_review` gate; #443 alert-label fix rode it. #290 CLOSED — late-entry backtest R-NEGATIVE (N=196; wide stop, no fresh ORB), don't extend. #439 part-b: G6 resting-test-order sweep (`_sweep_test_orders_until_clear`). Deploy lessons: root-file drift-guard classification + `# exec-boundary-ok` import tag. Advisor: verify the control, lead with the internal well-powered result.

### 2026-07-08 — ADR 0023 exit-cards · ADR 0024 M1-a/c · alert fix
- Dark/evidence: ADR 0023 Cards 1/2/3 (giveback_floor + harvest sweep → +$8,075 F1-evidence) + Card 5; ADR 0024 M1-a dark + M1-c draft. #443 paper/live alert mislabel fixed. #274 fragmentation root-caused → weekend Fable block. Lesson: asyncpg single-conn can't multiplex a query-gather.

### 2026-07-05 — day 3: hardening lane CLEARED
- #225 [5l/7] demotion fence · #382 money-path swallows 95→81 · #184a coverage-drift · F10 shared client · #256 restore-check crons (run-1 caught the dashboard_ro DR roles gap → roles.sql rides the bundle) · #417 pass-1. Lessons: fence dynamic text in Telegram legacy-Markdown; shell-helper drift → infra/ops_lib.sh.

### 2026-07-04 — day 2: #347 FLIPPED LIVE · axes built
- #347 flip live + same-day hardening · ADRs 0015/0016 signed · #328 shadow deployed · 8 reviews closed · #381→95.

### 2026-06-27 — anticipation rebuild · #381 gate · HTF detection · PDT/ET fix
- 3 builds verified-live: anticipation #327 coil-finder · #381 swallow-a-failure ratchet gate · HTF #356 detection (90/40 + Stage-2 NCI-catch + flag→HTF rename). Recurring PDT/ET confusion fixed MECHANICALLY: `check_plan`→PT · `scripts/operator_now.py` clock · the ⏰ two-frames rule.

### 2026-07-03 — sprint day 1: 12 Sonnet cards + careful-path trio · 2 deploys · /simplify
- 17 executor cards + careful-path F22/F16/F18+sibling/F14 + #364 removed. 2 deploys verified; 2 /simplify cycles. 113→101; #381 baseline 174→125. ADRs 0015/0016 (theme+structure axes, 0015 calibrated on N=452). Allocator study: undecidable (telemetry gap named). Operator: #305→#413 · pacing policy.

### 2026-07-02 — comprehensive review (23 findings) · 5 ships
- 6/12→7/1 review: 3 fixed + #412 filed — top = F22 breakeven_active lost-update (#361 regression, careful-path). Ships: #401 naked-live alarm · #400 instant-revert + #149 retire · #384(1) tweet-RVOL · #398 dashboard search + fresh snapshot (was 6/9-stale) · sweep-floor 3500→2200 (#340 evidence). Day-3: no fire; VIX fix clean.

### 2026-07-01 — MAGNA53 launch days 1-2 · read-layer fixes · monthly-sweep move
- Day-1 (6/30): FIRST auto-entry (AVAV) — armed clean (stop attached), declined the non-breakout, cancelled clean; arm VERIFIED-LIVE, first FILL ahead (#305). Day-2: no fire. VIX I:VIX-403 + /htf read-fixes live; monthly backward-check sweep moved 8AM→6PM ET (a9752b5).

### 2026-06-22 (Mon) — MAGNA53 launch Phase 1 (staged, $0 real money) · partials paused→restored
- Phase 1 live-validated (creds boot · preflight · `/status` · `/pause`); magna53 `live_real_enabled=FALSE`, real money = Phase 2 on ACH funds-settle (~Wed). 3 launch-blockers fixed live; two-phase runbook (`docs/roadmap/monday_go_runbook.md`).
- **Partial exits — PAUSED 6/22, RESTORED 6/26** (`_PARTIAL_EXIT_PAUSED=False`, order_manager.py; #151 DONE — never-naked invariant + clean partial-exit cycles 6/23–24). The 6/22 pause was a TEMPORARY `pending_replace`-race mitigation; the #151 fix RESTORED them (the TESTED sell discipline, never removed) — see THE LINE.
- *(Recovered 2026-07-01: the 6/27 graduation deleted this entry from CLAUDE.md without appending it here; restored from git `show <pre-6/27>:CLAUDE.md`.)*

### 2026-06-20 — real-money auto-entry wired · filing-quality gates · burndown
- **Live AUTO-ENTRY wired** (`entry_pipeline._should_auto_enter`, operator-signed): a `phase=live` + `live_real_enabled=True` strategy now AUTO-FIRES real money instead of sending a manual STAGED-PAPER proposal (the `if is_paper:` branch had been the ONLY auto-submit; live always proposed). FINAL CONFIG (operator-reasoned): `position_size_multiplier=1.0` (full 1% risk ~$50/trade), `max_concurrent_positions=NULL` (share global 5 — broad participation for a low-WR setup per #197), START-SMALL = the $5,000 account itself. HARD gate: `live_real_enabled=TRUE` not permitted until `/pause` (#345) verified-live. SSoT safeguards.md + magna53_ep.md change-logs (6/20). The live submit path can't be integration-tested pre-Monday — the first-fire stop-leg watch IS the test.
- **Filing-quality gates** in `check_plan.py` (the #305 lesson — a real-money GO slipped as a bare one-liner the stub-ban couldn't see): (1) **high-stakes pointer gate** — a launch/real-money/cutover task must POINT at its execution (runbook/doc/#xref/SSoT) or the commit fails; (2) **CLOSE-ritual `--audit-new`** — git-diffs PLAN.md and flags ADDED `- #NNN |` lines that `looks_thin` (over-flags by design; "adequate detail" is semantic → a surface, not a hard gate). Both pin-tested; all 12 live high-stakes tasks pass green with zero enrichment.
- **Backlog burndown 132→112** by REAL completion only (operator directive — reverse the every-week-grows count; close/ship, never reclassify): #246 theme_engine isinstance-guard · #324 deploy execution-drift warn · #285 M&A dedup · #65 per-strategy sizing/cap (verified-live) · #260 skip-set dedup (already done 6/10 — the three hand-synced copies are now one `_PREPOSITION_SKIP` base) · +13 contentless stubs git-archaeology'd to closure · #134/#165/#113/#55 operator-decided. #305 (the 6/22 GO) enriched to be self-sufficient (runbook + auto-entry + config + sequence + gates + verify-live).
- **#349 DR rehearsal PASSED** end-to-end on the split topology (drill-mode neuters `.env` before Phase 9; prod untouched) — **Monday GO runbook** built (`docs/roadmap/monday_go_runbook.md`), surfaces the SQL-only flip gotcha (live_real_enabled/size_mult/max_positions have no /strategy command). **#261 caught**: 5 sweep tools mis-moved to `probes/` → back to root.

### 2026-06-19 — launch-readiness push (6/22 GO/NO-GO set up)
- **#304 GO/NO-GO evidence pack** assembled + advisor-corrected: operator's HARD #344 catalyst gate recorded as THEIR call (agent must not self-close a HARD gate); GATE-3 numbers traced to primary `sip_replay_r_cohort.py` (+2.18R selection delta, crosses 0 at drop-4 -> START-SMALL).
- **#303 launch-readiness audit**: every safeguard code-verified (panic button both gates, day-1 equity guards, creds boot-block, band job, #349 DR fix); fixed a stale CLAUDE.md blurb ("max 4/5-loss" -> 5/10-deprecated).
- **#302 P6 replay-regression** built + deployed: Sunday-digest section surfacing the live R-dist vs the #268b calibration envelope — SURFACES only, no auto-verdict (invalid at low N); `CALIBRATION_ENVELOPE` promoted to a single constant in kill_scale_bands.py.
- **#55 revenue-stage re-check**: evidence does NOT support the $5M ratchet ($0–5M band has positive-edge tail winners) -> keep `REVENUE_STAGE_MIN_USD=0.01`; the gate touches only the earnings boost.
- **Operator launch decisions**: #344 shadow + Wed-6/24-earliest flip (Mon–Wed real money on the pre-fix grade, accepted) · #349 DR not gating GO · #55 ratified · #301 deferred post-cutover.

### 2026-06-12 — #268 CLOSED + kill/scale bands SIGNED · launch runway day 1
- **#268 Phase B**: 1307/1307 judged (rubric v2 uniform). Floor +0.90R/28% (n=410) vs judge +0.95R/30% (n=399); **promote cohort +1.26R/33% = the consistent edge both phases**; demote side flips sign vs Phase A → standing watch-metric, not an assumed save (`docs/analysis/selection_replay_268_phaseB.md`). Contamination fix verified live (SHAZ/AKTS fired with 1,522 replay rows in-table).
- **Kill/scale bands SIGNED** (safeguards.md, launch DoD-1): REDUCE t20 ≤ −0.70R / streak ≥ 16; KILL t20 ≤ −1.05R / cum −30R / BLOCK tier; SCALE 40 trades ≥ +0.5R; 20-trade floor — every threshold outside the healthy-year envelope (a +0.95R year contains −24R DD + 15-loss streak). Operator conditions mechanical: quarterly review (registry `kill_scale_bands_quarterly_review`, first 8/1) + logged override authority (bands only). #275 = digest eval + transition alerts + override (built 6/19).
- **Alert-layer gaps fixed** (off an operator question): `regime_transition` had readers but NO writer → now emitted + Telegram on flips; drawdown tier changes were audit-only (6/05 REDUCE was silent) → now Telegram with sizing effect.
- **LYG silent-trigger** = cleanest paper-IEX-trigger proof (SIP printed the 5.46 trigger, IEX never, order live); class unchanged P2. ECOR banned (#274 structural fix). #229 closed (dead-cat class → #267 test set).
- **Rubric v3 LIVE** (`v3-2026-06-12-catalyst-freshness`): AKTS = the judge's first WRONG load-bearing promote (May-2024 Lilly deal surfaced UNDATED by a web-only corpus, `has_direct_source=false`), operator-caught in hours = first live operator label. Freshness clause → judge rule 1 + grade prompt; ADR 0011 3c; AKTS = named regression probe.
- **W2 study #1 (#276)**: 5-min OR DEAD (+0.15R); skip-wide-open NOT SHIPPABLE after TWO artifact catches — ATR-coverage no-op faked a +49% lift across the sweep; post-backfill the +3.63R lift is 91% in 5 outliers, median −1R, total P&L −40%. Lessons: verify a conditional filter OPERATED across the cohort; decompose winners for outlier concentration.

### 2026-06-10
- **JUDGE LOAD-BEARING day** — the flip (operator-signed ~10:45 ET; first real call CBRL MODERATE→none, a one-time $47.4M litigation settlement misread as an EPS beat) · #249 advisory stack retired into the judge (`fire_axes` = the one fire signal; materiality_shadow/theme_gated/_compute_fire_status removed; ADR 0010 superseded) · cross-ticker synthesis feed live (18:05 nightly, `theme_synthesis.py` → `rs_slope_synthesis` shadow rows + judge narrative axis) · **JUDGE_MODEL Sonnet→Opus 4.8** on the operator-labeled closed-gap eval (9–2; lesson: 5/17 disagreements dissolved from input alone; timeouts raised 25s/110s with the flip; model-eval PLAYBOOK codified `model_selection_baseline.md` §PLAYBOOK) · **#256 W1** execution facade (`execution_client.py`, 16 sites + 28 W2 tags, deploy gate [5j/7]) · 3 prod bugs found via evening sweep: theme-assignment `[Fading]`-echo silent drop (month-old, 15/27 runs — Sonnet echoes the prompt's stage label, lookup missed), 17:00 nightly OOM kill (512M memcg cap at the RS burst — the real cause of the 6/01–6/05 "aborted" rows; → 1536M), state_alerts RS-deterioration dead since 3/20 (asyncpg `$1 - INTERVAL` inferred interval — un-anchored param) · EOD recap gained the judge-demote line · stale trades-dashboard snapshot rescued (export SQL saved, #194 widened to both snapshots) · v1.1/v2.0 successor roadmap drafted · prod-local compose hotfix (Postgres loopback for theme-dash tunnel) adopted into git.

### 2026-06-11
- **Rubric v2 + selection replay + MNTS cluster** (the non-NS half of the day): **#269 rubric v2** (`v2-2026-06-11-revenue-over-eps`, operator-signed off Pradeep tweets) — growth names = REVENUE growth is the signal, EPS-beat-with-revenue-miss ≠ HIGH, EPS only in SUSTAINABLE turnarounds (CBRL = the one-time-anomaly trap); shipped to judge `_RUBRIC` + fallback grader same commit; **prompt-era versioning** = `RUBRIC_VERSION`+sha1-hash on every decision row + `rubric_version` column ([5b/7] gate gained `_ensure_ep_alert_columns` pre-step). **#268 selection replay Phase A** (215 cands): demotes avoided −0.79R, BOTH systems ≈0R on current 1-min-ORB geometry = the W2 entry-mechanics thesis in data; Phase B (12mo) launched overnight; **pre-close review caught the contamination BLOCKER** — replay rows visible to 13 unfiltered `mi_ep_alerts` readers incl. the LIVE EP cooldown → `db.LIVE_SOURCE_SQL` sweep (`27e3ca5`), deployed pre-open 6/12, `mi_signal_outcomes` purged (132 rows). **MNTS case study** (operator chart walkthrough) → #270 composed delayed-EP detector (the flag detector disqualifies on the exact undercut that IS the U&R setup), #271 20%-in-5d breadth extremes (two series), pivots+stock-character SSoT (`docs/methodology/pivots-and-stock-character.md`). Phase B judge stage hit CREDIT exhaustion (2,122 silent fail-opens, first misread as 429s) → #273 `llm_health.py` credit alerting. Lesson: replay rows in live tables need source-scoping EVERYWHERE, and bias-to-action pre-close reviews catch what deploys don't.
- **🏁 NORTH STAR CLOSED (#240)** — one day ahead of the locked Fri 6/12 date. The EP Holistic Grade Judge (ADR 0011) is the live PAPER grade authority: flipped load-bearing 6/10 (operator-signed; first call CBRL MODERATE→none, a one-time litigation settlement misread as an EPS beat), advisory stack retired into it same day (#249 — `fire_axes` = the one fire signal, ADR 0010 superseded, #189/#200–#203 subsumed), model-checked via operator-labeled closed-gap eval → `JUDGE_MODEL = OPUS 4.8` (Opus 9–Sonnet 2; lesson: 5 of 17 disagreements dissolved from INPUT alone — input > model, #210), verified-live 6/11 (3 Opus-judged alerts NAVN/ELVN/IDCC, exact alert↔decision↔authority reconcile, 0 fail-opens). Waves #241–#245, #247, #249, #252, #253 all closed-verified. **Boundary: North Star is never reopened** — post-done items (chart-vision #267, #226 canonization, #212 precision, prompt tuning) live in the successor program `docs/roadmap/apollo-v1.1-v2.0.md`. Lesson of the program: speed-running a load-bearing flip is safe when fail-open-to-floor + decision traces + instant revert + operator gates are built FIRST — the vision-not-piecemeal directive delivered in days what the piecemeal path had scheduled for Q4.

### 2026-06-01
- **Partial-exit hardening trio + 16:45 cron RE-ENABLED**: RCAT `/partialnow` surfaced a sub-penny stop bug — stored ORB-low stop `11.955` hit `replace_order` raw → Alpaca 42210000 reject → the atomic replace failed leaving the old stop LIVE, but the abort handler false-flagged the position naked. Shipped + validated (`2215615`, `cd7fa27`; paper integration ×2, G6 ×2, 21 unit green): `alpaca_client._round_stop_to_tick()` floors sub-penny stops at the `replace_order` boundary (the lone unrounded path); `execute_partial_exit` except-handler verifies `old_stop_id` liveness on the broker before declaring naked (atomic-rejection → old stop intact → calm abort, no duplicate-stop-oversell CTA; fail-safe to naked on verify error); #150 sell-retry narrowly matches the `held_for_orders` share-reservation rejection only, 3×/0.5s. 16:45 `_live_position_update_job` re-enabled (paused 5/29 #151) — restoration conditions met (verify-stop-live + G6 + breaker on the force=False path). Banked CRSR +$1,394 + RCAT +$1,176 partials clean. Known-benign residual: DB-vs-broker stop-price display drift.

### 2026-05-29
- **#151 partial-exit safety TRIO + #150 confirmed + #153 bot watchdog + #154 deploy scope guard** (closed the IBM "partial broken 2 days" P0): advisor reframe — both IBM bugs were leaf-level in `alpaca_client.replace_order`, so the class is closed by (1) **G6** deploy gate `preflight_replace_order_smoke.py` ([5g], exercises replace_order against the real paper broker via `integration/paper_alpaca.py`); (2) **verify-stop-live** poll in `execute_partial_exit` Step 1b before the sell; (3) **circuit breaker** `_recent_partial_exit_failures()` (≥3/7d → refuse + Telegram; `force=True` bypass for `/partialnow`); plus a durable broker-ground-truth integration test (green ×2). IBM canary clean (+$226.37). #150 share-reservation race CONFIRMED (Alpaca releases the hold ~ms after the atomic replace; verify-poll incidentally mitigates → explicit fix Monday). #153 bot watchdog: `HeartbeatExtBot` Redis heartbeat on each get_updates + market-agent 2-min cross-container alarm (7-day silent-outage class → ~5 min; PTB 22.7 `__slots__` class override). #154 deploy.sh: no-arg now errors (tier-1) + abort when the pull touched files outside scope (tier-2). Architectural split deferred to before LIVE cutover (#172).

### 2026-05-27
- **INCIDENT: IBM partial-exit + DB mass-close cascade** (post-mortem `docs/incidents/2026-05-27-ibm-partial-sync-cascade.md`): IBM partial hit a cancel-new race (R1); Claude then ran `sync_positions` via `docker exec python -c`, bypassed the credential bootstrap, Alpaca returned `[]`, code mass-closed 3 trades in DB (R3 judgment error); manual SQL restore; an after-hours retry re-corrupted IBM `stop_order_id` (R4). Broker protection intact throughout; DB damaged twice, recovered both. Shipped P1: `alpaca_client.replace_order_by_id` atomic replace (`fa70976`, closes R1); `sync_positions` mass-close guard — refuses close when Alpaca returns `[]` AND DB has N>0 active filled (`fa49304`, would've prevented R3); same-window 2-attempt retry in `execute_partial_exit` (`f236976`). Filed #138 (/partialnow + /syncnow operator-confirm), #139 (alpaca boot-guard), #140 (DB-DRIFT vs NAKED alert taxonomy), #141 (after-hours partial reliability). Related P0 #142: RDW stuck `pending_new` whole session 5/26 (~$2,100 paper missed) → hard live-cutover gate = ship stuck-pending_new watchdog first (review `orb_entry_stuck_pending_new`, threshold=1). Lesson: on live trade state default is STOP and CONSULT, not ATTEMPT and RECOVER; never mutate trade state via `docker exec` one-shots outside the bootstrap path (memory `feedback_no_docker_exec_for_trade_state`).

### 2026-05-26
- **#123 DB↔Alpaca order-status reconcile**: 49 stuck `mi_live_orders` rows in prod (oldest 40 days) — silent WS gaps + container restarts leave DB out of sync with broker SoT; drift is endemic (periodic, not deploy-only). `reconcile_order_states(account_mode, lookback_days=90)` in `broker/order_manager.py` polls `alpaca.get_order` per non-terminal row, updates DB + `order_status_reconciled` audit on divergence; `_canonical_order_status` normalizes SDK-enum + bare strings. Cron `*/15 9-16 mon-fri` + one-shot 60s post-boot. Audit-only (no Telegram — retroactive "stop fired hours ago" confuses; drill via `/audit`). 11/11 tests. **#122 ORB single-tick outlier — NOT shipped, filed data-gated** (`orb_bar1_wick_outlier_persistence_filter`, threshold=10): backwardcheck found N=1/90d → measurement-first prevented a 1-second-bar fetcher on a phantom. **#127 mi_intraday_bars 9:30 gap**: 87% of live ORB entries lacked a 9:30 bar — `get_first_bar` fetched but never persisted; 5-line write-through INSERT … ON CONFLICT, forward-only. **#120 L2 holiday awareness**: Memorial-Day 0-value `metric_sample` contaminated the 30d baseline → false-positive L2; `_is_non_trading_day` (wraps `trading_calendar.get_market_status`) short-circuits `_compute_anomaly` + no-ops `_record_metric_sample` + read-time filters; 6/6 tests. Lesson: measure before shipping a filter (#122); persist what the live path fetches (#127); holidays poison rolling baselines.

### 2026-05-24
- **9-commit ship-day (`b52e3e6`→`76693df`): weekly-review fixes + 2 intraday detectors + DR tmpfs + L3 drift annotation + 2 regressions caught**: weekly-review — filter SYNTHETIC-TEST events from the audit roll-up; display-dedup `duplicate_scan` missed-opps rows; **YAML duplicate-key bug** in `data_gated_reviews.yaml` (last-wins silently re-surfaced a done review) → new `[5e/5]` preflight `preflight_yaml_dupe_keys.py` (the #111 "Sonnet hallucination" hypothesis was wrong — the bug was in the YAML). #95 Support-test (no vol gate, Morales) + #96 MA-pullback (light-vol gate) shadow detectors (mirror #94). #107/#108 DR-secrets tmpfs (`/dev/shm`, `trap RETURN` cleanup). #112 L3 drift annotation ("may be intentional") + fixed pre-existing `_CLAUDEMD_HEADER_RE` (looked for `## Changes Made` but the format is `### date —` → silently returned `[]` for unknown duration). Filed #115/#116/#117 (detector boilerplate/caches/constants) for after first-fire. Lesson: cross-cutting /simplify across many commits catches stale assumptions per-commit review misses; don't refactor active hot-path code day-before-firing.

### 2026-05-23
- **DR layer + Stocks-in-Play Phase 1 + #94 intraday flag-break detector**: DR — encrypted secrets bundle (`.env` + `gdrive-token.json` + `apollo.conf` + crontab → tar → gpg AES256) alongside the nightly pg_dump; `infra/restore.sh` idempotent 11-phase driver (~95 min RTO); `_backup_health_check_job` requires both `gdrive_backup` + `gdrive_secrets` audit rows; **HARD GATE = §6 decryption roundtrip on laptop** (caught a real `<paste-from-google-pw-manager>` placeholder via gpg "Bad session key"). #94 intraday flag-break detector (ADR 0005, shadow). #99 Stocks-in-Play Phase 1 (`mi_stocks_in_play` + `stocks_in_play_sources.py`). Polygon coverage (#71/#72/#73) closed: 25-45% is a structural floor, not regression (memory `feedback_polygon_coverage_structural_floor`). Lesson: a recoverability gate must run against the actual encrypted bytes, not against intent.

### 2026-05-22
- **Weekly-review surfacing fixes (#109/#110) + #92 flag graduation NO-GO**: #109 by-skip-reason avg/top NULL at 7d (`ret_5d` immature) → surface `avg/top_max_high_5d` + rank on `COALESCE(ret_5d, max_high_5d)`. #110 loser post-mortem → X/N form at N<5. #92 flag-detector graduation NO-GO at N=5 (TRIGGERED-as-EOD-state was post-hoc) → superseded by #94 moment-of-break detector. Lesson: low-N metrics need X/N not percentages; post-hoc state ≠ moment-of-signal.

### 2026-05-21
- **Backward-check hygiene caught polluted-cohort verdict + 4-checks-said-don't-ship discipline win**: yesterday's #50 verdict ("pre-revenue $0 band 67% 5d WR — keep code default at $0.01") was based on 32 duplicate rows of 3 events (TH×16, KOD×12, KPTI×4) inflating band-level WR. Post-dedup: $0 band → 14% WR / -8% avg — opposite of yesterday's read. #63 revert (lowered REVENUE_STAGE_MIN_USD code default to 0.01) urgent-reverted in AM; prod env override stays at 0.01 (operator-pinned, explicitly provisional). #53 (ATR-normalized gap) verdict also shifted but held direction (5x+ band 73% → 42% WR post-dedup; still don't ship). Duplicate root cause already fixed 5/14-class via insert-site two-layer dedup (`c0bcb48`, zero new duplicates in 50+ days). Plus 4 backward-checks all concluded "don't ship" — #50/#53 (post-dedup), #54 (9M Day 2 stop/ATR cohort too thin N=4), #39 (NBIS rubric corp-action distortion). ROIV 9M Day 2 trigger surfaced architectural gap #65 (9M Day 2 path is quant-only, no catalyst-driven filter like MAGNA53). Fixture re-review #57 confirmed operator labels systematically more generous than rubric (5/6 fixtures: rubric ≤ operator label) — Phase 5 meta-rubric calibration input, NOT a relabel. Lesson: "structural framing" doesn't bypass sample-size discipline; status-quo bias dressed as discipline ("hold the most recent ship") is a real failure mode the advisor catches. Code velocity is not the live-readiness rate-limit; trade outcomes are.

### 2026-05-20
- **UnboundLocalError outage (EP scans dead 7:00-8:21 ET) + preflight `[5d/5]` import-shadowing AST gate + 4 backward-checks all said "don't ship"**: 2026-05-19 commit `bb17e69` added `from ... import log_audit_event` inside `run_ep_scan` — Python made the name LOCAL for the entire 1000+ line function, blowing UnboundLocalError on every prior reference. Hotfix `b3df5a5`. Preflight `[5d/5]` shipped same day: AST-based shadowing check flagging Mode A (prior-ref) + Mode B (conditional). Caught 10 additional latent landmines across scheduler/live_tracker/ep_detector/catalyst_metrics_extractor/etc. — all fixed same session. Wired into `deploy.sh`. EP scan failure surfacing hardened (#49): durable `ep_scan_failed` audit + escalated Telegram "🚨 EP SCAN DOWN" with 1h dedup. Downgrade audit messages disambiguated (#51): 4 branches (`extraction_failed_*` / `news_corpus_sparse_no_q_rev` / `q_rev_yoy_missing_no_prior_year_comparable` / `non_earnings_catalyst_no_q_rev_in_news`). Gate 5 verified all 6 deliverables shipped (#43). Quarterly backward-check sweep wired (#62, Feb/May/Aug/Nov 1st 8 AM ET). 4 backward-checks all concluded "don't ship methodology change" (#50 revenue-stage, #53 ATR-gap, #54 9M ATR-stop, #39 NBIS rubric). Lesson: 4 of 4 said "don't ship" — methodology change anti-velocity discipline working as designed. UnboundLocalError class permanently catchable via preflight `[5d/5]`.

### 2026-05-17
- **Track 1 trade-state ownership refactor (3 hot-path bug surfaces) + Gate 5 G column-write authority preflight**: 5 trade-state corruption bugs in May (CRMD/KLAR/ARM/BW/AIXI), same root cause every time — multiple writers to same column with no ownership rule. T1.1 (`trade_stream._process_entry_fill`): no longer writes `stop_price`/`hard_stop` (KLAR/ARM root cause; entry-fill is NOT authorized writer; INSERT at `entry_pipeline._skip` sets initial value, `update_stop()` owns trail). T1.2 (`live_tracker.update_open_positions_live` partial-fired branch): no longer writes `stop_price` (when `update_stop()` failed + nulled `stop_order_id` per naked-position protocol, wrapping write falsely reported stop_price broker no longer held). T1.4 (no-partial branch): no longer writes `stop_price`/`total_pnl`/`partial_taken`/`remaining_shares` (idempotent no-op writes were LOST UPDATE hazards if WS fill arrived concurrently). Gate 5 G shipped (`scripts/audit_column_writes.py check` + `ALLOWED_WRITERS` 35 columns + `deploy.sh [5c/5]`): walks every UPDATE/INSERT site touching `mi_live_trades`, fails deploy non-zero on any (column, function) pair not in ALLOWED_WRITERS. Friction-by-design — adding new writer requires updating ALLOWED_WRITERS in same commit. Discipline that worked at hour 3+ of fatigued architectural work: advisor consult before EVERY commit, deploy + Gate 5 B prepare validation between EVERY commit, atomic commit-per-refactor, honest scope reduction when investigation surfaced complexity (T1.3 deferred mid-session). Lesson: column-ownership bug class needs column-ownership gate; type-mismatch (5 B), wrong-value (no gate yet), routing (purpose-tagged orders), ownership (5 G) are DIFFERENT bug classes that need DIFFERENT gates — single-gate thinking is wrong.

### 2026-05-14
- **CRMD naked-position incident + Gate 5 live-cutover blocker (4 deliverables filed)**: asyncpg AmbiguousParameterError on `_process_entry_fill` UPDATE (since 2026-05-10 `lowest/highest_price_seen` schema add reused `$2` across `double precision` and `numeric` columns) silently broke entry-fill DB write → OTO stop leg cancelled → CRMD naked 09:34-11:08 ET → -$778 (-$558 worse than methodology stop). Fix: explicit `::numeric` casts (commit 96fd7ee); defensive escalation Telegrams "POSITION MAY BE NAKED" on WS handler exception. KLAR/CSCO same window had `filled_at=NULL`. New `pnl_attribution` column excludes incident damage from methodology Gate 3 R-expectancy while keeping account equity / daily_loss_limit honest. Gate 5 added to live_cutover_decision: naked-position remediation, boot-time UPDATE prepare validation, escalated alert, stuck-fill watchdog, regression pytest, operator sign-off — all required before any strategy promotes to phase='live'+live_real_enabled=True. Lesson: column-type addition to hot UPDATE path is a SCHEMA CHANGE that needs regression coverage; boot-time preflight must walk every hot DB-mutation path (asyncpg `connection.prepare()` is cheap), not just credentials.
- **EP selectivity deep-dive review filed (user mandate: rare EPs)**: past 10 trading days = 87 HIGH alerts (8.7/day, extrapolates to ~550/quarter). User target: "handful per quarter, not 100+." Expanded review covers 6 lettered sections × ~30 selectivity dimensions (existing filters, scoring weights, entry mechanics incl. 5m ORB shadow, NEW dimensions like fundamentals magnitude / gap-above-MAs / round-number distance, setup-context like theme membership, recent-shipped gate changes). Phase 1 master cohort join across 9 tables. NBIS-class missed example: 700%+ revenue growth caught by M&A direction-blind bug AND would have only graded "strong" — grader doesn't see fundamentals magnitude. Lesson: shipped 5/17 (`docs/decisions/0003-ep-selectivity-overhaul.md`).
- **3 bugs in parallel + recurring "attempt-not-outcome" pattern**: (1) `splits_ingest._apply_one` compared `close_pre/close_post` from adjusted-feed where both sides are pre-adjusted → ratio≈1.0 for both real and phantom splits, skipped 10 real splits 5/08+ (AIXI/SMX/etc.). Fix: compare adjusted-feed pre-split close against un-adjusted DB value. (2) BW partial-exit post-close logic wrote `step` outcome unconditionally even when WS fill deferred state mutation → frankenstein row (`partial_taken=TRUE total_pnl=$1613 remaining_shares=776` vs broker 1163 unsold). Fix: skip partial-fields in wrapper UPDATE when `step.partial_fired=True`. (3) SNDK theme misclassification manually reassigned + assignment review filed. Lesson: every new boolean on a hot-path table needs a paired invariant query surfacing the row state when mechanical-condition met but semantic-outcome ISN'T (`adjustment_applied`, `partial_taken`, `phase='live'`, ambiguous-param outcome are all the same pattern — flag tracks ATTEMPT not OUTCOME).

### 2026-05-13
- **Outage: every paper trade failed with 'ALPACA_LIVE_API_KEY' KeyError**: `_seed_strategies_registry()` pre-dual-account-architecture set magna53/9m_day2 to `phase='live'` (then meaning "submit via single Alpaca account"). 2026-05-10 dual-account ship redefined `phase='live'` to mean LITERAL live TradingClient. Container `ENABLE_LIVE_MODE=false` → `ALPACA_LIVE_API_KEY` never set → `_check_safeguards` raised KeyError → every entry blocked with `setup:account_fetch_failed`. Fix: seed magna53/9m_day2 with `phase='paper'`; boot consistency check raises RuntimeError if any `phase='live'` row exists when `ENABLE_LIVE_MODE=false`. Lesson: re-defining a column's meaning without migrating existing values is the foundational error — catchable by full boot smoke (preflight #84 same day walks every enabled strategy through `_check_safeguards`).
- **M&A filter direction-blindness for acquirer-side (NBIS class)**: bare `acquire`/`acquisition` keywords matched NBIS (acquirer of Eigen AI) 10 audit events 7:55-9:31 ET, blocked NBIS HIGH alert despite +15.79% gap. 90d backtest: 13 FPs (NBIS/WAT/MNST/etc.) vs 3 nominal TPs. Fix: dropped `acquisition`/`acquire`; added `take-private` + `private deal for`. Other keywords retained (buyout, takeover, merger, halper sadeh) spot-check verified. Lesson: substring-keyword filter that gates trade entry must be direction-aware — replace grammatically-ambiguous verbs with unambiguous passive phrasings that mark ticker as target.
- **9M sugar baby M&A coverage closure + Tier A telemetry reviews verified**: WEN-class take-private caught on EP path but 9M Day 2 had zero M&A filter coverage. `is_likely_ma` added to `ninem_detector.run_9m_eod_sweep` (Polygon-news-only path, 21-day lookback, fail-open). Tier A telemetry reviews verified + closed (dead_zone_telemetry_smoke, fishhook_v3_first_eod_pass_verify, ep_adv_probe_volume_sanity). Lesson: filter at one detector site but not sibling sites is a coverage gap — when SSoT primitive exists (`ma_filter.is_likely_ma`), every actionable detector path should call it; sibling-site audit standard practice.
- **Theme assignment silent_stop fix #46 (max_tokens + prompt restructure)**: 2026-05-09 silent under-anchoring (MU/SNDK/MRAM/SIMO/MXL missing from themes despite top-30 RS). Root: `max_tokens=1000` truncated Sonnet response BEFORE `assign_stocks_to_themes` tool call could emit. Fix A: max_tokens → 4000. Fix B: prompt restructured — replaced "before calling X, ask yourself..." (invited pre-tool free text) with explicit "Do NOT write any free-text analysis before your tool call." Lesson: LLM tool-use loop without sufficient max_tokens headroom fails silently — truncated response → tool_use block never materializes → code-side treats as legitimate "no proposals."
- **Theme cross_run_dup probe over-firing rename + orphan_sub remediation**: cross_run_dup_candidate fired 7-9/day, all canonicalize-handled, mostly FPs. Renamed to `theme_name_variant_observed` (honest semantic — Sonnet drift telemetry, not active problem). Separately, `theme_orphan_sub` 7 firings/14d: parent dropped during merge/cap leaves sub with stale `parent_theme`. Fix: `_emit_pipeline_diagnostic` mutates orphaned child in place (clear parent_theme + drop entry from sub_theme_parents) so next-stage diagnostic doesn't re-fire. Lesson: audit event names shape operator triage — naming carries authority; renaming is lowest-risk when probe is over-emitting.
- **Preflight smoke test #84 + #10 STRL/EVER 5d outcome**: new `scripts/preflight_check.py` walks every enabled non-shadow strategy through `_check_safeguards` — exact code path that fires on real ORB entries. Two preflight bugs caught on first run (subprocess didn't inherit boot-time `ALPACA_API_KEY` remap; benign `setup:account_fetch_failed` initially mis-counted as pass-through). STRL +17%/+22% 5d, EVER +0.2%/+25.6% intraday-but-gave-back — both stop_too_wide-rejected, filed as #11 trigger condition (N=1, track).

### 2026-05-11
- **Missed-EP opportunity-cost telemetry (3-step ship)**: new `missed_outcomes.py` + `mi_ep_missed_outcomes` tracks every EP system saw but didn't enter (scan_filter / MODERATE-tier / HIGH-unentered) with forward returns from gap-day open to d+1/d+5/d+20 close + max-favorable-excursion. Three surfaces: nightly refresh after 5pm data pull, `/missed [days]` Telegram (top 15 + per-category roll-up), weekly review appendix (top 5 winners + per-skip-reason). 12 skip-category buckets (cooldown / score_below_50 / pm_rvol_low / etc.). Forward-return basis = gap-day open. Pure observability; exempt from sample-size discipline freeze. Lesson: "which gate bled the most upside" needs systematic surface, not single-trade anecdotes.

### 2026-05-10
- **#66 Dual-account architecture + #65 per-strategy sizing (live-cutover blocker)**: one Apollo container subscribes to BOTH Alpaca paper + live accounts; strategies route per `mi_strategies.phase` (shadow / paper / live + live_real_enabled). Enables 3-tier maturation pipeline without losing real Alpaca paper feedback when MAGNA53 promotes. Per-mode `TradingClient` singletons, mode-bound `client_order_id` (apollo_{mode}_{strategy}_{ticker}_{ms_epoch} format), two `TradingStream` instances with `_verify_event_account_mode` cross-account guard, `_check_safeguards(account_mode)` per-mode isolation, per-strategy `position_size_multiplier` + `max_concurrent_positions` columns. Migration: `ALPACA_PAPER_*` + `ALPACA_LIVE_*` env vars required when `ENABLE_LIVE_MODE=true`; legacy `ALPACA_API_KEY` remapped at boot (one-cycle rollback). Lesson: cleanest place to apply per-strategy parameters is the orchestration layer (entry_pipeline + safeguards), NOT each strategy's spec_builder — single application covers all current + future strategies.

### 2026-05-08
- **session 4 — Drawdown circuit breaker shadow ship (#39)**: Replaces count-based 10-loss breaker (self-perpetuating + methodology-blind for hold-winners). State machine evaluated daily 4:12 PM ET cron. Equity from Alpaca incl. unrealized; 30d rolling peak; asymmetric hysteresis (-5% trip / -2.5% release); stale-data fail-open at 48h. Two new tables: `mi_account_equity_snapshots` + `mi_safeguard_state`. SHADOW phase; promotion gated ≥14d post-cutover via `DRAWDOWN_BREAKER_PHASE=active`. Lesson: momentum-strategy safeguard must be methodology-aware; closed-trade window biased toward losses by design when winners are held.
- **session 3 — Circuit breaker threshold 5→10 (interim)**: Morning ORB blocked by 5-loss count breaker on 5-loss streak (24% probability at 25% WR — statistically too tight). Single-line constant bump as interim while drawdown breaker #39 is built. Daily loss limit remains magnitude-side backstop. Lesson: don't disable safeguard with caveats; tune down to non-firing while proper fix is engineered.
- **session 2 — 5/7 paper-session triage (5 additive fixes)**: (B) parabolic earnings exclusion + `days_up_streak ≥ 3` hard gate (AGL/XMTR class — pre-runup low inflated 35d cumulative); (C) EP earnings-day pre-score catalyst boost (DDOG/AAON: routine catalyst at `gap≥10%+earnings` upgrades to strong); (D) EP cooldown bypass on fresh earnings (HIMX: `gap≥15%+earnings_day` overrides 60d cooldown); (E) pm-shares floor relative-anomaly carve-out (`pm_rvol ≥ 5×` skips 25k absolute floor); (F) phantom split sanity check via post-apply ratio. Lesson: chained classifiers grading textual news need non-LLM independent second source (yfinance earnings_calendar) at multiple integration points.
- **— Stop-fill state desync purpose-tagged routing (ARM 5/07 + TEAM 5/06 class)**: ARM entry filled $224, stop partial-filled 87/89 @$219.50 = -$391.50, but row ended `status='cancelled' exits=[] total_pnl=$0`. Three stacked failures: WS partial_fill visibility-only, `stop_order_id` matching brittle on stale IDs, 10:00 ET cleanup overwrote prior fills. Fix: new `finalize_stop_fill` + `purpose='stop_loss'` tagging at ALL stop placement sites + `cancel_unfilled_entries` preserves exits[] history. Lesson: state-mutation paths in trading code must be routable by intent (purpose) not by identity (stop_order_id); purpose is durable, identity mutates.

### 2026-05-07
- **session 4 — `/pregame` + `/ep` post-midnight data tag**: `last_trading_day()` rolled Sat/Sun→Fri but not the post-midnight weekday pre-scan window. New `latest_market_data_date(as_of)` resolves freshest alert_date across mi_ep_alerts + mi_9m_ep_alerts. Tags header `_(data: <date>)_` when ≠ today. Generalizes to weekend/holiday/pre-scan with one query. Lesson: three states (weekend/holiday/pre-scan) collapse to one query when keyed on actual data freshness.
- **session 3 — Wave C #5 9M EP per-scan digest**: 15+ tickers per scan tick fired individual Telegram bubbles. Batched into ONE digest by tier (Actual / Pace) per scan; per-ticker DB inserts + audit events unchanged; only user-facing Telegram batched. Same shape as Wave A #4 flag TRIGGER digest. Lesson: informational alerts = digest; terminal/actionable = ping.
- **session 2 — splits_ingest premature-apply (systemic data corruption)**: Every split row had `applied_premature=t` — `get_unapplied_splits` lacked `execution_date <= today` filter, so freshly-detected splits applied via `adjusted=true` Polygon fetch BEFORE actual execution. Result: pre-execution bars un-adjusted, post-execution bars adjusted; mismatched units in same row. ERNA 5/04 25:1 reverse: $0.15 pre vs $3.74 post in DB → false parabolic climax. Fix: add the date gate + reconciliation SQL `UPDATE mi_splits SET adjustment_applied=FALSE WHERE applied_at < execution_date::timestamptz`. Affects every reader of `mi_daily_closes` (RS, EP, parabolic, flag, friday). Lesson: `adjustment_applied=TRUE` was a flag on the wrong axis — tracked "we ran apply step" not "data actually adjusted." For state-machine flags: gate on OUTCOME not ATTEMPT.
- **— Wave B #9 TEAM attempt counter 1 vs 2**: `/positions` aggregate did `COUNT(*) WHERE status='closed'` (rows, not attempts); `/trades` selected `NULL::jsonb AS entries` for live mode, `_attempt_count` returned 0. Schema migration introduced `entry_attempt` int column but left two readers pointing at dead `entries` JSONB. Fix: `COUNT(*) → SUM(entry_attempt)`; `/trades` select entry_attempt; prefer column with `_attempt_count` fallback. Lesson: a schema migration that adds a replacement column must update every reader at the same commit.

### 2026-05-06
- **session 2 — Unify EP volume gate to RVOL@T (one primitive, two anchors)**: HUT/BLMN/GLW HIGHs detected at 9:52 ET, missing 9:45 ORB cutoff. Three distinct volume gates (pre-9:30 RVOL@T / 9:30-9:45 no gate / 9:45+ today_vol / 390min_ADV) drifted; 9:30-9:45 was structurally < 2× by math regardless of hot tape. Removed artificial 9:45 cutoff in `compute_rvol_at_time`; collapsed conditional pre-market-only call into ONE uniform RVOL@T (pm anchor pre-9:30, session anchor 9:30+). Discriminator validated (HUT 9:31=7.29×, BLMN=11.84×, GLW 9:35=2.41×). Lesson: three implementations of same conceptual question (volume above normal at clock-minute) are three places for math to drift; single primitive (`mi_minute_volume_curves`) covers entire 4:00-16:00 window uniformly.
- **— Stale `stop_order_id` after `update_stop` failure**: TEAM 5/06 9:35 ET `update_stop` cancelled old stop, retry failed (insufficient qty), function returned False but never nulled `mi_live_trades.stop_order_id`. Downstream: `sync_positions` orphan-remediation gated on `stop_order_id IS NULL` never fired; `_handle_fill` step 2 matched stale ID, silently failed; `/trades` showed position normally, masked unprotected state. Fix at 3 sites (`update_stop` retry-failed, `execute_partial_exit` place-new-stop except, rollback-failed) — null `stop_order_id` + emit `naked_position_detected` audit. `sync_positions` Path C extended to detect stale-status broker IDs. `/trades` shows ⚠️ NAKED on `stop_order_id IS NULL`. Lesson: terminal failure paths must (a) leave DB in reconcilable state, (b) emit distinct audit event naming failure class.

### 2026-05-05
- **session 5b — Flag detector dry-volume gate hybrid + #163/164/188/189 verified, #177/179 closed**: `_compute_fresh_tightening` ceiling switched to `max(recent_avg_vol, 0.5×ADV20)` — anchors on contraction floor, prevents single sub-avg bar from over-tightening. OKLO 5/4 was the trigger (14.65M vs ADV20 15M = 0.98 barely passing). #177 closed (#180 covered the GOOGL/TEAM DB drift); #179 folded into P2. Lesson: "tomorrow"/"next day" in cross-session followup notes is a relative-date trap — always reconcile with today before acting.
- **session 5 — P0.1 enum stringification site-local fix (#211) + #187 doc honesty**: Empirical signature confirmed (GOOGL/TEAM `stop_update_started` firing every morning at identical from→to prices). Site-local fix at `live_tracker.py:565` — `str(order.status).split('.')[-1].lower()`. SSoT consolidation at `_order_to_dict` deferred until polling-reader race-shape audit. #187 trailing-10 docstring rewritten ("+25% above 10-session low", not "10-session return"). Lesson: pre-ship empirical gates pay off when the plan author writes them.
- **session 4 — Paper→live $ flip wave 3 (P0.9 staged-paper banner + P1.5 /dryrun + P0.2 partial-fill telemetry)**: `mi_strategies.live_real_enabled` gate → `🟡 STAGED-PAPER ramp` banner on Telegram proposal when False at live. `/dryrun` renders pre-trade sizing math against current Alpaca equity. P0.2 visibility-only per advisor (paper Alpaca atomic-fills, zero production exercise; deferred-commit handler designed against real fill-shape data later). Lesson: when plan describes a bug that doesn't match actual code, reconcile before shipping.
- **session 3 — Defer partial/full exit DB commit + Telegram to fill event (#180)**: GOOGL/TEAM 5/05 16:45 ET partial exits Telegram'd "Sold @$X" but no shares sold (after-hours queue, fill_price collapsed to entry_price). Fix: `mi_live_orders.purpose` ('partial_exit'/'full_exit') routes WS fill handler to `finalize_partial_exit`/`finalize_full_exit` against real Alpaca fill price. Submit-time dedup safeguard. Cancel/reject restore handlers re-place stop on full remaining. Lesson: "Order accepted" ≠ "shares sold" — defer commit + user-visible language to WS fill event for any after-close cron.
- **session 2 — compute_atr_14 close-to-close → Wilder TR (STRL/BAND skip class)**: `backtester/filters.py::compute_atr_14` was reading only close, computing `abs(close - prev_close)` as TR — silently understated on volatile/gappy stocks. Rewrite reads H/L/close, full Wilder TR. Matches `flag_detector._atr_14` SSoT. 30d replay: all 4 stop_too_wide skips pass under new ATR. Lesson: when two implementations of same conceptual computation drift, the stale one inverts coverage on the exact pattern it's supposed to surface.
- **— Earnings-day HIGH override (DOCN miss → yfinance backstop)**: 30 settled misses since 4/14 with `gap≥10 + catalyst=routine + score-blocked`, all 5d positive. New `earnings_calendar.py::is_earnings_day` via yfinance `earnings_dates` + `.calendar`. Override at tier-decision: MODERATE + gap≥10 + earnings → HIGH. Three audit events distinguish surfaces. Lesson: chained classifiers grading textual news are coupled to ingest latency; fix is a structurally independent second source (calendar API), not a smarter classifier.

### 2026-05-04
- **session 7 — ORB-extension shadow telemetry (#10 decision-support)**: N=5 cancelled-ORB lifecycle analysis inconclusive (TWLO single-leg drove $1,101 swing). New `mi_orb_extension_shadow` records 6 cutoffs (10/11/12/13/14/16:00) per cancellation. Hook in `cancel_unfilled_entries` gated on `orb_unfilled_cancelled`. Settlement appended to `_nightly_data_pull`. Data-gated review threshold 20 trades. Lesson: counterfactual decisions need counterfactual data — record now so when N=20 arrives the data exists.
- **session 6 — Flag detector burst-class universe + fresh-tightening COILED path (OKLO miss)**: Two structural gaps. (a) `get_flag_universe` top-200-RS-only excluded post-runup names whose 6M is dragged by pre-runup downtrend (OKLO rs_rank=981, rs_1m=94.3, trailing-10 +42.5%) — OR-clause `rs_1m_pct >= 80 OR (last_close / trailing10_min - 1) >= 0.25`. (b) Contraction math overlapping windows on short bases — new `_compute_fresh_tightening` predicate (base_age≥4, max(2bar TR%) ≤ 0.6×ATR14%, max(2bar vol) ≤ ADV20). Lesson: pattern detectors must anchor on ticker's own recent extreme; rigid thresholds against generic baselines silently veto the exact pattern.
- **session 5 — Cleanup queue: zombie themes + cancel_unfilled_entries audit + #183 filed**: 42 prod themes with `theme_date < CURRENT_DATE-7d AND stage NOT 'Retired'` fixed via DISTINCT ON. `cancel_unfilled_entries` gained audit events on success/failure paths (was silent). #183 filed: `_order_to_dict` returns `'OrderStatus.NEW'` not `'new'` for Python 3.11+ enum stringification — held to preserve discriminator in tomorrow's `stop_update_started` telemetry. Lesson: Python enum stringification is single root cause masquerading as N decoupled bugs; SSoT fix at wire boundary, not per-site patches.
- **session 4 — Audit logging in update_stop + execute_partial_exit (GOOGL/TEAM 5/4 silent failures)**: Both functions had zero audit-log writes; every failure path was silent. 7 sites in update_stop + 9 sites in execute_partial_exit added. No logic changed — captures recurrence with telemetry so #180 (defer DB commit) and #182 (write skew) can be fixed against real data. Lesson: silent return-False in trading-side functions violates no-silent-failure rule; audit logging is the bare minimum before any defer/rollback refactor.
- **session 3 — SSoT M&A filter `ma_filter.py` (AVNS slip → Polygon backstop)**: AVNS (4/14 take-private $24.62-72 pinned 14d) slipped flag scan because Perplexity returned no news → no keyword scan signal. New `ma_filter.py::is_likely_ma()` — three layered sources: (1) Claude `catalyst_quality=='mna'`, (2) `_MNA_KEYWORDS` text scan, (3) Polygon news headlines. EP + flag detectors share SSoT. L2 deal-pin price-signature filed as data-gated. Lesson: chained LLM-only catalyst lookups silently couple to one provider's blind spots; structurally independent second source closes coverage gap.
- **session 2 — Group skip Telegrams per strategy**: `submit_trade_entry._skip` per-ticker Telegram noise. New `aggregate_skips: bool` flag. MAGNA53 + 9M Day 2 cron callers emit grouped digest after `asyncio.gather`. Suppressed parallel `check_filters` per-ticker pings. Lesson: Telegram economy — terminal/actionable events get a ping; informational batches go in digest.
- **session 2 — Per-ticker open-position guard (TEAM 5/04 double-entry near-miss)**: 9M Day 2 placed bracket while MAGNA53 5/01 fill in TEAM was still open. Same-day dedup at `(ticker, alert_date)` doesn't cross days/strategies. New check after dedup: SELECT for prior `status='filled' AND remaining_shares > 0` — `BLOCK_TICKER_OPEN_POSITION`. Lesson: trade-level dedup keys ≠ exposure-level dedup keys; exposure check must live at position level.
- **session 2 — Parallelize 9M Day 2 cron + drop bar-retry delay (TEAM 5/04 root cause)**: Real cause was sequential for-loop in `_9m_day2_orb_job`. SOUN's 60s retry blocked TEAM. Replaced with `asyncio.gather + Semaphore(5)` mirroring MAGNA53. `BAR_RETRY_DELAY_SEC = 60→10`. Lesson: two strategies sharing unified pipeline must share fan-out shape.
- **— Bracket fill messaging + stop-limit buffer (TEAM 5/04 unfilled)**: "auto-entered" Telegram fired at PLACEMENT regardless of fill. Renamed defaults to "Order placed" with "pending trigger" footer. SSoT helper `stop_limit_buy_price(stop) = round(max(stop*1.005, stop+0.02), 2)` replaces 7 hand-rolled `*1.001` sites — penny tickers no longer get 1¢ effective buffer. TEVA 4/30 EOD-cleanup-not-ORB-cleanup filed. Lesson: per-share buffer formulas scaling linearly need an absolute floor; SSoT cleanup of 7 hand-rolled constants overdue.

### 2026-05-03
- **— Catalyst hedge-phrase downgrade (Track B Layer 2)**: RDDT 5/1 catalyst returned "strong" against hedged Perplexity output ("no specific information"). New post-hoc skepticism layer in EP detector: 9-phrase scan; downgrade catalyst one notch + cancel agreement boost when detected AND quality∈{game_changer, strong}. Audit event `catalyst_pplx_hedge_downgrade`. Lesson: chained LLM calls hide own data quality; downstream consumers must parse natural-language uncertainty as signal, not content.
- **— Forward-looking wick surfacing (Track A)**: Wick surfaces gated `filled_wick=TRUE` but settlement waits 10 forward sessions — freshly-fired wicks invisible during actionable window. New `get_wick_pending_candidates(lookback_sessions=3)` with `NOT EXISTS` clause for already-broken-prior-high. WICK_PENDING priority slot in friday_watchlist + morning briefing section. Lesson: outcome-telemetry filters and forward-looking trader filters are different shapes of same question — two-purpose surfaces need two filters.
- **— pm_rvol gate universe top-500 → $5M $-vol floor**: 84% of HIGH alerts silently bypassed pm_rvol gate because they were outside top-500 trailing $-vol. Pre-market gappers are systematically mid/small-cap — capping at top-500 inverts coverage. `get_top_dollar_volume_universe` swapped to `WHERE adv_20 * close >= $5M` (max_tickers=5000 cap). 2647 tickers vs 500. Lesson: fixed-N universe inverts coverage on a gate designed to catch gappers outside megacap set.
- **— Drop check_stuck_filled_row invariant (duplicate of naked_position)**: 24h threshold mismatched to multi-day Pradeep/Qullamaggie holds. GOOGL/TEAM tripped despite stops being attached. Deleted invariant — naked_position covers the actual SQL-detectable bug class. Lesson: two invariants targeting same bug class must differ in WHAT they catch, not just WHEN; 24h-delayed copy of naked_position is zero net detection value.
- **— Cold-start L2 gate: minimum-denominator skip for rate metrics**: Five false `HIGH_ep_entry_rate` L2 alerts (4/27→5/01) at sample_n<7. `MetricSpec.fetch_today` returns float|None; both rate metrics return None when denominator < 5. `_compute_anomaly` skips sample recording + classification when None. Cleaned 14 polluted baseline rows + 5 audit events. Lesson: rate metrics with small denominators need minimum-N gate, not just zero gate; declare uninformative days uninformative entirely.
- **— Reap stale mi_job_runs 'running' rows at scheduler startup**: 5 stale rows across 3 jobs from SIGTERM-during-await. `_reap_stale_running_runs` UPDATEs status='aborted' for rows >2h old. Filed: `scheduler.shutdown(wait=True)` on SIGTERM as actual prevention. Lesson: invariants assume process always reaches finally-equivalent path; container restart during await violates silently — reap is hygiene, not prevention.

### 2026-05-01
- **session 5 — /setup TICKER reverse-lookup detector chronology**: Per-ticker fan-out across ~10 detector tables in parallel (asyncpg per-call connection acquisition required). Merged + sorted by date DESC then source priority (TRADE > FLAG > EP > 9M > WICK > PARABOLIC > THEME > WATCHLIST). TradingView chart inline-keyboard button. `.isdigit()` token-discrimination guard for `/setup XNDU 30` parsing. Lesson: parallel async DB fanout in asyncpg requires per-call `pool.acquire()` — using single connection for gathered fetches raises "another operation in progress".
- **session 4 — Authoritative split handling, drop RS heuristic**: XNDU disappeared from RS list on +24% gap-up due to `MAX_PERIOD_RETURN=300` heuristic catching the very momentum it should surface. New `splits_ingest.py` Phase 0 of nightly fetches `/v3/reference/splits`, re-fetches 250d aggs with `adjusted=true`, overwrites mi_daily_closes. RS heuristic skip blocks DELETED, replaced with non-blocking `rs_extreme_return` audit. `scripts/backfill_splits.py` for one-shot reconciliation. Lesson: heuristic stand-ins for missing data sources misfire on the exact pattern they were built to catch — fix is the authoritative source, not a tighter threshold.
- **session 3 — Stop-remediation hardening + fishhook EOD pass fix**: Stop-remediation pattern is correct as-is (DAY TIF child stop auto-expires 4 PM, 4:05 PM remediates GTC). Three hardenings: (1) `expired` event downgrade ℹ️ vs ⚠️; (2) bounded retry 3× with backoff; (3) evening backstop 21:00 ET 2nd sync_positions. Fishhook EOD `operator does not exist: date >= interval` — `$1::date - INTERVAL '60 days'`. Lesson: verify "common pattern" is mechanically safe before adding paranoid plumbing.
- **session 2 — Continuation Flag Detector (TI-class shadow ship)**: New strategy `flag_continuation`. Five stages anchored on pivot-high bar (highest-volume within 5% of 25d max-high). Three calibration fixes from XNDU replay: (1) proximity on pivot_close not pivot_high; (2) state gates use closing extremes not intraday; (3) breakout vol references base recent_5d_avg not 20d trailing. ATR-14 inlined Wilder TR. Hysteresis (theme pattern). 17:25 ET cron. Lesson: pattern detectors on multi-day base must anchor metrics to ticker's own recent extreme; rigid thresholds against generic baselines silently veto exact pattern.
- **session 1 — 9M Day 2 stop clobber bug (critical)**: GOOGL announced stop $365.82 (prev day low), Alpaca received $379.43 (today's ORB low). 9 sites in `order_manager.py` hardcoded `stop_loss_price = trade["orb_low"]` — SSoT violation from 2026-04-24 unified pipeline. Fix: every site reads `trade["stop_price"]`. Lesson: unified pipeline preserved spec-authored stop_price at INSERT but downstream paths still pulled `orb_low` per original MAGNA53 assumption — two strategies sharing one funnel must read strategy-authored stop column.

### 2026-04-30
- **session 2 — Friday Curated Watchlist + entry-tag consistency**: New `friday_watchlist.py` Fri 18:00 ET cron. 6 per-source curators (EP, 9M sugar, themes, wick, parabolic, RS) cross-source dedup with priority hierarchy. Top-25 cap. TradingView two-depth integration: text import block (EXCHANGE:TICKER comma-separated) + per-ticker chart-link inline keyboard top-8. `_send_with_keyboard` posts directly to Bot API. `mi_weekly_watchlists` schema. Lesson: TV's no-write-API ceiling → text-import + chart-link buttons is highest depth; cross-source priority hierarchy prevents one ticker dominating multiple sections while preserving every source tag.
- **session 1 — EP scan dead-zone telemetry beef-up**: `mi_ep_scan_log` UNIQUE collapsed temporal trajectories. Dropped UNIQUE name-agnostically. Added `scan_time_et`, `rank_by_gap`, `projected_vol_multiple`, `pm_rvol`, `adv`, `adv_source`, `minutes_since_open` cols. Reader uses DISTINCT ON. New `mi_ep_scan_outcomes` caches fwd 5d/10d max-high. ADV probe bumped [:20]→[:50] with `ep_adv_probe_synthesized` audit. Two data-gated reviews filed. Lesson: UNIQUE on (scan_date, ticker) erased exactly the data needed for dead-zone reconstruction — append-only + DISTINCT ON for "current state" is the right shape.

### 2026-04-29 — naive `datetime.now()` false-flag (nightly_data_pull)

- A naive `datetime.now()` call in `system_audit.py` false-flagged `nightly_data_pull` as
  overdue ~2h before its real ET deadline. The defensive `or datetime.now(_ET)` fallback
  didn't fire because a naive datetime is not `None` — the bug silently produced a wrong
  wall-clock comparison instead of an obvious crash. Root cause folded into the broader
  pytz/ZoneInfo permanent fix (2026-06-05); see memory `timezone_lmt_pytz_permanent_fix`
  and CLAUDE.md's Time Handling section for the mechanical fix (deploy-gate ban on
  naive `datetime.now()`/`pytz`).

### 2026-04-28
- **session 4 — P22 Wick-Fill shadow tracker**: First strategy shipped through Strategy Maturity Framework. Negated shooting-star setup (`close_in_range_pct ∈ [0.50, 0.75)`) → Day 2+ break of `prior_high` is the short-trap fill. New `mi_wick_candidates` mirrors sugar-baby shape + adds `filled_wick BOOL` + dual fwd-return anchors (from-high conditional on fill, from-close unconditional baseline — gap = strategy edge). Reuses `_NINEM_CONTEXT_CTE` + `is_9m_directional` so WU 2026-04-24 fix stays enforced. Promotion model `telemetry_review` (n≥30, fill_rate≥0.50). Lesson: framework worked as designed — strategy #5 = config row + adapter + sweep call site.
- **session 3 — Strategy Maturity Framework (Option A)**: `agents/market_intelligence/strategies/` package (registry / adapters / promotion / telegram). `mi_strategies` table + `mi_live_trades.signal_type` column (backfilled). Phase gate at 3 entry points (entry_pipeline, shadow_orb_tracker, parabolic_detector). Three promotion evaluators (unpaired_r / paired_r / telemetry_review). Manual promotion. Lesson: thin overlay over per-strategy outcome tables is right when each has materially different telemetry semantics; schema unification first would block on backfill that doesn't pay off until ≥6 strategies.
- **session 2 — Theme Pass 1.5 protected-theme relief valve**: `protected_names` exemption made existing-vs-existing consolidation impossible once a duplicate slipped in. Replaced blanket protection skip with score-direction guard (more-established theme survives) + protection contract preserved (when t protected, target must also be protected). Lesson: protection mechanism implicitly assumed existing themes are mutually distinct.
- **session 1 — 5-min Shadow ORB tracker**: Live ORB hard-coded to 1-min; shadow records would-be 5-min entries + outcomes. Pure telemetry. Extracted `broker/exit_logic.py::apply_daily_exit_step` as SSOT (was duplicated across backtester+live). `mi_orb_shadow_trades` UNIQUE (ticker, alert_date, bar_size_minutes). Lesson: highest-risk step was exit_logic extraction — two parity gates (deterministic backtest + mocked-Alpaca call sequence) caught what unit tests can't.

### 2026-04-27
- **session 3 — Parabolic-short M&A/news exclusion**: OGN false positive (buyout-driven). Two-layer: `mi_parabolic_exclusions` (manual permanent + LLM 14-day TTL) + Perplexity news check on climax/anticipation. Three sources with precedence `manual_keep > manual > news_check`. `/parabolic exclude/include/exclusions`. Lesson: price-action detectors need news/context layer for one-shot catalysts.
- **session 2 — Audit-noise reduction**: Three L1 noise sources: (1) `validation_error` lumped 429s + 5xx + parse → split into `*_rate_limited` / `*_api_failure` / `*_error`; (2) zombie themes invariant queried `stage != 'Retired'` but codebase never writes `'Retired'` → narrowed to Mainstream + 10d; (3) `silent_audit_error_window` 30d trained dismissal → hardcoded 24h; (4) `nightly_data_pull` no-show deadline 17:30 too tight + ignored `mi_job_runs` running → bumped 18:30 + cross-check. Lesson: L1 invariants must model system's actual semantics, not textbook ideal.
- **session 1 — Tiered ORB fade guard**: midpoint pre-check stricter than Qullamaggie/Pradeep methodology, redundant with 10:00 ET cleanup + 15% stop-width gate. `check_fade_guard` takes `ratio: float | None`; MAGNA53 HIGH passes `None`, 9M Day 2 passes `0.25`. Lesson: stacked guardrails should each protect a distinct failure mode.

### 2026-04-26
- **session 3 — Job-run telemetry + `/audit job_runs`**: Closes gap between `notify_job_failure` (crashes) and data-layer audit (table invariants); slow runs and silent zeroes had no signal. New `mi_job_runs` + `core/job_audit.py::audit_run` asynccontextmanager + `audit_wrap(fn, job_id, expected_min_rows=N)`. ~30 jobs wrapped. Lesson: a wrapper that interprets return values must agree with wrapped function's failure semantics — `return None` for opt-out vs `return 0` for "wrote nothing".
- **session 2b — Theme merge min-shared-ticker gate**: Pass 1 trigger conditions (`is_subset`, `overlap_ratio ≥ 0.6`) collapse to noise on tiny themes (1/1=100%, 1/2=50%). New `MIN_SHARED_FOR_MERGE = 3` gates Pass 1. Pass 1.5 (small-theme absorption) intentionally targets ≤3-ticker themes, unchanged.
- **session 2a — Crypto RS surveillance (shadow-mode)**: Parallel crypto RS layer; nightly ingest + RS + alt-season trigger eval all run, Telegram surfaces gated by `CRYPTO_RS_ENABLED=false`. New `agents/market_intelligence/crypto/` package (Kraken + DexScreener + CoinGecko + DefiLlama, US-VPS-safe $0). Three-signal alt-season trigger (stablecoin slope + BTC.D + TOTAL3). 11 `crypto_*` tables. RS = 40/30/30 vs `close_btc` ratio. Lesson: data-source sanity check is non-trivial — rejected Polygon Crypto + CMC; DefiLlama doesn't expose BTC.D (caught mid-design).
- **session 1 — RVOL@T pre-open gate (closes INTC-class entry leak)**: Legacy `today_volume / 20d_daily_ADV` mismatched numerator/denominator (thin pre-market vs full-session). Canonical Relative Volume at Time: today's cumulative vs 20-day mean cumulative at same ET clock-minute. New `mi_minute_volume_curves` + `agents/market_intelligence/minute_volume.py`. Pre-9:30 only. Graceful degradation: not in top-500 universe → skip silently. Lesson: stacked filters with mismatched denominators are a class of "silently passes garbage" bug.

### 2026-04-25
- **session 2 — Weekend data fallback + HUD EP button**: Saturday queries returned "no data today" (handlers used `et_today()`). New `collector.last_trading_day(from_date=None)` (skips Sat/Sun → Friday). Wired into HUD/EP/9M/cluster/trades handlers. Added EP button back to HUD inline keyboard.
- **session 1 — Parabolic short detector (TI1 Stage 1)**: Telemetry-only Stamatoudis/Qullamaggie short. Three-tier state machine: `watch → anticipation → climax`. Velocity-delta gate (`roc_5d ≥ 1.10× roc_20d`) is the canonical "parabolic vs linear" discriminator. Backfill verified CAR/GME ✅, NVDA correctly rejected. New `mi_parabolic_candidates` (persists ALL stages incl. unqualified for offline tuning). 17:15 ET cron. Required one-time OHLC backfill via `scripts/backfill_ohlc.py`. Lesson: pure-compute detectors need historical replay tooling first.

### 2026-04-24
- **session 6 — Sugar baby intraday/EOD direction parity**: WU 4/24 surfaced as Day-2 sugar baby despite net −4.6% (gapped −10%, recovered close > open). Intraday filter gated net direction vs prev_close; EOD filter gated only on `close > open` (intraday recovery). Added `(d.close - m.prev_close) / m.prev_close >= 0.03` to EOD SQL. Lesson: SSoT violation — same conceptual filter, two different gates.
- **session 5 — Apollo Resilience & Self-Audit System (L1/L2/L3)**: `system_audit.py` + `audit_invariants.py` (shared invariants). L1 invariant breach → Telegram. L2 anomaly (30d trimmed median ± 3 MAD or > 5× median) → Telegram + Sonnet hypothesis. L3 drift → audit row + Sunday digest. Three jobs (16:15 / 17:30 / 02:00 ET). `/audit <topic>` on-demand. Cold-start tiers (n<7 hardcoded ceilings, 7-14 L3-only, ≥14 full L2). Backfill verification deferred until ≥30 days of baselines (~2026-05-24).
- **session 4 — Zombie theme cooldown flood**: `db.py::get_active_themes()` had no recency filter — returned every theme ever written. Fix: `get_active_themes(stale_after_days=7)`. Recency cap is de-facto retirement mechanism.
- **session 3 — Unified entry pipeline**: `broker/entry_pipeline.py::submit_trade_entry` is the single funnel for MAGNA53 EP + 9M Day 2 ORB. Strategy diffs (stop, sizing) inject via `spec_builder` callback. Bounded action vocabulary (AUTO_ENTERED/PROPOSED/etc). Per-alert work `asyncio.gather` with `Semaphore(5)`. Lesson: two near-identical entry paths drift in opposite directions; one funnel + injection is the fix.
- **session 2 — ORB late-entry & fade guard**: CHE gapped +17.9%, HIGH at 9:55 ET, bracket placed but tape had faded. Fade guard in `_submit_orb_trade`: skip if `last_price < (orb_high+orb_low)/2`. Tightened window `hour==9 and minute<45`. 10:00 ET cleanup job cancels stuck `order_placed`. Lesson: wide intraday windows let dead-cat orders linger.
- **session 1 — OTO bracket stop-leg ID capture**: INTC false UNPROTECTED + Untracked SELL traced to 4 separate "find stop leg" impls; one used strict `==`, broken under Py3.12 `str(OrderType.STOP)` → `"OrderType.STOP"`. Single canonical `alpaca_client.extract_stop_leg_id(order)` (stop_price primary, case-insensitive type fallback) at all 5 sites. Lesson: same conceptual operation in N places drifts; centralize.

### 2026-04-23
- **session 3 — Validation-window hardening**: `Dockerfile.market` now COPYs `scripts/`; `_eod_ep_recap_job` appends `📡 Feed (sip)` line + fires on zero-HIGH days when feed events present; new `scripts/readiness_check.py` encodes 6 SQL cutover gates. Cutover target 2026-05-23.
- **session 2 — Env-var-gated SIP feed**: URI ORB miss traced to IEX zero-range first-minute bars on mid-liquidity. `ALPACA_DATA_FEED` env (iex/sip), resolved by `alpaca_client.get_data_feed()`. Validated AAPL parity 0.037%, URI 4/22 IEX=$0 → SIP=$4.20. `ALPACA_DATA_FEED=sip` set in prod. Phase 2 (Polygon Advanced dual-feed) trigger: book 5–10×, feed incident, OHLC divergence > 0.2%, or 2nd broker.
- **session 1 — Broker alert gaps + bracket hardening**: BSX/GSHD/SIRI naked positions traced to `StopLimitOrderRequest(stop_loss=...)` without `order_class=OTO` — alpaca-py silently drops kwarg. Fix: always OTO + verify stop leg, cancel naked bracket. Silent state changes: 3 branches in `_handle_cancel_or_reject` (was rejected-only); untracked-sell rowcount alert; UNPROTECTED escalation in `_process_entry_fill`. Lesson: silent-drop kwargs are catastrophic — verify what came back.

### 2026-04-22
- **Strip to market/trading focus**: deleted 5 unused sub-agents + Dockerfiles + dead secrets. Lesson: rotting scaffolding is deploy surface.
- **9M Sugar Baby going-in shape telemetry**: 6 new shape cols + `_shape_tag()` bucket. Telemetry-only — promote to filter after 30+ outcomes.
- **Humanize skip reasons + theme validation rate-limit**: `humanize()` translator; `_VALIDATION_SEMAPHORE(2)` + retry on 429. Lesson: "parse errors" were really rate limits — split exception handlers.
- **EP entry diagnostics**: `broker/skip_reasons.py` (18 bounded constants); `/why TICKER [date]` lifecycle timeline; 4:10 PM EOD EP recap. Lesson: free-form skip reasons broke aggregation.

### 2026-04-21
- **Briefing fixes, 9M quality**: 9M intraday range gate (≥ 2%) + extension gate (prev_close ≤ 1.20× SMA-10), anticipation carve-out (gap ≥ 10% OR proj_vol ≥ 25M).
- **`/trades` richer summary**: open + last 5 closed + totals. UTC/ET boundary fix for `closed_at` via `AT TIME ZONE`.

### 2026-04-20
- **Hardening triage**: LLM rate-limit guard in `ep_detector`; correlation matrix off event loop; theme breadth decay (`pct_above_20sma` < 40% × 2d → forced Fading).
- **Weekly system self-audit**: `system_review.py` — Sunday 8 AM ET 7d aggregation → Sonnet synthesis → 4-section Telegram digest.
- **9M quality filters (74 → 2-5/day)**: price ≥ $5, dollar-vol ≥ $50M, directional conviction, 3× ADV ratio (not flat ceiling). Lesson: flat ceilings silently block mid-ADV genuine catalysts.
- **Theme validation + broker partials**: `_extract_json_object()` brace-depth-aware (replaces regex broken by Haiku nested JSON); cross-sector Unknown fallback. KURA partial-exit stop-first ordering.

### 2026-04-19
- **9M ETF flood + EP ETF leakage + catchup ORB orders**: 3-layer 9M ETF filter; EP secondary `mi_security_types` gate; ORB window `now_et.hour < 10` + `misfire_grace_time=300`.
- **`/pregame` + pinned HUD + inline keyboards**: compact trade-ready shortlist; HUD auto-refresh; `/eps`, `/themes`, `/trades` drill-down.
- **9M EP system (Pradeep Bonde "9M" tactic)**: parallel track, zero changes to MAGNA53. New `ninem_detector.py`, `mi_9m_ep_alerts`, `mi_9m_sugar_babies`. Day 2 ORB at 9:31, stop = prior day's low.

### 2026-04-17
- **P15 Correlation clustering**: `correlation_engine.py` — beta-adjusted SPY-residual Pearson 20d. Backtest inconclusive — revalidate ~June 2026.
- **Validation cooldown**: `mi_validation_cooldowns` (14-day cooldown on validation removal); fixes CAR-in-Data-Center churn.
- **Hardening for live trading prep**: orphaned stop remediation; yfinance 30s timeout wrapper; data pull 4:30 → 5:00 PM ET.
- **Theme engine + EP detector fixes**: scratchpad in tool schemas; Unknown sector keyword fallback; post-assignment validation. EP: 15-min projection gate (≥ 9:45 AM); extension via `MIN(close)` over 5d.

**2026-08-21 — deploy windows gated** — two windows only (12:00-13:00 · 21:15-22:15 ET); `deploy.sh` exits 12 outside them, operator-only override. Repeated ~17:02 deploys had been clipping nightly jobs.

**2026-08-22 — one catalyst grade everywhere** — the admission filters read the corrected news grade, the same one the score reads. The fork (corrected score, raw filters) was binning real EPs before the fix could act.
## 2026-08-28 — verify-live day: three confirmed, one defect caught, two rulings signed

Written straight to CHANGELOG because CLAUDE.md's Recent section has ~40 bytes of headroom.

- **#602 verified and CLOSED.** Rubric v4 live; the judge states a one-line reason on each of
  its two calls, and `judge_direction` finally agrees with a held tier — the OKTA contradiction
  that started the week is gone.
- **#603 and #559 verified.** All Perplexity traffic on the Agent API (no `sonar-pro` rows,
  reported costs); all five real-time flip-ups carried `authoritative: true`.
- **#233 half-verified** — the boost is gone in prod, but Perplexity agreed on the day's only
  alert, so rule 7 has still never fired live.
- **Verify-live caught a defect shipped the day before:** the alert asserted *"agrees, but boost
  cancelled — its own text found no news"* about a read that had found the news. Retiring the
  boost pinned `confidence_multiplier` at 1.0, so a branch meaning "the hedge fired" fired on
  every agreement. Deployed the fix at 12:07 ET.
- **#593 signed.** The sustain rule's revert condition is now a **10%** rate on *tradeable*
  misses over 30 trading days, min 30 declines, raising a **review** and never auto-reverting.
  Re-basing the +20% from the declined level rather than the day's open cut breaches 39 → 13;
  requiring the name to be tradeable cut 13 → 2. **3% today against a 10% trigger.**
- **#210 re-scoped** from "direct primary sources" to *accuracy*, on the operator's ruling. A
  task named for a means could only ever be re-dated, which is what seven bumps looked like.
- **#604 filed:** every naked-position L1 ever fired landed at 16:15:00, inside the
  expiry→replace window of a DAY stop. Not exposure — detection timing.

## 2026-08-27 — judge rubric v4, Perplexity changes hands, the real-time gap goes live

Graduated straight from CLAUDE.md the same day: the Recent section had no room left (see the
ceiling note below), so the detail lives here from the start.

- **Rubric v4** (`v4-2026-08-27-axis-split-second-opinion`). One word no longer means both
  "lower the grade" and "lower the tier" — rule 2 says raises/lowers the GRADE and an OUTPUT
  FIELDS block names each field's axis literally. The judge now returns `grade_reason` and
  `tier_reason`, one line each, rendered on the alert. Robustness eval 36/36, overall 1.0.
- **Perplexity's agreement boost retired.** It multiplied the EP score by 1.2 whenever two
  models concurred; measured over 419 alerts, boosted names ran a *smaller* 5-day move and the
  effect is a null once score band is held constant. Its **disagreement** now reaches the judge
  as a re-read prompt (rule 7), with double-count telemetry in the monthly judge review.
- **Perplexity moved to the Agent API** — their `/chat/completions` sunsets 2026-09-27. No model
  string anywhere (a preset routes it) and cost is the figure the API reports, so both
  hand-maintained values are gone. A failed or empty second opinion is now *unavailable*, never
  a grade; it used to return "routine", so a degraded provider could argue every catalyst down.
- **`ep_rt_gap_authoritative` ON** — the real-time gap decides the 9% floor in both directions.
  The held reason was cost, priced at "+25 candidates/day"; deduped and in-window it is 2.5.
- ⚠ **CLAUDE.md hit its 40,000-BYTE ceiling this day.** The gate counts bytes, not characters,
  and the file is dense with em-dashes and emoji — it reads ~550 bytes lighter than it is.
  Graduating the 2026-08-23 entry was not enough; today's entry is a stub. The next entry will
  fail the commit until real operating sections are graduated.

## 2026-08-23 — weekend deploys ungated

Deploy windows are for market days; Sat/Sun unrestricted. Weekend jobs still run — check the clock yourself, the gate no longer will. (Graduated from CLAUDE.md 2026-08-27; the live rule stays in CLAUDE.md's Deploy Windows table, which is where it was being duplicated from.)

## 2026-08-01 → 08-03 — money-path filters live · chart-vision paused · four rules made mechanical

- **3 entry filters live** (+2R profit trigger, stale-gap cleanup, submission-time gap re-check).
  The latter two would have blocked 3 of the last 17 live trades, all losers. Each shipped OFF →
  verified inert → then flipped.
- **#490 gate 1 resolved with no market day** — coverage ruled out on the production call (98%,
  the absent names all preferred shares); tick replay showed the residual metric over-counts
  intrabar crosses. Sustain rule (3 consecutive bars) live, argued from mechanism not returns.
- **Chart-vision drip PAUSED, capability kept (#519)** — 336 calls, ~85% of judge spend, zero trade
  influence. Root cause was a DEFAULT: `grade_holistic`'s `log_caller` defaulted to the live bucket,
  so an experiment was invisible inside production spend. Now required; 8 lanes were mislabelled,
  not 1. New-lane detector added (the anomaly detector is cold-start gated by design).
- **Skip rows silently failing since 8/01** — #465 swapped the `mi_live_trades` unique constraint
  and `_insert_skipped_trade` kept naming the old 2-column target, so every skip insert raised into
  a swallowed log line. Cost the durable terminal state AND the duplicate-suppression anchor;
  surfaced only as a double Telegram on 8/03. Fixed at 3 sites + guarded.
- **9M Day 2 ENTRY deleted (#515)**, character kept. The coupling (a shadow lane running on a
  builder named after a dead strategy) was the diagnosis, not an obstacle → renamed, not merged;
  a proposed merge died when a diff found 8 real differences against a docstring claiming one.
- **Reviews registry**: ran every ripe predicate — the pile was half what was claimed, and the
  oldest items were TRIPWIRES whose zero count means the bug has not recurred. Age was rendering
  good news as neglect.
- **4 rules made MECHANICAL** because prose and memory both failed: report format (a Stop hook),
  SETUP vs FAMILY, deprecated-but-enabled strategies, and plain-words reporting.
- **Lesson repeated all week**: a guard that always fires is not a guard; a gate that exists on one
  machine is not a gate; and a deletion is not done when the code compiles.
