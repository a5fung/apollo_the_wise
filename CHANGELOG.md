# Apollo the Wise — Change Log

Compressed historical change log. **Live operational reference + recent prose entries (~2 weeks) live in `CLAUDE.md`.** Entries graduate here once they age out of the recent window.

Each entry is a one-liner: `topic — key change & lesson`. Full prose lives in git history at the listed commits — `git log --grep="<topic>"` or `git blame` for code-level context.

When consulted: investigating "why did we change X?", design reviews, retrospectives. Not loaded by default into agent context.

---

### 2026-05-08
- **session 4 — Drawdown circuit breaker shadow ship (#39)**: Replaces count-based 10-loss breaker (self-perpetuating + methodology-blind for hold-winners). State machine evaluated daily 4:12 PM ET cron. Equity from Alpaca incl. unrealized; 30d rolling peak; asymmetric hysteresis (-5% trip / -2.5% release); stale-data fail-open at 48h. Two new tables: `mi_account_equity_snapshots` + `mi_safeguard_state`. SHADOW phase; promotion gated ≥14d post-cutover via `DRAWDOWN_BREAKER_PHASE=active`. Lesson: momentum-strategy safeguard must be methodology-aware; closed-trade window biased toward losses by design when winners are held.
- **session 3 — Circuit breaker threshold 5→10 (interim)**: Morning ORB blocked by 5-loss count breaker on 5-loss streak (24% probability at 25% WR — statistically too tight). Single-line constant bump as interim while drawdown breaker #39 is built. Daily loss limit remains magnitude-side backstop. Lesson: don't disable safeguard with caveats; tune down to non-firing while proper fix is engineered.
- **session 2 — 5/7 paper-session triage (5 additive fixes)**: (B) parabolic earnings exclusion + `days_up_streak ≥ 3` hard gate (AGL/XMTR class — pre-runup low inflated 35d cumulative); (C) EP earnings-day pre-score catalyst boost (DDOG/AAON: routine catalyst at `gap≥10%+earnings` upgrades to strong); (D) EP cooldown bypass on fresh earnings (HIMX: `gap≥15%+earnings_day` overrides 60d cooldown); (E) pm-shares floor relative-anomaly carve-out (`pm_rvol ≥ 5×` skips 25k absolute floor); (F) phantom split sanity check via post-apply ratio. Lesson: chained classifiers grading textual news need non-LLM independent second source (yfinance earnings_calendar) at multiple integration points.
- **— Stop-fill state desync purpose-tagged routing (ARM 5/07 + TEAM 5/06 class)**: ARM entry filled $224, stop partial-filled 87/89 @$219.50 = -$391.50, but row ended `status='cancelled' exits=[] total_pnl=$0`. Three stacked failures: WS partial_fill visibility-only, `stop_order_id` matching brittle on stale IDs, 10:00 ET cleanup overwrote prior fills. Fix: new `finalize_stop_fill` + `purpose='stop_loss'` tagging at ALL stop placement sites + `cancel_unfilled_entries` preserves exits[] history. Lesson: state-mutation paths in trading code must be routable by intent (purpose) not by identity (stop_order_id); purpose is durable, identity mutates.

### 2026-05-07
- **session 4 — `/pregame` + `/ep` post-midnight data tag**: `last_trading_day()` rolled Sat/Sun→Fri but not the post-midnight weekday pre-scan window. New `latest_market_data_date(as_of)` resolves freshest alert_date across mi_ep_alerts + mi_9m_ep_alerts. Tags header `_(data: <date>)_` when ≠ today. Generalizes to weekend/holiday/pre-scan with one query. Lesson: three states (weekend/holiday/pre-scan) collapse to one query when keyed on actual data freshness.
- **session 3 — Wave C #5 9M EP per-scan digest**: 15+ tickers per scan tick fired individual Telegram bubbles. Batched into ONE digest by tier (Actual / Pace) per scan; per-ticker DB inserts + audit events unchanged; only user-facing Telegram batched. Same shape as Wave A #4 flag TRIGGER digest. Lesson: informational alerts = digest; terminal/actionable = ping.
- **session 2 — splits_ingest premature-apply (systemic data corruption)**: Every split row had `applied_premature=t` — `get_unapplied_splits` lacked `execution_date <= today` filter, so freshly-detected splits applied via `adjusted=true` Polygon fetch BEFORE actual execution. Result: pre-execution bars un-adjusted, post-execution bars adjusted; mismatched units in same row. ERNA 5/04 25:1 reverse: $0.15 pre vs $3.74 post in DB → false parabolic climax. Fix: add the date gate + reconciliation SQL `UPDATE mi_splits SET adjustment_applied=FALSE WHERE applied_at < execution_date::timestamptz`. Affects every reader of `mi_daily_closes` (RS, EP, parabolic, flag, friday). Lesson: `adjustment_applied=TRUE` was a flag on the wrong axis — tracked "we ran apply step" not "data actually adjusted." For state-machine flags: gate on OUTCOME not ATTEMPT.
- **— Wave B #9 TEAM attempt counter 1 vs 2**: `/positions` aggregate did `COUNT(*) WHERE status='closed'` (rows, not attempts); `/trades` selected `NULL::jsonb AS entries` for live mode, `_attempt_count` returned 0. Schema migration introduced `entry_attempt` int column but left two readers pointing at dead `entries` JSONB. Fix: `COUNT(*) → SUM(entry_attempt)`; `/trades` select entry_attempt; prefer column with `_attempt_count` fallback. Lesson: a schema migration that adds a replacement column must update every reader at the same commit.

### 2026-05-06
- **session 2 — Unify EP volume gate to RVOL@T (one primitive, two anchors)**: HUT/BLMN/GLW HIGHs detected at 9:52 ET, missing 9:45 ORB cutoff. Three distinct volume gates (pre-9:30 RVOL@T / 9:30-9:45 no gate / 9:45+ today_vol / 390min_ADV) drifted; 9:30-9:45 was structurally < 2× by math regardless of hot tape. Removed artificial 9:45 cutoff in `compute_rvol_at_time`; collapsed conditional pre-market-only call into ONE uniform RVOL@T (pm anchor pre-9:30, session anchor 9:30+). Discriminator validated (HUT 9:31=7.29×, BLMN=11.84×, GLW 9:35=2.41×). Lesson: three implementations of same conceptual question (volume above normal at clock-minute) are three places for math to drift; single primitive (`mi_minute_volume_curves`) covers entire 4:00-16:00 window uniformly.
- **— Stale `stop_order_id` after `update_stop` failure**: TEAM 5/06 9:35 ET `update_stop` cancelled old stop, retry failed (insufficient qty), function returned False but never nulled `mi_live_trades.stop_order_id`. Downstream: `sync_positions` orphan-remediation gated on `stop_order_id IS NULL` never fired; `_handle_fill` step 2 matched stale ID, silently failed; `/trades` showed position normally, masked unprotected state. Fix at 3 sites (`update_stop` retry-failed, `execute_partial_exit` place-new-stop except, rollback-failed) — null `stop_order_id` + emit `naked_position_detected` audit. `sync_positions` Path C extended to detect stale-status broker IDs. `/trades` shows ⚠️ NAKED on `stop_order_id IS NULL`. Lesson: terminal failure paths must (a) leave DB in reconcilable state, (b) emit distinct audit event naming failure class.

### 2026-05-05
- **session 5b — Flag detector dry-volume gate hybrid + #163/164/188/189 verified, #177/179 closed**: `_compute_fresh_tightening` ceiling switched to `max(recent_avg_vol, 0.5×ADV20)` — anchors on contraction floor, prevents single sub-avg bar from over-tightening. OKLO 5/4 was the trigger (14.65M vs ADV20 15M = 0.98 barely passing). #177 closed (#180 covered the GOOGL/TEAM DB drift); #179 folded into P2. Lesson: "tomorrow"/"next day" in cross-session followup notes is a relative-date trap — always reconcile with today before acting.
- **session 5 — P0.1 enum stringification site-local fix (#211) + #187 doc honesty**: Empirical signature confirmed (GOOGL/TEAM `stop_update_started` firing every morning at identical from→to prices). Site-local fix at `live_tracker.py:565` — `str(order.status).split('.')[-1].lower()`. SSoT consolidation at `_order_to_dict` deferred until polling-reader race-shape audit. #187 trailing-10 docstring rewritten ("+25% above 10-session low", not "10-session return"). Lesson: pre-ship empirical gates pay off when the plan author writes them.
- **session 4 — Paper→live $ flip wave 3 (P0.9 staged-paper banner + P1.5 /dryrun + P0.2 partial-fill telemetry)**: `mi_strategies.live_real_enabled` gate → `🟡 STAGED-PAPER ramp` banner on Telegram proposal when False at live. `/dryrun` renders pre-trade sizing math against current Alpaca equity. P0.2 visibility-only per advisor (paper Alpaca atomic-fills, zero production exercise; deferred-commit handler designed against real fill-shape data later). Lesson: when plan describes a bug that doesn't match actual code, reconcile before shipping.
- **session 3 — Defer partial/full exit DB commit + Telegram to fill event (#180)**: GOOGL/TEAM 5/05 16:45 ET partial exits Telegram'd "Sold @$X" but no shares sold (after-hours queue, fill_price collapsed to entry_price). Fix: `mi_live_orders.purpose` ('partial_exit'/'full_exit') routes WS fill handler to `finalize_partial_exit`/`finalize_full_exit` against real Alpaca fill price. Submit-time dedup safeguard. Cancel/reject restore handlers re-place stop on full remaining. Lesson: "Order accepted" ≠ "shares sold" — defer commit + user-visible language to WS fill event for any after-close cron.
- **session 2 — compute_atr_14 close-to-close → Wilder TR (STRL/BAND skip class)**: `backtester/filters.py::compute_atr_14` was reading only close, computing `abs(close - prev_close)` as TR — silently understated on volatile/gappy stocks. Rewrite reads H/L/close, full Wilder TR. Matches `flag_detector._atr_14` SSoT. 30d replay: all 4 stop_too_wide skips pass under new ATR. Lesson: when two implementations of same conceptual computation drift, the stale one inverts coverage on the exact pattern it's supposed to surface.
- **— Earnings-day HIGH override (DOCN miss → yfinance backstop)**: 30 settled misses since 4/14 with `gap≥10 + catalyst=routine + score-blocked`, all 5d positive. New `earnings_calendar.py::is_earnings_day` via yfinance `earnings_dates` + `.calendar`. Override at tier-decision: MODERATE + gap≥10 + earnings → HIGH. Three audit events distinguish surfaces. Lesson: chained classifiers grading textual news are coupled to ingest latency; fix is a structurally independent second source (calendar API), not a smarter classifier.

### 2026-05-04
- **session 7 — ORB-extension shadow telemetry (#10 decision-support)**: N=5 cancelled-ORB lifecycle analysis inconclusive (TWLO single-leg drove $1,101 swing). New `mi_orb_extension_shadow` records 6 cutoffs (10/11/12/13/14/16:00) per cancellation. Hook in `cancel_unfilled_entries` gated on `orb_unfilled_cancelled`. Settlement appended to `_nightly_data_pull`. Data-gated review threshold 20 trades. Lesson: counterfactual decisions need counterfactual data — record now so when N=20 arrives the data exists.
- **session 6 — Flag detector burst-class universe + fresh-tightening COILED path (OKLO miss)**: Two structural gaps. (a) `get_flag_universe` top-200-RS-only excluded post-runup names whose 6M is dragged by pre-runup downtrend (OKLO rs_rank=981, rs_1m=94.3, trailing-10 +42.5%) — OR-clause `rs_1m_pct >= 80 OR (last_close / trailing10_min - 1) >= 0.25`. (b) Contraction math overlapping windows on short bases — new `_compute_fresh_tightening` predicate (base_age≥4, max(2bar TR%) ≤ 0.6×ATR14%, max(2bar vol) ≤ ADV20). Lesson: pattern detectors must anchor on ticker's own recent extreme; rigid thresholds against generic baselines silently veto the exact pattern.
- **session 5 — Cleanup queue: zombie themes + cancel_unfilled_entries audit + #183 filed**: 42 prod themes with `theme_date < CURRENT_DATE-7d AND stage NOT 'Retired'` fixed via DISTINCT ON. `cancel_unfilled_entries` gained audit events on success/failure paths (was silent). #183 filed: `_order_to_dict` returns `'OrderStatus.NEW'` not `'new'` for Python 3.11+ enum stringification — held to preserve discriminator in tomorrow's `stop_update_started` telemetry. Lesson: Python enum stringification is single root cause masquerading as N decoupled bugs; SSoT fix at wire boundary, not per-site patches.
- **session 4 — Audit logging in update_stop + execute_partial_exit (GOOGL/TEAM 5/4 silent failures)**: Both functions had zero audit-log writes; every failure path was silent. 7 sites in update_stop + 9 sites in execute_partial_exit added. No logic changed — captures recurrence with telemetry so #180 (defer DB commit) and #182 (write skew) can be fixed against real data. Lesson: silent return-False in trading-side functions violates no-silent-failure rule; audit logging is the bare minimum before any defer/rollback refactor.
- **session 3 — SSoT M&A filter `ma_filter.py` (AVNS slip → Polygon backstop)**: AVNS (4/14 take-private $24.62-72 pinned 14d) slipped flag scan because Perplexity returned no news → no keyword scan signal. New `ma_filter.py::is_likely_ma()` — three layered sources: (1) Claude `catalyst_quality=='mna'`, (2) `_MNA_KEYWORDS` text scan, (3) Polygon news headlines. EP + flag detectors share SSoT. L2 deal-pin price-signature filed as data-gated. Lesson: chained LLM-only catalyst lookups silently couple to one provider's blind spots; structurally independent second source closes coverage gap.
- **session 2 — Group skip Telegrams per strategy**: `submit_trade_entry._skip` per-ticker Telegram noise. New `aggregate_skips: bool` flag. MAGNA53 + 9M Day 2 cron callers emit grouped digest after `asyncio.gather`. Suppressed parallel `check_filters` per-ticker pings. Lesson: Telegram economy — terminal/actionable events get a ping; informational batches go in digest.
- **session 2 — Per-ticker open-position guard (TEAM 5/04 double-entry near-miss)**: 9M Day 2 placed bracket while MAGNA53 5/01 fill in TEAM was still open. Same-day dedup at `(ticker, alert_date)` doesn't cross days/strategies. New check after dedup: SELECT for prior `status='filled' AND remaining_shares > 0` — `BLOCK_TICKER_OPEN_POSITION`. Lesson: trade-level dedup keys ≠ exposure-level dedup keys; exposure check must live at position level.
- **session 2 — Parallelize 9M Day 2 cron + drop bar-retry delay (TEAM 5/04 root cause)**: Real cause was sequential for-loop in `_9m_day2_orb_job`. SOUN's 60s retry blocked TEAM. Replaced with `asyncio.gather + Semaphore(5)` mirroring MAGNA53. `BAR_RETRY_DELAY_SEC = 60→10`. Lesson: two strategies sharing unified pipeline must share fan-out shape.
- **— Bracket fill messaging + stop-limit buffer (TEAM 5/04 unfilled)**: "auto-entered" Telegram fired at PLACEMENT regardless of fill. Renamed defaults to "Order placed" with "pending trigger" footer. SSoT helper `stop_limit_buy_price(stop) = round(max(stop*1.005, stop+0.02), 2)` replaces 7 hand-rolled `*1.001` sites — penny tickers no longer get 1¢ effective buffer. TEVA 4/30 EOD-cleanup-not-ORB-cleanup filed. Lesson: per-share buffer formulas scaling linearly need an absolute floor; SSoT cleanup of 7 hand-rolled constants overdue.

### 2026-05-03
- **— Catalyst hedge-phrase downgrade (Track B Layer 2)**: RDDT 5/1 catalyst returned "strong" against hedged Perplexity output ("no specific information"). New post-hoc skepticism layer in EP detector: 9-phrase scan; downgrade catalyst one notch + cancel agreement boost when detected AND quality∈{game_changer, strong}. Audit event `catalyst_pplx_hedge_downgrade`. Lesson: chained LLM calls hide own data quality; downstream consumers must parse natural-language uncertainty as signal, not content.
- **— Forward-looking wick surfacing (Track A)**: Wick surfaces gated `filled_wick=TRUE` but settlement waits 10 forward sessions — freshly-fired wicks invisible during actionable window. New `get_wick_pending_candidates(lookback_sessions=3)` with `NOT EXISTS` clause for already-broken-prior-high. WICK_PENDING priority slot in friday_watchlist + morning briefing section. Lesson: outcome-telemetry filters and forward-looking trader filters are different shapes of same question — two-purpose surfaces need two filters.
- **— pm_rvol gate universe top-500 → $5M $-vol floor**: 84% of HIGH alerts silently bypassed pm_rvol gate because they were outside top-500 trailing $-vol. Pre-market gappers are systematically mid/small-cap — capping at top-500 inverts coverage. `get_top_dollar_volume_universe` swapped to `WHERE adv_20 * close >= $5M` (max_tickers=5000 cap). 2647 tickers vs 500. Lesson: fixed-N universe inverts coverage on a gate designed to catch gappers outside megacap set.
- **— Drop check_stuck_filled_row invariant (duplicate of naked_position)**: 24h threshold mismatched to multi-day Pradeep/Qullamaggie holds. GOOGL/TEAM tripped despite stops being attached. Deleted invariant — naked_position covers the actual SQL-detectable bug class. Lesson: two invariants targeting same bug class must differ in WHAT they catch, not just WHEN; 24h-delayed copy of naked_position is zero net detection value.
- **— Cold-start L2 gate: minimum-denominator skip for rate metrics**: Five false `HIGH_ep_entry_rate` L2 alerts (4/27→5/01) at sample_n<7. `MetricSpec.fetch_today` returns float|None; both rate metrics return None when denominator < 5. `_compute_anomaly` skips sample recording + classification when None. Cleaned 14 polluted baseline rows + 5 audit events. Lesson: rate metrics with small denominators need minimum-N gate, not just zero gate; declare uninformative days uninformative entirely.
- **— Reap stale mi_job_runs 'running' rows at scheduler startup**: 5 stale rows across 3 jobs from SIGTERM-during-await. `_reap_stale_running_runs` UPDATEs status='aborted' for rows >2h old. Filed: `scheduler.shutdown(wait=True)` on SIGTERM as actual prevention. Lesson: invariants assume process always reaches finally-equivalent path; container restart during await violates silently — reap is hygiene, not prevention.

### 2026-05-01
- **session 5 — /setup TICKER reverse-lookup detector chronology**: Per-ticker fan-out across ~10 detector tables in parallel (asyncpg per-call connection acquisition required). Merged + sorted by date DESC then source priority (TRADE > FLAG > EP > 9M > WICK > PARABOLIC > THEME > WATCHLIST). TradingView chart inline-keyboard button. `.isdigit()` token-discrimination guard for `/setup XNDU 30` parsing. Lesson: parallel async DB fanout in asyncpg requires per-call `pool.acquire()` — using single connection for gathered fetches raises "another operation in progress".
- **session 4 — Authoritative split handling, drop RS heuristic**: XNDU disappeared from RS list on +24% gap-up due to `MAX_PERIOD_RETURN=300` heuristic catching the very momentum it should surface. New `splits_ingest.py` Phase 0 of nightly fetches `/v3/reference/splits`, re-fetches 250d aggs with `adjusted=true`, overwrites mi_daily_closes. RS heuristic skip blocks DELETED, replaced with non-blocking `rs_extreme_return` audit. `scripts/backfill_splits.py` for one-shot reconciliation. Lesson: heuristic stand-ins for missing data sources misfire on the exact pattern they were built to catch — fix is the authoritative source, not a tighter threshold.
- **session 3 — Stop-remediation hardening + fishhook EOD pass fix**: Stop-remediation pattern is correct as-is (DAY TIF child stop auto-expires 4 PM, 4:05 PM remediates GTC). Three hardenings: (1) `expired` event downgrade ℹ️ vs ⚠️; (2) bounded retry 3× with backoff; (3) evening backstop 21:00 ET 2nd sync_positions. Fishhook EOD `operator does not exist: date >= interval` — `$1::date - INTERVAL '60 days'`. Lesson: verify "common pattern" is mechanically safe before adding paranoid plumbing.
- **session 2 — Continuation Flag Detector (TI-class shadow ship)**: New strategy `flag_continuation`. Five stages anchored on pivot-high bar (highest-volume within 5% of 25d max-high). Three calibration fixes from XNDU replay: (1) proximity on pivot_close not pivot_high; (2) state gates use closing extremes not intraday; (3) breakout vol references base recent_5d_avg not 20d trailing. ATR-14 inlined Wilder TR. Hysteresis (theme pattern). 17:25 ET cron. Lesson: pattern detectors on multi-day base must anchor metrics to ticker's own recent extreme; rigid thresholds against generic baselines silently veto exact pattern.
- **session 1 — 9M Day 2 stop clobber bug (critical)**: GOOGL announced stop $365.82 (prev day low), Alpaca received $379.43 (today's ORB low). 9 sites in `order_manager.py` hardcoded `stop_loss_price = trade["orb_low"]` — SSoT violation from 2026-04-24 unified pipeline. Fix: every site reads `trade["stop_price"]`. Lesson: unified pipeline preserved spec-authored stop_price at INSERT but downstream paths still pulled `orb_low` per original MAGNA53 assumption — two strategies sharing one funnel must read strategy-authored stop column.

### 2026-04-30
- **session 2 — Friday Curated Watchlist + entry-tag consistency**: New `friday_watchlist.py` Fri 18:00 ET cron. 6 per-source curators (EP, 9M sugar, themes, wick, parabolic, RS) cross-source dedup with priority hierarchy. Top-25 cap. TradingView two-depth integration: text import block (EXCHANGE:TICKER comma-separated) + per-ticker chart-link inline keyboard top-8. `_send_with_keyboard` posts directly to Bot API. `mi_weekly_watchlists` schema. Lesson: TV's no-write-API ceiling → text-import + chart-link buttons is highest depth; cross-source priority hierarchy prevents one ticker dominating multiple sections while preserving every source tag.
- **session 1 — EP scan dead-zone telemetry beef-up**: `mi_ep_scan_log` UNIQUE collapsed temporal trajectories. Dropped UNIQUE name-agnostically. Added `scan_time_et`, `rank_by_gap`, `projected_vol_multiple`, `pm_rvol`, `adv`, `adv_source`, `minutes_since_open` cols. Reader uses DISTINCT ON. New `mi_ep_scan_outcomes` caches fwd 5d/10d max-high. ADV probe bumped [:20]→[:50] with `ep_adv_probe_synthesized` audit. Two data-gated reviews filed. Lesson: UNIQUE on (scan_date, ticker) erased exactly the data needed for dead-zone reconstruction — append-only + DISTINCT ON for "current state" is the right shape.

### 2026-04-28
- **session 4 — P22 Wick-Fill shadow tracker**: First strategy shipped through Strategy Maturity Framework. Negated shooting-star setup (`close_in_range_pct ∈ [0.50, 0.75)`) → Day 2+ break of `prior_high` is the short-trap fill. New `mi_wick_candidates` mirrors sugar-baby shape + adds `filled_wick BOOL` + dual fwd-return anchors (from-high conditional on fill, from-close unconditional baseline — gap = strategy edge). Reuses `_NINEM_CONTEXT_CTE` + `is_9m_directional` so WU 2026-04-24 fix stays enforced. Promotion model `telemetry_review` (n≥30, fill_rate≥0.50). Lesson: framework worked as designed — strategy #5 = config row + adapter + sweep call site.
- **session 3 — Strategy Maturity Framework (Option A)**: `agents/market_intelligence/strategies/` package (registry / adapters / promotion / telegram). `mi_strategies` table + `mi_live_trades.signal_type` column (backfilled). Phase gate at 3 entry points (entry_pipeline, shadow_orb_tracker, parabolic_detector). Three promotion evaluators (unpaired_r / paired_r / telemetry_review). Manual promotion. Lesson: thin overlay over per-strategy outcome tables is right when each has materially different telemetry semantics; schema unification first would block on backfill that doesn't pay off until ≥6 strategies.
- **session 2 — Theme Pass 1.5 protected-theme relief valve**: `protected_names` exemption made existing-vs-existing consolidation impossible once a duplicate slipped in. Replaced blanket protection skip with score-direction guard (more-established theme survives) + protection contract preserved (when t protected, target must also be protected). Lesson: protection mechanism implicitly assumed existing themes are mutually distinct.
- **session 1 — 5-min Shadow ORB tracker**: Live ORB hard-coded to 1-min; shadow records would-be 5-min entries + outcomes. Pure telemetry. Extracted `broker/exit_logic.py::apply_daily_exit_step` as SSOT (was duplicated across backtester+live). `mi_orb_shadow_trades` UNIQUE (ticker, alert_date, bar_size_minutes). Lesson: highest-risk step was exit_logic extraction — two parity gates (deterministic backtest + mocked-Alpaca call sequence) caught what unit tests can't.

### 2026-04-27
- **session 3 — Parabolic-short M&A/news exclusion**: OGN false positive (buyout-driven). Two-layer: `mi_parabolic_exclusions` (manual permanent + LLM 14-day TTL) + Perplexity news check on climax/anticipation. Three sources with precedence `manual_keep > manual > news_check`. `/parabolic exclude/include/exclusions`. Lesson: price-action detectors need news/context layer for one-shot catalysts.
- **session 2 — Audit-noise reduction**: Three L1 noise sources: (1) `validation_error` lumped 429s + 5xx + parse → split into `*_rate_limited` / `*_api_failure` / `*_error`; (2) zombie themes invariant queried `stage != 'Retired'` but codebase never writes `'Retired'` → narrowed to Mainstream + 10d; (3) `silent_audit_error_window` 30d trained dismissal → hardcoded 24h; (4) `nightly_data_pull` no-show deadline 17:30 too tight + ignored `mi_job_runs` running → bumped 18:30 + cross-check. Lesson: L1 invariants must model system's actual semantics, not textbook ideal.
- **session 1 — Tiered ORB fade guard**: midpoint pre-check stricter than Qullamaggie/Pradeep methodology, redundant with 10:00 ET cleanup + 15% stop-width gate. `check_fade_guard` takes `ratio: float | None`; MAGNA53 HIGH passes `None`, 9M Day 2 passes `0.25`. Lesson: stacked guardrails should each protect a distinct failure mode.

### 2026-04-26
- **session 3 — Job-run telemetry + `/audit job_runs`**: Closes gap between `notify_job_failure` (crashes) and data-layer audit (table invariants); slow runs and silent zeroes had no signal. New `mi_job_runs` + `core/job_audit.py::audit_run` asynccontextmanager + `audit_wrap(fn, job_id, expected_min_rows=N)`. ~30 jobs wrapped. Lesson: a wrapper that interprets return values must agree with wrapped function's failure semantics — `return None` for opt-out vs `return 0` for "wrote nothing".
- **session 2b — Theme merge min-shared-ticker gate**: Pass 1 trigger conditions (`is_subset`, `overlap_ratio ≥ 0.6`) collapse to noise on tiny themes (1/1=100%, 1/2=50%). New `MIN_SHARED_FOR_MERGE = 3` gates Pass 1. Pass 1.5 (small-theme absorption) intentionally targets ≤3-ticker themes, unchanged.
- **session 2a — Crypto RS surveillance (shadow-mode)**: Parallel crypto RS layer; nightly ingest + RS + alt-season trigger eval all run, Telegram surfaces gated by `CRYPTO_RS_ENABLED=false`. New `agents/market_intelligence/crypto/` package (Kraken + DexScreener + CoinGecko + DefiLlama, US-VPS-safe $0). Three-signal alt-season trigger (stablecoin slope + BTC.D + TOTAL3). 11 `crypto_*` tables. RS = 40/30/30 vs `close_btc` ratio. Lesson: data-source sanity check is non-trivial — rejected Polygon Crypto + CMC; DefiLlama doesn't expose BTC.D (caught mid-design).
- **session 1 — RVOL@T pre-open gate (closes INTC-class entry leak)**: Legacy `today_volume / 20d_daily_ADV` mismatched numerator/denominator (thin pre-market vs full-session). Canonical Relative Volume at Time: today's cumulative vs 20-day mean cumulative at same ET clock-minute. New `mi_minute_volume_curves` + `agents/market_intelligence/minute_volume.py`. Pre-9:30 only. Graceful degradation: not in top-500 universe → skip silently. Lesson: stacked filters with mismatched denominators are a class of "silently passes garbage" bug.

### 2026-04-25
- **session 2 — Weekend data fallback + HUD EP button**: Saturday queries returned "no data today" (handlers used `et_today()`). New `collector.last_trading_day(from_date=None)` (skips Sat/Sun → Friday). Wired into HUD/EP/9M/cluster/trades handlers. Added EP button back to HUD inline keyboard.
- **session 1 — Parabolic short detector (TI1 Stage 1)**: Telemetry-only Stamatoudis/Qullamaggie short. Three-tier state machine: `watch → anticipation → climax`. Velocity-delta gate (`roc_5d ≥ 1.10× roc_20d`) is the canonical "parabolic vs linear" discriminator. Backfill verified CAR/GME ✅, NVDA correctly rejected. New `mi_parabolic_candidates` (persists ALL stages incl. unqualified for offline tuning). 17:15 ET cron. Required one-time OHLC backfill via `scripts/backfill_ohlc.py`. Lesson: pure-compute detectors need historical replay tooling first.

### 2026-04-24
- **session 6 — Sugar baby intraday/EOD direction parity**: WU 4/24 surfaced as Day-2 sugar baby despite net −4.6% (gapped −10%, recovered close > open). Intraday filter gated net direction vs prev_close; EOD filter gated only on `close > open` (intraday recovery). Added `(d.close - m.prev_close) / m.prev_close >= 0.03` to EOD SQL. Lesson: SSoT violation — same conceptual filter, two different gates.
- **session 5 — Apollo Resilience & Self-Audit System (L1/L2/L3)**: `system_audit.py` + `audit_invariants.py` (shared invariants). L1 invariant breach → Telegram. L2 anomaly (30d trimmed median ± 3 MAD or > 5× median) → Telegram + Sonnet hypothesis. L3 drift → audit row + Sunday digest. Three jobs (16:15 / 17:30 / 02:00 ET). `/audit <topic>` on-demand. Cold-start tiers (n<7 hardcoded ceilings, 7-14 L3-only, ≥14 full L2). Backfill verification deferred until ≥30 days of baselines (~2026-05-24).
- **session 4 — Zombie theme cooldown flood**: `db.py::get_active_themes()` had no recency filter — returned every theme ever written. Fix: `get_active_themes(stale_after_days=7)`. Recency cap is de-facto retirement mechanism.
- **session 3 — Unified entry pipeline**: `broker/entry_pipeline.py::submit_trade_entry` is the single funnel for MAGNA53 EP + 9M Day 2 ORB. Strategy diffs (stop, sizing) inject via `spec_builder` callback. Bounded action vocabulary (AUTO_ENTERED/PROPOSED/etc). Per-alert work `asyncio.gather` with `Semaphore(5)`. Lesson: two near-identical entry paths drift in opposite directions; one funnel + injection is the fix.
- **session 2 — ORB late-entry & fade guard**: CHE gapped +17.9%, HIGH at 9:55 ET, bracket placed but tape had faded. Fade guard in `_submit_orb_trade`: skip if `last_price < (orb_high+orb_low)/2`. Tightened window `hour==9 and minute<45`. 10:00 ET cleanup job cancels stuck `order_placed`. Lesson: wide intraday windows let dead-cat orders linger.
- **session 1 — OTO bracket stop-leg ID capture**: INTC false UNPROTECTED + Untracked SELL traced to 4 separate "find stop leg" impls; one used strict `==`, broken under Py3.12 `str(OrderType.STOP)` → `"OrderType.STOP"`. Single canonical `alpaca_client.extract_stop_leg_id(order)` (stop_price primary, case-insensitive type fallback) at all 5 sites. Lesson: same conceptual operation in N places drifts; centralize.

### 2026-04-23
- **session 3 — Validation-window hardening**: `Dockerfile.market` now COPYs `scripts/`; `_eod_ep_recap_job` appends `📡 Feed (sip)` line + fires on zero-HIGH days when feed events present; new `scripts/readiness_check.py` encodes 6 SQL cutover gates. Cutover target 2026-05-23.
- **session 2 — Env-var-gated SIP feed**: URI ORB miss traced to IEX zero-range first-minute bars on mid-liquidity. `ALPACA_DATA_FEED` env (iex/sip), resolved by `alpaca_client.get_data_feed()`. Validated AAPL parity 0.037%, URI 4/22 IEX=$0 → SIP=$4.20. `ALPACA_DATA_FEED=sip` set in prod. Phase 2 (Polygon Advanced dual-feed) trigger: book 5–10×, feed incident, OHLC divergence > 0.2%, or 2nd broker.
- **session 1 — Broker alert gaps + bracket hardening**: BSX/GSHD/SIRI naked positions traced to `StopLimitOrderRequest(stop_loss=...)` without `order_class=OTO` — alpaca-py silently drops kwarg. Fix: always OTO + verify stop leg, cancel naked bracket. Silent state changes: 3 branches in `_handle_cancel_or_reject` (was rejected-only); untracked-sell rowcount alert; UNPROTECTED escalation in `_process_entry_fill`. Lesson: silent-drop kwargs are catastrophic — verify what came back.

### 2026-04-22
- **Strip to market/trading focus**: deleted 5 unused sub-agents + Dockerfiles + dead secrets. Lesson: rotting scaffolding is deploy surface.
- **9M Sugar Baby going-in shape telemetry**: 6 new shape cols + `_shape_tag()` bucket. Telemetry-only — promote to filter after 30+ outcomes.
- **Humanize skip reasons + theme validation rate-limit**: `humanize()` translator; `_VALIDATION_SEMAPHORE(2)` + retry on 429. Lesson: "parse errors" were really rate limits — split exception handlers.
- **EP entry diagnostics**: `broker/skip_reasons.py` (18 bounded constants); `/why TICKER [date]` lifecycle timeline; 4:10 PM EOD EP recap. Lesson: free-form skip reasons broke aggregation.

### 2026-04-21
- **Briefing fixes, 9M quality**: 9M intraday range gate (≥ 2%) + extension gate (prev_close ≤ 1.20× SMA-10), anticipation carve-out (gap ≥ 10% OR proj_vol ≥ 25M).
- **`/trades` richer summary**: open + last 5 closed + totals. UTC/ET boundary fix for `closed_at` via `AT TIME ZONE`.

### 2026-04-20
- **Hardening triage**: LLM rate-limit guard in `ep_detector`; correlation matrix off event loop; theme breadth decay (`pct_above_20sma` < 40% × 2d → forced Fading).
- **Weekly system self-audit**: `system_review.py` — Sunday 8 AM ET 7d aggregation → Sonnet synthesis → 4-section Telegram digest.
- **9M quality filters (74 → 2-5/day)**: price ≥ $5, dollar-vol ≥ $50M, directional conviction, 3× ADV ratio (not flat ceiling). Lesson: flat ceilings silently block mid-ADV genuine catalysts.
- **Theme validation + broker partials**: `_extract_json_object()` brace-depth-aware (replaces regex broken by Haiku nested JSON); cross-sector Unknown fallback. KURA partial-exit stop-first ordering.

### 2026-04-19
- **9M ETF flood + EP ETF leakage + catchup ORB orders**: 3-layer 9M ETF filter; EP secondary `mi_security_types` gate; ORB window `now_et.hour < 10` + `misfire_grace_time=300`.
- **`/pregame` + pinned HUD + inline keyboards**: compact trade-ready shortlist; HUD auto-refresh; `/eps`, `/themes`, `/trades` drill-down.
- **9M EP system (Pradeep Bonde "9M" tactic)**: parallel track, zero changes to MAGNA53. New `ninem_detector.py`, `mi_9m_ep_alerts`, `mi_9m_sugar_babies`. Day 2 ORB at 9:31, stop = prior day's low.

### 2026-04-17
- **P15 Correlation clustering**: `correlation_engine.py` — beta-adjusted SPY-residual Pearson 20d. Backtest inconclusive — revalidate ~June 2026.
- **Validation cooldown**: `mi_validation_cooldowns` (14-day cooldown on validation removal); fixes CAR-in-Data-Center churn.
- **Hardening for live trading prep**: orphaned stop remediation; yfinance 30s timeout wrapper; data pull 4:30 → 5:00 PM ET.
- **Theme engine + EP detector fixes**: scratchpad in tool schemas; Unknown sector keyword fallback; post-assignment validation. EP: 15-min projection gate (≥ 9:45 AM); extension via `MIN(close)` over 5d.
